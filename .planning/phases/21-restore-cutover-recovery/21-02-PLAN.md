---
phase: 21-restore-cutover-recovery
plan: 02
type: execute
wave: 1
depends_on:
  - 21-01
files_modified:
  - scripts/restore-solidstats-memory.py
  - scripts/validate-phase-21.py
  - tests/test-memory-cutover-contract.py
  - k8s/memory/10-qdrant.yaml
  - k8s/memory/20-mempalace.yaml
  - k8s/memory/30-network-policy.yaml
  - k8s/memory/40-backup.yaml
  - .planning/phases/21-restore-cutover-recovery/21-BACKUP-RESTORE-EVIDENCE.json
autonomous: false
requirements:
  - ISO-03
  - OPS-02
  - OPS-03
  - OPS-05
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
premises:
  - claim: The Phase 20 handoff binds the current source inventory, approved mapping, retained bundle files, passing parity report, pinned oracle, and pinned Qdrant image.
    src: .planning/phases/20-local-corpus-migration/20-PHASE21-HANDOFF.json
    verify: jq -e '.handoff_schema == "solidstats-memory-phase21-handoff/v1" and .vector_strategy == "reuse" and (.phase21_required_checks | index("recompute-provenance-digests"))' .planning/phases/20-local-corpus-migration/20-PHASE21-HANDOFF.json
  - claim: The checked-in backup CronJob is suspended and already packages a Qdrant snapshot, MemPalace metadata, checksums, and a manifest under the accepted object-store prefix.
    src: k8s/memory/40-backup.yaml#solidstats-memory-backup
    verify: rg -n 'suspend: true|backups/solidstats-memory/|qdrant.snapshot|mempalace-metadata.tar|SHA256SUMS|manifest.json' k8s/memory/40-backup.yaml
carried_forward:
  - claim: The successful local target was deliberately removed, so Phase 21 must restore from retained digest-bound inputs rather than assume a surviving runtime.
    src: .planning/phases/20-local-corpus-migration/20-06-SUMMARY.md#Accomplishments
must_haves:
  truths:
    - "Every handoff, source-inventory, mapping-contract, transform-manifest, bundle-file, and parity-report digest is recomputed before private bundle content is read or Qdrant is contacted."
    - "A missing, null, empty, zero-byte, checksum-mismatched, or incomplete snapshot/metadata/manifest/checksum package is rejected before upload or recovery; exactly one complete package is accepted."
    - "Backup order is fixed: quiescence proof, Qdrant snapshot, MemPalace metadata archive, checksums, manifest, local checksum re-read, object upload, remote inventory, and download checksum verification."
    - "The backup object set and checksum lines are emitted in deterministic lexical order, preserving stable equal-element order across an exact rerun."
    - "An exact rerun for the same run and package digests reuses the verified backup; an occupied prefix or run binding with unequal content fails without overwrite."
    - "Only one run owns the backup/restore lock; a parallel different run is refused and an interrupted exact run resumes only from its last verified evidence digest."
    - "The generated physical restore binding must be absent from collection inventory, alias inventory, and target-specific lookup; any equality or collision with an active/candidate name or alias stops without automatic renaming."
    - "Restore order is fixed: provenance, package completeness, target absence, disk headroom, snapshot-priority recovery, green health/config/count, exact parity, then exact-image alias compatibility."
    - "The isolated drill leaves the active alias, public nginx site, machine-local MCP registrations, and legacy service state byte-for-byte or semantically unchanged."
  artifacts:
    - path: scripts/restore-solidstats-memory.py
      provides: "Batched provenance, backup, target-absence, isolated recovery, parity, and rollback control plane"
    - path: scripts/validate-phase-21.py
      provides: "Backup/restore evidence schema and Phase 20 digest-chain validation"
    - path: tests/test-memory-cutover-contract.py
      provides: "Synthetic Qdrant snapshot/recovery fixtures, collision cases, concurrency, interruption, and failure injection"
    - path: .planning/phases/21-restore-cutover-recovery/21-BACKUP-RESTORE-EVIDENCE.json
      provides: "Aggregate passing live backup and isolated-restore evidence"
  key_links:
    - from: scripts/restore-solidstats-memory.py
      to: .planning/phases/20-local-corpus-migration/20-PHASE21-HANDOFF.json
      via: "recomputed public and retained-private digest chain before private parsing"
      pattern: "recompute_phase20_bindings"
    - from: scripts/restore-solidstats-memory.py
      to: k8s/memory/40-backup.yaml
      via: "one-shot Job derived from the suspended CronJob contract with runtime-only private inputs"
      pattern: "solidstats-memory-backup"
    - from: scripts/restore-solidstats-memory.py
      to: Qdrant snapshot and alias APIs
      via: "bounded authenticated HTTP adapter with raw bodies confined to restrictive temporary storage"
      pattern: "(create_snapshot|recover_snapshot|require_absent_target|probe_alias_compatibility)"
  assumptions:
    - statement: "The pinned MemPalace image resolves the derived Qdrant logical name through an alias exactly as it resolves a physical collection."
      status: flagged-unverified
      resolution_gate: "Create a temporary run-bound alias after isolated parity, prove the exact image can recall through it, then restore alias absence before Plan 21-03."
    - statement: "The MemPalace metadata archive is consistent while no capture-capable client exists and writes are quiesced."
      status: flagged-unverified
      resolution_gate: "Prove no public/client write path, capture a before/after metadata digest around the archive, and stop if it changes."
  prohibitions:
    - "[flagged-unverified] No restore request may address an existing collection or alias, even when the existing target appears empty."
    - "[flagged-unverified] No private name, path, response body, or corpus-bearing value may appear in the committed backup/restore evidence."
---

<!-- markdownlint-disable MD013 MD033 MD041 -->

<objective>
Create and execute a provenance-bound one-shot backup plus isolated Qdrant
restore drill, proving exact parity and exact-image alias compatibility while
leaving every active/public/client state unchanged.

Purpose: Satisfy OPS-02 and OPS-03 before any cutover lever becomes eligible.
Output: Tested restore control plane and one aggregate backup/restore evidence
envelope.
</objective>

<execution_context>
@/home/afgan0r/.codex/gsd-core/workflows/execute-plan.md
@/home/afgan0r/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-restore-cutover-recovery/21-CONTEXT.md
@.planning/phases/21-restore-cutover-recovery/21-RESEARCH.md
@.planning/phases/21-restore-cutover-recovery/21-VALIDATION.md
@.planning/phases/21-restore-cutover-recovery/21-PATTERNS.md
@.planning/phases/21-restore-cutover-recovery/COVERAGE.md
@.planning/phases/21-restore-cutover-recovery/21-01-SUMMARY.md
@.planning/phases/20-local-corpus-migration/20-PHASE21-HANDOFF.json
@.planning/phases/20-local-corpus-migration/20-PARITY-REPORT.json
@.planning/phases/20-local-corpus-migration/20-TRANSFORM-MANIFEST.json
@k8s/memory/40-backup.yaml

<interfaces>
Existing interfaces to preserve and reuse:
- `scripts/validate-phase-21.py`:
  `validate_evidence_envelope()`, `validate_transition_chain()`, and
  `validate_value_free_payload()` from Plan 21-01.
- `scripts/verify-solidstats-memory-parity.py`:
  `validate_provenance()`, `compare_id_bijection()`,
  `compare_documents_metadata_timestamps()`, `compare_vectors()`, and the
  accepted strict ANN-equivalence checks.
- Qdrant REST capabilities are enumerated in `COVERAGE.md`; raw API responses
  are transient inputs, never evidence payloads.
</interfaces>
</context>

## Artifacts this phase produces

- New file `scripts/restore-solidstats-memory.py` with functions
  `load_phase20_handoff()`, `recompute_phase20_bindings()`,
  `acquire_run_lock()`, `qdrant_request()`, `require_absent_target()`,
  `require_restore_capacity()`, `create_snapshot()`, `download_snapshot()`,
  `archive_quiescent_metadata()`, `verify_backup_package()`,
  `recover_snapshot()`, `verify_restored_collection()`,
  `probe_alias_compatibility()`, `restore_alias_prestate()`, and `main()`.
- New CLI commands `preflight`, `backup`, `isolated-restore`, and
  `verify-restore`, with flags `--handoff`, `--evidence-dir`, `--run-id`,
  `--resume-run`, and `--check-only`; credentials and private bindings are
  environment-only inputs.
- New evidence file `21-BACKUP-RESTORE-EVIDENCE.json` using schema
  `solidstats-memory-backup-restore-evidence/v1` with fields `schema`,
  `run_id`, `phase20_bindings`, `quiescence`, `capacity`, `package_checks`,
  `object_checks`, `target_absence`, `restore_checks`, `parity_checks`,
  `alias_compatibility`, `rollback_state`, and `verdict`.
- No new Service, public port, recurring schedule, database schema, package,
  or external dependency.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement fail-closed backup and isolated restore control</name>
  <files>scripts/restore-solidstats-memory.py, scripts/validate-phase-21.py, tests/test-memory-cutover-contract.py</files>
  <read_first>
    <file>.planning/phases/21-restore-cutover-recovery/21-01-SUMMARY.md</file>
    <file>.planning/phases/21-restore-cutover-recovery/COVERAGE.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-PHASE21-HANDOFF.json</file>
    <file>.planning/phases/20-local-corpus-migration/20-PARITY-REPORT.json</file>
    <file>scripts/verify-solidstats-memory-parity.py</file>
    <file>k8s/memory/40-backup.yaml</file>
  </read_first>
  <behavior>
    - "Provenance drift stops before private reads and HTTP calls."
    - "Null/empty/incomplete packages, active-target equality, alias collision, insufficient headroom, and non-snapshot recovery priority stop before recovery."
    - "Equal reruns are idempotent, unequal prefix/run collisions fail, parallel runs are locked out, and exact interrupted runs resume from the last chained stage."
    - "Recovery success requires green state, expected vector configuration and aggregate count, exact Phase 20 parity, and a reversible exact-image alias compatibility result."
  </behavior>
  <action>Extend the Phase 21 evidence validator first, then implement one standard-library Python control plane. Recompute every Phase 20 public and retained-bundle binding before opening bundle content. Generate a one-shot declarative Job from the suspended backup contract in restrictive temporary storage, inject secrets and private bindings from environment variables, validate and server-side-dry-run it, then create snapshot, metadata archive, deterministic checksums/manifest, object upload, remote inventory, download, and hash recheck in the declared order. Query collection inventory, alias inventory, and target-specific lookup independently; stop on any candidate equality/collision instead of choosing a new name. Enforce measured restore headroom, snapshot-priority recovery, green state/config/count, exact parity, and a temporary exact-image alias compatibility probe whose cleanup restores the recorded alias absence. Keep raw HTTP and private artifact data in restrictive temporary storage and emit only the declared aggregate envelope. [src: .planning/phases/21-restore-cutover-recovery/21-CONTEXT.md#Restore-and-Provenance] [src: .planning/phases/21-restore-cutover-recovery/21-RESEARCH.md#Exact-Verification-Matrix] [src: k8s/memory/40-backup.yaml#solidstats-memory-backup]</action>
  <acceptance_criteria>
    - "Synthetic Qdrant fixtures cover create, list, download, recover, alias compatibility, timeout, malformed response, occupied target, collision, interruption, and concurrent-run refusal."
    - "The same exact run/package can resume or re-verify without a second upload; changed content under the same binding is refused."
    - "No alias, nginx, client, or legacy-service mutation remains after the compatibility probe."
  </acceptance_criteria>
  <verify>
    <automated>timeout 30s python3 -m unittest tests/test-memory-cutover-contract.py &amp;&amp; timeout 20s python3 scripts/restore-solidstats-memory.py --help &amp;&amp; timeout 20s python3 scripts/validate-phase-21.py --help &amp;&amp; timeout 20s python3 -m py_compile scripts/restore-solidstats-memory.py scripts/validate-phase-21.py</automated>
  </verify>
  <done>The backup and isolated-restore adapters pass all synthetic and privacy contracts and cannot mutate active/public/client state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Authorize and prove one live backup plus isolated restore</name>
  <files>k8s/memory/10-qdrant.yaml, k8s/memory/20-mempalace.yaml, k8s/memory/30-network-policy.yaml, k8s/memory/40-backup.yaml, .planning/phases/21-restore-cutover-recovery/21-BACKUP-RESTORE-EVIDENCE.json</files>
  <read_first>
    <file>.planning/phases/21-restore-cutover-recovery/21-CONTEXT.md</file>
    <file>.planning/phases/21-restore-cutover-recovery/21-RESEARCH.md</file>
    <file>.planning/phases/21-restore-cutover-recovery/21-01-SUMMARY.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-06-SUMMARY.md</file>
    <file>scripts/restore-solidstats-memory.py</file>
  </read_first>
  <precondition>The complete Plan 21-01 offline suite is green; retained Phase 20 private inputs are locally available; cluster, object-store, and exact-image credentials are available without printing them.</precondition>
  <reversibility rating="costly">The isolated restore consumes cluster and object-store capacity, but it has no active alias/public/client effect and preserves failed candidates for diagnosis.</reversibility>
  <what-built>A batched, restartable preflight -> backup -> isolated-restore -> exact-parity -> alias-compatibility workflow with no active-state change.</what-built>
  <action>After explicit operator approval, run one batched execution with bounded SSH/kubectl/Qdrant/S3 timeouts. Recompute provenance, prove write quiescence, measure PVC/node/snapshot headroom, and replace only the measured Qdrant/MemPalace storage, immutable MemPalace image, backup object-store CIDR, immutable uploader image, and private collection operator markers in their owning manifests; the private collection value may enter its runtime manifest but not evidence or command output. Validate and server-side-dry-run the exact targeted manifest set before apply. Acquire the run lock, create and download the backup, verify the complete package locally and remotely, restore into the proven-absent physical binding, rerun exact field/identifier/metadata/timestamp/vector/exclusion/ANN gates, exercise and remove the temporary compatibility alias, and verify active alias, nginx, client, and legacy-service pre-state remained unchanged. On interruption, reconnect and resume only the same run from chained evidence; on any mismatch stop without delete or overwrite. Write only the aggregate evidence schema and run the recursive privacy validator before retaining it. [src: .planning/phases/21-restore-cutover-recovery/21-CONTEXT.md#Cutover-and-Recovery] [src: .planning/phases/21-restore-cutover-recovery/21-RESEARCH.md#Pattern-3-One-batched-operator-run-with-explicit-checkpoints] [src: docs/solidstats-memory.md#Unresolved-Operator-Evidence]</action>
  <how-to-verify>
    1. Confirm the preflight reports current public handoff bindings, quiescence, and sufficient measured headroom without private values.
    2. Confirm one backup prefix contains exactly the required package and its downloaded checksums match.
    3. Confirm the restored physical target was absent beforehand and passes health, configuration, aggregate count, exact parity, and exact-image alias compatibility.
    4. Confirm active alias, nginx, MCP client registrations, legacy service, and recurring CronJob state are unchanged after the drill.
  </how-to-verify>
  <acceptance_criteria>
    - "One live backup is complete, immutable for its run binding, and download-verified."
    - "One isolated restore passes all exact gates and retains no compatibility alias."
    - "The committed evidence passes the value-free validator and contains no raw response."
  </acceptance_criteria>
  <verify>
    <automated>timeout 1800s python3 scripts/restore-solidstats-memory.py preflight --handoff .planning/phases/20-local-corpus-migration/20-PHASE21-HANDOFF.json --evidence-dir .planning/phases/21-restore-cutover-recovery --resume-run &amp;&amp; timeout 3600s python3 scripts/restore-solidstats-memory.py backup --handoff .planning/phases/20-local-corpus-migration/20-PHASE21-HANDOFF.json --evidence-dir .planning/phases/21-restore-cutover-recovery --resume-run &amp;&amp; timeout 3600s python3 scripts/restore-solidstats-memory.py isolated-restore --handoff .planning/phases/20-local-corpus-migration/20-PHASE21-HANDOFF.json --evidence-dir .planning/phases/21-restore-cutover-recovery --resume-run &amp;&amp; timeout 1800s python3 scripts/restore-solidstats-memory.py verify-restore --handoff .planning/phases/20-local-corpus-migration/20-PHASE21-HANDOFF.json --evidence-dir .planning/phases/21-restore-cutover-recovery --resume-run &amp;&amp; timeout 30s python3 scripts/validate-phase-21.py --evidence .planning/phases/21-restore-cutover-recovery/21-BACKUP-RESTORE-EVIDENCE.json</automated>
  </verify>
  <resume-signal>Type "approved" to authorize the isolated live backup/restore run, or describe the blocking evidence.</resume-signal>
  <done>Backup and restore evidence is green, the two flagged assumptions are resolved by live proof, and no active/public/client state changed.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Retained local bundle -> restore control plane | Private artifacts are opened only after public digest bindings pass. |
| Operator workstation -> Kubernetes/Qdrant/S3 | Authenticated mutations cross three external systems through one batched run. |
| Qdrant snapshot -> absent physical target | Recovery can overwrite unless the stronger project absence gate blocks it. |
| Raw API/package data -> committed evidence | Private names, paths, values, and bodies must not cross. |

## STRIDE Threat Register — ASVS L2

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-21-01 | Tampering | Qdrant recovery target | critical | mitigate | Three independent absence checks, canonical collision checks, no force path, and failure-injection tests before recovery. |
| T-21-02 | Tampering | Phase 20/bundle provenance | high | mitigate | Recompute the complete digest chain before private read and again before mutation; chain successor evidence. |
| T-21-03 | Information disclosure | Backup/restore logs and evidence | critical | mitigate | Environment/Secret inputs, restrictive raw temporary storage, aggregate allowlist, recursive rejection, and delete-on-digest temporary handling. |
| T-21-04 | Information disclosure | Qdrant API | high | mitigate | Access only through the private service path and verify no NodePort, LoadBalancer, nginx route, or public 6333/6334 reachability. |
| T-21-09 | Denial of service | PVC/node capacity | high | mitigate | Measure live headroom against snapshot plus restored coexistence before upload/recovery and stop below the declared threshold. |
| T-21-10 | Repudiation | Backup identity and object prefix | medium | mitigate | Run lock, canonical package manifest, deterministic checksums/order, remote inventory, downloaded recheck, and immutable collision refusal. |

</threat_model>

<verification>
1. Run the Phase 21 synthetic contract suite and compile both tools.
2. At the operator checkpoint, run exactly one batched live preflight/backup/
   restore/verify sequence.
3. Validate the aggregate evidence recursively and recompute its predecessor
   digest.
4. Confirm all active/public/client state equals the recorded pre-state.
</verification>

<success_criteria>

- The complete Phase 20 provenance chain matches before private read and live mutation.
- One complete backup is upload- and download-checksum verified.
- One proven-absent restored physical target passes exact parity and exact-image alias compatibility.
- Active alias, nginx, client, legacy runtime, and recurring schedule remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/21-restore-cutover-recovery/21-02-SUMMARY.md` when done.
</output>
