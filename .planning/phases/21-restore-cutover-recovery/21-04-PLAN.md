---
phase: 21-restore-cutover-recovery
plan: 04
type: execute
wave: 3
depends_on:
  - 21-03
files_modified:
  - scripts/cutover-solidstats-memory.sh
  - scripts/validate-phase-21.py
  - tests/test-memory-cutover-contract.py
  - docs/solidstats-memory.md
  - .github/workflows/deploy-memory.yml
  - k8s/memory/05-rbac.yaml
  - k8s/memory/40-backup.yaml
  - scripts/render-memory-manifests.py
  - scripts/validate-memory-manifests.py
  - tests/test-memory-runtime-contract.py
  - .planning/phases/21-restore-cutover-recovery/21-RECOVERY-EVIDENCE.json
  - .planning/phases/21-restore-cutover-recovery/21-CUTOVER-SEAL.json
autonomous: false
requirements:
  - ISO-01
  - ISO-03
  - OPS-02
  - OPS-03
  - OPS-05
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
premises:
  - claim: The accepted recovery contract requires both process restart and VPS reboot proof for the public MCP endpoint and private Qdrant collection before cutover acceptance.
    src: .planning/phases/21-restore-cutover-recovery/21-CONTEXT.md#Cutover-and-Recovery
    verify: rg -n 'process restart and VPS reboot|public MCP endpoint and the private Qdrant' .planning/phases/21-restore-cutover-recovery/21-CONTEXT.md
  - claim: The backup CronJob directly archives `/metadata/palace` from the live MemPalace PVC, uses concurrency-forbid, and remains suspended.
    src: k8s/memory/40-backup.yaml#solidstats-memory-backup
    verify: rg -n 'suspend: true|concurrencyPolicy: Forbid|tar --create|claimName: mempalace-data' k8s/memory/40-backup.yaml
  - claim: The backup pod currently suppresses its ServiceAccount token and its only explicit egress paths are Qdrant TCP/6333 and S3 TCP/443, so RBAC alone cannot make the in-cluster scale protocol runnable.
    src: k8s/memory/40-backup.yaml#solidstats-memory-backup
    verify: rg -n 'automountServiceAccountToken: false|allow-backup-to-qdrant|allow-backup-upload-egress' k8s/memory/40-backup.yaml k8s/memory/30-network-policy.yaml
  - claim: Operator bootstrap currently consists of `00-namespace.yaml` and `01-ci-rbac.yaml`; no backup control-plane RBAC/API-egress manifest exists or is excluded from CI workload apply.
    src: .github/workflows/deploy-memory.yml#Render-validate-and-dry-run-memory-resources
    verify: test ! -e k8s/memory/05-rbac.yaml && rg -n "00-namespace.yaml.*01-ci-rbac.yaml|! -name '01-ci-rbac.yaml'" .github/workflows/deploy-memory.yml
carried_forward:
  - claim: Plan 21-03 leaves the exact legacy client and full rollback path intact while the new service is live at CLIENT_ADDED.
    src: .planning/phases/21-restore-cutover-recovery/21-03-SUMMARY.md#Accomplishments
must_haves:
  truths:
    - "MemPalace and Qdrant are restarted one at a time; after each restart the same private/public/auth/MCP/recall/miss/archive/capture/read-after-write matrix passes before the next disruption."
    - "A complete post-restart steady-state backup scales the sole MemPalace writer to zero, proves no writer remains, and requires canonical metadata source-before, source-after, and extracted-archive digests to match before accepting the package."
    - "Every successful or failed steady-state backup restores the exact recorded MemPalace replica count and proves availability plus capture/read-after-write resumption; incomplete cleanup or resumption keeps the recurring schedule suspended and the phase unsealed."
    - "The consistency-proven backup produces a fresh snapshot/metadata/checksum/manifest package, remote inventory, and successful download checksum recheck."
    - "The VPS reboot proof records a changed boot identity, bounded SSH reconnection, node Ready, PVC Bound, Qdrant Ready, MemPalace Available, nginx active, and the full behavior matrix; unchanged boot identity or readiness-only evidence fails."
    - "An actual reverse-order rollback restores client, nginx, alias, workload, and legacy pre-state, passes its behavior checks, then the verified forward path is reapplied and passes again."
    - "Only after restart, backup, reboot, rollback, and forward recovery pass is the exact legacy `mempalace` registration removed or disabled; unrelated MCP registrations remain untouched and `solidstats_memory` remains the sole SolidStats client."
    - "The recurring backup CronJob is enabled only after one operator-authorized Job derived from its exact template proves writer quiescence, before/after/archive consistency, package verification, exact writer restoration, and write resumption; the complete recovery chain must also pass and concurrency-forbid remains intact."
    - "The backup control plane authenticates with a short-lived projected token, can read and patch only the `mempalace` Deployment/scale subresource plus the minimum namespace pod inventory, and has no wildcard or cluster-scoped RBAC."
    - "No Kubernetes API IP is committed: the operator discovers the live `kubernetes.default` Service and ready API endpoint candidates, measures kube-router pre/post-translation behavior with fresh pods, and renders one exact `/32` plus observed TCP port into the operator-only policy."
    - "Before the first scale-to-zero and before schedule activation, an exact-label/backup-ServiceAccount pod must complete an authenticated scale GET, the same ServiceAccount under a nonmatching label must fail at the network boundary, and an exact-label no-permission identity must be denied by RBAC."
    - "The new `05-rbac.yaml` remains operator bootstrap: the renderer and validator track it, CI excludes it from workload dry-run/apply, and a missing or stale live API-egress binding fails closed instead of widening default deny."
    - "The final seal chains every predecessor digest and contains aggregate booleans/counts/digests only; a false-positive recovery or privacy mutation invalidates the seal."
  artifacts:
    - path: scripts/probe-solidstats-memory.py
      provides: "Restart/reboot phase-aware full behavior probes"
    - path: scripts/cutover-solidstats-memory.sh
      provides: "Ordered restart, reboot/reconnect, exercised rollback/forward, and seal orchestration"
    - path: scripts/validate-phase-21.py
      provides: "Recovery and final-seal schema validation"
    - path: tests/test-memory-cutover-contract.py
      provides: "False-positive recovery, unchanged-boot, partial-reconnect, rollback/forward, and client-removal-order tests"
    - path: docs/solidstats-memory.md
      provides: "Exact operator sequence, evidence boundaries, rollback, recovery, and delayed legacy-removal runbook"
    - path: k8s/memory/05-rbac.yaml
      provides: "Operator-applied least-privilege backup scale RBAC and runtime-rendered exact Kubernetes API egress template"
    - path: k8s/memory/40-backup.yaml
      provides: "Projected-token, least-privilege writer-quiescing, metadata-stability-checked recurring backup enabled only after live predecessor proof"
    - path: scripts/render-memory-manifests.py
      provides: "Manifest-set contract that carries the new operator bootstrap file without applying or resolving it in CI"
    - path: scripts/validate-memory-manifests.py
      provides: "Exact backup RBAC, projected-token, API-egress template, and rendered binding validation"
    - path: tests/test-memory-runtime-contract.py
      provides: "Mutation coverage for backup RBAC, API egress, token projection, renderer inventory, and CI bootstrap exclusion"
    - path: .github/workflows/deploy-memory.yml
      provides: "CI workload list that excludes all three operator bootstrap manifests"
    - path: .planning/phases/21-restore-cutover-recovery/21-RECOVERY-EVIDENCE.json
      provides: "Aggregate restart, steady-state backup consistency, writer-resumption, reboot, rollback, and forward evidence"
    - path: .planning/phases/21-restore-cutover-recovery/21-CUTOVER-SEAL.json
      provides: "Final predecessor-bound Phase 21 acceptance seal"
  key_links:
    - from: scripts/cutover-solidstats-memory.sh
      to: scripts/probe-solidstats-memory.py
      via: "the same full matrix after each restart, reboot, rollback, and forward transition"
      pattern: "(restart-recovery|reboot-recovery|exercise-rollback|full)"
    - from: scripts/cutover-solidstats-memory.sh
      to: k8s/memory/05-rbac.yaml
      via: "runtime discovery of Service/Endpoint API candidates, kube-router positive/negative measurement, exact temporary rendering, and operator-only apply before quiescence"
      pattern: "(prove-backup-api-access|MEMORY_OPERATOR_MEASURED_KUBERNETES_API_EGRESS)"
    - from: scripts/cutover-solidstats-memory.sh
      to: k8s/memory/40-backup.yaml
      via: "API-access predecessor, exact-template quiescence, metadata digest equality, package verification, and writer-resumption proof before source/live schedule activation"
      pattern: "(prove-backup-api-access|prove-backup-consistency|activate-backup-schedule)"
    - from: k8s/memory/40-backup.yaml
      to: k8s/memory/20-mempalace.yaml
      via: "projected ServiceAccount token, narrow scale-subresource access, exact replica pre-state, zero-writer wait, and guaranteed replica restoration"
      pattern: "mempalace"
    - from: scripts/validate-memory-manifests.py
      to: k8s/memory/05-rbac.yaml
      via: "exact Role/RoleBinding, backup pod selector, `/32` destination, observed port, and operator-bootstrap marker validation"
      pattern: "allow-backup-to-kubernetes-api"
    - from: scripts/validate-phase-21.py
      to: .planning/phases/21-restore-cutover-recovery/21-CUTOVER-SEAL.json
      via: "predecessor digest chain and false-positive recovery rejection"
      pattern: "validate_cutover_seal"
    - from: scripts/cutover-solidstats-memory.sh
      to: machine-local MCP configuration
      via: "exact legacy-name removal after seal gates, followed by exact new-client get/live call"
      pattern: "codex mcp (get|remove)"
  prohibitions:
    - "[flagged-unverified] No cutover may be accepted and no legacy client may be removed before the complete behavior, rollback, and recovery chain is sealed."
---

<!-- markdownlint-disable MD013 MD033 MD041 -->

<objective>
Prove process restart, a steady-state-consistent backup with an executable
least-privilege Kubernetes API control path and exact writer
quiescence/resumption, VPS reboot, actual rollback, and forward recovery using
the same full behavior matrix, then enable the verified backup schedule and
seal the final exact client state.

Purpose: Prevent readiness-only recovery claims and remove the legacy client
only after recovery is demonstrated, not assumed.
Output: Recovery tooling/tests/runbook, aggregate recovery evidence, and a
predecessor-bound Phase 21 seal.
</objective>

<execution_context>
@/home/afgan0r/.codex/gsd-core/workflows/execute-plan.md
@/home/afgan0r/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-restore-cutover-recovery/21-CONTEXT.md
@.planning/phases/21-restore-cutover-recovery/21-RESEARCH.md
@.planning/phases/21-restore-cutover-recovery/21-VALIDATION.md
@.planning/phases/21-restore-cutover-recovery/21-PATTERNS.md
@.planning/phases/21-restore-cutover-recovery/COVERAGE.md
@.planning/phases/21-restore-cutover-recovery/21-03-SUMMARY.md
@scripts/probe-solidstats-memory.py
@scripts/cutover-solidstats-memory.sh
@k8s/memory/00-namespace.yaml
@k8s/memory/01-ci-rbac.yaml
@k8s/memory/20-mempalace.yaml
@k8s/memory/30-network-policy.yaml
@k8s/memory/40-backup.yaml
@scripts/render-memory-manifests.py
@scripts/validate-memory-manifests.py
@tests/test-memory-runtime-contract.py
@.github/workflows/deploy-memory.yml

<interfaces>
Existing interfaces to preserve and reuse:
- Plan 21-03 `probe_behavior_matrix()` is the single acceptance oracle after
  every disruption; recovery code must call it rather than create a weaker
  readiness probe.
- Plan 21-03 rollback restores client, nginx, alias, workloads, and legacy
  state in reverse order from the local stage journal.
- `k8s/memory/40-backup.yaml` retains `concurrencyPolicy: Forbid`; only the
  evidence-gated `spec.suspend` state changes at seal.
- `k8s/memory/00-namespace.yaml` and `01-ci-rbac.yaml` are operator bootstrap;
  the new `05-rbac.yaml` joins that excluded set because the namespace-scoped
  CI deployer is intentionally unable to create Role or RoleBinding resources.
- Kubernetes does not define whether Service destination translation occurs
  before or after `ipBlock` evaluation. The exact kube-router path must be
  measured with fresh pods before rendering the operator-only API egress
  policy; neither Service ClusterIP nor endpoint semantics are assumed.
</interfaces>
</context>

## Artifacts this phase produces

- New functions `restart_recovery()`, `reboot_recovery()`,
  `measure_backup_api_egress()`, `prove_backup_api_access()`,
  `prove_backup_consistency()`, `activate_backup_schedule()`,
  `exercise_live_rollback()`, and `seal_cutover()` in
  `scripts/cutover-solidstats-memory.sh`.
- New functions `validate_recovery_evidence()` and
  `validate_cutover_seal()` in `scripts/validate-phase-21.py`.
- New CLI commands `restart-recovery`, `reboot-recovery`,
  `prove-backup-api-access`, `prove-backup-consistency`, `exercise-rollback`,
  `activate-backup-schedule`, and `seal`; all reuse `--resume-run` and add
  `--reconnect-timeout` for bounded reboot recovery.
- New evidence file `21-RECOVERY-EVIDENCE.json` using schema
  `solidstats-memory-recovery-evidence/v1` with fields `schema`, `run_id`,
  `cutover_evidence_sha256`, `restart_checks`,
  `backup_api_control_checks`,
  `steady_state_backup_consistency`, `fresh_backup_checks`,
  `writer_resumption_checks`, `reboot_checks`, `rollback_checks`,
  `forward_checks`, `client_checks`, and `verdict`.
- New evidence file `21-CUTOVER-SEAL.json` using schema
  `solidstats-memory-cutover-seal/v1` with fields `schema`, `run_id`,
  `recovery_evidence_sha256`, `requirements`, `prohibitions`,
  `legacy_client_absent`, `new_client_live`, `backup_schedule_live`, and
  `verdict`.
- New operator-bootstrap `k8s/memory/05-rbac.yaml` with the exact backup Role,
  RoleBinding, and dynamically rendered API-egress policy; the source contains
  no environment IP.
- Updated manifest inventory/validation/tests/CI exclusion for `05-rbac.yaml`,
  updated `docs/solidstats-memory.md` operator sequence, and
  `k8s/memory/40-backup.yaml` projected-token quiesce/recovery contract; its
  `spec.suspend: false` transition occurs only after all gates pass.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement behavior-based recovery and measured API-control gates</name>
  <files>scripts/cutover-solidstats-memory.sh, scripts/validate-phase-21.py, tests/test-memory-cutover-contract.py, docs/solidstats-memory.md, .github/workflows/deploy-memory.yml</files>
  <read_first>
    <file>.planning/phases/21-restore-cutover-recovery/21-03-SUMMARY.md</file>
    <file>.planning/phases/21-restore-cutover-recovery/21-CUTOVER-EVIDENCE.json</file>
    <file>scripts/probe-solidstats-memory.py</file>
    <file>scripts/cutover-solidstats-memory.sh</file>
    <file>scripts/validate-phase-21.py</file>
    <file>k8s/memory/00-namespace.yaml</file>
    <file>k8s/memory/01-ci-rbac.yaml</file>
    <file>k8s/memory/20-mempalace.yaml</file>
    <file>k8s/memory/30-network-policy.yaml</file>
    <file>k8s/memory/40-backup.yaml</file>
    <file>scripts/render-memory-manifests.py</file>
    <file>scripts/validate-memory-manifests.py</file>
    <file>tests/test-memory-runtime-contract.py</file>
    <file>.github/workflows/deploy-memory.yml</file>
  </read_first>
  <behavior>
    - "Ready workloads with failed auth, alias, recall, capture, or read-after-write cannot produce recovery success."
    - "An unchanged boot identity, expired reconnect deadline, unbound PVC, unavailable workload, inactive nginx, or failed behavior probe rejects reboot recovery."
    - "The actual rollback and forward paths both invoke the complete behavior matrix and preserve candidate/prior collections."
    - "Legacy removal and backup-schedule activation are illegal before every predecessor gate is green and digest-bound."
    - "API-path measurement discovers both the live Kubernetes Service destination and ready endpoint destination, tests each under the installed kube-router policy path with a fresh pod, and never writes either IP into repository source."
    - "The final selected API policy passes three fresh-pod checks: backup label plus backup identity succeeds on a read-only scale GET; the same identity with another label is network-denied; the backup label with an unprivileged identity is authorization-denied."
    - "A missing, ambiguous, stale, or negative API-path measurement keeps the CronJob suspended and prevents any scale-to-zero request."
    - "Metadata source-before, source-after, and extracted-archive canonical digests must match; every success or failure restores exact replicas and proves capture/read-after-write before schedule eligibility."
  </behavior>
  <action>Add failing tests for readiness-only false positives, unchanged boot identity, reconnect timeout, restart ordering, rollback-only success, forward-only success, metadata drift during archive, extracted-archive mismatch, quiesce timeout, another PVC writer, upload/download failure, stuck replica restoration, failed post-resume capture, early legacy removal, early schedule activation, unmeasured API egress, ambiguous Service-versus-endpoint results, stale bindings, and each positive/negative access failure. Implement ordered MemPalace then Qdrant restart stages by calling the existing Plan 21-03 full probe after each, plus one bounded host reboot that records only changed-boot/workload/PVC/nginx booleans before that same probe. Add `prove-backup-api-access`: discover the `kubernetes.default` ClusterIP/service port and ready EndpointSlice addresses/ports with operator credentials; construct only `/32` Service and endpoint candidates; apply one candidate at a time from `05-rbac.yaml` in restrictive temporary storage; and use a fresh pod per attempt so conntrack cannot create a false pass. The positive pod must carry the exact CronJob label and `solidstats-memory-backup` ServiceAccount and perform an authenticated read-only GET of `deployments/mempalace/scale`; a fresh pod with the same ServiceAccount but a nonmatching label must fail transport, while an exact-label pod with an unprivileged ServiceAccount must reach the API but receive authorization denial. Prefer the stable Service candidate when it passes; accept a direct endpoint candidate only when the Service candidate fails and exactly one ready endpoint passes. Reapply the selected exact policy and repeat all three checks; retain only mode/digest/boolean evidence, never the IP or token. Implement `prove-backup-consistency` so it refuses to scale before that evidence, runs a one-shot from the still-suspended exact CronJob template, requires zero writers/PVC consumers, requires equal source-before/source-after/extracted-archive digests plus upload inventory and downloaded checksums, and executes the recorded exact-replica restoration plus the full availability/capture/read-after-write matrix on success and every failure. Exercise real reverse-order rollback, verify the legacy path, reapply the exact forward chain, and verify it again. Extend seal validation with both API-control and consistency predecessors. Update the runbook with measurement, quiescence, failure restoration, and rerun commands, and update the memory deploy workflow so `05-rbac.yaml` is copied for validation but excluded with `00-namespace.yaml` and `01-ci-rbac.yaml` from CI server dry-run/apply. [src: .planning/phases/21-restore-cutover-recovery/21-CONTEXT.md#Cutover-and-Recovery] [src: .planning/phases/21-restore-cutover-recovery/21-RESEARCH.md#Kubernetes-API-control-path-for-recurring-quiescence] [src: .planning/phases/21-restore-cutover-recovery/21-RESEARCH.md#Steady-state-MemPalace-metadata-consistency-decision] [src: k8s/memory/30-network-policy.yaml#default-deny-all] [src: .github/workflows/deploy-memory.yml#Render-validate-and-dry-run-memory-resources]</action>
  <acceptance_criteria>
    - "Every false-positive recovery fixture fails and the complete synthetic recovery chain passes."
    - "Rollback and forward use the same production functions as live execution; no test-only mutation path exists."
    - "Seal validation requires every declared predecessor field and rejects missing or false recovery/consistency checks."
    - "Offline fixtures prove Service and endpoint translation outcomes are measured, not assumed, and no committed artifact contains an API destination IP."
    - "The three final fresh-pod access checks are all required before backup consistency can start."
    - "CI tests prove `05-rbac.yaml` cannot enter the workload apply list."
    - "Every consistency/quiesce/package failure runs the recorded restoration path, keeps both schedules suspended, and records whether writes resumed."
  </acceptance_criteria>
  <verify>
    <automated>timeout 30s python3 -m unittest tests/test-memory-cutover-contract.py &amp;&amp; timeout 20s bash -n scripts/cutover-solidstats-memory.sh &amp;&amp; timeout 20s python3 -m py_compile scripts/validate-phase-21.py &amp;&amp; timeout 30s python3 scripts/validate-phase-21.py --check-chain .planning/phases/21-restore-cutover-recovery &amp;&amp; timeout 30s python3 -m unittest tests/test-memory-runtime-contract.py</automated>
  </verify>
  <done>Recovery and seal code rejects readiness-only evidence, the measured API-control predecessor is fail-closed and value-free, and the same complete behavior matrix runs after every disruption and direction change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make least-privilege quiescence an executable recurring template</name>
  <files>k8s/memory/05-rbac.yaml, k8s/memory/40-backup.yaml, scripts/render-memory-manifests.py, scripts/validate-memory-manifests.py, tests/test-memory-runtime-contract.py</files>
  <read_first>
    <file>.planning/phases/21-restore-cutover-recovery/21-RESEARCH.md</file>
    <file>.planning/phases/21-restore-cutover-recovery/21-03-SUMMARY.md</file>
    <file>k8s/memory/00-namespace.yaml</file>
    <file>k8s/memory/01-ci-rbac.yaml</file>
    <file>k8s/memory/20-mempalace.yaml</file>
    <file>k8s/memory/30-network-policy.yaml</file>
    <file>k8s/memory/40-backup.yaml</file>
    <file>scripts/render-memory-manifests.py</file>
    <file>scripts/validate-memory-manifests.py</file>
    <file>tests/test-memory-runtime-contract.py</file>
    <file>.github/workflows/deploy-memory.yml</file>
  </read_first>
  <behavior>
    - "The exact recurring template records the MemPalace replica/generation/pod pre-state, scales only that writer to zero, and waits until no selected writer remains before reading `/data/palace`."
    - "Metadata source-before, source-after, and extracted-archive canonical digests must match; any drift, partial archive, or package/checksum failure rejects the backup and schedule transition."
    - "Success and every injected failure restore the exact recorded replica count, wait for availability, and require the full matrix including capture/read-after-write before writes are considered resumed."
    - "The checked-in and live CronJobs remain suspended until one operator-authorized Job derived from the exact template passes consistency, package, restoration, and write-resumption gates."
    - "Backup RBAC can get only the `mempalace` Deployment, get/patch only its scale subresource, and list only namespace pods needed to prove zero writers/PVC consumers; wildcard resources, names, verbs, or cluster scope fail validation."
    - "The backup pod keeps global automount disabled and receives only an explicitly projected, rotating ServiceAccount token and cluster CA at a read-only mount for authenticated API calls."
    - "The API NetworkPolicy selects only `app.kubernetes.io/name: solidstats-memory-backup`, contains exactly one runtime-rendered `/32` destination and one observed TCP port, and remains operator-applied outside the CI identity."
    - "Renderer, validator, tests, and workflow agree on the exact eight-file source inventory and the three-file operator-bootstrap exclusion set."
  </behavior>
  <action>Start with failing manifest/runtime tests for missing or broad scale RBAC, missing `resourceNames: [mempalace]`, cluster scope, static/automatic long-lived credentials, missing token/CA projection, broad API CIDRs, non-`/32` destinations, extra ports, non-backup selectors, unresolved workload markers, a missing `05-rbac.yaml` inventory entry, and CI inclusion of operator bootstrap. Create `05-rbac.yaml` as operator bootstrap: a namespace Role may get only Deployment `mempalace`, get/patch only `deployments/scale` for resource name `mempalace`, and list pods for the label/PVC-consumer zero-writer proof; its RoleBinding names only `solidstats-memory-backup`. In the same file define `allow-backup-to-kubernetes-api`, selected only by the exact backup label, with one source placeholder for a runtime `/32` destination and one source placeholder for the measured TCP port. Do not commit a Service ClusterIP, endpoint IP, node CIDR, Service CIDR, or all-destination rule. Extend the CronJob with an explicit projected ServiceAccount token and `kube-root-ca.crt` projection while retaining `automountServiceAccountToken: false`; use the injected `KUBERNETES_SERVICE_HOST`/HTTPS port for API requests, the projected CA/token for TLS/auth, read-only scale GET before mutation, scale patch only after the Task 1 measurement evidence passes, and the existing guaranteed exact-replica restoration path. Update renderer inventory to carry `05-rbac.yaml` byte-for-byte, and update validator source mode to allow the two API-binding markers only in that operator-bootstrap resource while strict runtime validation requires an IPv4 or IPv6 host-prefix CIDR plus one numeric measured port and the exact Role/RoleBinding/selector shape. Update runtime tests for the exact manifest inventory, every RBAC/NetworkPolicy/token mutation, and the workflow's exclusion of `00-namespace.yaml`, `01-ci-rbac.yaml`, and `05-rbac.yaml`. Keep `spec.suspend: true` throughout this task. [src: .planning/phases/21-restore-cutover-recovery/21-RESEARCH.md#Kubernetes-API-control-path-for-recurring-quiescence] [src: k8s/memory/40-backup.yaml#solidstats-memory-backup] [src: k8s/memory/30-network-policy.yaml#default-deny-all] [src: k8s/memory/01-ci-rbac.yaml#memory-ci-deployer]</action>
  <acceptance_criteria>
    - "Offline tests prove exact namespace/resource/name/verb RBAC, explicit rotating credentials, exact API policy shape, and every broadening mutation fails closed."
    - "No repository file contains a resolved Kubernetes API destination; only the operator-rendered temporary/live `05-rbac.yaml` does."
    - "The generated one-shot Job and recurring CronJob share one backup command/template contract, and the source manifest stays suspended before live proof."
    - "CI can validate `05-rbac.yaml` source shape but cannot dry-run or apply its Role, RoleBinding, or API-egress policy."
  </acceptance_criteria>
  <verify>
    <automated>timeout 30s python3 -m unittest tests/test-memory-runtime-contract.py &amp;&amp; timeout 30s python3 scripts/validate-memory-manifests.py --allow-operator-placeholders &amp;&amp; timeout 20s python3 -m py_compile scripts/render-memory-manifests.py scripts/validate-memory-manifests.py</automated>
  </verify>
  <done>The still-suspended recurring template has an exact authenticated and network-reachable scale path without a committed cluster IP, and every RBAC, token, policy, inventory, and CI boundary is contract-tested before live proof.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Authorize consistency proof and recovery disruptions, then seal cutover</name>
  <files>k8s/memory/05-rbac.yaml, k8s/memory/40-backup.yaml, .planning/phases/21-restore-cutover-recovery/21-RECOVERY-EVIDENCE.json, .planning/phases/21-restore-cutover-recovery/21-CUTOVER-SEAL.json</files>
  <read_first>
    <file>.planning/phases/21-restore-cutover-recovery/21-CONTEXT.md</file>
    <file>.planning/phases/21-restore-cutover-recovery/21-03-SUMMARY.md</file>
    <file>.planning/phases/21-restore-cutover-recovery/21-CUTOVER-EVIDENCE.json</file>
    <file>scripts/cutover-solidstats-memory.sh</file>
    <file>docs/solidstats-memory.md</file>
    <file>k8s/memory/00-namespace.yaml</file>
    <file>k8s/memory/01-ci-rbac.yaml</file>
    <file>k8s/memory/20-mempalace.yaml</file>
    <file>k8s/memory/30-network-policy.yaml</file>
    <file>k8s/memory/05-rbac.yaml</file>
    <file>k8s/memory/40-backup.yaml</file>
  </read_first>
  <precondition>The validated Plan 21-03 evidence is at CLIENT_ADDED, the public service and exact new client are healthy, the exact legacy registration plus rollback journal remain available, and the source/live backup CronJobs are still suspended.</precondition>
  <reversibility rating="costly">Restart, reboot, live rollback/forward, schedule activation, and legacy-client removal cross host, cluster, object storage, nginx, and machine-local state; exact pre-state and recovery commands are retained.</reversibility>
  <what-built>Behavior-gated restart/reboot recovery, a measured and least-privilege backup API path, an exact-template steady-state consistency proof with guaranteed writer restoration, a real rollback/forward exercise, delayed schedule activation, and exact legacy-client retirement.</what-built>
  <action>After explicit operator approval, restart MemPalace and Qdrant one at a time with the full matrix after each. Before any scale mutation, run `prove-backup-api-access`: discover the live Service and ready endpoint candidates, trial each exact `/32`/port policy with a fresh pod, select only the measured kube-router path, render/apply the operator-bootstrap `05-rbac.yaml`, and require the final positive backup-identity GET plus network-negative and RBAC-negative controls. If no exact candidate passes, more than one endpoint would need direct allowance, a negative control passes, or the final recheck differs, keep the CronJob suspended and stop without scaling. While the new client and write path are live, run `prove-backup-consistency` from the exact still-suspended CronJob template: record the writer pre-state, scale only MemPalace to zero, prove no writer/PVC consumer remains, require equal canonical metadata source-before/source-after/extracted-archive digests, complete upload inventory and downloaded checksum verification, restore the exact replica state, and require availability plus capture/read-after-write resumption. On any error, execute the recorded replica-restoration command, preserve diagnostics, confirm whether writes recovered, keep both CronJobs suspended, and stop without sealing. After the API-control and consistency predecessors pass, record pre-reboot aggregate state, issue one authorized VPS reboot, reconnect within the declared deadline, prove changed boot identity plus node/PVC/workload/nginx recovery, and rerun the complete private/public/auth/MCP matrix. Execute the real reverse-order rollback and prove the legacy path, then reapply the exact forward chain and prove the new path again. Only after all API-control, consistency, recovery, rollback, and forward checks pass, run `activate-backup-schedule` to change the checked-in and live backup CronJob from suspended to scheduled while retaining concurrency-forbid, remove or disable the exact discovered legacy SolidStats registration, prove `mempalace` is absent when it was the legacy name, prove unrelated registrations are unchanged, and live-call `solidstats_memory`. Validate the aggregate recovery envelope and predecessor-bound seal before accepting the phase. [src: .planning/phases/21-restore-cutover-recovery/21-CONTEXT.md#Cutover-and-Recovery] [src: .planning/phases/21-restore-cutover-recovery/21-RESEARCH.md#Kubernetes-API-control-path-for-recurring-quiescence] [src: .planning/phases/21-restore-cutover-recovery/21-RESEARCH.md#Steady-state-MemPalace-metadata-consistency-decision] [src: .planning/phases/21-restore-cutover-recovery/21-RESEARCH.md#Exact-Verification-Matrix]</action>
  <how-to-verify>
    1. Confirm each ordered process restart changed the expected process identity and passed the complete matrix before the next restart.
    2. Confirm the selected API destination/port was discovered live, not committed; the exact backup pod passed an authenticated scale GET; the wrong-label same-identity pod was network-denied; and the exact-label unprivileged identity was RBAC-denied.
    3. Confirm the exact-template backup observed zero writers, matched metadata source-before/source-after/extracted-archive digests, created/downloaded/checksum-verified the package, restored exact replicas, and passed capture/read-after-write after resumption.
    4. Confirm boot identity changed and node, PVC, workloads, nginx, private Qdrant, public auth, and full MCP behavior recovered after reboot.
    5. Confirm live rollback passed, the exact forward replay passed, and no candidate/prior collection or snapshot was deleted.
    6. Confirm the recurring backup is active with concurrency-forbid, the exact legacy SolidStats client is absent/disabled, unrelated clients are unchanged, and `solidstats_memory` completes a live MCP call.
  </how-to-verify>
  <acceptance_criteria>
    - "Restart, steady-state-consistent backup, writer resumption, reboot, rollback, and forward recovery all pass the full matrix."
    - "The measured exact API egress, projected identity, and least-privilege scale RBAC pass positive, network-negative, and authorization-negative live controls before quiescence."
    - "The backup schedule becomes active only after exact-template consistency and recovery evidence are green."
    - "The exact legacy client is absent/disabled only after rollback is sealed, and `solidstats_memory` remains live."
    - "Both final JSON artifacts pass recursive privacy and predecessor-digest validation."
  </acceptance_criteria>
  <verify>
    <automated>timeout 3600s bash scripts/cutover-solidstats-memory.sh restart-recovery --resume-run &amp;&amp; timeout 1800s bash scripts/cutover-solidstats-memory.sh prove-backup-api-access --resume-run &amp;&amp; timeout 3600s bash scripts/cutover-solidstats-memory.sh prove-backup-consistency --resume-run &amp;&amp; timeout 5400s bash scripts/cutover-solidstats-memory.sh reboot-recovery --resume-run --reconnect-timeout 900 &amp;&amp; timeout 3600s bash scripts/cutover-solidstats-memory.sh exercise-rollback --resume-run &amp;&amp; timeout 1800s bash scripts/cutover-solidstats-memory.sh activate-backup-schedule --resume-run &amp;&amp; timeout 1800s bash scripts/cutover-solidstats-memory.sh seal --resume-run &amp;&amp; timeout 30s python3 scripts/validate-phase-21.py --evidence .planning/phases/21-restore-cutover-recovery/21-RECOVERY-EVIDENCE.json &amp;&amp; timeout 30s python3 scripts/validate-phase-21.py --evidence .planning/phases/21-restore-cutover-recovery/21-CUTOVER-SEAL.json &amp;&amp; timeout 10s codex mcp get solidstats_memory</automated>
  </verify>
  <resume-signal>Type "approved" to authorize writer quiescence/restoration, process restarts, one VPS reboot, live rollback/forward, schedule activation, and exact legacy-client retirement, or describe the blocking evidence.</resume-signal>
  <done>The exact API control path, steady-state consistency, and write resumption are proven; recovery is behavior-proven; rollback and forward are exercised; backups are active; only `solidstats_memory` remains for SolidStats; and the Phase 21 seal is valid.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Provenance-bound isolated restore, reversible `/solidstats/mcp` and `solidstats_memory` cutover, restart/reboot recovery | 21-01..21-04 | COVERED | Tracer, drill, cutover, and recovery seal form one dependency chain. |
| REQ | ISO-01 | Exact `solidstats_memory`; no legacy `mempalace` alias after seal | 21-03, 21-04 | COVERED | Add only after live pass; remove exact legacy only after rollback/forward proof. |
| REQ | ISO-03 | Qdrant private; only authenticated MemPalace public | 21-01, 21-03, 21-04 | COVERED | Reciprocal policy, negative public probes, and recovery re-probes. |
| REQ | OPS-02 | Snapshot + metadata + manifest + checksums under accepted prefix | 21-02, 21-04 | COVERED | One isolated-drill package plus one fresh recovery-cycle package. |
| REQ | OPS-03 | Isolated restore never targets active collection | 21-01, 21-02, 21-03 | COVERED | Synthetic refusal, three-way absence proof, and compare-and-switch only after parity. |
| REQ | OPS-05 | Auth, MCP schema, recall/miss/archive/capture, read-after-write, restart, reboot | 21-03, 21-04 | COVERED | Same real behavior matrix at cutover and every recovery boundary. |
| RESEARCH | W0-NETPOL | Reciprocal MemPalace egress to Qdrant | 21-01 | COVERED | Exact same-namespace TCP/6333 policy plus validator/tests. |
| RESEARCH | W0-PYTHON | Python 3.14.4 Phase 20 suite failure | 21-01 | COVERED | Repairs leaking observer clock fixture; no arbitrary runtime pin. |
| RESEARCH | CONTROL | Fail-closed staged state machine and value-free evidence | 21-01 | COVERED | Production tracer/schema precedes all external adapters. |
| RESEARCH | BACKUP-RESTORE | Backup, target absence, recovery priority, capacity, exact parity, alias compatibility | 21-02 | COVERED | Both flagged assumptions have live resolution gates. |
| RESEARCH | STEADY-METADATA | Recurring `/data/palace` backup must quiesce writers, prove before/after/archive consistency, restore exact replicas, and prove writes resumed | 21-04 | COVERED | A dedicated auto task builds the protocol; the operator checkpoint must pass it before activation. |
| RESEARCH | BACKUP-API-CONTROL | Recurring quiescence needs authenticated scale RBAC plus exact kube-router API egress without a committed cluster IP | 21-04 | COVERED | Operator-bootstrap RBAC/policy, projected token, Service-versus-endpoint measurement, and positive/network-negative/RBAC-negative live gates precede scale and activation. |
| RESEARCH | CUTOVER | Alias CAS, nginx/auth/MCP/client, partial-SSH rollback | 21-03 | COVERED | One batched operator checkpoint and exact reverse-order rollback. |
| RESEARCH | RECOVERY | Ordered restarts, steady-state-consistent backup, reboot, rollback/forward, seal | 21-04 | COVERED | Full behavior matrix and metadata consistency, not readiness or checksum alone, gate acceptance. |
| RESEARCH | PATTERNS | Immutable evidence, physical-plus-alias restore, batched checkpoints, behavior probes | 21-01..21-04 | COVERED | Each pattern is a named artifact, gate, or critical link in the ordered chain. |
| RESEARCH | STACK | Standard-library Python/Bash plus native Qdrant, MCP, kubectl, nginx, S3, and Codex surfaces | 21-01..21-04 | COVERED | No package install or hand-rolled protocol replacement is planned. |
| RESEARCH | PITFALLS | Reciprocal policy, absent-target restore, snapshot priority, external alias state, live-metadata archive drift, bounded reconnect, delayed legacy removal, safe evidence | 21-01..21-04 | COVERED | Every listed pitfall has an automated refusal or operator acceptance gate. |
| RESEARCH | VALIDATION | Fast offline contract suite plus live stage and recovery evidence | 21-01..21-04 | COVERED | Wave 0 makes the test harness trustworthy before live verification. |
| RESEARCH | SECURITY | ASVS L2 threat-driven gates | 21-01..21-04 | COVERED | Every plan carries a severity, disposition, and concrete mitigation register. |
| CONTEXT | Restore/1 | Recompute all Phase 20 bindings before private read or target creation | 21-01, 21-02 | COVERED | PREPARED and live preflight both enforce it. |
| CONTEXT | Restore/2 | Restore only into an isolated absent target | 21-02 | COVERED | Empty/missing/collision/ordering/concurrency cases are explicit truths. |
| CONTEXT | Restore/3 | Preserve reused-vector and strict ANN contract plus zero-tolerance exact parity | 21-02 | COVERED | Reuses Phase 20 parity implementation and acceptance rule. |
| CONTEXT | Cutover/1 | Qdrant private; authenticated MemPalace only | 21-01, 21-03, 21-04 | COVERED | Policy and negative reachability are rerun after recovery. |
| CONTEXT | Cutover/2 | Operator-owned reversible state change with backup, probes, rollback | 21-02, 21-03 | COVERED | Explicit blocking-human checkpoints precede mutations. |
| CONTEXT | Cutover/3 | Process restart and VPS reboot proof | 21-04 | COVERED | Both include private and public behavior checks. |
| CONTEXT | Cutover/4 | Add new client after live pass; remove legacy after rollback seal | 21-03, 21-04 | COVERED | Exact ordering is machine-tested and live-verified. |
| CONTEXT | Deferred | Archive distillation | EXCLUDED | EXCLUDED | Explicitly Phase 22. |

## Prohibition Recall

- Kept as flagged value/safety prohibitions: active-target overwrite,
  sensitive/private evidence leakage, and recovery/cutover acceptance before
  the complete behavior-plus-rollback chain.
- Routine engineering prohibitions are enforced by task tests, syntax checks,
  Markdown lint, and `git diff --check`, not duplicated in `must_haves`.
- Canonical authentication, network exposure, session, input-validation, and
  data-protection controls are tracked in each ASVS L2 threat register, not
  duplicated as recalled value prohibitions.

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Recovery orchestrator -> Kubernetes/host reboot | Disruptive operations can lose the active connection and return partial state. |
| Kubernetes readiness -> application behavior | Ready status is insufficient evidence for alias, auth, MCP, or data recovery. |
| Backup CronJob -> MemPalace Deployment/PVC | The backup identity temporarily controls the sole writer and reads private metadata; exact RBAC, consistency, and restoration gates are required. |
| Backup pod -> Kubernetes API server | A projected workload identity and kube-router-translated network path can scale the sole writer only after exact RBAC, destination, port, and negative controls pass. |
| Rollback journal -> reverse and forward mutations | Both directions must consume the exact recorded state, not reconstructed guesses. |
| Machine-local client registry -> final seal | Exact legacy removal must not affect unrelated clients or precede recovery proof. |

## STRIDE Threat Register — ASVS L2

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-21-03 | Information disclosure | Recovery and seal evidence | critical | mitigate | Aggregate-only schemas, recursive rejection, no raw bodies/paths/identifiers, and predecessor-digest binding. |
| T-21-06 | Denial of service | Restart/reboot SSH batch | high | mitigate | One disruption at a time, bounded reconnect, changed-boot proof, staged waits, local journal, and executable rollback. |
| T-21-07 | Spoofing | Recovery success | critical | mitigate | Require changed process/boot state and the complete private/public/auth/MCP behavior matrix after every disruption. |
| T-21-13 | Tampering | Rollback/forward state | high | mitigate | Use exact recorded alias/nginx/client/workload state in both directions, read back each mutation, and preserve candidate/prior data. |
| T-21-14 | Elevation of privilege | Legacy client removal | medium | mitigate | Exact discovered name only, unrelated-registration before/after digest, removal after sealed rollback, and live new-client call. |
| T-21-15 | Repudiation | Backup schedule activation | high | mitigate | Chain exact-template quiescence, before/after/archive equality, package revalidation, writer restoration, and capture/read-after-write evidence into the seal; retain concurrency-forbid and verify checked-in/live schedule parity. |
| T-21-16 | Elevation of privilege | Backup writer-quiesce identity | high | mitigate | Use an explicit projected rotating token; restrict Role/RoleBinding to named MemPalace deployment/scale operations and minimum pod-state reads; reject wildcards/cluster scope; record exact replica pre-state; and execute restoration on every exit path. |
| T-21-17 | Information disclosure | Backup-to-API NetworkPolicy | high | mitigate | Keep default deny; select only the backup pod label; render one measured `/32` plus observed TCP port in operator-only `05-rbac.yaml`; reject Service/node CIDRs, extra destinations, and broad ports in validator/tests. |
| T-21-18 | Spoofing | kube-router API-path measurement | high | mitigate | Trial Service and ready-endpoint candidates with fresh pods, then require final authenticated positive, wrong-label network-negative, and unprivileged-identity RBAC-negative checks before quiescence or schedule activation. |

</threat_model>

<verification>
1. Run all Phase 21 offline tests, syntax checks, validators, and chain checks.
2. At the checkpoint, discover and measure the kube-router API destination,
   render the operator-only exact policy, and pass the positive,
   network-negative, and RBAC-negative fresh-pod controls.
3. Prove ordered restarts and one exact-template backup with
   zero writers, before/after/archive digest equality, exact replica recovery,
   and capture/read-after-write resumption.
4. Prove changed-boot VPS recovery with the complete behavior matrix.
5. Exercise the real rollback and exact forward replay; validate both.
6. Activate the recurring backup, retire only the exact legacy client, and
   validate both final evidence files plus the live new client.
</verification>

<success_criteria>

- Process restart and VPS reboot recovery pass the complete behavior matrix.
- The backup API path is least-privilege, dynamically measured under kube-router, and live-proven without a committed environment IP.
- A fresh post-restart backup is consistency- and checksum-verified, exact writer state and writes recover, and only then the recurring schedule is active.
- Live rollback and exact forward replay both pass without deleting retained data.
- The legacy SolidStats client is absent/disabled and `solidstats_memory` remains live.
- The final seal is value-free, predecessor-bound, and valid.
</success_criteria>

<output>
Create `.planning/phases/21-restore-cutover-recovery/21-04-SUMMARY.md` when done.
</output>
