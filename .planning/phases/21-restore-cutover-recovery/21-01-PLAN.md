---
phase: 21-restore-cutover-recovery
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/validate-phase-21.py
  - tests/test-memory-cutover-contract.py
  - k8s/memory/30-network-policy.yaml
  - scripts/validate-memory-manifests.py
  - tests/test-memory-runtime-contract.py
autonomous: true
requirements:
  - ISO-01
  - ISO-03
  - OPS-02
  - OPS-03
  - OPS-05
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
premises:
  - claim: The observer contract tests directly replace the shared standard-library monotonic clock and leak that replacement into the later Phase 20 inventory suite.
    src: tests/test-memory-runtime-contract.py#MemoryObserverContractTests
    verify: rg -n 'observer\["time"\]\.monotonic' tests/test-memory-runtime-contract.py
  - claim: The checked-in default-deny boundary permits MemPalace ingress to Qdrant but has no reciprocal MemPalace-selected egress policy.
    src: k8s/memory/30-network-policy.yaml#allow-mempalace-to-qdrant
    verify: sed -n '30,55p' k8s/memory/30-network-policy.yaml | rg 'policyTypes: \[Ingress\]'
carried_forward:
  - claim: Phase 20 sealed a passing digest-bound handoff only after exact field, identifier, metadata, timestamp, vector, exclusion, and strict ANN-equivalence checks passed.
    src: .planning/phases/20-local-corpus-migration/20-06-SUMMARY.md#Phase-21-Boundary
must_haves:
  truths:
    - "A complete synthetic PREPARED-to-SEALED chain validates end to end through the same evidence schemas and transition rules later live stages must emit."
    - "Missing, null, empty, reordered, repeated-with-different-content, and out-of-sequence stage evidence is rejected before any external action is eligible."
    - "A repeated transition with the same run identity, prior digest, and payload digest is accepted idempotently; a colliding transition with unequal content is rejected."
    - "A second run cannot acquire an occupied stage lock, while an interrupted exact run can resume only from its last digest-verified stage."
    - "MemPalace has exact same-namespace egress to Qdrant TCP/6333 and Qdrant retains the reciprocal exact ingress under default deny."
    - "The Phase 20 policy, migration, and memory runtime suites pass under the current Python 3.14.4 interpreter without selecting another runtime."
    - "Every public Phase 21 evidence payload is allowlisted and recursively rejects secrets, corpus content, private paths, collection identifiers, vectors, metadata values, and raw response bodies."
  artifacts:
    - path: scripts/validate-phase-21.py
      provides: "Phase 21 evidence, transition, provenance-binding, and privacy validator"
    - path: tests/test-memory-cutover-contract.py
      provides: "Synthetic end-to-end state-machine and failure-injection coverage"
    - path: k8s/memory/30-network-policy.yaml
      provides: "Reciprocal MemPalace-to-Qdrant TCP/6333 egress"
    - path: scripts/validate-memory-manifests.py
      provides: "Exact reciprocal NetworkPolicy shape validation"
    - path: tests/test-memory-runtime-contract.py
      provides: "Clock-isolated observer tests and reciprocal policy regression coverage"
  key_links:
    - from: tests/test-memory-cutover-contract.py
      to: scripts/validate-phase-21.py
      via: "complete synthetic transition chain and adversarial evidence mutations"
      pattern: "validate_(evidence_envelope|transition_chain|value_free_payload)"
    - from: scripts/validate-memory-manifests.py
      to: k8s/memory/30-network-policy.yaml
      via: "exact policy selector, direction, destination, protocol, and port comparison"
      pattern: "allow-mempalace.*qdrant"
    - from: tests/test-memory-runtime-contract.py
      to: tests/test-solidstats-memory-migration.py
      via: "restored monotonic clock state before the combined suite advances"
      pattern: "patch.object.*monotonic"
  prohibitions:
    - "[flagged-unverified] No existing collection or alias may be overwritten, renamed, or deleted by restore or cutover."
    - "[flagged-unverified] No corpus-bearing value, private artifact path, token, collection identifier, vector, metadata value, or raw response body may enter committed evidence."
    - "[flagged-unverified] No recovery state may be accepted from workload readiness alone."
---

<!-- markdownlint-disable MD013 MD033 MD041 -->

<objective>
Create the production evidence contract and a tracer that proves the complete
restore-to-recovery state chain offline, then close the two blocker-quality
repository defects before any retained private artifact or external runtime is
eligible for use.

Purpose: Make every later operator mutation consume one tested, fail-closed
state machine and restore the trusted Phase 20 provenance gate.
Output: Phase 21 validator and tests, reciprocal MemPalace/Qdrant policy, and a
green combined offline suite.
</objective>

<execution_context>
@/home/afgan0r/.codex/gsd-core/workflows/execute-plan.md
@/home/afgan0r/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-restore-cutover-recovery/21-CONTEXT.md
@.planning/phases/21-restore-cutover-recovery/21-RESEARCH.md
@.planning/phases/21-restore-cutover-recovery/21-VALIDATION.md
@.planning/phases/21-restore-cutover-recovery/21-PATTERNS.md
@.planning/phases/21-restore-cutover-recovery/COVERAGE.md
@.planning/phases/20-local-corpus-migration/20-PHASE21-HANDOFF.json
@.planning/phases/20-local-corpus-migration/20-06-SUMMARY.md
@k8s/memory/30-network-policy.yaml
@scripts/validate-memory-manifests.py
@tests/test-memory-runtime-contract.py

<interfaces>
Existing interfaces to preserve:
- `scripts/validate-memory-manifests.py`: `ValidationError`, `require()`,
  `require_network_contract()`, and the single `main()` error boundary.
- `tests/test-memory-runtime-contract.py`:
  `MemoryObserverContractTests.load_observer()` and
  `memory_network_policies()` parse checked-in source rather than rendered
  test doubles.
- Phase 20 handoff schema:
  `solidstats-memory-phase21-handoff/v1`; consume only its allowlisted public
  digests, versions, aggregate count, and required-check names.
</interfaces>
</context>

## Artifacts this phase produces

- New file `scripts/validate-phase-21.py` with functions
  `canonical_json_bytes()`, `sha256_file()`,
  `validate_evidence_envelope()`, `validate_transition_chain()`,
  `validate_value_free_payload()`, and `main()`.
- New file `tests/test-memory-cutover-contract.py` with class
  `MemoryCutoverContractTests` and a complete synthetic state-chain fixture.
- New CLI flags `--handoff`, `--evidence`, and `--check-chain` on
  `validate-phase-21.py`.
- New schemas `solidstats-memory-phase21-evidence/v1` and
  `solidstats-memory-phase21-stage/v1` with fields `schema`, `run_id`, `stage`,
  `prior_evidence_sha256`, `input_digests`, `checks`, `started_at`,
  `completed_at`, and `verdict`.
- New NetworkPolicy `allow-mempalace-qdrant-egress`; no new Kubernetes port,
  namespace, Service, account, or external dependency.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one complete value-free restore-to-recovery state chain</name>
  <files>scripts/validate-phase-21.py, tests/test-memory-cutover-contract.py</files>
  <read_first>
    <file>.planning/phases/21-restore-cutover-recovery/21-CONTEXT.md</file>
    <file>.planning/phases/21-restore-cutover-recovery/21-RESEARCH.md</file>
    <file>.planning/phases/21-restore-cutover-recovery/COVERAGE.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-PHASE21-HANDOFF.json</file>
    <file>.planning/phases/20-local-corpus-migration/20-PARITY-REPORT.json</file>
    <file>scripts/verify-solidstats-memory-parity.py</file>
  </read_first>
  <behavior>
    - "A synthetic chain advances only PREPARED -> STAGED -> RESTORE_PROVEN -> PRIVATE_LIVE -> DATA_SWITCHED -> PUBLIC_LIVE -> CLIENT_ADDED -> RECOVERY_PROVEN -> SEALED."
    - "The same transition replay is idempotent only when run identity, prior digest, and canonical payload digest are equal."
    - "Null, empty, missing, out-of-order, unequal-collision, and concurrent-run evidence fails closed."
    - "The validator accepts aggregate booleans, counts, timestamps, and SHA-256 digests while rejecting every private or corpus-bearing category from the parent handoff."
  </behavior>
  <action>Start with a failing end-to-end test that builds one complete synthetic transition chain, mutates each boundary, and proves a final sealed verdict. Implement exact schema allowlists, canonical JSON hashing, prior-evidence chaining, a per-run stage lock, legal-transition ordering, exact idempotent replay, collision refusal, and recursive privacy validation in `scripts/validate-phase-21.py`. Bind PREPARED to recomputed public Phase 20 handoff/parity digests before any later stage can be represented. The tracer is production evidence validation used unchanged by later plans; it performs no network, Kubernetes, Qdrant, nginx, client, restart, or reboot mutation. [src: .planning/phases/21-restore-cutover-recovery/21-CONTEXT.md#Restore-and-Provenance] [src: .planning/phases/21-restore-cutover-recovery/21-RESEARCH.md#Pattern-1-Immutable-evidence-envelope] [src: .planning/phases/20-local-corpus-migration/20-06-SUMMARY.md#Phase-21-Boundary]</action>
  <acceptance_criteria>
    - "One synthetic happy path reaches SEALED through every declared state."
    - "Every illegal ordering, empty input, unequal replay, lock collision, and private-value mutation fails before a successor evidence file is written."
    - "The tool emits only one pass/fail line; test diagnostics identify fields and indexes, never rejected values."
  </acceptance_criteria>
  <verify>
    <automated>timeout 30s python3 -m unittest tests/test-memory-cutover-contract.py</automated>
  </verify>
  <done>The complete offline tracer passes end to end, and its exact state/evidence contract is ready for the real backup, restore, cutover, and recovery adapters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close reciprocal NetworkPolicy and Python clock-leak blockers</name>
  <files>k8s/memory/30-network-policy.yaml, scripts/validate-memory-manifests.py, tests/test-memory-runtime-contract.py</files>
  <read_first>
    <file>k8s/memory/30-network-policy.yaml</file>
    <file>scripts/validate-memory-manifests.py</file>
    <file>tests/test-memory-runtime-contract.py</file>
    <file>tests/test-solidstats-memory-migration.py</file>
    <file>.agents/skills/kubernetes-specialist/references/networking.md</file>
  </read_first>
  <behavior>
    - "The policy selects only MemPalace for egress, only Qdrant for destination, and only TCP/6333."
    - "Broad selectors, namespace selectors, CIDRs, extra ports, wrong labels, or wrong policy direction are rejected by both validator and mutation tests."
    - "Observer clock fixtures are restored after each test, so the later inventory elapsed-time and drift checks use the real clock."
  </behavior>
  <action>Add the exact reciprocal MemPalace-selected egress policy to Qdrant TCP/6333 under the existing namespace default deny. Extend `require_network_contract()` and mutation-based runtime tests to compare the complete selector, direction, destination, protocol, and port shape. Replace direct assignment to the shared `time` module in both observer tests with a scoped patch that restores `monotonic` on exit; then run the observer tests before the Phase 20 migration tests in the same interpreter to prove the order-dependent failure is gone. Keep the current Python 3.14.4 runtime because the observed defect is test isolation, and select no alternate interpreter or package. [src: k8s/memory/30-network-policy.yaml#default-deny-all] [src: .planning/phases/21-restore-cutover-recovery/21-RESEARCH.md#Critical-Gaps-Before-Live-Work] [src: tests/test-memory-runtime-contract.py#MemoryObserverContractTests]</action>
  <acceptance_criteria>
    - "Source and validator agree on one exact reciprocal policy pair."
    - "The combined runtime, policy, and migration suite passes in the previously failing order under the current interpreter."
    - "No live cluster state or external endpoint is contacted."
  </acceptance_criteria>
  <verify>
    <automated>timeout 30s python3 scripts/validate-memory-manifests.py --allow-operator-placeholders &amp;&amp; timeout 30s python3 -m unittest tests/test-memory-runtime-contract.py &amp;&amp; timeout 90s python3 -m unittest tests/test-memory-runtime-contract.py tests/test-solidstats-memory-policy.py tests/test-solidstats-memory-migration.py tests/test-memory-cutover-contract.py</automated>
  </verify>
  <done>The reciprocal private data path is contract-enforced and the complete trusted offline gate is green on Python 3.14.4.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Phase 20 public handoff -> Phase 21 evidence validator | Untrusted file bytes become authority for later private access. |
| Test process -> standard-library clock | A fixture can corrupt later provenance timing and drift checks. |
| MemPalace pod egress -> Qdrant pod ingress | Both NetworkPolicy directions must permit exactly one private flow. |
| Private runtime output -> committed evidence | Sensitive values must collapse to allowlisted aggregates and digests. |

## STRIDE Threat Register — ASVS L2

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-21-01 | Tampering | Transition validator | high | mitigate | Reject any restore state that lacks target-absence and legal predecessor checks; cover equality and collision mutations in `test-memory-cutover-contract.py`. |
| T-21-02 | Tampering | Phase 20 provenance | high | mitigate | Recompute all public handoff/parity bindings and chain every successor evidence digest before private access becomes eligible. |
| T-21-03 | Information disclosure | Evidence writer | critical | mitigate | Exact field allowlists, recursive rejection, value-free diagnostics, restrictive temporary files, and mutation tests for every prohibited category. |
| T-21-04 | Information disclosure | Qdrant network boundary | high | mitigate | Keep ClusterIP/default deny and enforce the reciprocal MemPalace TCP/6333 pair without namespace, CIDR, wildcard, or extra-port expansion. |
| T-21-08 | Denial of service | Offline test gate | medium | mitigate | Scope and restore the monotonic fixture so combined-suite order cannot exhaust a fake iterator or suppress real snapshot drift. |

</threat_model>

<verification>
1. Run the Phase 21 tracer suite by itself.
2. Run manifest source validation with unresolved operator markers allowed.
3. Run runtime tests before migration tests in one Python process.
4. Compile both Python validators.
5. Run `git diff --check`; no schema push or ORM command applies to this phase.
</verification>

<success_criteria>

- A complete synthetic state chain reaches SEALED and all adversarial transition/evidence mutations fail.
- The exact reciprocal NetworkPolicy is present and contract-tested.
- The full offline gate passes under Python 3.14.4 with no runtime pin.
- No external state was read or mutated beyond offline files and synthetic fixtures.
</success_criteria>

<output>
Create `.planning/phases/21-restore-cutover-recovery/21-01-SUMMARY.md` when done.
</output>
