---
context: phase
phase: 21-restore-cutover-recovery
plan: 2
task: 2
total_tasks: 2
status: paused
last_updated: 2026-08-21T03:51:27+07:00
---

# Phase 21 Plan 02 checkpoint

## Current state

The implementation head is `f94a14c` on branch
`worktree-agent-p21-02-wave1`.
Task 1 remains complete. The missing private operator and an exact-image CLI
defect were fixed before any live mutation:

- `bf61d1a` moves Qdrant readiness behind isolated manifest application.
- `d27578a` aligns the one-shot producer with the accepted package schema and
  deterministic checksum order.
- `d097272` replaces unavailable `tar` calls in the pinned uploader image with
  its available Python `tarfile` implementation.
- `672867a` uploads the controller-local snapshot through Qdrant's multipart
  recovery API instead of passing an invalid node-local `file://` URI.
- `dc6d694` implements the strict 15-operation JSON adapter boundary, bounded
  Kubernetes/Qdrant/S3 commands, private file retention, and observable request
  and denial contracts.
- `f94a14c` runs the pinned MemPalace image through its supported
  `mempalace-mcp` CLI instead of the unsupported `serve` subcommand.

The full memory suite passes 120/120. Read-only cluster checks confirm that the
cluster and existing S3 binding are reachable and namespace
`solidstats-memory` remains absent. The authorized controller preflight has not
run. No Kubernetes, S3, Qdrant, alias, nginx, client, legacy-service, or schedule
mutation occurred.

## Blocking precondition

The durable artifact `phase20-rebuild-20260821` is available through the stable
mode-0600 locator
`/home/afgan0r/.local/share/solidstats-memory/phase20-rebuild-20260821/ARTIFACT-LOCATOR.json`.
All three retained-file digests were recomputed and exactly match the Phase 20
public handoff. Directory and file modes satisfy the retention contract.

The durable operator materialization `phase21-operator-20260821` is available
through the stable mode-0600 locator
`/home/afgan0r/.local/share/solidstats-memory/phase21-operator-20260821/ARTIFACT-LOCATOR.json`.
Its executable is mode 0700, its checkpoint is mode 0600, and all recorded
digests were re-read and verified after materialization.

Live execution is blocked before the single authorized controller preflight.
The exact MemPalace image passes an isolated CLI and environment-contract check,
but its pinned local digest has no pullable registry manifest. Applying a Pod
with that binding would therefore rely on an unproven node cache or fail during
image pull. The generated one-shot backup command's required uploader tools are
present. No partial controller preflight was started.

## Authorization boundary

The existing approval remains valid only for one Plan 21-02 preflight,
one-shot backup, isolated restore, exact parity check, and temporary alias
compatibility probe. It does not authorize active alias, nginx, client
registration, legacy-service, or recurring-schedule changes. The CronJob must
remain suspended. Every provenance, capacity, quiescence, collision, package,
parity, alias-cleanup, and pre-state mismatch remains fail-closed.

## Next action

Publish or otherwise supply a cluster-pullable immutable digest for the exact
MemPalace image without changing its verified CLI contract. Then bind that
digest in the durable private operator configuration and resume Task 2 in this
same worktree. The existing authorization and durable artifacts remain
sufficient; do not rerun Phase 20 or start a partial preflight. Do not create a
Plan 21-02 summary until the complete live evidence chain passes.
