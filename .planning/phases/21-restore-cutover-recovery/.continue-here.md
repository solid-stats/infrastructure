---
context: phase
phase: 21-restore-cutover-recovery
plan: 2
task: 2
total_tasks: 2
status: paused
last_updated: 2026-08-20T16:11:04.531Z
---

# Phase 21 pause handoff

<!-- markdownlint-disable MD033 -->

## Critical anti-patterns

<!-- markdownlint-disable MD013 -->

| Pattern | Description | Severity | Prevention mechanism |
| --- | --- | --- | --- |
| Checkpoint-only live CLI | The first Task 1 implementation advanced stage checkpoints without invoking live adapters. Commit `7c0ccab` fixed this by routing every stage through a bounded adapter. | advisory | Before live execution, inspect the concrete adapter path and prove every JSON operation is implemented; do not accept checkpoint creation as live evidence. |
| Unresolved live operator | `JsonOperatorAdapter` requires an absolute executable, but no private operator executable has been built. | blocking | Build it under a mode-0700 private run root, validate every operation before mutation, and keep secret-bearing files and raw responses out of repository output. |

<!-- markdownlint-enable MD013 -->

<current_state>

Phase 21 is executing. Plan 21-01 is complete on the milestone branch at
`f2e4c6b`. Plan 21-02 Task 1 is complete in the clean isolated worktree
`.claude/worktrees/agent-p21-02-wave1`, branch
`worktree-agent-p21-02-wave1`, at `7c0ccab`.

Task 2 has not started. A fresh live executor was dispatched, but the session
was interrupted before it produced work. Read-only checks confirmed that the
`solidstats-memory` namespace does not exist, no namespace resources were
created, no backup or restore process is running, and no async job manifest
exists.

The main milestone worktree is at `f2e4c6b`, 12 commits ahead of its remote.
It contains the pre-existing untracked
`.gsd/dispatch-isolation-sentinel.json`; preserve it and do not stage it.

</current_state>

<completed_work>

- Plan 21-01: completed and merged into the milestone branch.
- Plan 21-02 Task 1: implemented the fail-closed backup and isolated restore
  control plane.
- Task 1 commits: `9dbd7e9`, `8639ee5`, `dbf5db5`, and `7c0ccab`.
- The live CLI now routes `preflight`, `backup`, `isolated-restore`, and
  `verify-restore` through a bounded `JsonOperatorAdapter`.
- Quick contract suite: 23/23 passed.
- Full memory suite: 112/112 passed.
- Manifest validation: 28 resources passed with operator placeholders.
- CLI help, Python compilation, commit hooks, and `git diff --check` passed.
- Phase 21 planning was revised so recurring backup activation fails closed
  unless Kubernetes API access, writer quiescence, metadata consistency,
  recovery, replica restoration, and write resumption are proven.

</completed_work>

<remaining_work>

- Plan 21-02 Task 2: create a restrictive ephemeral operator, run one batched
  preflight, one-shot backup, download verification, isolated restore, exact
  parity, temporary alias compatibility probe, and pre-state verification.
- Commit only value-free aggregate evidence and the Plan 21-02 summary.
- Merge the clean Plan 21-02 worktree through the GSD wave cleanup flow, rerun
  verification from the milestone branch, and update tracking.
- Execute Plans 21-03 and 21-04, then continue autonomous work through Phase
  22.

</remaining_work>

<decisions_made>

- The user explicitly approved exactly one live Plan 21-02 backup and isolated
  restore drill.
- That approval excludes active alias, nginx, machine-local client
  registrations, legacy-service state, and recurring schedule activation.
- The CronJob must remain suspended during Plan 21-02.
- Retained Phase 20 inputs must be selected by recomputed public digests, never
  by guessing a private directory name.
- Credentials and private bindings stay in restrictive transient storage.
  They must not appear in Git, stdout, stderr, evidence, or this handoff.
- Any provenance, quiescence, capacity, collision, package, parity,
  alias-cleanup, or pre-state mismatch stops the run without delete,
  overwrite, or automatic renaming.

</decisions_made>

<blockers>

- A private absolute `SOLIDSTATS_MEMORY_OPERATOR` executable must implement
  every bounded JSON operation expected by `JsonOperatorAdapter` before the
  first live mutation.
- This is a technical execution prerequisite, not a request for new user
  approval. The existing approval remains valid for the exact Task 2 scope.

</blockers>

## Required reading (in order)

<!-- markdownlint-disable MD013 -->

1. `AGENTS.md` — repository, security, Git, delegation, and GSD rules.
1. `.planning/HANDOFF.json` — machine-readable current position.
1. `.planning/phases/21-restore-cutover-recovery/21-02-PLAN.md` — exact Task 2 scope and gates.
1. `.planning/phases/21-restore-cutover-recovery/21-CONTEXT.md` — locked Phase 21 decisions.
1. `.planning/phases/21-restore-cutover-recovery/21-RESEARCH.md` — resolved consistency and recovery design.
1. `.planning/phases/21-restore-cutover-recovery/21-VALIDATION.md` — offline and live evidence gates.
1. `.planning/phases/21-restore-cutover-recovery/21-PATTERNS.md` — manifest analogs and ownership.
1. `.planning/phases/21-restore-cutover-recovery/21-01-SUMMARY.md` — completed wave-zero contract.
1. `.planning/phases/20-local-corpus-migration/20-PHASE21-HANDOFF.json` — public provenance root.
1. `scripts/restore-solidstats-memory.py` — live stage and operator protocol.
1. `scripts/validate-phase-21.py` — value-free evidence contract.
1. `k8s/memory/10-qdrant.yaml`, `20-mempalace.yaml`, `30-network-policy.yaml`, and `40-backup.yaml` — exact Task 2 manifest set.

<!-- markdownlint-enable MD013 -->

## Critical anti-patterns (do not repeat)

- Do not treat a successfully written checkpoint as proof that an external
  adapter ran. Inspect the concrete call path and require live result fields.
- Do not print or commit secret values, private collection names, retained
  bundle paths, corpus-bearing values, or raw external responses.
- Do not run Kubernetes, Qdrant, or S3 work as command-by-command drip-feed.
  Use one bounded, restartable batch.
- Do not mutate active alias, nginx, clients, legacy service, or the recurring
  schedule during Plan 21-02.

## Infrastructure state

- Kubernetes access through the machine's existing kubeconfig worked during
  read-only checks.
- Namespace `solidstats-memory`: absent at pause time.
- Task 2 resources, Secrets, Jobs, Pods, PVCs, and NetworkPolicies: absent.
- Backup/restore/operator processes: none running.
- Async external jobs: none.
- Retained Phase 20 private bundle: locally available; path intentionally
  omitted. Rediscover it by matching public digests.
- Live S3 and MCP credentials: locally available; values intentionally
  omitted. Never read `.env`, `.env.*`, or `.secrets` files.
- Active/public/client/legacy state: not changed during this session.

<context>

The immediate goal is to finish Plan 21-02 without widening its authorization.
Build and review the private operator first. Then run the complete live drill
as one fail-closed sequence. A successful Task 2 produces only aggregate,
value-free evidence; it does not make the new MemPalace public or register
clients. Plans 21-03 and 21-04 own cutover, recovery, restart/reboot proof, and
recurring schedule activation.

</context>

<next_action>

Start with: run `$gsd-resume-work`, verify the isolated worktree is clean at
`7c0ccab` and the `solidstats-memory` namespace remains absent, then dispatch a
fresh GSD executor for Plan 21-02 Task 2 using the existing explicit approval.

</next_action>

<!-- markdownlint-enable MD033 -->
