---
context: phase
phase: 21-restore-cutover-recovery
plan: 2
task: 2
total_tasks: 2
status: paused
last_updated: 2026-08-21T20:00:00+07:00
---

# Phase 21 Plan 02 checkpoint

## Current state

Task 1 remains complete. The additional pre-live contract is implemented and
verified without consuming the authorized controller preflight. No Kubernetes,
S3, live Qdrant, alias, nginx, MCP client, legacy-service, or recurring-schedule
mutation occurred.

The offline contract now:

- matches the legacy SolidStats MCP registration only by exact name and exact
  legacy URL, and matches the replacement only by exact `solidstats_memory`
  name and URL;
- treats the enabled, write-frozen legacy registration as read-only state, not
  proof of an active writer;
- enables Qdrant JWT RBAC while retaining the admin key only for Qdrant and the
  private controller;
- gives MemPalace and backup distinct collection-scoped read-write JWTs, and
  gives the observer a distinct collection-scoped read-only JWT sufficient for
  its collection-health and snapshot-list probes;
- binds live restore capacity to a retained real snapshot baseline and stops
  before recovery on excessive snapshot growth or insufficient headroom.

The full explicit memory suite passes 131 tests. Python compilation, source
manifest validation, migration policy validation, nginx validation, and Git
diff checks pass. Phase 21 aggregate evidence remains intentionally absent
until the authorized live sequence runs.

## Retained private artifacts

The existing `phase20-rebuild-20260821` artifact remains available through its
stable mode-0600 locator. All retained file digests and containment boundaries
were rechecked before the private points bundle was read.

The new logical artifact `phase21-preflight-capacity-20260821` retains a real
Qdrant collection snapshot built with the pinned Qdrant image. Its verification
record proves the exact expected count and vector configuration plus positive
own-collection and negative foreign-collection JWT behavior. The artifact
directory is mode 0700; the snapshot, verification record, and locator are mode
0600; every retained digest was re-read after materialization. The disposable
container, internal Docker network, and volume were removed.

The durable `phase21-operator-20260821` materialization remains the operator
entry point. Its private configuration must bind the baseline snapshot through
the new locator before the controller starts.

## Image and authorization boundary

The exact MemPalace image is now proven pullable through the official immutable
manifest:

- image: `ghcr.io/mempalace/mempalace@sha256:d9d75fab4138a22d013a244bb4153fa1938830be3726cf826ffd02aeba73fe8e`;
- config digest: `sha256:19453ae121bc14f4e9515fed1840179add7a8ad638cce1c0dc90a92df777d9e8`;
- application version: MemPalace 3.5.0.

The existing approval remains valid for one Plan 21-02 controller preflight,
one-shot backup, isolated restore, exact parity check, and temporary alias
compatibility probe. It does not authorize active alias, nginx, client
registration, legacy-service, or recurring-schedule changes. The CronJob must
remain suspended. Every provenance, capacity, quiescence, collision, package,
parity, alias-cleanup, and pre-state mismatch remains fail-closed.

## Next action

Materialize the committed operator plus the
`phase21-preflight-capacity-20260821` locator into the private operator config,
recompute its locator digests, and run the single authorized controller
preflight. If it passes, the same run may proceed to the one-shot live backup
and isolated restore. Do not create a Plan 21-02 summary until the complete live
evidence chain passes.
