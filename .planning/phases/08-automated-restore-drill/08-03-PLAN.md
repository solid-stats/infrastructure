---
phase: 08-automated-restore-drill
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - docs/backup-restore.md
autonomous: false
requirements:
  - DRILL-01
  - DRILL-02
  - DRILL-03
  - DRILL-04

must_haves:
  truths:
    - "docs/backup-restore.md contains an automated restore drill section with scripts/restore-drill.sh usage"
    - "Runbook explains: drill never touches live postgres-0 / postgres-data; DRILL-04 subdirectory placement"
    - "Runbook documents expected output (DRILL_RESULT=PASS line) and recommended cadence"
    - "Live drill on staging VPS produces DRILL_RESULT=PASS evidence before phase sign-off"
  artifacts:
    - path: "docs/backup-restore.md"
      provides: "Operator runbook for automated restore drill"
      contains: "DRILL_RESULT=PASS"
  key_links:
    - from: "docs/backup-restore.md"
      to: "scripts/restore-drill.sh"
      via: "Usage instruction referencing the script directly"
      pattern: "restore-drill.sh"
---

<objective>
Replace the manual restore drill section in docs/backup-restore.md with an automated drill runbook,
then perform a live drill on the staging cluster as the phase gate.

Purpose: The old manual drill (kubectl exec into postgres-0) is superseded by the automated Job.
The updated runbook is the operator's reference for Phase 11 cutover readiness verification.
The live drill is the DRILL-01..03 evidence artifact required before /gsd-verify-work.

Output:
- docs/backup-restore.md — updated Restore Drill section with automated runbook
- Evidence: DRILL_RESULT=PASS line from kubectl logs captured in SUMMARY
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-automated-restore-drill/08-CONTEXT.md
@docs/backup-restore.md
@.planning/phases/08-automated-restore-drill/08-01-SUMMARY.md
@.planning/phases/08-automated-restore-drill/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/backup-restore.md — replace manual drill with automated runbook</name>
  <files>docs/backup-restore.md</files>
  <action>
Replace the entire "## Restore Drill" section in docs/backup-restore.md with the automated runbook below.
The existing section starts at the line "## Restore Drill" and ends at the last line of the file
("Record the backup id..."). Replace that section (from "## Restore Drill" to end of file) with the
following content. Do not touch any content before "## Restore Drill".

--- New "## Restore Drill" section ---

## Restore Drill

The restore drill is an automated Kubernetes Job that downloads the latest S3 backup, restores
it into an ephemeral scratch PostgreSQL instance (never the live `postgres-0` pod or
`postgres-data` PVC), runs sanity checks, and tears down — all in one command.

### Run the drill

From a machine with `kubectl` access to the cluster (WireGuard tunnel must be up):

```bash
K8S_NAMESPACE=solid-stats-staging bash scripts/restore-drill.sh
```

The script:
1. Deletes any prior `restore-drill` Job (idempotent).
2. Applies `k8s/staging/restore-drill/70-restore-drill.yaml`.
3. Waits up to 900 s for the Job to complete.
4. Prints the full Job logs and extracts the evidence line.
5. Deletes the Job object (the pod self-cleans via `ttlSecondsAfterFinished: 3600`).

### Expected output

```text
backup_id=20260613T060000Z
DRILL_RESULT=PASS backup_id=20260613T060000Z table_count=12 total_rows=48231 duration_s=73
=== Restore Drill Evidence ===
DRILL_RESULT=PASS backup_id=20260613T060000Z table_count=12 total_rows=48231 duration_s=73
RESTORE DRILL PASSED
```

A `DRILL_RESULT=FAIL` line means one or more sanity assertions failed (table count < 5,
total row count = 0, or empty dump list). The Job exits non-zero and the script exits 1.
Check `kubectl -n solid-stats-staging logs job/restore-drill` for the `FAIL:` lines.

### What the drill does NOT do

- It does **not** connect to the live `postgres` Service. The drill pod uses `PGHOST=localhost`
  and refuses to run if it detects the live Service host. The scratch postgres runs entirely on
  an `emptyDir` volume, isolated from `postgres-data`.
- It does **not** write to S3. S3 credentials are read-only for backup retrieval only.
- It does **not** auto-schedule. The manifest lives in `k8s/staging/restore-drill/` (a
  subdirectory) so the CD apply glob (`find k8s/staging -maxdepth 1 -name '*.yaml'`) never
  matches it. Drill manifests at depth-1 trigger a CI failure via `scripts/validate-staging.py`.

### Recommended cadence

Run the drill:
- Before any production cutover (Phase 11 gate).
- After restoring a manual backup to confirm the backup file is valid.
- Monthly as a recoverability confidence check.

Record the `DRILL_RESULT=PASS` evidence line (backup_id, table_count, total_rows, duration_s)
in the operations log when using the drill as a cutover gate.

### Scheduled drill (future)

Automated scheduling and alerting (DRILL-05) are deferred to v2.x. The drill is on-demand only
in this version.
  </action>
  <verify>
    <automated>grep -q 'DRILL_RESULT=PASS' docs/backup-restore.md &amp;&amp; grep -q 'restore-drill.sh' docs/backup-restore.md &amp;&amp; grep -q 'postgres-data' docs/backup-restore.md &amp;&amp; grep -q 'DRILL-05' docs/backup-restore.md &amp;&amp; echo OK</automated>
  </verify>
  <done>docs/backup-restore.md contains the automated drill runbook with DRILL_RESULT=PASS example, restore-drill.sh usage, live-host safety note, DRILL-04 subdirectory explanation, and DRILL-05 deferred note.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All three Phase 8 artifacts are in place:
- k8s/staging/restore-drill/70-restore-drill.yaml (ServiceAccount + Job)
- scripts/restore-drill.sh (operator trigger script)
- scripts/validate-staging.py (DRILL-04 guard + syntax check)
- docs/backup-restore.md (updated runbook)

Offline validation passes:
  python3 scripts/validate-staging.py — all checks green
  bash -n scripts/restore-drill.sh — syntax ok

Now the live drill must run on the staging cluster to produce DRILL-01..03 evidence.
  </what-built>
  <how-to-verify>
1. Ensure WireGuard tunnel is up (see docs/operator-bootstrap.md).
2. Run the drill:
     K8S_NAMESPACE=solid-stats-staging bash scripts/restore-drill.sh
3. Confirm the output contains a DRILL_RESULT=PASS line, e.g.:
     DRILL_RESULT=PASS backup_id=... table_count=... total_rows=... duration_s=...
4. Confirm the live postgres is untouched:
     kubectl -n solid-stats-staging exec postgres-0 -- psql -U solid -d solid_stats -c "\l"
     The database list must NOT contain solid_stats_drill.
5. Confirm the Job self-cleaned or was removed:
     kubectl -n solid-stats-staging get job restore-drill
     Expected: "not found" or the job is in Completed state awaiting ttl.
6. Run offline validator one final time:
     python3 scripts/validate-staging.py
     Expected: all "ok:" lines, exit 0.
  </how-to-verify>
  <resume-signal>
Paste the DRILL_RESULT= line from step 2 output, then type "approved" to close the phase.
If the drill failed, describe the error — executor will investigate.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → cluster | kubectl apply/wait/logs from operator machine over WireGuard |
| drill pod → S3 | Read-only download of latest backup; no writes |
| drill pod → scratch postgres | localhost only; live postgres Service untouched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | live drill inadvertently touches postgres-0 | mitigate | Refuse-if-live-host check in Job script (T-08-01 from Plan 08-01); scratch postgres on emptyDir; guarded DB name solid_stats_drill |
| T-08-03-02 | Information Disclosure | DRILL_RESULT evidence logged with row counts | accept | Row counts are operational metrics, not PII or secret data; they are intended as evidence |
| T-08-03-03 | Tampering | docs runbook instructs operator to skip the drill | accept | Docs describe the drill as mandatory before cutover (Phase 11 gate); non-enforcement is an operator decision, not a technical control |
| T-08-SC | Tampering | slopcheck | accept | No package installs in this plan; docs update only |
</threat_model>

<verification>
Offline (before live drill):
  python3 scripts/validate-staging.py   # must exit 0
  bash -n scripts/restore-drill.sh      # must exit 0
  grep -q 'DRILL_RESULT=PASS' docs/backup-restore.md  # must succeed

Live gate (human checkpoint above):
  K8S_NAMESPACE=solid-stats-staging bash scripts/restore-drill.sh
  Expected: DRILL_RESULT=PASS backup_id=... table_count>=5 total_rows>0
</verification>

<success_criteria>
- docs/backup-restore.md updated with automated drill runbook (DRILL_RESULT=PASS example, restore-drill.sh usage, safety notes, cadence guidance)
- Live drill on staging cluster produces DRILL_RESULT=PASS evidence line
- Live postgres-0 / postgres-data untouched after drill (verified by operator in checkpoint)
- python3 scripts/validate-staging.py exits 0 (all ok: lines)
- Phase 8 requirements DRILL-01..04 verified: DRILL-01 (ephemeral scratch restore), DRILL-02 (PASS/FAIL assertions), DRILL-03 (teardown + evidence), DRILL-04 (subdirectory + CI guard)
</success_criteria>

<output>
Create .planning/phases/08-automated-restore-drill/08-03-SUMMARY.md when done
</output>
