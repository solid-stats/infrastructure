---
phase: 16-error-tracking-glitchtip
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/render-obs-secrets.py
  - scripts/validate-obs-manifests.py
  - .github/workflows/deploy-observability.yml
  - scripts/validate-phase-16.sh
  - scripts/test-glitchtip-ingest.sh
  - docs/glitchtip.md
autonomous: true
requirements: [ERR-01, ERR-02, ERR-03]
must_haves:
  truths:
    - "render-obs-secrets.py emits the error-tracking GlitchTip secrets (postgres password, SECRET_KEY, DATABASE_URL, superuser email/password) from env — no values in git (ERR-01)"
    - "validate-obs-manifests.py accepts namespace error-tracking for GlitchTip manifests without weakening the monitoring rules (ERR-01)"
    - "The obs deploy workflow applies the GlitchTip manifests + secrets into error-tracking and waits on their rollouts/Jobs (ERR-01)"
    - "validate-phase-16.sh proves ERR-01/02/03 live via port-forward; test-glitchtip-ingest.sh POSTs a forced Sentry envelope and confirms the issue appears (ERR-03)"
  artifacts:
    - path: "scripts/validate-phase-16.sh"
      provides: "live ERR-01/02/03 harness (pods Running, registration closed, project/DSN exist, forced error appears)"
      contains: "user_registration_enabled"
    - path: "scripts/test-glitchtip-ingest.sh"
      provides: "forced-error ingest test via port-forward (envelope POST + issue verify)"
      contains: "envelope"
    - path: "docs/glitchtip.md"
      provides: "operator runbook: GitHub secrets, first-run order, public URL cutover"
      contains: "ENABLE_USER_REGISTRATION"
  key_links:
    - from: ".github/workflows/deploy-observability.yml"
      to: "scripts/render-obs-secrets.py"
      via: "render error-tracking secrets then kubectl apply -n error-tracking"
      pattern: "error-tracking"
    - from: "scripts/validate-obs-manifests.py"
      to: "k8s/observability/90-glitchtip-postgres.yaml"
      via: "namespace allowlist includes error-tracking"
      pattern: "error-tracking"
---

<objective>
Extend the existing observability pipeline so GlitchTip can be rendered, validated, and deployed into the `error-tracking` namespace, and create the two live test harnesses that prove ERR-01/02/03. Specifically: (1) extend `render-obs-secrets.py` to emit GlitchTip's error-tracking secrets; (2) extend `validate-obs-manifests.py` to accept `error-tracking` namespace; extend the obs deploy workflow to render+apply the GlitchTip secrets and manifests and verify their rollouts; (3) write `validate-phase-16.sh` + `test-glitchtip-ingest.sh` + the `docs/glitchtip.md` operator runbook.

Purpose: The GlitchTip manifests (16-01/16-02) declare `namespace: error-tracking`, which today's static gate hard-rejects (it enforces `monitoring`) and which the obs deploy workflow never applies (it is hardcoded to `-n monitoring` with the monitoring-bound token). Without these extensions, the phase cannot deploy or pass its gate.
Output: edited `render-obs-secrets.py`, `validate-obs-manifests.py`, `deploy-observability.yml`; new `validate-phase-16.sh`, `test-glitchtip-ingest.sh`, `docs/glitchtip.md`.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-error-tracking-glitchtip/16-RESEARCH.md
@.planning/phases/16-error-tracking-glitchtip/16-VALIDATION.md

@scripts/render-obs-secrets.py
@scripts/validate-obs-manifests.py
@.github/workflows/deploy-observability.yml
@scripts/kubeconfig-setup.sh
@k8s/staging/01-obs-rbac.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend render-obs-secrets.py + validate-obs-manifests.py for error-tracking</name>
  <files>scripts/render-obs-secrets.py, scripts/validate-obs-manifests.py</files>
  <read_first>
    - scripts/render-obs-secrets.py (the secret() helper hardcodes NAMESPACE="monitoring"; the postgres DSN sslmode=disable note)
    - scripts/validate-obs-manifests.py (_check_namespace line ~162 hard-compares to "monitoring"; _CLUSTER_SCOPED_KINDS; _FORBIDDEN_OBS_KINDS)
    - 16-RESEARCH.md "Secret Rendering Extension", "Pitfall 5", anti-pattern "Don't set DATABASE_URL with prefer sslmode"
  </read_first>
  <action>
    render-obs-secrets.py: the current secret() helper bakes in the module-level NAMESPACE. Refactor secret() to take an explicit namespace argument (default keep "monitoring" so existing calls are unchanged), OR add a second namespace-parameterized path — choose the minimal-diff approach that preserves the existing grafana/postgres-monitor secrets exactly. Add four NEW required() env reads: GLITCHTIP_SECRET_KEY, GLITCHTIP_POSTGRES_PASSWORD, GLITCHTIP_SUPERUSER_EMAIL, GLITCHTIP_SUPERUSER_PASSWORD (all via the existing required() helper so a missing one exits 64). Emit two error-tracking Secrets:
      - glitchtip-postgres-auth (namespace error-tracking): POSTGRES_PASSWORD = GLITCHTIP_POSTGRES_PASSWORD.
      - glitchtip-secrets (namespace error-tracking): SECRET_KEY = GLITCHTIP_SECRET_KEY; DATABASE_URL = postgresql://glitchtip:<url-encoded GLITCHTIP_POSTGRES_PASSWORD>@glitchtip-postgres.error-tracking.svc:5432/glitchtip?sslmode=disable (use urllib.parse.quote like the existing pg_dsn — sslmode=disable per the anti-pattern, NOT prefer); GLITCHTIP_SUPERUSER_EMAIL; GLITCHTIP_SUPERUSER_PASSWORD.
      Append these to the documents list so the script prints monitoring + error-tracking secrets as multi-doc YAML. Keep the sslmode=disable rationale comment.
    validate-obs-manifests.py: extend _check_namespace so the allowed set is {"monitoring", "error-tracking"} instead of the single "monitoring" literal (Pitfall 5). Update the error message to name both. Do NOT weaken any other check — the no-secret-values, no-clusterrole, and priorityClassName checks must still apply to GlitchTip manifests unchanged (GlitchTip pods are obs-background, so they pass). Keep the T-13-03 token-as-variable discipline intact.
  </action>
  <verify>
    <automated>GLITCHTIP_SECRET_KEY=x GLITCHTIP_POSTGRES_PASSWORD='p@ss/word' GLITCHTIP_SUPERUSER_EMAIL=a@b.c GLITCHTIP_SUPERUSER_PASSWORD=y GRAFANA_ADMIN_PASSWORD=g PG_MONITOR_PASSWORD=m python3 scripts/render-obs-secrets.py | tee /tmp/sec16.yaml | grep -q 'namespace: error-tracking' && grep -q 'glitchtip-secrets' /tmp/sec16.yaml && grep -q 'sslmode=disable' /tmp/sec16.yaml && grep -q 'glitchtip-postgres.error-tracking.svc' /tmp/sec16.yaml; GRAFANA_ADMIN_PASSWORD=g PG_MONITOR_PASSWORD=m python3 scripts/render-obs-secrets.py >/dev/null 2>&1; test $? -eq 64; python3 scripts/validate-obs-manifests.py 2>&1 | grep -q PASSED</automated>
  </verify>
  <done>render-obs-secrets.py emits glitchtip-postgres-auth + glitchtip-secrets in error-tracking (DATABASE_URL with url-encoded password + sslmode=disable, superuser email/password), exits 64 when any GLITCHTIP_* var is missing, and leaves the monitoring secrets byte-identical. validate-obs-manifests.py accepts both monitoring and error-tracking namespaces with all other checks intact and still PASSES against the existing obs manifests.</done>
</task>

<task type="auto">
  <name>Task 2: Extend deploy-observability.yml to deploy GlitchTip into error-tracking</name>
  <files>.github/workflows/deploy-observability.yml</files>
  <read_first>
    - .github/workflows/deploy-observability.yml (the monitoring render/apply/rollout steps; K8S_OBS_TOKEN + obs-ci-deployer kubeconfig pattern)
    - scripts/kubeconfig-setup.sh (K8S_NAMESPACE/K8S_USER_NAME/K8S_CONTEXT_NAME are parameterizable)
    - k8s/staging/01-obs-rbac.yaml (there is a SEPARATE obs-ci-deployer SA + Role + RoleBinding in error-tracking; its token must auth the error-tracking apply)
  </read_first>
  <action>
    The current workflow renders+applies all secrets and manifests to `monitoring` with the monitoring-scoped obs-ci-deployer token. GlitchTip manifests declare `namespace: error-tracking`, and the monitoring-bound token cannot create resources there. Add GlitchTip deployment WITHOUT breaking the monitoring path:
    - In the existing "Render and apply obs secrets" step, the script now also emits error-tracking secrets. Apply them with namespace from the manifest metadata: split the rendered output or apply the full multi-doc with `kubectl apply -f -` (no -n flag) so each Secret lands in its own declared namespace — BUT the obs-ci-deployer monitoring token may lack error-tracking Secret create rights. Therefore add the error-tracking apply under a kubeconfig built with the error-tracking-scoped token. Add a new GitHub Actions secret reference K8S_OBS_ET_TOKEN (the error-tracking obs-ci-deployer SA token) — document it in docs/glitchtip.md and reference it in env. Add the four GlitchTip GitHub secrets (GLITCHTIP_SECRET_KEY, GLITCHTIP_POSTGRES_PASSWORD, GLITCHTIP_SUPERUSER_EMAIL, GLITCHTIP_SUPERUSER_PASSWORD) to the render step env.
    - Add a "Setup kubeconfig (error-tracking)" step calling kubeconfig-setup.sh with K8S_TOKEN=${{ secrets.K8S_OBS_ET_TOKEN }}, K8S_NAMESPACE=error-tracking, K8S_USER_NAME=obs-ci-deployer-et, K8S_CONTEXT_NAME=obs-et-k3s-staging (so it builds a separate context; use `kubectl --context obs-et-k3s-staging` for the error-tracking apply/rollout steps).
    - Add a "Render+apply GlitchTip secrets (error-tracking)" step: render-obs-secrets.py emits both namespaces; pipe only the error-tracking docs to `kubectl --context obs-et-k3s-staging apply -n error-tracking -f -` (simplest: re-run the renderer and apply the full output with the et context but `-n error-tracking` only applies namespace-less docs to error-tracking; since the monitoring secrets carry explicit namespace: monitoring, applying them with the et-token will 403 — so split the output by `---` documents into a monitoring file and an error-tracking file, or filter. Implement a small split: write the rendered YAML, then apply monitoring docs with the monitoring context and error-tracking docs with the et context). Keep it robust: a `python3 -c` or `awk`-free `grep`/`csplit` split on `namespace:` is acceptable, OR simplest of all — apply the FULL rendered multi-doc once with EACH context relying on namespace metadata and `|| true` is NOT acceptable (must fail loudly). Choose: split into two temp files by namespace and apply each with the matching context. Document the chosen split in a comment.
    - Add a "Apply GlitchTip manifests (error-tracking)" step: `find k8s/observability -maxdepth 1 -name '9*-glitchtip*.yaml' | sort | sed 's/^/-f /' | xargs kubectl --context obs-et-k3s-staging apply --server-side --force-conflicts -n error-tracking`. The 90-93 GlitchTip manifests already carry namespace: error-tracking in metadata, so the -n flag is harmless/consistent.
    - Add a "Verify GlitchTip rollouts" step: rollout status statefulset/glitchtip-postgres, deployment/glitchtip-web, deployment/glitchtip-worker (all --context obs-et-k3s-staging -n error-tracking --timeout=300s); and `kubectl --context obs-et-k3s-staging -n error-tracking wait --for=condition=complete job/glitchtip-migrate --timeout=180s`. (The seed Job completion + org/project/DSN + forced-error test live in plan 16-04, run by the operator/agent with cluster access — keep the CI verify to rollouts + migrate completion.)
    - IMPORTANT: the existing monitoring steps must remain unchanged in behavior (same token, same -n monitoring, same rollout list). Only ADD steps; do not retarget the monitoring path.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; list(yaml.safe_load_all(open('.github/workflows/deploy-observability.yml')))" 2>/dev/null || python3 -c "import sys; open('.github/workflows/deploy-observability.yml').read()"; grep -q 'error-tracking' .github/workflows/deploy-observability.yml && grep -q 'glitchtip-web' .github/workflows/deploy-observability.yml && grep -q 'K8S_OBS_ET_TOKEN' .github/workflows/deploy-observability.yml && grep -q 'job/glitchtip-migrate' .github/workflows/deploy-observability.yml && grep -q 'GLITCHTIP_SECRET_KEY' .github/workflows/deploy-observability.yml</automated>
  </verify>
  <done>deploy-observability.yml has new steps that build an error-tracking-scoped kubeconfig (K8S_OBS_ET_TOKEN), render+apply GlitchTip secrets to error-tracking, apply the 9*-glitchtip*.yaml manifests, and verify glitchtip-postgres/web/worker rollouts + migrate Job completion — with the existing monitoring deploy path unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Write validate-phase-16.sh, test-glitchtip-ingest.sh, and docs/glitchtip.md</name>
  <files>scripts/validate-phase-16.sh, scripts/test-glitchtip-ingest.sh, docs/glitchtip.md</files>
  <read_first>
    - 16-RESEARCH.md "ERR-02 Verification Protocol", "Pattern 6: ERR-03 Forced-Error Test", "Pattern 5", "Autonomous vs Operator-Gated Work", "Pitfall 8"
    - 16-VALIDATION.md "Per-Requirement Verification Map" (lift each check)
    - scripts/bootstrap-obs-edge.sh (the operator cutover the runbook documents; errors. case already exists)
  </read_first>
  <action>
    scripts/test-glitchtip-ingest.sh (bash, #!/usr/bin/env bash, set -euo pipefail, required() helper exit 64): ERR-03 forced-error test via port-forward. Inputs (env): NAMESPACE=error-tracking (default), GLITCHTIP_DSN (required — the project DSN from 16-04, format http://PUBLIC_KEY@host/PROJECT_ID), SUPERUSER_TOKEN (optional, for the verify query). Steps: parse PUBLIC_KEY and PROJECT_ID out of the DSN; start `kubectl port-forward svc/glitchtip-web 8000:8000 -n "$NAMESPACE"` in the background, wait until localhost:8000/api/0/config/ answers, trap to kill the port-forward on exit. Build a Sentry envelope (3 newline-separated JSON lines per 16-RESEARCH Pattern 6: header with event_id+dsn, item header type:event length, the event payload with a unique marker message like "Phase 16 forced test error <timestamp>"). POST it to http://localhost:8000/api/${PROJECT_ID}/envelope/ with Content-Type application/x-sentry-envelope; assert HTTP 200/202 (NOT 403 — Pitfall 8 means a wrong DSN key). Then, if SUPERUSER_TOKEN is set, poll the issues API for the unique marker and assert it appears within a timeout; print "ERR-03 PASS". Exit non-zero on any failure.
    scripts/validate-phase-16.sh (bash, set -euo pipefail): the live ERR-01/02/03 harness lifting 16-VALIDATION's map. Support `--internal` (port-forward mode, default) and an optional `--public` (curl the errors. URL — only meaningful post-cutover). Checks:
      ERR-01: `kubectl get pods -n error-tracking` shows glitchtip-postgres + glitchtip-web + glitchtip-worker all Running/Ready; assert NO redis/valkey workload exists (`kubectl get deploy,statefulset -n error-tracking` contains no valkey/redis); assert `kubectl get job glitchtip-migrate -n error-tracking -o jsonpath='{.status.succeeded}'` == 1; assert the web pod env has VALKEY_URL set to empty (`kubectl get deploy glitchtip-web -n error-tracking -o jsonpath` over the env shows VALKEY_URL present with empty value).
      ERR-02: via port-forward, `curl -s localhost:8000/api/0/config/` and assert user_registration_enabled is false (Method A from 16-RESEARCH — the reliable one); optionally POST the registration endpoint and assert 4xx (Method B, best-effort).
      ERR-03: assert a project/DSN exists (query the projects API with SUPERUSER_TOKEN if provided, else check that 16-04 recorded the DSN); then delegate the forced-error proof to test-glitchtip-ingest.sh (call it, or instruct that 16-04 runs it). Print a clear PASS/FAIL per requirement and exit non-zero if any fails. Reuse the port-forward setup helper (factor a small function or source the ingest script's port-forward logic).
    docs/glitchtip.md (operator runbook): document (1) the four NEW GitHub Actions secrets (GLITCHTIP_SECRET_KEY — 50-char random, GLITCHTIP_POSTGRES_PASSWORD, GLITCHTIP_SUPERUSER_EMAIL, GLITCHTIP_SUPERUSER_PASSWORD) + K8S_OBS_ET_TOKEN (how to mint it from the error-tracking obs-ci-deployer SA token Secret) — NO values, just where they come from; (2) the first-run order (migrate Job → ENABLE_USER_REGISTRATION=False closed-from-boot → seed superuser Job → 16-04 creates org/project/DSN); (3) the autonomous forced-error test (test-glitchtip-ingest.sh via port-forward); (4) the OPERATOR public cutover: DNS A record for errors.stats-staging.solid-stats.ru, then re-run `DOMAIN=errors.stats-staging.solid-stats.ru ADMIN_EMAIL=... UPSTREAM=$(kubectl get svc glitchtip-web -n error-tracking -o jsonpath='{.spec.clusterIP}:{.spec.ports[0].port}') scripts/bootstrap-obs-edge.sh` (cert reused from Phase 14) — and that plan 16-05 covers this gated step. Reference docs/obs-edge-bootstrap.md.
    chmod +x both scripts.
  </action>
  <verify>
    <automated>bash -n scripts/validate-phase-16.sh && bash -n scripts/test-glitchtip-ingest.sh && test -x scripts/validate-phase-16.sh && test -x scripts/test-glitchtip-ingest.sh && grep -q 'user_registration_enabled' scripts/validate-phase-16.sh && grep -q 'envelope' scripts/test-glitchtip-ingest.sh && grep -q 'port-forward' scripts/test-glitchtip-ingest.sh && grep -q 'ENABLE_USER_REGISTRATION' docs/glitchtip.md && grep -q 'bootstrap-obs-edge.sh' docs/glitchtip.md</automated>
  </verify>
  <done>validate-phase-16.sh syntactically valid + executable, checks all three ERR rows (pods Running + no valkey + migrate succeeded + VALKEY_URL empty; registration closed via /api/0/config/; project/DSN + forced error). test-glitchtip-ingest.sh POSTs a real Sentry envelope via port-forward and asserts 200/202 then optionally verifies the issue. docs/glitchtip.md documents the 5 secrets, first-run order, the forced-error test, and the operator cutover command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env/GitHub secrets → rendered k8s Secrets | render-obs-secrets.py reads env, never commits values |
| CI (error-tracking token) → cluster | a SECOND obs-ci-deployer token scoped to error-tracking; cannot touch monitoring or app namespaces |
| operator laptop → glitchtip-web (port-forward) | the only path used for the autonomous ERR-02/03 tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Information Disclosure | GlitchTip secrets rendered in CI logs | mitigate | rendered into a mktemp file applied then trap-removed (existing obs pattern); required() exits 64 on missing var; no value echoed |
| T-16-11 | Elevation of Privilege | a single over-broad CI token spans both namespaces | mitigate | separate K8S_OBS_ET_TOKEN bound only to error-tracking RoleBinding (01-obs-rbac.yaml); monitoring path keeps its own token unchanged |
| T-16-12 | Spoofing | forced-error test accepts any DSN key | mitigate | test-glitchtip-ingest.sh parses the REAL DSN (PUBLIC_KEY+PROJECT_ID) and asserts a non-403 ingest (Pitfall 8) — a wrong key fails loudly |
| T-16-13 | Tampering | validator weakened to pass error-tracking lets a bad namespace through | mitigate | namespace allowlist is an explicit {monitoring, error-tracking} set; all other static checks unchanged; gate still PASSES existing manifests |
| T-16-SC | Tampering | no new packages installed | accept | scripts are stdlib bash/python; no npm/pip/cargo install (16-RESEARCH audit) |
</threat_model>

<verification>
- Renderer emits both namespaces, url-encodes the DB password, exits 64 on a missing GLITCHTIP_* var, and leaves monitoring secrets unchanged.
- `python3 scripts/validate-obs-manifests.py` PASSES against existing obs manifests AND (once 16-01/16-02 land) the GlitchTip error-tracking manifests.
- `bash -n` passes on both new scripts; both are executable.
- deploy-observability.yml gains GlitchTip steps without altering the monitoring path.
</verification>

<success_criteria>
- The obs pipeline can render, validate, and deploy GlitchTip into error-tracking end-to-end.
- ERR-01/02/03 each have an executable live check; ERR-03 has a real envelope ingest test.
- The operator runbook documents every new secret and the DNS-gated public cutover.
</success_criteria>

<output>
Create `.planning/phases/16-error-tracking-glitchtip/16-03-SUMMARY.md` when done.
</output>
