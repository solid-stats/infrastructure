<!-- markdownlint-disable MD013 -->

# Phase 21.1: Curator Update Surface - Discussion Log (Assumptions Mode)

> **Audit trail only.** Do not use as input to planning, research, or execution
> agents. Decisions are captured in `21.1-CONTEXT.md`; this log preserves the
> analysis.

**Date:** 2026-08-25
**Phase:** 21.1-curator-update-surface
**Mode:** assumptions
**Areas analyzed:** policy transition, preservation contract, materialization
boundary, health cleanup

## Assumptions Presented

### Policy Transition

| Assumption | Confidence | Evidence |
| --- | --- | --- |
| Model the change as an exact seven-tool predecessor to eight-tool successor transaction, accepting the successor idempotently and failing closed on any other state. | Confident | `scripts/configure-solidstats-memory-client.py`, Phase 21 verification |

### Preservation Contract

| Assumption | Confidence | Evidence |
| --- | --- | --- |
| The approved correction changes only the wing; content, room, provenance, and all metadata except `metadata.wing` remain stable. | Confident | User-approved correction batch; MemPalace v3.5.0 update implementation |

### Materialization Boundary

| Assumption | Confidence | Evidence |
| --- | --- | --- |
| Materialize only the home Ubuntu client policy, require a fresh Codex session, and leave Windows/shared config and the final managed contract as explicit follow-ups. | Confident | User scope; client-side `enabled_tools` boundary |

### Health Cleanup

| Assumption | Confidence | Evidence |
| --- | --- | --- |
| Retain the stale lock through the private-artifact protocol, remove the clean stale worktree while preserving its branch, and archive the stale checkpoint under milestone history. | Confident | GSD health W017/W019 evidence; private-artifact retention contract |

## Corrections Made

- The user selected preservation model A: only `metadata.wing` may change.
- The user accepted temporary Linux/Windows policy drift and kept the managed
  contract out of scope.
- The user selected retain-before-remove cleanup and milestone archival for the
  stale checkpoint.

## External Research

- MemPalace v3.5.0 exposes `mempalace_update_drawer` with required `drawer_id`
  and optional `content`, `wing`, and `room`. Source: [MCP tool schema](https://github.com/MemPalace/mempalace/blob/v3.5.0/mempalace/mcp_server.py#L3772-L3793).
- The handler fetches the existing logical drawer, copies its metadata, and
  updates only explicitly supplied fields. Source: [update handler](https://github.com/MemPalace/mempalace/blob/v3.5.0/mempalace/mcp_server.py#L2592-L2631).
