---
context: phase
phase: 21.1-curator-update-surface
task: 2
total_tasks: 3
status: blocked
last_updated: 2026-08-24T20:03:02.327Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> This is a deliberate process-boundary checkpoint. A genuinely new Codex
> process must resume the plan.

- [ ] Confirm the new process is rooted at this repository on branch
  `gsd/v4.0-solidstats-memory-isolation` and contains pushed Task 1 commit
  `d685ba2c3b4221d5b31c29c75b51b4150dd0bb90`.
- [ ] Confirm the current materializing session has ended. It made no
  `solidstats_memory` MCP call and must not execute Plan 02 Task 3.
- [ ] Inspect the new process's actual process-loaded `solidstats_memory`
  namespace before any stateful call. Require exactly the ordered eight-tool
  successor and the exact `mempalace_update_drawer` schema. Raw server
  `tools/list` is not acceptable client-policy evidence.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

<!-- markdownlint-disable MD013 -->

| Pattern | Description | Severity | Prevention Mechanism |
| --- | --- | --- | --- |
| Reusing the materializing process | The MCP client surface was loaded before the successor policy materialized. | blocking | End this process and resume from a newly started Codex session. |
| Treating server enumeration as client evidence | Raw server `tools/list` can include tools excluded by the Codex client policy. | blocking | Inspect the new process-loaded `solidstats_memory` namespace first. |
| Starting live mutation before exact surface proof | A broader or stale client could mutate outside the approved boundary. | blocking | Require the exact ordered eight tools and exact update schema before Task 3 calls. |

<!-- markdownlint-enable MD013 -->

<current_state>

Plan 02 Task 1 is complete. The exact home-Ubuntu successor policy and
value-free evidence are committed and pushed at
`d685ba2c3b4221d5b31c29c75b51b4150dd0bb90` on
`gsd/v4.0-solidstats-memory-isolation`. Task 2 is waiting for the old process to
close and a fresh Codex process to resume. Task 3 has not started.

</current_state>

<completed_work>

- Task 1: Seal, push, and materialize the exact home-Ubuntu policy — done.
- Exact successor validation passed with the unchanged existing URL binding.
- Legacy client absence and accepted predecessor/successor state passed.
- Both value-free evidence files passed recursive validation.
- The private locator passed semantic, mode, and digest validation.
- The complete three-module suite passed all 173 tests.
- Final review status is clean and GSD health is healthy.
- Remote branch and local Task 1 commit matched exactly.

</completed_work>

<remaining_work>

- Task 2: Close this process and start a genuinely new Codex process rooted at
  the repository.
- Task 3: In the fresh top-level process, first inspect the process-loaded
  exact eight-tool namespace and update schema; then execute the plan's exact
  disposable UAT, approved correction, cleanup, invariant, evidence, push, and
  verification sequence.

</remaining_work>

<decisions_made>

- The machine-local URL was read from the existing registration and preserved
  exactly; no environment-specific URL was copied into the plan.
- The checked-in upgrade was not replayed because accepted successor bytes,
  metadata, and the durable locator already matched exactly.
- No live MCP acceptance or drawer operation is valid in this materializing
  process.

</decisions_made>

<!-- markdownlint-disable MD033 -->

<blockers>

- Human action: close this Codex process and start a new one. There is no valid
  in-process workaround for the process-loaded client capability boundary.

</blockers>

<!-- markdownlint-enable MD033 -->

## Required Reading (in order)

1. `AGENTS.md` — repository, security, and GSD constraints.
1. `.planning/phases/21.1-curator-update-surface/21.1-02-PLAN.md` — resume at
   Task 3 only.
1. `.planning/phases/21.1-curator-update-surface/21.1-CONTEXT.md` — locked
   materialization, UAT, correction, and boundary decisions.
1. `.planning/phases/21.1-curator-update-surface/21.1-RESEARCH.md` — fresh
   capability gate and exact live acceptance sequence.
1. `.planning/phases/21.1-curator-update-surface/21.1-CLIENT-POLICY-EVIDENCE.json`
   — value-free successor evidence.
1. `.planning/phases/21.1-curator-update-surface/21.1-OFFLINE-EVIDENCE.json` —
   value-free offline evidence.
1. `.planning/STATE.md` — authoritative paused position.

## Critical Anti-Patterns (do NOT repeat these)

- Do not call any `solidstats_memory` MCP tool from the materializing process.
- Do not use raw MCP `tools/list` as proof of the Codex client namespace.
- Do not broaden Task 3 into runtime, Kubernetes, nginx, VPS, Phase 21, Phase
  22, Windows, shared-policy, or managed-contract work.

## Infrastructure State

- Machine policy: exact eight-tool successor validated.
- Runtime/Kubernetes/nginx/VPS: unchanged and out of scope.
- Durable private artifact logical ID:
  `solidstats-phase21-1-client-policy-prestate`.
- Stable locator:
  `/home/afgan0r/.local/state/solidstats/private-artifacts/phase-21-1-client-policy/locator.json`.
- Locator directory is mode `0700`; prestate, accepted-state, locator, and
  materialized config are mode `0600`; all recorded digests matched.
- No background process or async external job belongs to this checkpoint.

<!-- markdownlint-disable MD033 -->

<context>

The exact Task 3 acceptance sequence is: inspect the fresh process-loaded
eight-tool namespace and update schema; complete one disposable
add → exact-ID get → update → exact-ID read-back → exact-ID delete cycle with
zero residue; pre-read the one approved active drawer; submit only its ID and
`wing: devops`; exact-ID read back; prove every other field, archive inventory,
and unrelated inventory unchanged; require the old active infrastructure wing
empty; then write value-free evidence and complete the remaining offline,
security, push, and GSD verification gates.

</context>

<!-- markdownlint-enable MD033 -->

<next_action>

Start with: open a fresh Codex session rooted at this repository, load branch
`gsd/v4.0-solidstats-memory-isolation`, verify pushed Task 1 commit
`d685ba2c3b4221d5b31c29c75b51b4150dd0bb90`, then inspect the process-loaded
`solidstats_memory` namespace before any MCP call and resume Plan 02 Task 3.

</next_action>
