# Phase 21.1 Follow-Ups

Phase 21.1 closes only the home-Ubuntu curator client surface. The following
owner-scoped work remains deliberately unexecuted.

## Windows and Shared Policy Materialization

**Owner:** `solid-stats/agent-instructions` through the existing Windows/shared
configuration materialization path.

**Acceptance:** Materialize the exact ordered eight-tool `solidstats_memory`
policy on Windows, preserve its existing endpoint and token binding, prove the
same exact-ID update schema, reject every broader or reordered surface, and
verify Linux and Windows policy parity without copying secret values into Git.

## Managed Contract Reconciliation

**Owner:** The managed SolidStats memory contract in
`solid-stats/agent-instructions`.

**Acceptance:** After Windows parity is proven, reconcile the shared
instructions, common GSD configuration and adapter, bridge tests, contract
version, and release notes so the managed contract describes the exact
eight-tool curator surface. Consumer repositories must receive the normal
managed release update; this infrastructure phase must not edit those owners.

Neither follow-up authorizes archive distillation or any Phase 22 execution.
