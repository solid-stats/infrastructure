---
phase: 21.1-curator-update-surface
plan: 01
subsystem: memory-client-policy
status: complete
tags:
  - mempalace
  - client-policy
  - curator
  - rollback
  - health-cleanup
dependency_graph:
  requires:
    - phase-21 exact client transaction seams
    - pinned MemPalace v3.5.0 tool schema evidence
  provides:
    - exact seven-to-eight curator policy upgrade
    - offline curator UAT and correction validators
    - durable stale-lock retention with verified locator
  affects:
    - 21.1-02 live materialization and UAT
tech_stack:
  added: []
  patterns:
    - exact predecessor/successor policy classification
    - byte-idempotent transactional replay
    - exact-ID lost-acknowledgement reconciliation
    - aggregate-only evidence validation
key_files:
  created:
    - .planning/phases/21.1-curator-update-surface/21.1-01-SUMMARY.md
  modified:
    - scripts/configure-solidstats-memory-client.py
    - scripts/probe-solidstats-memory.py
    - tests/test-memory-cutover-contract.py
    - .planning/milestones/v4.0-phases/21-restore-cutover-recovery/21-CONTINUE-HERE.md
decisions:
  - The stable successor appends only mempalace_update_drawer to the exact ordered seven-tool predecessor.
  - Curator evidence remains offline, aggregate-only, and bound to exact-ID complete-object comparisons.
  - The stale restore lock is retained as logical artifact solidstats-phase21-restore-lock-p21-02-wave1 before worktree removal.
metrics:
  duration: 20m43s
  completed: 2026-08-24
actuals:
  tokens: 15393
  tasks: 3
  commits: 6
---

# Phase 21.1 Plan 01: Curator Update Surface Summary

Exact atomic curator policy upgrade with strict offline schema, UAT,
correction, cleanup, and rollback contracts.

## Accomplishments

- Added a dedicated exact predecessor-to-successor transaction that preserves
  the existing lock, exchange, read-back, failure-injection, accepted-state
  metadata, and rollback boundaries.
- Added deterministic validators for the fresh eight-tool client surface, the
  pinned update schema, strict `uat-<nonce>` lifecycle cleanup, lost
  acknowledgements, and the approved one-field correction.
- Retained the stale restore lock under durable private storage, revalidated
  its locator and digest, removed only the registered stale worktree, and
  preserved its branch at the original commit.
- Moved the Phase 21 continuation handoff byte-for-byte into the established
  v4.0 milestone archive path.

## Task Commits

1. `821100f` — `test(21.1-01): add failing client upgrade tracer`
2. `9b4acbd` — `feat(21.1-01): add exact curator policy upgrade`
3. `dcc27f2` — `test(21.1-01): add failing curator update contracts`
4. `6cff9f4` — `feat(21.1-01): add curator update validators`
5. `6055315` — `chore(21.1-01): retain stale restore state`

## Verification

- The focused `client_policy` contract run passed all 6 tests.
- Full three-module memory contract suite: 171 tests passed.
- Python compilation passed for both implementation scripts and all three
  contract modules.
- The archived continuation retained SHA-256
  `982949a7f55ed278b8e55ba50a93e092e638f8958d06e93225c9f14b22caed7f`,
  and Git recorded a 100% rename.
- The stale worktree is absent, while `worktree-agent-p21-02-wave1` still
  resolves to `5b09bc8a8516c033d2d93f547eb1f0342fc98f30`.
- Markdown lint and `git diff --check` passed.
- GSD health is healthy with no errors or warnings; its sole informational item
  is the intentionally unexecuted Plan 02.

## Durable Private Retention

- Logical artifact ID: `solidstats-phase21-restore-lock-p21-02-wave1`.
- Stable locator:
  `/home/afgan0r/.local/state/solidstats/private-artifacts/phase-21-restore-lock/locator.json`.
- The directory is mode 0700; the retained artifact and locator are mode 0600.
  Locator fields and source/destination digests were semantically re-read and
  recomputed before and after worktree removal.
- No private values or corpus-bearing source paths were copied into repository
  artifacts.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated current-policy retirement fixtures for the exact successor**

- **Found during:** Task 2 full contract-suite verification.
- **Issue:** Three existing retirement and rollback fixtures still encoded the
  seven-tool predecessor where they represented the current installed policy.
  The new exact successor classifier correctly rejected them before their
  intended transaction assertions.
- **Fix:** Appended only `mempalace_update_drawer` to those current-policy
  fixtures while preserving the dedicated seven-tool upgrade predecessor.
- **Files modified:** `tests/test-memory-cutover-contract.py`.
- **Commit:** `6cff9f4`.

## Decisions Made

- Kept the initial Phase 21 apply path separate from the new upgrade transaction
  so an existing policy cannot be mistaken for first-time registration.
- Bound successor replay to the dedicated mode-0600 prestate and accepted-state
  metadata, making replay byte-idempotent without weakening fail-closed
  classification.
- Kept all live operations out of Plan 01; only pure fixtures and offline
  evidence validation were introduced.

## Known Stubs

None. Empty collections in the changed implementation are bounded accumulators.
Empty and null test values are deliberate rejection cases.

## Authentication Gates

None.

## Self-Check: PASSED

- All created and modified repository files exist at their expected paths.
- All five task commits are present in Git history.
- The retained artifact and locator still exist and pass mode, semantic-field,
  and digest validation.
- The preserved stale-worktree branch still points to its original commit.
