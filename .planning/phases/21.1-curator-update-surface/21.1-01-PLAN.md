---
phase: 21.1-curator-update-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/configure-solidstats-memory-client.py
  - scripts/probe-solidstats-memory.py
  - tests/test-memory-cutover-contract.py
  - .planning/.continue-here.md
  - .planning/milestones/v4.0-phases/21-restore-cutover-recovery/21-CONTINUE-HERE.md
autonomous: true
requirements:
  - CUR-06
  - CUR-07
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
premises:
  - claim: The checked-in client policy is the exact seven-tool predecessor and retains the Phase 21 atomic transaction seams.
    src: scripts/configure-solidstats-memory-client.py#ENABLED_TOOLS
    verify: python3 -B -m unittest tests/test-memory-cutover-contract.py -k test_client_policy_add_validate_and_exact_rollback
  - claim: The stale worktree is clean, contains `.21-02-restore.lock`, and its branch is `worktree-agent-p21-02-wave1`.
    src: .planning/phases/21.1-curator-update-surface/21.1-RESEARCH.md#Health-Cleanup-Constraints
    verify: git worktree list --porcelain
carried_forward:
  - source: .planning/phases/21-restore-cutover-recovery/21-VERIFICATION.md
    relevance: Reuse Phase 21 exact policy, atomic replacement, evidence, and rollback contracts without modifying closed Phase 21 artifacts.
must_haves:
  truths:
    - "The stable authoritative successor is the exact ordered seven-tool predecessor followed only by `mempalace_update_drawer` (D-01/D-02)."
    - "Only exact predecessor -> exact successor is mutable; exact successor replay is byte-idempotent; missing, empty, null, reordered, duplicate, partial, conflicting, malformed, or broader policy fails closed (D-03)."
    - "Publication, read-back, failure-injection, lost-acknowledgement, and rollback boundaries preserve an exact predecessor or successor and never overwrite unrelated-writer bytes (D-04)."
    - "Offline validators require one exact drawer ID, stable sequence order, complete object comparison, strict `uat-<nonce>` cleanup, and rejection of zero, multiple, or ambiguous matches (D-05/D-06/D-07/D-08)."
    - "The stale lock and locator are retained and digest-verified before only the clean stale worktree is removed; branch `worktree-agent-p21-02-wave1` is preserved (D-13)."
    - "The tracked continuation is losslessly moved to `.planning/milestones/v4.0-phases/21-restore-cutover-recovery/21-CONTINUE-HERE.md` (D-14)."
  artifacts:
    - path: scripts/configure-solidstats-memory-client.py
      provides: "Exact seven-to-eight atomic upgrade, validation, idempotency, and rollback commands"
    - path: scripts/probe-solidstats-memory.py
      provides: "Pure exact-surface, exact-ID, preservation, cleanup, and value-free evidence validators"
    - path: tests/test-memory-cutover-contract.py
      provides: "Upgrade-state, failure-injection, schema, UAT, and correction contract coverage"
    - path: .planning/milestones/v4.0-phases/21-restore-cutover-recovery/21-CONTINUE-HERE.md
      provides: "Lossless milestone-owned archive of the stale Phase 21 continuation"
  key_links:
    - from: scripts/configure-solidstats-memory-client.py#PREDECESSOR_TOOLS
      to: scripts/configure-solidstats-memory-client.py#ENABLED_TOOLS
      via: "append-only exact successor classifier"
      pattern: "PREDECESSOR_TOOLS.*mempalace_update_drawer"
    - from: scripts/configure-solidstats-memory-client.py#_atomic_replace
      to: tests/test-memory-cutover-contract.py#test_client_atomic_exchange_preserves_boundary_writer
      via: "existing lock/exchange/read-back transaction and adversarial failure tests"
      pattern: "atomic.*rollback.*boundary_writer"
    - from: scripts/probe-solidstats-memory.py#validate_probe_evidence
      to: tests/test-memory-cutover-contract.py#test_behavior_probe_cleans_exact_capture_after_lost_add_ack
      via: "value-free evidence and exact cleanup fixtures"
      pattern: "exact_id.*cleanup.*lost_ack"
  prohibitions:
    - "[flagged-unverified] Never broaden the client surface beyond the exact eight."
    - "[flagged-unverified] Never mutate, archive, or reclassify an archive drawer."
    - "[flagged-unverified] Never alter unrelated palace data or any approved-drawer field except metadata.wing."
    - "[flagged-unverified] Never leave a disposable UAT drawer."
    - "[flagged-unverified] Never run runtime/Kubernetes/nginx/VPS operations."
---

<!-- markdownlint-disable MD013 MD033 MD041 -->

<objective>
Implement and exhaustively test the exact curator update surface, then clear the
two stale Phase 21 health artifacts without data loss.

Purpose: Deliver all offline code, rollback, schema, fixture, and cleanup work
before any machine-local policy materialization.
Output: A tested exact successor transaction, deterministic live validators,
private lock retention, preserved branch, and lossless continuation archive.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21.1-curator-update-surface/21.1-CONTEXT.md
@.planning/phases/21.1-curator-update-surface/21.1-RESEARCH.md
@.planning/phases/21.1-curator-update-surface/21.1-PATTERNS.md
@.planning/phases/21.1-curator-update-surface/21.1-VALIDATION.md
@.planning/phases/21-restore-cutover-recovery/21-VERIFICATION.md
@.planning/phases/21-restore-cutover-recovery/21-SECURITY.md
@scripts/configure-solidstats-memory-client.py
@scripts/probe-solidstats-memory.py
@tests/test-memory-cutover-contract.py
@config/solidstats-memory/migration-policy.json
</context>

<interfaces>

- `PREDECESSOR_TOOLS`: immutable ordered seven-tool tuple.
- `ENABLED_TOOLS`: exact successor formed by appending only `mempalace_update_drawer`.
- CLI modes: `upgrade-validate-predecessor`, `upgrade`, `upgrade-validate-successor`, `upgrade-rollback`.
- Evidence mode: `validate-evidence --evidence <path>`; offline and network-free.

</interfaces>

## Artifacts this phase produces

- Exact-state client-policy upgrade and rollback implementation.
- Adversarial offline coverage for policy, schema, UAT, correction, and evidence.
- Retained private lock state, preserved worktree branch, and archived continuation.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the exact seven-to-eight atomic policy upgrade</name>
  <reversibility rating="reversible">Checked-in code and tests are commit-revertible, and the live transaction retains exact rollback state.</reversibility>
  <read_first>
    - [src: .planning/phases/21.1-curator-update-surface/21.1-CONTEXT.md#Exact-Client-Surface]
    - [src: .planning/phases/21.1-curator-update-surface/21.1-RESEARCH.md#Pattern-2-Dedicated-Upgrade-Transaction]
    - [src: scripts/configure-solidstats-memory-client.py#inspect_policy]
    - [src: scripts/configure-solidstats-memory-client.py#_atomic_replace]
    - [src: tests/test-memory-cutover-contract.py#test_client_policy_add_validate_and_exact_rollback]
  </read_first>
  <files>scripts/configure-solidstats-memory-client.py; tests/test-memory-cutover-contract.py</files>
  <behavior>
    - Exact predecessor input -> exact successor in stable tuple order.
    - Exact successor input -> success with zero config or metadata byte changes.
    - Any missing, empty, null, reordered, duplicate, partial, conflicting, malformed, or broader state -> failure with zero config byte changes.
    - Publication/read-back/rollback uncertainty -> exact predecessor or successor; never a mixed state or unrelated-writer overwrite.
  </behavior>
  <action>Write the failing CLI tracer first. Preserve the old seven values as `PREDECESSOR_TOOLS`; define `ENABLED_TOOLS` by appending only `mempalace_update_drawer` per D-01/D-02. Add explicit `upgrade-validate-predecessor`, `upgrade`, `upgrade-validate-successor`, and `upgrade-rollback` commands. Classify the complete parsed policy before mutation, require the exact `solidstats_memory` registration/token binding and legacy absence, and refuse every state other than exact predecessor or exact successor per D-03. Reuse the existing lock, exchange, read-back, dedicated mode-0600 prestate/accepted-state metadata, and exact rollback transaction per D-04; never route this through the Phase 21 initial no-policy apply path. Retain exact tuple equality and every tunnel, KG, diary, checkpoint, mine, sync, hook, admin, bulk-delete, and unrelated mutation defense while removing only the obsolete substring collision with the newly approved tool. [src: .planning/phases/21.1-curator-update-surface/21.1-RESEARCH.md#Required-Upgrade-State-Matrix]</action>
  <acceptance_criteria>The exact predecessor upgrades once, exact successor replay changes zero bytes, all other policy shapes fail closed, and every publication/read-back/rollback failure preserves an accepted exact state plus unrelated bytes.</acceptance_criteria>
  <verify>
    <automated>timeout 150s python3 -B -m unittest tests/test-memory-cutover-contract.py -k client_policy</automated>
  </verify>
  <done>The production policy path proves one exact predecessor-to-successor transaction with complete fail-closed and rollback behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete schema, failure-injection, UAT, and correction contracts</name>
  <reversibility rating="reversible">Pure validators, in-memory fixtures, and tests do not mutate live palace state.</reversibility>
  <read_first>
    - [src: .planning/phases/21.1-curator-update-surface/21.1-CONTEXT.md#UAT-and-Approved-Correction]
    - [src: .planning/phases/21.1-curator-update-surface/21.1-RESEARCH.md#Required-Failure-Injection-Matrix]
    - [src: .planning/phases/21.1-curator-update-surface/21.1-RESEARCH.md#Live-Evidence-and-Cleanup-Contract]
    - [src: scripts/probe-solidstats-memory.py#_listed_drawer_ids]
    - [src: scripts/probe-solidstats-memory.py#_validate_evidence_node]
    - [src: tests/test-memory-cutover-contract.py#test_behavior_probe_cleans_exact_capture_after_lost_add_ack]
  </read_first>
  <files>scripts/probe-solidstats-memory.py; tests/test-memory-cutover-contract.py</files>
  <behavior>
    - Fresh client source plus exact ordered tuple/schema -> accepted; raw-protocol or non-exact source -> rejected.
    - One `uat-<nonce>` drawer -> add/get/update/get/delete with final inventory equality; zero/multiple/ambiguous match -> rejected.
    - Approved exact drawer -> only metadata.wing may differ; archive/unapproved target, extra payload field, or any other object delta -> rejected.
    - Lost add/update/delete acknowledgement -> exact-ID reconciliation before retry and mandatory residue cleanup.
  </behavior>
  <action>Add failing fixtures, then pure validators for the exact fresh-client tuple/update schema, private complete-object snapshots, strict `uat-&lt;nonce&gt;` sequence, exact-ID reconciliation, final inventory equality, and the approved one-field correction per D-05/D-06/D-07/D-08. Reject raw MCP enumeration as client-filter proof, archive IDs/wings, substitutions, missing/empty/null objects, zero/multiple/ambiguous matches, extra update fields, and any canonical delta except `metadata.wing`. Model server application both before and after acknowledgement loss for add/update/delete and require exact-ID read-back before retry. Extend recursive aggregate-only public evidence validation without permitting content, raw IDs, room, wing, provenance, metadata, session values, tokens, or private paths. Expose deterministic offline `validate-evidence --evidence &lt;path&gt;`; it must never open a network connection. Run the complete three-module memory contract suite and Python compilation after focused tests. [src: .planning/phases/21.1-curator-update-surface/21.1-RESEARCH.md#Pattern-4-Private-Snapshot-Minimal-Mutation-Value-Free-Evidence]</action>
  <acceptance_criteria>All policy, schema, failure-injection, lost-acknowledgement, exact-ID, preservation, cleanup, and evidence cases pass; the full offline suite is green and value-free.</acceptance_criteria>
  <verify>
    <automated>timeout 150s python3 -B -m unittest tests/test-memory-cutover-contract.py tests/test-memory-runtime-contract.py tests/test-memory-operator-contract.py &amp;&amp; python3 -B -m py_compile scripts/configure-solidstats-memory-client.py scripts/probe-solidstats-memory.py</automated>
  </verify>
  <done>The complete offline acceptance contract is deterministic before any client materialization or live call.</done>
</task>

<task type="auto">
  <name>Task 3: Retain stale private state and archive the continuation</name>
  <precondition>The registered stale worktree is clean, on branch `worktree-agent-p21-02-wave1`, and contains only the expected `.21-02-restore.lock` private artifact.</precondition>
  <reversibility rating="costly">The branch can recreate the worktree and the private lock is retained, but restoring the exact operator checkout requires deliberate reconstruction.</reversibility>
  <read_first>
    - [src: .planning/phases/21.1-curator-update-surface/21.1-CONTEXT.md#Health-Cleanup]
    - [src: .planning/phases/21.1-curator-update-surface/21.1-RESEARCH.md#Health-Cleanup-Constraints]
    - [src: .planning/.continue-here.md#BLOCKING-CONSTRAINTS-Read-Before-Anything-Else]
    - [src: .planning/phases/21-restore-cutover-recovery/21-SECURITY.md#Residual-Risks-and-Operational-Assumptions]
  </read_first>
  <files>.planning/.continue-here.md; .planning/milestones/v4.0-phases/21-restore-cutover-recovery/21-CONTINUE-HERE.md; durable machine-local lock artifact and locator</files>
  <action>Record the stale branch commit and abort on any unexpected tracked/untracked worktree change. Verify `.21-02-restore.lock` is a non-symlink regular mode-0600 file; copy it into a durable current-user mode-0700 directory as a mode-0600 logical artifact, write a sibling mode-0600 locator with logical ID, stable paths, source/destination SHA-256, and branch/worktree identity, then re-read both files and recompute digests. Only after retention passes, remove the exact clean registered worktree through `git worktree remove` and verify branch `worktree-agent-p21-02-wave1` still resolves to its original commit per D-13. Hash `.planning/.continue-here.md`, create only the established v4.0 milestone/phase parent, and `git mv` it to `.planning/milestones/v4.0-phases/21-restore-cutover-recovery/21-CONTINUE-HERE.md`; require identical digest and a Git rename without content normalization per D-14. Keep private values and corpus-bearing paths out of repository output. [src: .planning/phases/21.1-curator-update-surface/21.1-RESEARCH.md#Assumptions-Log]</action>
  <acceptance_criteria>The retained artifact/locator pass mode and digest checks; only the clean stale worktree is absent; the branch is unchanged; and the tracked continuation is losslessly archived at the exact milestone-owned path.</acceptance_criteria>
  <verify>
    <automated>! git worktree list --porcelain | rg -q 'agent-p21-02-wave1' &amp;&amp; git show-ref --verify refs/heads/worktree-agent-p21-02-wave1 &amp;&amp; test ! -e .planning/.continue-here.md &amp;&amp; test -f .planning/milestones/v4.0-phases/21-restore-cutover-recovery/21-CONTINUE-HERE.md &amp;&amp; git diff --check</automated>
  </verify>
  <done>Health cleanup preserves every private and tracked artifact while removing only the stale clean checkout.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Parsed Codex config -> atomic replacement | Untrusted local bytes must classify exactly before mutation. |
| Policy process -> filesystem | Lock, exchange, read-back, rollback, and external-writer races cross here. |
| Live evidence fixtures -> repository evidence | Private drawer-shaped values must never enter Git artifacts. |
| Stale worktree -> durable private retention | The only private lock copy must survive checkout removal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-21.1-01 | Elevation of privilege | Tool allowlist | high | mitigate | Exact ordered tuple equality accepts only seven-to-eight transition or exact successor replay. |
| T-21.1-02 | Tampering | Policy classifier | high | mitigate | Reject missing, empty, null, partial, conflicting, reordered, duplicate, malformed, and broader states before write. |
| T-21.1-03 | Tampering | Atomic transaction | high | mitigate | Preserve lock/exchange/read-back/rollback seams and adversarial external-writer tests. |
| T-21.1-04 | Repudiation | Lost acknowledgement | high | mitigate | Exact-state/config read-back and exact-ID reconciliation determine outcome before retry. |
| T-21.1-05 | Information disclosure | Evidence validators | high | mitigate | Complete objects stay private; recursive public schema permits only aggregate values and digests. |
| T-21.1-06 | Spoofing | Approved target fixtures | high | mitigate | Exact ID plus wing/room checks and complete canonical comparison precede acceptance. |
| T-21.1-07 | Information disclosure | Stale lock | high | mitigate | Retain under 0700/0600 with locator and verified digests before worktree removal. |
| T-21.1-08 | Tampering | Continuation archive | medium | mitigate | Hash before/after and require a Git rename to the established milestone path. |

</threat_model>

<verification>
The exact successor and all adversarial offline cases pass before materialization.
The stale worktree cleanup cannot complete until private retention, branch
preservation, and lossless tracked continuation archival are all proven.
</verification>

<success_criteria>

- Exact predecessor, successor, idempotency, forbidden-state, and rollback contracts pass.
- Schema, UAT, correction, lost-acknowledgement, and public-evidence fixtures pass.
- The complete offline suite and compilation pass.
- Private lock state, branch recoverability, and tracked continuation history are preserved.
</success_criteria>

<output>
Create `.planning/phases/21.1-curator-update-surface/21.1-01-SUMMARY.md` and
commit all offline implementation, tests, and health cleanup before Plan 02.
</output>
