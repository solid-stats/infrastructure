---
phase: "03"
plan: "03-01"
title: "App CD ownership boundary"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - docs/staging.md
  - README.md
  - scripts/validate-staging.py
autonomous: true
requirements:
  - OWN-03
  - OWN-04
requirements_addressed:
  - OWN-03
  - OWN-04
---

<objective>
Document and validate the boundary where app repositories build images while
this repository owns staging deploy wiring and pinned image tags.
</objective>

<tasks>
<task id="03-01-01" type="auto">
<read_first>
- docs/staging.md
- README.md
</read_first>
<action>
Add a staging handoff matrix to `docs/staging.md` naming resources app
repositories should stop applying in v1, and add an image tag update procedure
for `server-2`, `replay-parser-2`, and `replays-fetcher`. Update `README.md`
to point operators to that handoff section.
</action>
<acceptance_criteria>
- `grep -q "Staging Handoff Matrix" docs/staging.md`
- `grep -q "Update a pinned app image" docs/staging.md`
- `grep -q "server-2" docs/staging.md`
- `grep -q "replay-parser-2" docs/staging.md`
- `grep -q "replays-fetcher" docs/staging.md`
- `grep -q "Staging Handoff Matrix" README.md`
</acceptance_criteria>
</task>

<task id="03-01-02" type="auto">
<read_first>
- scripts/validate-staging.py
- k8s/staging/35-server-2-deployment.yaml
- k8s/staging/40-replay-parser-2.yaml
- k8s/staging/50-replays-fetcher.yaml
</read_first>
<action>
Extend `scripts/validate-staging.py` so GHCR app images for `server-2`,
`replay-parser-2`, and `replays-fetcher` must have an explicit non-`latest`
tag. The validator must fail on `:latest`.
</action>
<acceptance_criteria>
- `grep -q "latest" scripts/validate-staging.py`
- `python3 scripts/validate-staging.py`
</acceptance_criteria>
</task>
</tasks>

<verification>
- Run `python3 scripts/validate-staging.py`.
</verification>

<success_criteria>
- Operators can identify app-CD overlap.
- Operators can update pinned image tags in this repo.
- Validation blocks mutable `latest` app images.
</success_criteria>
