---
phase: 10-s3-lifecycle-retention
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/s3/backups-lifecycle.json
  - scripts/apply-s3-lifecycle.sh
  - scripts/validate-s3-lifecycle.py
  - scripts/validate-staging.py
autonomous: true
requirements:
  - S3-01
  - S3-02
must_haves:
  truths:
    - "config/s3/backups-lifecycle.json exists, is valid JSON, and contains an Expiration rule scoped to backups/postgres/ with DaysAfterInitiation >= 30"
    - "config/s3/backups-lifecycle.json contains an AbortIncompleteMultipartUpload rule (S3-02)"
    - "scripts/apply-s3-lifecycle.sh passes bash -n and contains a GET-before-PUT warn, --endpoint-url https://s3.twcstorage.ru, path-style flag, and references config/s3/backups-lifecycle.json"
    - "scripts/validate-s3-lifecycle.py passes offline (stdlib-only, json) and confirms both rules without touching S3"
    - "validate-staging.py script-syntax check covers validate-s3-lifecycle.py and apply-s3-lifecycle.sh"
  artifacts:
    - path: "config/s3/backups-lifecycle.json"
      provides: "PutBucketLifecycleConfiguration JSON — Expiration + AbortIncompleteMultipartUpload"
      contains: "backups/postgres/"
    - path: "scripts/apply-s3-lifecycle.sh"
      provides: "Operator script: applies lifecycle config via in-cluster Job"
      min_lines: 40
    - path: "scripts/validate-s3-lifecycle.py"
      provides: "Offline validator for S3-01/S3-02 regression guard"
      exports: ["main"]
  key_links:
    - from: "scripts/apply-s3-lifecycle.sh"
      to: "config/s3/backups-lifecycle.json"
      via: "aws s3api put-bucket-lifecycle-configuration --lifecycle-configuration file:///repo/config/s3/backups-lifecycle.json"
      pattern: "backups-lifecycle.json"
    - from: "scripts/validate-staging.py"
      to: "scripts/validate-s3-lifecycle.py"
      via: "py_compile.compile() in validate_scripts()"
      pattern: "validate-s3-lifecycle"
---

<objective>
Store the S3 retention policy as a repo artifact and provide both the offline regression guard and
the apply mechanism — all without touching a live S3 endpoint.

Purpose: S3-01 (per-prefix expiration policy, repo-stored, script-applied) and S3-02 (abort
incomplete multipart uploads) are fully satisfied offline. The lifecycle JSON is the authoritative
source; the apply script is the one-command operator action that wires it to the bucket.

Output:
- config/s3/backups-lifecycle.json — PutBucketLifecycleConfiguration shape with two rules
- scripts/apply-s3-lifecycle.sh — operator script that creates an in-cluster Job to apply it
- scripts/validate-s3-lifecycle.py — stdlib-only offline validator
- scripts/validate-staging.py updated to cover both new files in validate_scripts()
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-s3-lifecycle-retention/10-CONTEXT.md

@k8s/staging/60-postgres-backup.yaml
@scripts/backup-postgres-now.sh
@scripts/validate-staging.py
@scripts/validate-edge.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lifecycle JSON + offline validator (S3-01, S3-02)</name>
  <files>config/s3/backups-lifecycle.json, scripts/validate-s3-lifecycle.py</files>
  <behavior>
    - validate_lifecycle_json(): loads config/s3/backups-lifecycle.json, asserts Rules list is non-empty
    - expiration_rule: exactly one Rule with Filter.Prefix == "backups/postgres/" AND Expiration.Days >= 30 AND Status == "Enabled"
    - abort_rule: at least one Rule with AbortIncompleteMultipartUpload.DaysAfterInitiation >= 1 AND Status == "Enabled"
    - No boto3/requests — stdlib json + pathlib only
    - validate_s3_lifecycle_script_syntax(): bash -n scripts/apply-s3-lifecycle.sh returns 0
    - main() prints "ok: s3 lifecycle JSON" and "ok: apply script syntax", returns 0 on pass; prints "error: ..." to stderr, returns 1 on fail
  </behavior>
  <action>
  Create config/s3/backups-lifecycle.json with the PutBucketLifecycleConfiguration JSON body.
  Use two rules:
    Rule 1 — ID "expire-postgres-backups", Status "Enabled", Filter { Prefix: "backups/postgres/" },
    Expiration { Days: 30 }. Justification for 30d: daily backups retained for 30 days gives a
    comfortable recovery window (30 backup points) without unbounded storage growth; conservative
    enough that an operator can adjust upward before applying without risk.
    Rule 2 — ID "abort-incomplete-multipart", Status "Enabled", Filter { Prefix: "" } (bucket-wide),
    AbortIncompleteMultipartUpload { DaysAfterInitiation: 7 }. Justification: 7 days is the standard
    AWS-recommended window; catches stuck uploads from any prefix without touching completed objects.

  Create scripts/validate-s3-lifecycle.py following the style of scripts/validate-edge.py:
  - #!/usr/bin/env python3, stdlib only (json, pathlib, subprocess, sys)
  - ROOT = Path(__file__).resolve().parents[1]
  - require(condition, message) helper identical to existing validators
  - validate_lifecycle_json(): read and json.loads(config/s3/backups-lifecycle.json),
    walk Rules list, assert one Expiration rule scoped to "backups/postgres/" with Days >= 30
    and Status "Enabled", assert one AbortIncompleteMultipartUpload rule with
    DaysAfterInitiation >= 1 and Status "Enabled". Print "ok: s3 lifecycle JSON".
  - validate_apply_script_syntax(): subprocess bash -n scripts/apply-s3-lifecycle.sh;
    assert returncode == 0. Also assert "set -euo pipefail" in content,
    "exit 64" in content (missing-config convention), "get-bucket-lifecycle-configuration" in content
    (GET-before-PUT warn), "put-bucket-lifecycle-configuration" in content,
    "https://s3.twcstorage.ru" in content, "backups-lifecycle.json" in content.
    Print "ok: apply script syntax and markers".
  - main() runs both checks in a try/except ValidationError block, returns 0/1.
  </action>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/infrastructure && python3 scripts/validate-s3-lifecycle.py</automated>
  </verify>
  <done>validate-s3-lifecycle.py exits 0; both "ok:" lines printed; config/s3/backups-lifecycle.json is valid JSON with Expiration on backups/postgres/ (30d) and AbortIncompleteMultipartUpload (7d)</done>
</task>

<task type="auto">
  <name>Task 2: apply-s3-lifecycle.sh — operator apply script (S3-01)</name>
  <files>scripts/apply-s3-lifecycle.sh</files>
  <action>
  Create scripts/apply-s3-lifecycle.sh following the style of scripts/backup-postgres-now.sh.
  The script creates a one-shot k8s Job that mounts the server-2-runtime secret and runs
  aws s3api commands inside the cluster so S3 credentials never leave the cluster.

  Script structure:
  - #!/usr/bin/env bash, set -euo pipefail
  - required() helper: prints "FATAL: $1" to stderr and exits 64 if env var is unset/empty.
    Use: required KUBECONFIG or rely on default kubectl context.
  - Variables: namespace="${K8S_NAMESPACE:-solid-stats-staging}", job_name="apply-s3-lifecycle-$(date -u +%Y%m%d%H%M%S)", timeout="${JOB_TIMEOUT:-120s}".
  - The Job spec is embedded as a here-doc (bash, not fish — this runs server-side as bash in the Job container) passed to kubectl apply -f -.
    Job name: $job_name, namespace: $namespace.
    Container image: amazon/aws-cli:2.15.30 (pin to a specific tag; aws-cli is already in the backup image via apk but a standalone aws-cli image is cleaner for an operator one-shot).
    Alternatively, use the same postgres:17-alpine + apk add aws-cli pattern from 60-postgres-backup.yaml to avoid a new image dependency — prefer this for consistency (no new image legitimacy audit needed since it mirrors the backup pattern exactly).
    Mount server-2-runtime as env vars: AWS_ACCESS_KEY_ID from S3_ACCESS_KEY_ID key, AWS_SECRET_ACCESS_KEY from S3_SECRET_ACCESS_KEY key, S3_BUCKET from S3_BUCKET key.
    Env literals: AWS_DEFAULT_REGION=ru-1, AWS_EC2_METADATA_DISABLED=true, S3_ENDPOINT=https://s3.twcstorage.ru.
    Container command (sh -ec):
      1. aws configure set default.s3.addressing_style path (export AWS_CONFIG_FILE=/tmp/aws-config first)
      2. GET-before-PUT: run aws --endpoint-url="$S3_ENDPOINT" s3api get-bucket-lifecycle-configuration --bucket "$S3_BUCKET" 2>&1; capture exit code.
         If exit code == 0, print "WARN: bucket already has a lifecycle configuration — review before applying" to stderr.
         If exit code != 0 AND output contains "NoSuchLifecycleConfiguration", treat as expected (first apply).
         If exit code != 0 AND output does NOT contain "NoSuchLifecycleConfiguration" AND does not contain "NotImplemented", abort with "FATAL: unexpected error from get-bucket-lifecycle-configuration" exit 1.
      3. Copy the lifecycle JSON into the Job pod: the JSON file must be embedded in the Job command or volume-mounted. Use a ConfigMap mount approach: the script creates a ConfigMap from the file before launching the Job, then mounts it as a volume at /config/backups-lifecycle.json. Clean up the ConfigMap after the Job completes.
         Alternative (simpler): embed the JSON as a heredoc inside the container command string, written to /tmp/backups-lifecycle.json inside the container. This avoids a ConfigMap. Use this approach — write the JSON content via printf or cat to /tmp/backups-lifecycle.json in the container, then reference file:///tmp/backups-lifecycle.json.
         To avoid drift between the repo file and the embedded copy, the script reads config/s3/backups-lifecycle.json at Job-creation time and embeds the content via a shell variable substitution inside the Job's command string. The script must abort if config/s3/backups-lifecycle.json is not found (exit 64).
      4. aws --endpoint-url="$S3_ENDPOINT" s3api put-bucket-lifecycle-configuration --bucket "$S3_BUCKET" --lifecycle-configuration "$(< /tmp/backups-lifecycle.json)"
      5. Print "lifecycle configuration applied successfully" and exit 0.
    Job spec: restartPolicy Never, backoffLimit 0, ttlSecondsAfterFinished 86400.
    ServiceAccount: use postgres-backup SA (already exists, has no S3 RBAC — that is correct; the SA is just for pod identity, not k8s API access). Set automountServiceAccountToken: false.
    securityContext: allowPrivilegeEscalation: false. Resource requests: cpu 50m, memory 128Mi; limits: cpu 200m, memory 256Mi.
  - After kubectl apply -f - creates the Job, wait: kubectl -n "$namespace" wait --for=condition=complete "job/$job_name" --timeout="$timeout"; on failure print logs and exit 1.
  - Print logs on success (kubectl -n "$namespace" logs "job/$job_name").
  - Delete the Job after success (kubectl -n "$namespace" delete "job/$job_name" --ignore-not-found) with a note that ttl will also clean it.
  - The lifecycle JSON manifest directory: The Job spec is created inline by the script (kubectl apply -f -), so NO manifest file is written to k8s/staging/ — the Job is purely operator-driven, never in the CD glob. Document this in a comment at the top of the script.
  </action>
  <verify>
    <automated>bash -n /home/afgan0r/Projects/SolidGames/infrastructure/scripts/apply-s3-lifecycle.sh && python3 /home/afgan0r/Projects/SolidGames/infrastructure/scripts/validate-s3-lifecycle.py</automated>
  </verify>
  <done>bash -n passes; validate-s3-lifecycle.py exits 0 (both "ok:" lines including "ok: apply script syntax and markers")</done>
</task>

<task type="auto">
  <name>Task 3: Wire validate-staging.py to cover new files (S3-01, S3-02 regression guard)</name>
  <files>scripts/validate-staging.py</files>
  <action>
  Extend validate_scripts() in scripts/validate-staging.py to add:
  1. py_compile.compile(str(ROOT / "scripts" / "validate-s3-lifecycle.py"), doraise=True) — after the existing validate-edge.py compile line.
  2. bash -n check for scripts/apply-s3-lifecycle.sh — add "scripts/apply-s3-lifecycle.sh" to the list of shell scripts checked with run(["bash", "-n", script]).

  Also add a new top-level check function validate_s3_lifecycle_config() that:
  - Reads config/s3/backups-lifecycle.json as text, json.loads it.
  - Asserts the parsed dict has a "Rules" key with a non-empty list.
  - Asserts at least one Rule has Filter.Prefix == "backups/postgres/" with Expiration.Days >= 1 and Status == "Enabled". (Intentionally >= 1, not >= 30, so the validator does not break if the window is ever raised by the operator; the validate-s3-lifecycle.py script is the strict 30d gate.)
  - Asserts at least one Rule with AbortIncompleteMultipartUpload present and Status == "Enabled".
  - Prints "ok: s3 lifecycle config".

  Wire validate_s3_lifecycle_config into the main() checks list after the existing checks:
  ("s3 lifecycle config", validate_s3_lifecycle_config)

  Do not change any existing check names, order, or logic — only append.
  </action>
  <verify>
    <automated>cd /home/afgan0r/Projects/SolidGames/infrastructure && python3 scripts/validate-staging.py</automated>
  </verify>
  <done>validate-staging.py exits 0; new "ok: s3 lifecycle config" line appears in output</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator machine → k8s API | apply-s3-lifecycle.sh submits a Job via kubectl; kubectl creds from local kubeconfig or KUBECONFIG env var |
| k8s Job → S3 endpoint | Job pod holds S3 creds from server-2-runtime Secret; creds never written to disk or echoed in logs |
| config/s3/ directory → git | lifecycle JSON contains no secrets — only prefix strings and day counts; safe to commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | config/s3/backups-lifecycle.json | mitigate | validate-s3-lifecycle.py asserts Prefix == "backups/postgres/" and Days >= 30 in CI; drift from mis-edit is caught before operator runs apply |
| T-10-02 | Tampering | apply-s3-lifecycle.sh overwrites existing lifecycle config | mitigate | GET-before-PUT: script calls get-bucket-lifecycle-configuration first; prints WARN if existing config found; operator must review before applying |
| T-10-03 | Information Disclosure | S3 creds (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY) | mitigate | creds stay in-cluster (k8s Secret → Job env vars); script never echoes secret values; aws-cli does not log them by default |
| T-10-04 | Denial of Service | Expiration rule too broad (no prefix filter) | mitigate | Rule 1 Filter.Prefix = "backups/postgres/" scopes deletion; Rule 2 AbortIncompleteMultipartUpload is bucket-wide but targets only in-progress uploads, not completed objects |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | no package manager used; aws-cli installed via apk inside the Job container at runtime, matching the existing backup pattern; no new registry packages in this plan |
</threat_model>

<verification>
## Offline Phase Checks

1. python3 scripts/validate-s3-lifecycle.py → exits 0, two "ok:" lines
2. python3 scripts/validate-staging.py → exits 0, includes "ok: s3 lifecycle config"
3. bash -n scripts/apply-s3-lifecycle.sh → exits 0
4. python3 -c "import json; d=json.load(open('config/s3/backups-lifecycle.json')); assert any(r.get('Filter',{}).get('Prefix')=='backups/postgres/' for r in d['Rules'])" → exits 0
</verification>

<success_criteria>
- config/s3/backups-lifecycle.json: valid JSON, Expiration rule on backups/postgres/ with Days 30, AbortIncompleteMultipartUpload rule with DaysAfterInitiation 7
- scripts/apply-s3-lifecycle.sh: bash -n clean; contains GET-before-PUT warn, path-style config, https://s3.twcstorage.ru, exit 64 for missing config
- scripts/validate-s3-lifecycle.py: exits 0 offline; validates both rules
- scripts/validate-staging.py: exits 0; includes s3 lifecycle config check
</success_criteria>

<output>
Create `.planning/phases/10-s3-lifecycle-retention/10-01-SUMMARY.md` when done
</output>
