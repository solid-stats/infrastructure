---
phase: 19-solidstats-memory-foundation
plan: 05
type: execute
gap_closure: true
wave: 5
depends_on:
  - 19-04
files_modified:
  - k8s/observability/10-prometheus.yaml
  - scripts/validate-obs-manifests.py
  - tests/test-memory-runtime-contract.py
autonomous: true
requirements:
  - OPS-04
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low
premises:
  - claim: "A fresh chart v29.11.0 render places the SolidStats memory alerts in ConfigMap data.alerting_rules.yml, while the committed render leaves that key empty and puts them in data.rules."
    src: ".planning/phases/19-solidstats-memory-foundation/19-UAT.md#pinned-chart-render-parity"
    verify: "timeout 10s rg -n 'result: issue|data.alerting_rules.yml|data.rules' .planning/phases/19-solidstats-memory-foundation/19-UAT.md"
  - claim: "The authoritative values define memory recording rules under serverFiles.recording_rules.yml and alerts under serverFiles.alerting_rules.yml."
    src: "k8s/observability/values/prometheus-values.yaml#serverFiles"
    verify: "timeout 10s rg -n '^  recording_rules.yml:|^  alerting_rules.yml:|solidstats-memory-recording|solidstats-memory-alerts' k8s/observability/values/prometheus-values.yaml"
  - claim: "The observability deploy workflow applies the committed top-level Prometheus manifest and does not render the chart during deployment."
    src: ".github/workflows/deploy-observability.yml#apply-obs-manifests-monitoring"
    verify: "timeout 10s rg -n 'Apply obs manifests \(monitoring\)|find k8s/observability|maxdepth 1|kubectl --context obs-k3s-staging apply' .github/workflows/deploy-observability.yml"
  - claim: "The current runtime test reads the rendered snapshot alert from data.rules, so it accepts the wrong chart key."
    src: "tests/test-memory-runtime-contract.py#PrometheusMemoryContractTests.snapshot_alert_expression"
    verify: "timeout 10s rg -n 'snapshot_alert_expression|document\[\"data\"\]\[\"rules\"\]' tests/test-memory-runtime-contract.py"
  - claim: "The observability validator checks marker presence and the snapshot expression globally, but does not scope alerts and recording rules to chart-owned ConfigMap data keys."
    src: "scripts/validate-obs-manifests.py#_check_memory_monitoring_contract"
    verify: "timeout 10s rg -n '_check_memory_monitoring_contract|markers =|_snapshot_alert_expression' scripts/validate-obs-manifests.py"
carried_forward:
  - source: ".planning/phases/19-solidstats-memory-foundation/19-04-SUMMARY.md#deferred-operator-gates"
    item: "Fresh pinned-chart render parity remained an operator gate after Plan 04."
    disposition: "This plan closes the source defect and leaves a fresh exact-command render plus cmp as the final networked UAT evidence."
  - source: ".planning/phases/19-solidstats-memory-foundation/19-04-SUMMARY.md#decisions-made"
    item: "Snapshot alert identity is derived from the sole backup CronJob in k8s/memory/40-backup.yaml."
    disposition: "Preserve that derivation and update every rendered-alert reader to the chart-owned alerting_rules.yml key."
  - source: ".planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md#behavior-unverified-items"
    item: "kube-router enforcement still requires isolated-cluster operator evidence."
    disposition: "Preserve as a separate human gate; no task changes or claims runtime NetworkPolicy behavior."
must_haves:
  truths:
    - "k8s/observability/10-prometheus.yaml is produced only by the exact pinned Helm command from authoritative prometheus-values.yaml, with no hand edits to generated output."
    - "ConfigMap/prometheus-server stores all eight SolidStats memory alerts only in data.alerting_rules.yml and all seven memory recording rules only in data.recording_rules.yml."
    - "No SolidStats memory alert or recording-rule definition appears in data.rules, data.alerts, data.prometheus.yml, or any other non-authoritative ConfigMap data key."
    - "Offline validator and PyYAML tests fail when a memory rule is missing, duplicated, moved to a wrong key, or semantically different from authoritative values."
    - "Observer jobs, Kubernetes node-volume scraping, all accepted rule expressions and durations, and the manifest-derived solidstats-memory-backup selector survive the mechanical rerender."
    - "Ordinary repository validation remains offline: it does not download charts, contact a cluster, read secrets, or mutate live infrastructure."
  artifacts:
    - path: "k8s/observability/10-prometheus.yaml"
      provides: "Unmodified output of prometheus-community/prometheus v29.11.0 rendered from the authoritative values."
    - path: "scripts/validate-obs-manifests.py"
      provides: "ConfigMap-data-key-aware rule placement and source/render semantic validation."
    - path: "tests/test-memory-runtime-contract.py"
      provides: "TDD coverage for correct chart-owned files, wrong-key rejection, accepted signal preservation, and CronJob-derived snapshot selection."
  key_links:
    - from: "k8s/observability/values/prometheus-values.yaml serverFiles.alerting_rules.yml"
      to: "ConfigMap/prometheus-server data.alerting_rules.yml"
      via: "exact Helm v29.11.0 template serialization and semantic equality tests"
    - from: "k8s/observability/values/prometheus-values.yaml serverFiles.recording_rules.yml"
      to: "ConfigMap/prometheus-server data.recording_rules.yml"
      via: "exact Helm v29.11.0 template serialization and semantic equality tests"
    - from: "k8s/memory/40-backup.yaml CronJob metadata.name"
      to: "SolidStatsMemorySnapshotMissingOrStale in data.alerting_rules.yml"
      via: "existing manifest-derived selector helper and updated rendered-key reader"
    - from: ".github/workflows/deploy-observability.yml"
      to: "k8s/observability/10-prometheus.yaml"
      via: "top-level manifest apply after offline validation"
---

<!-- markdownlint-disable MD013 MD033 MD041 -->

<objective>
Restore pinned-chart render parity and make the Prometheus rule-file ownership contract fail closed.

Purpose: Ensure the committed manifest that observability deployment consumes is the exact chart v29.11.0 output and that Prometheus loads every accepted SolidStats memory rule from the chart-owned files.

Output: One mechanically regenerated Prometheus manifest plus structural validator and TDD coverage that reject future wrong-key or source/render drift.
</objective>

## Artifacts this phase produces

| File | Exact additions or target state |
| --- | --- |
| `k8s/observability/10-prometheus.yaml` | Byte-for-byte output of `helm template prometheus prometheus-community/prometheus --version 29.11.0 --namespace monitoring --values k8s/observability/values/prometheus-values.yaml`; memory alerts are serialized under `data.alerting_rules.yml`, recording rules under `data.recording_rules.yml`, and no generated line is hand-edited. |
| `scripts/validate-obs-manifests.py` | Helpers `_prometheus_server_data_blocks`, `_memory_rule_names`, and `_check_memory_rule_files`; `_check_memory_monitoring_contract` reads the correct chart-owned blocks, compares them with authoritative values, rejects memory rules in other data keys, and keeps `_memory_backup_cronjob_name` authoritative for snapshot selection. No new CLI flag or networked validation path. |
| `tests/test-memory-runtime-contract.py` | `PrometheusMemoryContractTests.prometheus_server_data`, updated `snapshot_alert_expression`, semantic source/render rule-file assertions, and negative fixtures for moved, duplicated, missing, or wrong-key memory rules. |

No render helper is added: the exact pinned command is already recorded in the authoritative values header. Ordinary validation remains offline, while fresh networked UAT renders to a temporary file and uses `cmp` against the committed artifact.

<execution_context>
@/home/afgan0r/.codex/gsd-core/workflows/execute-plan.md
@/home/afgan0r/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-solidstats-memory-foundation/19-CONTEXT.md
@.planning/phases/19-solidstats-memory-foundation/19-RESEARCH.md
@.planning/phases/19-solidstats-memory-foundation/19-SPEC.md
@.planning/phases/19-solidstats-memory-foundation/19-04-SUMMARY.md
@.planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md
@.planning/phases/19-solidstats-memory-foundation/19-UAT.md
@.planning/graphs/GRAPH_COMMUNITIES.md
@k8s/memory/40-backup.yaml
@k8s/observability/values/prometheus-values.yaml
@k8s/observability/10-prometheus.yaml
@scripts/validate-obs-manifests.py
@tests/test-memory-runtime-contract.py
@.github/workflows/deploy-observability.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Regenerate and consume the chart-owned memory rule files</name>
  <files>k8s/observability/10-prometheus.yaml, scripts/validate-obs-manifests.py, tests/test-memory-runtime-contract.py</files>
  <precondition>The approved render environment resolves `prometheus-community/prometheus` version `29.11.0` and has `helm` available; if that exact chart cannot be rendered, halt without changing the committed manifest.</precondition>
  <read_first>
    - `.planning/phases/19-solidstats-memory-foundation/19-UAT.md` — fresh-render evidence, exact command, and the observed `data.alerting_rules.yml` versus `data.rules` mismatch.
    - `.planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md` — accepted OPS-04 signals and the still-separate kube-router human gate.
    - `.planning/phases/19-solidstats-memory-foundation/19-04-SUMMARY.md` — the CronJob-derived selector and all Plan 04 contracts that must survive rerendering.
    - `k8s/observability/values/prometheus-values.yaml` — authoritative Helm input, pinned chart command, seven recording rules, eight alerts, scrape jobs, and `solidstats-memory-backup` expression.
    - `k8s/observability/10-prometheus.yaml` — current generated ConfigMap with empty `alerting_rules.yml`, populated `recording_rules.yml`, and wrongly populated `rules`.
    - `scripts/validate-obs-manifests.py` — `_split_documents`, `_metadata_name`, `_snapshot_alert_expression`, `_check_cadvisor_scrape_config`, and `_check_memory_monitoring_contract` extension points.
    - `tests/test-memory-runtime-contract.py` — `PrometheusMemoryContractTests`, especially the current rendered snapshot reader that targets `data.rules`.
    - `.github/workflows/deploy-observability.yml` — proof that the committed top-level manifest is the deployed consumer and validation must remain offline.
    - `.agents/skills/kubernetes-specialist/references/helm-charts.md` — pinned values-driven render and test conventions.
  </read_first>
  <behavior>
    - Test 1: parsed `ConfigMap/prometheus-server.data.alerting_rules.yml` is semantically equal to authoritative `serverFiles.alerting_rules.yml` and contains exactly the eight accepted memory alerts.
    - Test 2: parsed `ConfigMap/prometheus-server.data.recording_rules.yml` is semantically equal to authoritative `serverFiles.recording_rules.yml` and contains exactly the seven accepted memory recording rules.
    - Test 3: the rendered snapshot alert is read from `alerting_rules.yml`, selects the sole CronJob name parsed from `k8s/memory/40-backup.yaml`, and preserves the suspension, age, and duration contract.
    - Test 4: the rendered scrape configuration still contains `solidstats-memory-observer`, `kubernetes-nodes-volume-stats`, authenticated node-proxy access, and both chart-owned paths in `rule_files`.
  </behavior>
  <action>
    Start RED by replacing `PrometheusMemoryContractTests.snapshot_alert_expression` with a parsed-ConfigMap reader that indexes the `prometheus-server` document's `data` mapping and loads `alerting_rules.yml`. Add one semantic test for both chart-owned rule files: PyYAML-load each rendered block, compare it with the matching `serverFiles` mapping from authoritative values, and assert the exact seven recording names and eight alert names already accepted by OPS-04. Keep the existing backup-CronJob helper as the only source for the snapshot selector. The current committed render must fail because `alerting_rules.yml` is empty and the alert reader must not fall back to `rules`. [src: tests/test-memory-runtime-contract.py#PrometheusMemoryContractTests] [src: k8s/observability/values/prometheus-values.yaml#serverFiles] [src: k8s/memory/40-backup.yaml#CronJob]

    Add `_prometheus_server_data_blocks`, `_memory_rule_names`, and `_check_memory_rule_files` to `validate-obs-manifests.py` using the existing standard-library text/YAML-document conventions. Scope extraction to the `ConfigMap` named `prometheus-server`, return each top-level `data` literal block by key, and fail unless the accepted alert names occur in `alerting_rules.yml` and the accepted recording names occur in `recording_rules.yml`. Update `_snapshot_alert_expression` to accept the alerting block rather than scanning the entire manifest. Require `prometheus.yml` to reference `/etc/config/alerting_rules.yml` and `/etc/config/recording_rules.yml`. Do not add PyYAML to the production validator or add a networked Helm call to `validate()`. [src: scripts/validate-obs-manifests.py#_split_documents] [src: scripts/validate-obs-manifests.py#_check_memory_monitoring_contract] [src: .planning/graphs/GRAPH_COMMUNITIES.md#observability-manifests-validation]

    Render to a temporary file with exactly `helm template prometheus prometheus-community/prometheus --version 29.11.0 --namespace monitoring --values k8s/observability/values/prometheus-values.yaml`. Redirect stdout only, require a successful exit and a non-empty multi-document YAML result, then atomically replace `k8s/observability/10-prometheus.yaml`. Do not edit, reorder, trim, or selectively copy any generated line. If a contract check fails, correct the test/validator only when it misunderstood genuine chart v29.11.0 output; if authoritative values are wrong, stop and surface the conflict because this gap does not authorize changing them. Run the memory tests and observability validator immediately after replacement. [src: k8s/observability/values/prometheus-values.yaml#rendered-with] [src: .planning/phases/19-solidstats-memory-foundation/19-UAT.md#pinned-chart-render-parity]
  </action>
  <acceptance_criteria>
    - The committed manifest is the complete stdout of the exact pinned render command and has no manual post-render changes.
    - The `prometheus-server` ConfigMap's `alerting_rules.yml` and `recording_rules.yml` blocks parse to the same mappings as their authoritative values inputs.
    - All eight accepted alerts are in `alerting_rules.yml`; all seven accepted recording rules are in `recording_rules.yml`.
    - `SolidStatsMemorySnapshotMissingOrStale` is found through `alerting_rules.yml` and still derives `cronjob="solidstats-memory-backup"` from the sole backup CronJob contract.
    - The memory observer job, node-volume job, authenticated node-proxy markers, PVC expression, alert thresholds, and durations remain present after the full mechanical rerender.
  </acceptance_criteria>
  <verify>
    <automated>timeout 30s python3 tests/test-memory-runtime-contract.py PrometheusMemoryContractTests &amp;&amp; timeout 10s python3 scripts/validate-obs-manifests.py &amp;&amp; timeout 10s python3 -m py_compile scripts/validate-obs-manifests.py tests/test-memory-runtime-contract.py</automated>
  </verify>
  <done>The deployed Prometheus manifest is a genuine chart v29.11.0 render, and its two chart-owned rule files carry every accepted OPS-04 rule with the actual backup selector.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject wrong-key and duplicate memory rules offline</name>
  <files>scripts/validate-obs-manifests.py, tests/test-memory-runtime-contract.py</files>
  <read_first>
    - `.planning/phases/19-solidstats-memory-foundation/19-UAT.md` — the exact wrong-key regression that a global marker search failed to detect.
    - `k8s/observability/values/prometheus-values.yaml` — read-only authoritative rule sets and order-insensitive semantic source.
    - `k8s/observability/10-prometheus.yaml` — mechanically generated correct-key layout from Task 1, including chart default `rules` and `alerts` keys.
    - `scripts/validate-obs-manifests.py` — Task 1 data-block extractor and rule-file checker to exercise with negative fixtures.
    - `tests/test-memory-runtime-contract.py` — Task 1 parsed source/render assertions and existing CronJob missing/duplicate/drift tests.
    - `k8s/memory/40-backup.yaml` — read-only source for the exact snapshot CronJob name.
  </read_first>
  <behavior>
    - Test 1: moving the alert group from `alerting_rules.yml` to `rules`, `alerts`, `recording_rules.yml`, or an arbitrary data key fails even when every alert name remains globally present.
    - Test 2: moving the recording group from `recording_rules.yml` to `rules`, `alerts`, `alerting_rules.yml`, or an arbitrary data key fails even when every recording name remains globally present.
    - Test 3: duplicating a memory alert or recording rule under both the correct key and any other ConfigMap data key fails.
    - Test 4: removing or renaming one accepted rule, changing one source/render expression, or changing the actual backup CronJob selector fails; YAML mapping serialization order alone does not fail.
  </behavior>
  <action>
    Add table-driven negative fixtures that deep-copy the parsed `prometheus-server.data` mapping, relocate or duplicate the real memory groups across `rules`, `alerts`, the opposite chart-owned file, and one synthetic key, then call the same structural assertion used for the committed manifest. Each fixture must retain the names globally so the test proves placement rather than mere presence. Add missing-name, duplicate-name, changed-expression, and reordered-mapping cases; only semantic changes fail, while mapping-key serialization order is ignored. [src: .planning/phases/19-solidstats-memory-foundation/19-UAT.md#gaps] [src: tests/test-memory-runtime-contract.py#PrometheusMemoryContractTests]

    Harden `_check_memory_rule_files` so `alert:` declarations for the known memory alerts are allowed only in `alerting_rules.yml` and `record:` declarations for the known memory recording rules are allowed only in `recording_rules.yml`. Scan every other `prometheus-server.data` block and reject those declarations or the named memory rule groups there; reject duplicates in the correct block as well. Alert expressions in `alerting_rules.yml` are allowed to reference `solidstats_memory:*` recording names and must not be mistaken for duplicate recording-rule definitions. Compare exact declaration-name sets and the named snapshot expression between authoritative values and rendered blocks instead of accepting whole-file substring presence. Preserve `_memory_backup_cronjob_name` and require the rendered snapshot expression to use its result. Do not reject the chart's empty `rules` or `alerts` defaults; reject only memory definitions under those keys. [src: scripts/validate-obs-manifests.py#_check_memory_monitoring_contract] [src: k8s/observability/10-prometheus.yaml#ConfigMap-prometheus-server]

    Keep the validator's normal entry point fully offline and pointed at committed files. Do not add a Helm download, chart repository update, live Prometheus query, `kubectl`, or secret read. The exact networked render plus byte comparison remains UAT evidence after execution, not part of ordinary CI validation. [src: .github/workflows/deploy-observability.yml#validate] [src: .planning/phases/19-solidstats-memory-foundation/19-CONTEXT.md#locked-decisions]
  </action>
  <acceptance_criteria>
    - Every moved or duplicated memory rule fixture fails for its exact wrong data key even though global marker presence is unchanged.
    - Exact alert and recording name sets, group ownership, and source/render expressions are checked in their own chart-owned files.
    - The chart's empty default `rules` and `alerts` keys are accepted only while they contain no SolidStats memory rule content.
    - Snapshot validation still reads the sole backup CronJob from `40-backup.yaml`; a missing, duplicate, renamed, source-only, or render-only selector fails.
    - The deploy-observability validation job remains network-free and requires no Helm installation or chart repository access.
  </acceptance_criteria>
  <verify>
    <automated>timeout 30s python3 tests/test-memory-runtime-contract.py PrometheusMemoryContractTests &amp;&amp; timeout 10s python3 scripts/validate-obs-manifests.py &amp;&amp; timeout 10s python3 -m py_compile scripts/validate-obs-manifests.py tests/test-memory-runtime-contract.py &amp;&amp; timeout 10s git diff --check -- k8s/observability/10-prometheus.yaml scripts/validate-obs-manifests.py tests/test-memory-runtime-contract.py</automated>
  </verify>
  <done>Any future memory-rule move, duplication, omission, expression drift, or backup-selector drift fails offline at the chart-owned ConfigMap data boundary.</done>
</task>

</tasks>

## Prohibitions

- Do not modify `k8s/observability/values/prometheus-values.yaml`; it is the accepted authoritative input for this gap.
- Do not hand-edit, reorder, trim, or selectively transplant lines in `k8s/observability/10-prometheus.yaml`; rerun the full exact pinned command for every generated change.
- Do not add a render script, parity CLI, dependency, Helm plugin, chart repository mutation, or workflow fetch step when the documented exact command plus fresh UAT `cmp` already proves parity.
- Do not move memory alerts back to `data.rules` or `data.alerts`, move recording rules out of `data.recording_rules.yml`, or accept global marker presence as proof of Prometheus consumption.
- Do not change accepted scrape jobs, recording expressions, alert expressions/thresholds/durations, `solidstats-memory-backup`, PVC selectors, chart version, release name, namespace, RBAC, NetworkPolicies, or deployment identities.
- Do not apply manifests, run `kubectl`, open SSH tunnels, contact the live Prometheus endpoint, mutate DNS/nginx, or read secrets.
- Do not run Helm downloads during ordinary offline verification or claim fresh render parity without the separate approved networked render plus byte comparison.
- Do not claim kube-router enforcement; its isolated-cluster UAT remains pending.

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Authoritative values to Helm chart | Pinned repository input crosses an external chart renderer before becoming committed desired state. |
| Generated ConfigMap to Prometheus | ConfigMap data keys become mounted rule files; a correct expression under the wrong key can be present in Git yet not consumed as intended. |
| Backup workload to alert expression | The snapshot alert duplicates a Kubernetes workload identity and must remain tied to the sole manifest-owned CronJob. |
| Offline validation to networked UAT | Repository checks prove structure and semantics; only an approved fresh chart render can prove byte parity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-19-05-01 | Tampering | Generated Prometheus manifest | high | mitigate | Pin chart v29.11.0, use the exact values/namespace/release command, replace the complete stdout atomically, forbid hand edits, and rerun fresh `cmp` UAT. |
| T-19-05-02 | Denial of Service | Prometheus rule loading | high | mitigate | Require alerts and recording rules in their chart-owned files and reject known memory content under every other ConfigMap data key. |
| T-19-05-03 | Repudiation | Source/render contract | medium | mitigate | Compare parsed source/render rule mappings, exact rule-name sets, named expressions, and preserve a reproducible byte-parity command. |
| T-19-05-04 | Tampering | Snapshot CronJob selector | high | mitigate | Continue deriving the expected label from the sole CronJob in `40-backup.yaml`; fail on missing, duplicate, or divergent selectors. |
| T-19-05-05 | Information Disclosure | Validation and render process | low | mitigate | Use public chart/value inputs only; do not read, render, log, or commit secret material. |
| T-19-05-SC | Tampering | Helm chart retrieval | medium | mitigate | Use the existing official chart reference and exact pinned version only in the approved render environment; add no package/plugin installation and keep ordinary validation offline. |

</threat_model>

<verification>
Run the repository-local gate without chart downloads or live infrastructure:

1. `timeout 30s python3 tests/test-memory-runtime-contract.py`
2. `timeout 10s python3 scripts/validate-obs-manifests.py`
3. `timeout 10s python3 -m py_compile scripts/validate-obs-manifests.py tests/test-memory-runtime-contract.py`
4. `timeout 10s git diff --check -- k8s/observability/10-prometheus.yaml scripts/validate-obs-manifests.py tests/test-memory-runtime-contract.py`

Fresh networked Helm UAT remains a separate operator gate. In the approved environment, render with exactly `helm template prometheus prometheus-community/prometheus --version 29.11.0 --namespace monitoring --values k8s/observability/values/prometheus-values.yaml` to a temporary file, then run `cmp -s` against `k8s/observability/10-prometheus.yaml`. Record success only when the files are byte-identical and the offline gate above passes.

The existing kube-router UAT is unchanged: render approved values in an isolated cluster and prove the documented observer, Qdrant, Prometheus, host-nginx, DNS, and denied paths. This plan must leave that item pending until operator evidence exists.
</verification>

<success_criteria>

- The failed pinned-chart render-parity truth in `19-UAT.md` has an exact mechanical source correction and a repeatable networked `cmp` gate.
- OPS-04 alerts and recording rules are consumed from `alerting_rules.yml` and `recording_rules.yml`, with wrong-key content rejected rather than merely detected globally.
- All previously accepted observer, node-volume, PVC, Qdrant, snapshot, and backup-CronJob contracts remain semantically unchanged.
- Ordinary tests and validation pass offline without Helm fetches, cluster access, workflow mutation, or secret reads.
- Kube-router policy enforcement remains honestly human-unverified and outside this gap closure.
</success_criteria>

## Source Coverage Audit

| Source | Item | Coverage | Plan evidence |
| --- | --- | --- | --- |
| GOAL | Dedicated repository-local monitoring boundary | COVERED | Task 1 repairs the deployed chart render; Task 2 makes rule ownership fail closed. |
| REQ | OPS-04 operational MCP/Qdrant/snapshot/PVC monitoring | COVERED | Both tasks preserve all accepted signals and correct their consumed rule files. |
| RESEARCH | Do not invent unverified metric names; use the existing observer and source-backed Kubernetes metrics | COVERED | The plan changes no metric or expression and validates the accepted names semantically. |
| CONTEXT | Monitoring wiring is repository-owned; no live deploy, secret handling, DNS, or token action occurs autonomously | COVERED | Exact files, prohibitions, and offline verification keep the work inside the infrastructure boundary. |
| UAT | Fresh chart v29.11.0 output differs from the committed manifest at the alert data key | COVERED | Task 1 performs the exact full render; Task 2 detects recurrence; networked `cmp` re-verifies parity. |

Exclusions are not gaps: kube-router enforcement remains a human UAT item; namespace/runtime/storage/backup/deploy contracts already verified by Plans 01–04 are preserved and not reopened.

<output>
Create `.planning/phases/19-solidstats-memory-foundation/19-05-SUMMARY.md` when execution completes.
</output>

<!-- markdownlint-enable MD013 MD033 MD041 -->
