---
phase: 19-solidstats-memory-foundation
plan: 06
type: execute
gap_closure: true
wave: 6
depends_on:
  - 19-05
files_modified:
  - .github/workflows/deploy-memory.yml
  - k8s/memory/40-backup.yaml
  - scripts/render-memory-manifests.py
  - scripts/render-memory-secrets.py
  - scripts/validate-memory-manifests.py
  - tests/test-memory-runtime-contract.py
  - docs/solidstats-memory.md
autonomous: true
requirements:
  - ISO-02
  - OPS-01
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
premises:
  - claim: "The memory deploy workflow currently receives eight ordinary staging values through vars.MEMORY_* and four backup settings through new MEMORY_BACKUP_S3_* secrets."
    src: ".github/workflows/deploy-memory.yml#render-and-validate-memory-manifests"
    verify: "timeout 10s rg -n 'vars\\.MEMORY_|secrets\\.MEMORY_BACKUP_S3_' .github/workflows/deploy-memory.yml"
  - claim: "The non-secret renderer currently converts eight environment values into MEMORY_OPERATOR_* replacements, so the deployed desired state is not reviewable as a repository diff."
    src: "scripts/render-memory-manifests.py#main"
    verify: "timeout 10s rg -n 'MEMORY_OPERATOR_|valid_image|valid_size|valid_cidr|valid_collection' scripts/render-memory-manifests.py"
  - claim: "The secret renderer currently requires memory-specific aliases for endpoint, bucket, and S3 credentials and stores the non-secret endpoint in memory-backup-runtime."
    src: "scripts/render-memory-secrets.py#module-body"
    verify: "timeout 10s rg -n 'MEMORY_BACKUP_S3_|S3_ENDPOINT' scripts/render-memory-secrets.py"
  - claim: "The backup manifest already fixes the approved backup prefix but reads its Timeweb endpoint from a Secret."
    src: "k8s/memory/40-backup.yaml#CronJob-solidstats-memory-backup"
    verify: "timeout 10s rg -n 'S3_ENDPOINT|backups/solidstats-memory/' k8s/memory/40-backup.yaml"
  - claim: "Eight unresolved image, PVC, CIDR, and collection values remain intentional operator evidence gates, and strict validation rejects them."
    src: ".planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md#anti-patterns-found"
    verify: "timeout 10s rg -n 'all 8 unresolved operator inputs|MEMORY_OPERATOR_\\* inputs' .planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md"
  - claim: "Fresh pinned Prometheus rendering is approved, while kube-router enforcement remains the only Phase 19 behavior requiring human evidence."
    src: ".planning/phases/19-solidstats-memory-foundation/19-UAT.md"
    verify: "timeout 10s rg -n 'status:|helm|kube-router|pending|passed' .planning/phases/19-solidstats-memory-foundation/19-UAT.md"
carried_forward:
  - source: ".planning/phases/19-solidstats-memory-foundation/19-CONTEXT.md#deployment-contract"
    item: "Secret values come only from GitHub Environment secrets; namespace and RBAC bootstrap remain operator-owned and excluded from CI."
    disposition: "Keep secret rendering and the exact namespace-scoped identity gate, but remove GitHub Environment variables from non-secret desired state."
  - source: ".planning/phases/19-solidstats-memory-foundation/19-RESEARCH.md#live-evidence-blockers"
    item: "MemPalace/observer/uploader images, both PVC sizes, two CIDRs, and the collection name lack current primary evidence."
    disposition: "Leave each existing MEMORY_OPERATOR_* marker visible in its owning manifest and fail strict deploy validation until an evidence-backed repository change replaces it."
  - source: ".planning/phases/19-solidstats-memory-foundation/19-05-SUMMARY.md#verification-results"
    item: "Prometheus chart v29.11.0 values/render parity and memory rule placement are accepted."
    disposition: "Do not modify any observability values, rendered manifest, validator, or deployment workflow."
  - source: ".planning/phases/19-solidstats-memory-foundation/19-UAT.md"
    item: "kube-router NetworkPolicy behavior still needs isolated-cluster operator evidence."
    disposition: "Preserve it as a human gate; no task applies resources or claims CNI behavior."
must_haves:
  truths:
    - "Ordinary SolidStats memory staging configuration is visible in k8s/memory manifests; deploy-memory.yml contains no vars.MEMORY_* input layer."
    - "The backup workload consumes the checked-in Timeweb endpoint https://s3.twcstorage.ru and checked-in prefix backups/solidstats-memory/ while bucket and credentials remain secret-backed."
    - "The workflow reuses S3_ACCESS_KEY_ID, S3_SECRET_ACCESS_KEY, and S3_BUCKET directly; the only memory-specific secret contracts are K8S_MEMORY_TOKEN, MEMORY_QDRANT_API_KEY, and MEMORY_MCP_HTTP_TOKEN."
    - "render-memory-manifests.py stages the exact checked-in YAML bytes without reading configuration environment variables or replacing operator markers."
    - "All eight unresolved operator values remain explicit in source, immutable-image and input-shape rules are enforced by the validator, and strict deployment fails before any mutation while a marker remains."
    - "Offline tests reject reintroduced memory vars, backup-secret aliases, secret-backed endpoint/prefix configuration, mutable resolved images, malformed resolved values, and source/render drift."
    - "No secret value is read or committed, no live infrastructure is mutated, and accepted Prometheus artifacts remain byte-untouched."
  artifacts:
    - path: "k8s/memory/40-backup.yaml"
      provides: "Checked-in Timeweb endpoint and backup-prefix values with only bucket/access credentials sourced from memory-backup-runtime."
    - path: "scripts/render-memory-manifests.py"
      provides: "Copy-only OUTPUT_DIR renderer for the exact top-level k8s/memory YAML source set."
    - path: "scripts/render-memory-secrets.py"
      provides: "Fail-closed stdout renderer consuming two memory runtime tokens plus the repository's three existing S3 secret names."
    - path: ".github/workflows/deploy-memory.yml"
      provides: "Memory deployment without vars.MEMORY_* or MEMORY_BACKUP_S3_* secret aliases, preserving exact identity and pre-mutation gates."
    - path: "scripts/validate-memory-manifests.py"
      provides: "Source/config/workflow contract checks plus strict validation of evidence-backed replacements."
    - path: "tests/test-memory-runtime-contract.py"
      provides: "Executable renderer, secret inventory, direct-config, fail-closed marker, and workflow regression coverage."
    - path: "docs/solidstats-memory.md"
      provides: "Operator-facing source-of-truth, secret inventory, evidence-replacement, and bootstrap-gate instructions."
  key_links:
    - from: "k8s/memory/*.yaml"
      to: "temporary rendered manifest directory"
      via: "scripts/render-memory-manifests.py byte-preserving copy of top-level YAML files"
    - from: ".github/workflows/deploy-memory.yml"
      to: "scripts/render-memory-secrets.py"
      via: "direct secrets.S3_* and the two memory runtime secret expressions in a sanitized render step"
    - from: "k8s/memory/40-backup.yaml S3_ENDPOINT and S3_PREFIX"
      to: "solidstats-memory-backup aws s3 cp destination"
      via: "checked-in value fields combined with secret-backed S3_BUCKET"
    - from: "MEMORY_OPERATOR_* markers or evidence-backed replacements"
      to: "pre-mutation deploy gate"
      via: "scripts/validate-memory-manifests.py strict mode before server-side dry-run and apply"
    - from: "K8S_MEMORY_TOKEN"
      to: "system:serviceaccount:solidstats-memory:memory-ci-deployer"
      via: "existing explicit-context kubectl auth whoami exact-username assertion"
---

<!-- markdownlint-disable MD013 MD033 MD041 -->

<objective>
Move ordinary SolidStats memory staging configuration out of GitHub Environment variables and into reviewable repository-owned manifests without weakening any fail-closed or least-privilege boundary.

Purpose: Make the applied non-secret desired state reproducible from a code review while retaining secret-only GitHub inputs and evidence-backed operator gates.

Output: Direct backup configuration, a copy-only non-secret renderer, a narrowed secret renderer/workflow contract, strict validators/tests, and an updated operator runbook.
</objective>

## Artifacts this phase produces

| File | Exact additions or target state |
| --- | --- |
| `k8s/memory/40-backup.yaml` | `S3_ENDPOINT` has literal value `https://s3.twcstorage.ru`; `S3_PREFIX` has literal value `backups/solidstats-memory/`; the upload target is `s3://${S3_BUCKET}/${S3_PREFIX}${backup_id}/`; only `S3_BUCKET`, `AWS_ACCESS_KEY_ID`, and `AWS_SECRET_ACCESS_KEY` remain Secret refs. Existing uploader-image, backup-CIDR, and collection markers remain unchanged. |
| `scripts/render-memory-manifests.py` | `main()` retains the positional `OUTPUT_DIR` CLI and copies the exact top-level `k8s/memory/*.yaml` set byte-for-byte. Remove `required`, `valid_image`, `valid_size`, `valid_cidr`, `valid_collection`, the replacement map, and all non-secret environment consumption. |
| `scripts/render-memory-secrets.py` | `required()` consumes exactly `MEMORY_QDRANT_API_KEY`, `MEMORY_MCP_HTTP_TOKEN`, `S3_BUCKET`, `S3_ACCESS_KEY_ID`, and `S3_SECRET_ACCESS_KEY`; output Secret keys remain the runtime names expected by manifests, with no `S3_ENDPOINT` or `S3_PREFIX` key. |
| `.github/workflows/deploy-memory.yml` | Zero `vars.MEMORY_*` expressions; direct references to the three existing `secrets.S3_*` names; exactly the three memory-specific secret contracts `K8S_MEMORY_TOKEN`, `MEMORY_QDRANT_API_KEY`, and `MEMORY_MCP_HTTP_TOKEN`; unchanged operator-bootstrap exclusions and exact `kubectl auth whoami` identity proof. |
| `scripts/validate-memory-manifests.py` | New `require_checked_in_staging_config()` and `require_deploy_input_contract()` helpers; strict mode validates immutable image digests, positive Kubernetes storage quantities, CIDRs, and collection format after markers are replaced; `--allow-operator-placeholders` remains the only source-validation exception. |
| `tests/test-memory-runtime-contract.py` | New `CheckedInMemoryConfigContractTests` and `MemorySecretRendererContractTests`; expanded workflow tests prove copy parity, sanitized fake-secret rendering, exact secret inventory, direct endpoint/prefix ownership, marker behavior, and resolved-value validation. |
| `docs/solidstats-memory.md` | Sections `Checked-in staging configuration` and `Secrets and operator gates` name the exact source files, literals, unresolved markers, existing S3 secrets, three memory-specific secrets, and offline/operator handoff. |

No staging-values file is added. The manifests already own each Kubernetes value and are the shortest single source of truth; another values layer would duplicate the state this plan removes from GitHub Environment variables.

<execution_context>
@/home/afgan0r/.codex/gsd-core/workflows/execute-plan.md
@/home/afgan0r/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-solidstats-memory-foundation/19-CONTEXT.md
@.planning/phases/19-solidstats-memory-foundation/19-RESEARCH.md
@.planning/phases/19-solidstats-memory-foundation/19-05-SUMMARY.md
@.planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md
@.planning/phases/19-solidstats-memory-foundation/19-UAT.md
@.planning/graphs/GRAPH_COMMUNITIES.md
@.github/workflows/deploy-memory.yml
@k8s/memory/10-qdrant.yaml
@k8s/memory/20-mempalace.yaml
@k8s/memory/30-network-policy.yaml
@k8s/memory/40-backup.yaml
@k8s/memory/50-monitoring.yaml
@scripts/render-memory-manifests.py
@scripts/render-memory-secrets.py
@scripts/validate-memory-manifests.py
@tests/test-memory-runtime-contract.py
@docs/solidstats-memory.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one checked-in config path through both renderers</name>
  <files>k8s/memory/40-backup.yaml, scripts/render-memory-manifests.py, scripts/render-memory-secrets.py, tests/test-memory-runtime-contract.py</files>
  <read_first>
    - `.planning/phases/19-solidstats-memory-foundation/19-CONTEXT.md` — locked repository ownership, secret-rendering, bootstrap, and no-live-action decisions.
    - `.planning/phases/19-solidstats-memory-foundation/19-RESEARCH.md` — exact unresolved evidence gates that must not receive invented values.
    - `.planning/phases/19-solidstats-memory-foundation/19-05-SUMMARY.md` — accepted Prometheus state that this plan must not touch.
    - `k8s/memory/40-backup.yaml` — current upload command, approved prefix, endpoint Secret ref, secret key refs, and three unresolved backup markers.
    - `scripts/render-memory-manifests.py` — positional output CLI, validators, replacement map, and current environment-driven rendering behavior.
    - `scripts/render-memory-secrets.py` — `required()` exit contract, three emitted Secrets, old memory-specific S3 aliases, and `stringData` key mapping.
    - `tests/test-memory-runtime-contract.py` — existing subprocess/PyYAML conventions and manifest contract fixtures to extend before implementation.
    - `.agents/skills/kubernetes-specialist/references/configuration.md` — ConfigMap/Secret separation and reviewable non-secret configuration rules.
    - `.agents/skills/kubernetes-specialist/references/storage.md` — persistent-data and backup configuration safety rules.
  </read_first>
  <behavior>
    - Test 1: running `scripts/render-memory-manifests.py OUTPUT_DIR` in a sanitized environment produces exactly the source top-level YAML filenames and byte-identical content, including all unresolved markers.
    - Test 2: supplying fake obsolete MEMORY_* configuration variables does not alter renderer output, proving that GitHub Environment values cannot shadow checked-in desired state.
    - Test 3: the backup CronJob reads literal `S3_ENDPOINT=https://s3.twcstorage.ru` and `S3_PREFIX=backups/solidstats-memory/`, builds the exact bucket/prefix destination, and has no Secret ref for either non-secret value.
    - Test 4: running the secret renderer with five synthetic values emits exactly the three expected Secret documents and runtime keys; endpoint and prefix are absent from Secret data.
    - Test 5: omission of any one required secret-renderer input exits 64 before emitting YAML, and the test subprocess receives an explicit synthetic environment rather than inherited credentials.
  </behavior>
  <action>
    Start RED with `CheckedInMemoryConfigContractTests` and `MemorySecretRendererContractTests`. Use temporary directories and subprocesses with an explicit allowlisted environment containing only the Python/runtime necessities and synthetic test strings. Compare source and rendered YAML names and bytes rather than normalized YAML so substitutions, dropped comments, or serialization drift fail. Run a second non-secret render with obsolete configuration names populated by harmless sentinels and assert identical bytes. Parse the synthetic secret render with PyYAML and assert exact document names, namespaces, and `stringData` key sets; never import, print, or inherit repository/user secret values. [src: tests/test-memory-runtime-contract.py] [src: scripts/render-memory-manifests.py#main] [src: scripts/render-memory-secrets.py#required]

    In `40-backup.yaml`, replace the `S3_ENDPOINT` Secret ref with `value: https://s3.twcstorage.ru`, add `S3_PREFIX` with `value: backups/solidstats-memory/`, and construct the destination as `s3://${S3_BUCKET}/${S3_PREFIX}${backup_id}/`. Keep `S3_BUCKET`, `AWS_ACCESS_KEY_ID`, and `AWS_SECRET_ACCESS_KEY` on the existing `memory-backup-runtime` Secret; keep the Qdrant API key and every image/CIDR/collection marker unchanged. Do not infer a bucket, image, collection, size, or CIDR from the endpoint/prefix decision. [src: k8s/memory/40-backup.yaml#CronJob-solidstats-memory-backup] [src: .planning/phases/19-solidstats-memory-foundation/19-RESEARCH.md#live-evidence-blockers]

    Reduce `render-memory-manifests.py` to a deterministic staging utility: keep its positional `OUTPUT_DIR` contract, locate repository `k8s/memory`, enumerate only its top-level `.yaml` files, require the exact expected source set, and copy every file byte-for-byte. Remove all calls to `os.environ`, all value validators, and all replacement logic; value validity moves to the authoritative validator in Task 2. Fail before partial output if the source set is missing, duplicated, or contains a non-file match. Do not mutate source files and do not include nested/generated directories. [src: scripts/render-memory-manifests.py#main] [src: .agents/skills/solidstats-shared-project-standards/references/ci-cd-pattern.md]

    Change `render-memory-secrets.py` to require `MEMORY_QDRANT_API_KEY`, `MEMORY_MCP_HTTP_TOKEN`, `S3_BUCKET`, `S3_ACCESS_KEY_ID`, and `S3_SECRET_ACCESS_KEY`. Preserve the three Secret names and map the existing S3 values to runtime keys `S3_BUCKET`, `AWS_ACCESS_KEY_ID`, and `AWS_SECRET_ACCESS_KEY`; remove endpoint/prefix data and every `MEMORY_BACKUP_S3_*` input. Preserve fail-before-output behavior and exit 64 for any missing value. No generated or checked-in file may contain the synthetic test strings after the test process exits. [src: scripts/render-memory-secrets.py#module-body] [src: k8s/memory/40-backup.yaml#memory-backup-runtime]
  </action>
  <acceptance_criteria>
    - `render-memory-manifests.py OUTPUT_DIR` has no non-secret configuration input and produces a byte-identical top-level manifest set.
    - `40-backup.yaml` contains the exact approved endpoint and trailing-slash prefix as plain values, with an exact nonduplicated bucket/prefix upload path.
    - `memory-backup-runtime` contains only the Qdrant key, bucket, and two S3 credential keys required by the backup container; endpoint and prefix are not secret material.
    - The secret renderer consumes the repository's three existing `S3_*` secret names directly plus the two runtime memory tokens and exits 64 before YAML output when any is absent.
    - Tests use only synthetic inputs in an explicit subprocess environment and do not inspect the caller's environment for credentials.
    - The eight unresolved operator markers remain byte-visible in rendered output and no evidence-backed value is guessed.
  </acceptance_criteria>
  <verify>
    <automated>timeout 30s python3 tests/test-memory-runtime-contract.py CheckedInMemoryConfigContractTests MemorySecretRendererContractTests &amp;&amp; timeout 10s python3 -m py_compile scripts/render-memory-manifests.py scripts/render-memory-secrets.py tests/test-memory-runtime-contract.py &amp;&amp; timeout 10s git diff --check -- k8s/memory/40-backup.yaml scripts/render-memory-manifests.py scripts/render-memory-secrets.py tests/test-memory-runtime-contract.py</automated>
  </verify>
  <done>One repository-owned endpoint/prefix path flows unchanged through manifest staging and one exact secret-only path flows through secret rendering, with executable proof that GitHub Environment variables cannot rewrite ordinary desired state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the checked-in workflow and validator contract fail closed</name>
  <files>.github/workflows/deploy-memory.yml, scripts/validate-memory-manifests.py, tests/test-memory-runtime-contract.py, docs/solidstats-memory.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — ISO-02 credential isolation and OPS-01 dedicated workflow/identity requirements.
    - `.planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md` — exact identity, mutation allowlist, strict marker rejection, and human-only kube-router truth already accepted.
    - `.planning/phases/19-solidstats-memory-foundation/19-UAT.md` — accepted pinned-Helm evidence and remaining kube-router operator gate.
    - `.github/workflows/deploy-memory.yml` — current vars/secrets inputs, exact identity assertion, manifest allowlist, dry-run/apply order, and operator-bootstrap exclusions.
    - `scripts/validate-memory-manifests.py` — `--allow-operator-placeholders`, placeholder checks, backup checks, image checks, NetworkPolicy checks, and workflow/RBAC contract validation.
    - `scripts/render-memory-manifests.py` — Task 1 copy-only output contract that strict validation now consumes.
    - `scripts/render-memory-secrets.py` — Task 1 exact required input names and emitted Secret key contract.
    - `k8s/memory/10-qdrant.yaml` — proven immutable Qdrant digest and PVC-size marker.
    - `k8s/memory/20-mempalace.yaml` — MemPalace image/PVC markers and secret-backed runtime token/key wiring.
    - `k8s/memory/30-network-policy.yaml` — host-nginx and backup-S3 CIDR markers whose human evidence gates remain unchanged.
    - `k8s/memory/40-backup.yaml` — Task 1 direct endpoint/prefix and remaining backup markers.
    - `k8s/memory/50-monitoring.yaml` — observer image/collection markers and accepted monitoring contract.
    - `tests/test-memory-runtime-contract.py` — existing `MemoryDeployWorkflowContractTests` exact identity and no-cross-namespace assertions to preserve and expand.
    - `docs/solidstats-memory.md` — current Environment-variable instructions, marker list, backup contract, and operator procedure to replace.
    - `.agents/skills/kubernetes-specialist/references/gitops.md` — Git-owned desired state and drift prevention rules.
    - `.agents/skills/kubernetes-specialist/references/networking.md` — NetworkPolicy evidence boundary that remains human-verified.
    - `.agents/skills/solidstats-shared-project-standards/references/ci-cd-pattern.md` — validate-before-deploy, scoped credentials, and immutable deployment inputs.
  </read_first>
  <behavior>
    - Test 1: deploy-memory.yml contains no `vars.MEMORY_*` or `secrets.MEMORY_BACKUP_S3_*` expression and requires direct `secrets.S3_ACCESS_KEY_ID`, `secrets.S3_SECRET_ACCESS_KEY`, and `secrets.S3_BUCKET` references.
    - Test 2: the only memory-specific GitHub secret contracts are `K8S_MEMORY_TOKEN`, `MEMORY_QDRANT_API_KEY`, and `MEMORY_MCP_HTTP_TOKEN`; established SSH and CA inputs remain allowed but cannot be renamed into memory aliases.
    - Test 3: exact `kubectl auth whoami` assertion for `system:serviceaccount:solidstats-memory:memory-ci-deployer` still precedes server-side dry-run and apply, and namespace/RBAC bootstrap files remain excluded.
    - Test 4: source validation with `--allow-operator-placeholders` requires all eight exact markers in their owning fields, while strict validation fails before workflow mutation until all are resolved.
    - Test 5: resolved manifest fixtures accept only `name@sha256:<64 lowercase hex>` images, positive Kubernetes storage quantities, parseable CIDRs, and one matching lowercase collection name; mutable/malformed replacements fail.
    - Test 6: endpoint, prefix, secret inventory, and manifest byte-copy drift each fail the same offline runtime-contract suite used by the workflow validation job.
  </behavior>
  <action>
    Start RED by expanding `MemoryDeployWorkflowContractTests` and the Task 1 contract classes. Parse workflow text/YAML to assert zero `vars.MEMORY_*` references, zero old backup aliases, the three direct existing S3 secret expressions, and the exact three memory-specific secret names. Preserve assertions for the established deploy-host/private-key/user and CA inputs rather than counting them as new memory secrets. Add mutation fixtures for a reintroduced var, old secret alias, fourth memory-specific secret, secret-backed endpoint/prefix, mutable image, zero/malformed size, invalid CIDR, collection mismatch, and a missing marker under source-validation mode. Each negative test must alter parsed source in a temporary tree or call a pure validator helper; it must not edit repository fixtures or invoke a cluster. [src: tests/test-memory-runtime-contract.py#MemoryDeployWorkflowContractTests] [src: .github/workflows/deploy-memory.yml]

    Remove every `vars.MEMORY_*` entry and `MEMORY_BACKUP_S3_*` secret reference from the workflow render environment. Pass `secrets.S3_ACCESS_KEY_ID`, `secrets.S3_SECRET_ACCESS_KEY`, and `secrets.S3_BUCKET` under those exact environment names, plus `MEMORY_QDRANT_API_KEY` and `MEMORY_MCP_HTTP_TOKEN`. Keep `K8S_MEMORY_TOKEN` in the authentication path and preserve established SSH/CA inputs. Do not add defaults, fallback expressions, secret-value logging, environment dumps, or generated values. Preserve the exact `memory-k3s-staging` context, exact service-account username check before both server-side dry-run and apply, negative staging/monitoring authorization checks, exclusive resource allowlist, operator-bootstrap exclusions, rollout checks, and `if: always()` managed tunnel cleanup. [src: .github/workflows/deploy-memory.yml#deploy] [src: .planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md#key-link-verification]

    Add `require_checked_in_staging_config()` to validate the literal endpoint, trailing-slash prefix, exact upload expression, and absence of endpoint/prefix Secret refs. Add `require_deploy_input_contract()` to validate the workflow and secret renderer as one contract: direct existing S3 names, exact memory-specific secret inventory, no memory vars/old aliases, and unchanged exact identity-before-mutation ordering. Keep source-mode marker validation position-specific: all eight markers must occupy their expected image, PVC request, NetworkPolicy CIDR, or collection fields, not merely occur in comments. [src: scripts/validate-memory-manifests.py#main] [src: scripts/render-memory-secrets.py#required]

    Move the removed renderer's value-shape gates into strict manifest validation. Validate every resolved MemPalace, observer, and backup image against an immutable digest reference ending in `@sha256:` plus 64 lowercase hex characters; retain the proven Qdrant digest assertion. Validate both resolved PVC requests as positive Kubernetes quantities, both CIDRs with `ipaddress.ip_network(..., strict=False)`, and the Qdrant collection as one lowercase identifier shared by the backup and observer consumers; continue validating MemPalace's separate `MEMPALACE_QDRANT_NAMESPACE=solidstats_memory` locked contract. Strict mode must report all unresolved markers before deploy; `--allow-operator-placeholders` is restricted to repository source checks and must reject missing, renamed, duplicated, or misplaced markers. Do not auto-resolve a marker, read live capacity/network state, or broaden an allow rule. [src: scripts/validate-memory-manifests.py#PLACEHOLDER_RE] [src: scripts/render-memory-manifests.py#valid_image] [src: .planning/phases/19-solidstats-memory-foundation/19-RESEARCH.md#live-evidence-blockers]

    Rewrite the runbook's deployment-input section. State that `k8s/memory/*.yaml` is the non-secret source of truth; list the exact eight markers and their owning files; name the checked-in endpoint/prefix; classify `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY`, and `S3_BUCKET` as existing reused secrets; and list only `K8S_MEMORY_TOKEN`, `MEMORY_QDRANT_API_KEY`, and `MEMORY_MCP_HTTP_TOKEN` as memory-specific secrets. Record that the two runtime tokens are provisioned, while `K8S_MEMORY_TOKEN` remains an operator/CI bootstrap gate because no local cluster action is authorized. Instruct operators to replace a marker only through an evidence-backed manifest diff, then run source and strict offline gates before approved deployment. Do not include secret values, a live command, or a success claim for kube-router. [src: docs/solidstats-memory.md] [src: .planning/phases/19-solidstats-memory-foundation/19-UAT.md]
  </action>
  <acceptance_criteria>
    - Workflow and validator tests prove that no ordinary memory value is sourced from GitHub Environment variables and no old backup-secret alias is accepted.
    - Exact direct S3 secret reuse and the exact three memory-specific secret contracts are documented and mechanically enforced without reading their values.
    - Checked-in endpoint/prefix values are consumed by the backup CronJob and cannot silently move back into Secret data.
    - Strict validation fully replaces the removed renderer validation and rejects unresolved or malformed image, storage, CIDR, and collection fields before any server-side dry-run or apply.
    - Source-mode validation requires every evidence marker in its exact owning field and cannot be satisfied by comment text or a duplicate elsewhere.
    - Exact memory identity, bootstrap exclusions, namespace boundary, negative authorization checks, rollout coverage, and safe tunnel cleanup remain unchanged and tested.
    - Documentation distinguishes already-provisioned runtime tokens from the outstanding K8S bootstrap gate and discloses no value.
    - `k8s/observability/10-prometheus.yaml` and all other observability files remain unchanged; accepted Helm parity is not regenerated or reopened.
  </acceptance_criteria>
  <verify>
    <automated>timeout 30s python3 tests/test-memory-runtime-contract.py &amp;&amp; timeout 10s python3 scripts/validate-memory-manifests.py --allow-operator-placeholders &amp;&amp; timeout 10s python3 -m py_compile scripts/render-memory-manifests.py scripts/render-memory-secrets.py scripts/validate-memory-manifests.py tests/test-memory-runtime-contract.py &amp;&amp; timeout 10s git diff --check -- .github/workflows/deploy-memory.yml k8s/memory/40-backup.yaml scripts/render-memory-manifests.py scripts/render-memory-secrets.py scripts/validate-memory-manifests.py tests/test-memory-runtime-contract.py docs/solidstats-memory.md</automated>
  </verify>
  <done>The dedicated workflow derives all ordinary staging state from reviewed manifests, accepts only the approved existing/new secret inputs, and fails closed on every unresolved or malformed operator value before mutation.</done>
</task>

</tasks>

## Prohibitions

- Do not create a staging-values file, ConfigMap generator, template language, Helm chart, or second non-secret source when direct manifests already own the values.
- Do not add, rename, expose, rotate, print, query, or otherwise read any GitHub or Kubernetes secret value.
- Do not introduce a new secret beyond `K8S_MEMORY_TOKEN`, `MEMORY_QDRANT_API_KEY`, and `MEMORY_MCP_HTTP_TOKEN`; reuse the exact existing `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY`, and `S3_BUCKET` names.
- Do not retain `vars.MEMORY_*`, `MEMORY_BACKUP_S3_*`, an endpoint/prefix Secret key, a renderer default, or any environment-driven fallback for ordinary configuration.
- Do not replace any unresolved image, PVC size, CIDR, or collection marker without the primary evidence named in research. All resolved images must remain immutable digest references.
- Do not weaken the exact deploy identity assertion, cross-namespace denial checks, allowlisted manifest set, server-side dry-run order, operator-bootstrap exclusions, rollout checks, or managed SSH cleanup.
- Do not modify `00-namespace.yaml`, `01-ci-rbac.yaml`, any observability value/render/validator/workflow, any staging manifest, DNS, nginx, or git state.
- Do not run `kubectl`, Helm, SSH, a live deploy, a cluster probe, a DNS/nginx action, or any command that reads local/environment secret material.
- Do not claim kube-router enforcement or K8S token bootstrap completion without operator evidence. Preserve the accepted Helm UAT result and the pending CNI gate as human-owned facts.

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Git-reviewed manifests to deployment render | Repository-owned non-secret desired state crosses a staging helper before validation and apply. |
| GitHub Environment secrets to Kubernetes Secrets | Five required secret values cross CI process memory and renderer stdout into namespace-scoped Secret objects. |
| Deployment identity to Kubernetes API | A long-lived token authenticates a principal that may mutate only the `solidstats-memory` namespace and never bootstrap resources. |
| Operator evidence to fail-closed markers | Image, capacity, network, and collection evidence becomes desired state only through reviewed marker replacement. |
| Backup pod to Timeweb S3 | Checked-in endpoint/prefix combine with secret bucket/credentials across an external storage boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-19-06-01 | Tampering | Non-secret deployment inputs | high | mitigate | Remove GitHub vars, copy checked-in manifests byte-for-byte, and validate direct endpoint/prefix plus exact source/render parity. |
| T-19-06-02 | Information Disclosure | Secret renderer and tests | high | mitigate | Accept only exact secret names, render at the existing CI boundary, use sanitized synthetic subprocess environments in tests, and never log or inspect actual values. |
| T-19-06-03 | Elevation of Privilege | Memory deploy identity | critical | mitigate | Preserve exact `kubectl auth whoami` username proof, negative staging/monitoring checks, namespace-only RBAC, and exclusion of Namespace/RBAC bootstrap resources before mutation. |
| T-19-06-04 | Spoofing | Workload images | high | mitigate | Keep unresolved image gates visible and require exact immutable `@sha256` references in strict validation; never synthesize an image reference. |
| T-19-06-05 | Tampering | Evidence-gated configuration | high | mitigate | Require all eight markers in exact fields during source validation and validate the type/shape and cross-consumer equality of every resolved replacement. |
| T-19-06-06 | Denial of Service | Backup destination | high | mitigate | Pin the proven endpoint/prefix in Git, keep bucket/credentials secret-backed, and test the exact destination expression and trailing-slash semantics. |
| T-19-06-07 | Repudiation | Configuration provenance | medium | mitigate | Make manifests the sole non-secret source, reject environment overrides, and require reviewable diffs plus deterministic offline contract tests. |

</threat_model>

<verification>
Run only repository-local, offline checks:

1. `timeout 30s python3 tests/test-memory-runtime-contract.py`
2. `timeout 10s python3 scripts/validate-memory-manifests.py --allow-operator-placeholders`
3. `timeout 10s python3 -m py_compile scripts/render-memory-manifests.py scripts/render-memory-secrets.py scripts/validate-memory-manifests.py tests/test-memory-runtime-contract.py`
4. `timeout 10s git diff --check -- .github/workflows/deploy-memory.yml k8s/memory/40-backup.yaml scripts/render-memory-manifests.py scripts/render-memory-secrets.py scripts/validate-memory-manifests.py tests/test-memory-runtime-contract.py docs/solidstats-memory.md`
5. `timeout 10s bash -c '! python3 scripts/validate-memory-manifests.py &gt;/tmp/solidstats-memory-strict.log 2&gt;&amp;1 &amp;&amp; rg -q "unresolved operator" /tmp/solidstats-memory-strict.log'`
6. `timeout 10s rg -n 'https://s3\.twcstorage\.ru|backups/solidstats-memory/' k8s/memory/40-backup.yaml docs/solidstats-memory.md`

The strict-validator rejection in step 5 is expected while any approved evidence marker remains. It is a deploy gate, not a request to invent the missing values. The executor must remove `/tmp/solidstats-memory-strict.log` after inspecting only the non-secret validation message.

No live verification occurs in this plan. `K8S_MEMORY_TOKEN` bootstrap and kube-router path enforcement remain operator gates. The already-approved pinned Prometheus render remains valid because this plan is prohibited from changing its source or generated artifact.
</verification>

<success_criteria>

- ISO-02 retains separate memory credentials and a namespace-only deployment identity while reusing only the approved repository-level S3 secrets.
- OPS-01 deploys from a Git-reviewable non-secret source and has no GitHub Environment variable layer for memory configuration.
- Endpoint and prefix are exact checked-in workload configuration; secret material remains limited to the approved names and never enters repository content or test logs.
- Copy-only rendering, source validation, strict validation, and runtime-contract tests make configuration provenance and unresolved evidence gates mechanically visible.
- No live action, invented operator value, observability drift, cross-namespace permission, or secret read is introduced.
- Helm render evidence remains accepted and kube-router enforcement remains explicitly human-unverified.
</success_criteria>

## Source Coverage Audit

| Source | Item | Coverage | Plan evidence |
| --- | --- | --- | --- |
| GOAL | Repository-local policy, validation, and dedicated CD boundary | COVERED | Task 1 makes manifests the non-secret source; Task 2 makes the workflow/validator contract fail closed. |
| REQ | ISO-02 separate namespace and credentials | COVERED | Exact memory secret inventory, reused S3 names, and preserved identity boundary are enforced in Task 2. |
| REQ | OPS-01 dedicated namespace-scoped deploy workflow | COVERED | Task 2 removes vars while preserving exact identity, deny checks, bootstrap exclusions, dry-run/apply order, and rollout scope. |
| RESEARCH | Image, capacity, CIDR, and collection values lack evidence | COVERED | Both tasks preserve all eight markers and strict validation; no value is invented. |
| CONTEXT | GitHub handles secrets only; operator owns bootstrap; no live action | COVERED | Secret-only renderer/workflow inputs, K8S token gate, prohibitions, and offline verification implement the locked boundary. |
| UAT | Prometheus parity is accepted; kube-router behavior remains human-owned | COVERED | Observability artifacts are prohibited from change, and the CNI gate remains explicitly pending. |

Exclusions are not gaps: workload implementation, monitoring rules, Prometheus rendering, NetworkPolicy shape, namespace/RBAC bootstrap, live token creation, and CNI enforcement were already implemented or remain intentionally operator-owned. This plan changes only non-secret configuration provenance and its offline enforcement.

<output>
Create `.planning/phases/19-solidstats-memory-foundation/19-06-SUMMARY.md` when execution completes.
</output>

<!-- markdownlint-enable MD013 MD033 MD041 -->
