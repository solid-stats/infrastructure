---
phase: 19-solidstats-memory-foundation
plan: 03
type: execute
gap_closure: true
wave: 3
depends_on:
  - 19-01
  - 19-02
files_modified:
  - .github/workflows/deploy-memory.yml
  - k8s/memory/30-network-policy.yaml
  - k8s/memory/50-monitoring.yaml
  - k8s/observability/10-prometheus.yaml
  - k8s/observability/95-netpol-monitoring.yaml
  - k8s/observability/values/prometheus-values.yaml
  - scripts/render-memory-manifests.py
  - scripts/ssh-tunnel-up.sh
  - scripts/validate-memory-manifests.py
  - scripts/validate-obs-manifests.py
  - tests/test-memory-runtime-contract.py
autonomous: true
requirements:
  - ISO-02
  - ISO-04
  - RUN-03
  - OPS-01
  - OPS-04
user_setup:
  - service: github-environment-staging
    why: "Authenticate the memory workflow as the operator-created, namespace-scoped memory-ci-deployer ServiceAccount."
    env_vars:
      - name: K8S_MEMORY_TOKEN
        source: "GitHub staging environment secret populated from the operator-owned solidstats-memory/memory-ci-deployer token Secret; never commit its value."
      - name: MEMORY_OBSERVER_IMAGE
        source: "GitHub staging environment variable containing an operator-approved immutable Python image digest that can run as UID/GID 65532."
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
premises:
  - claim: "The memory deploy identity is created outside this workflow and its Role is namespaced to solidstats-memory."
    src: "k8s/memory/01-ci-rbac.yaml#memory-ci-deployer-role-and-binding"
    verify: "timeout 10s python3 scripts/validate-memory-manifests.py --allow-operator-placeholders"
  - claim: "The shared tunnel and kubeconfig helpers accept an explicit token, namespace, user, and context without granting permissions themselves."
    src: "scripts/kubeconfig-setup.sh#kubeconfig-construction-and-whoami-gate"
    verify: "timeout 30s python3 tests/test-memory-runtime-contract.py MemoryDeployWorkflowContractTests SshTunnelLifecycleContractTests"
  - claim: "The observability workflow, not the memory workflow, applies committed files under k8s/observability with the monitoring-scoped identity."
    src: ".github/workflows/deploy-observability.yml#apply-observability-manifests"
    verify: "timeout 10s python3 scripts/validate-obs-manifests.py"
  - claim: "The existing Prometheus identity can discover nodes and proxy their authenticated /metrics endpoint."
    src: "k8s/staging/01-obs-rbac.yaml#prometheus-cluster-role"
    verify: "timeout 10s python3 tests/test-memory-runtime-contract.py PrometheusMemoryContractTests"
  - claim: "Qdrant documents authenticated collection details and snapshot-list APIs, including result.status, optimizer_status, and creation_time."
    src: "https://api.qdrant.tech/master/api-reference/collections/get-collection and https://api.qdrant.tech/master/api-reference/snapshots/list-snapshots"
    verify: "timeout 10s python3 tests/test-memory-runtime-contract.py MemoryObserverContractTests"
carried_forward:
  - source: ".planning/phases/19-solidstats-memory-foundation/19-02-SUMMARY.md#known-stubs"
    item: "The memory workflow intentionally stopped before authentication and apply."
    disposition: "Closed by Task 1 with a fail-closed memory-ci-deployer path."
  - source: ".planning/phases/19-solidstats-memory-foundation/19-02-SUMMARY.md#known-stubs"
    item: "Monitoring was emitted only as an unconsumed handoff."
    disposition: "Closed by Tasks 2 and 3 with a namespace observer, consumed Prometheus scrape jobs, and evaluated rules."
must_haves:
  truths:
    - "A successful memory workflow proves the exact identity system:serviceaccount:solidstats-memory:memory-ci-deployer, server-side dry-runs and applies rendered secrets and workload manifests, and cannot apply namespace or RBAC bootstrap resources."
    - "The memory workflow fails before mutation when exact identity proof, namespace authorization, required operator substitutions, rendering, strict validation, or server-side dry-run fails, and always stops only the SSH process recorded in its validated pidfile."
    - "Prometheus actively scrapes a namespace-local observer for MCP and Qdrant signals and an authenticated kubelet endpoint for memory PVC capacity; no unconsumed monitoring handoff remains."
    - "Recording and alerting rules cover MCP readiness, latency, and probe errors; Qdrant readiness and collection state; enabled-backup snapshot freshness; and capacity or missing-capacity metrics for both memory PVCs."
    - "The memory deploy identity receives no monitoring, staging, namespace, Role, RoleBinding, or cluster-scoped mutation permission."
    - "The kube-router default-deny runtime behavior remains explicitly unverified until an operator performs the live UAT; repository checks do not claim that result."
  artifacts:
    - path: ".github/workflows/deploy-memory.yml"
      provides: "Authenticated, namespace-scoped render, validate, dry-run, apply, and rollout workflow."
    - path: "scripts/ssh-tunnel-up.sh"
      provides: "Backward-compatible managed tunnel start/stop contract with an atomically written pidfile and process-identity validation before signaling."
    - path: "k8s/memory/50-monitoring.yaml"
      provides: "Hardened solidstats-memory-observer Deployment, Service, ConfigMap exporter, and dedicated ServiceAccount."
    - path: "k8s/memory/30-network-policy.yaml"
      provides: "Exact observer-to-MCP/Qdrant and Prometheus-to-observer network paths under default deny."
    - path: "k8s/observability/values/prometheus-values.yaml"
      provides: "Source configuration for memory scrape jobs and memory recording/alerting rules."
    - path: "k8s/observability/10-prometheus.yaml"
      provides: "Committed rendered Prometheus configuration consumed by deploy-observability.yml."
    - path: "tests/test-memory-runtime-contract.py"
      provides: "Offline regression coverage for deploy isolation, observer semantics, consumed scrape configuration, and rule coverage."
  key_links:
    - from: ".github/workflows/deploy-memory.yml"
      to: "scripts/ssh-tunnel-up.sh"
      via: "--start-managed/--stop-managed with one workflow-owned SSH_TUNNEL_PID_FILE and unconditional cleanup"
    - from: ".github/workflows/deploy-memory.yml"
      to: "scripts/kubeconfig-setup.sh"
      via: "K8S_MEMORY_TOKEN with K8S_NAMESPACE=solidstats-memory and K8S_CONTEXT_NAME=memory-k3s-staging, followed by an exact kubectl auth whoami assertion"
    - from: ".github/workflows/deploy-memory.yml"
      to: "rendered-memory/*.yaml"
      via: "one allowlisted manifest set that excludes 00-namespace.yaml and 01-ci-rbac.yaml for dry-run and apply"
    - from: "k8s/memory/50-monitoring.yaml"
      to: "mempalace:8765 and qdrant:6333"
      via: "bounded HTTP probes, existing qdrant-runtime Secret references, and NetworkPolicy egress"
    - from: "k8s/observability/values/prometheus-values.yaml"
      to: "solidstats-memory-observer.solidstats-memory.svc.cluster.local:9108"
      via: "static Prometheus scrape target permitted by reciprocal NetworkPolicies"
    - from: "k8s/observability/values/prometheus-values.yaml"
      to: "kubelet /metrics through the Kubernetes API proxy"
      via: "existing Prometheus ServiceAccount bearer token, CA, node discovery, and nodes/proxy RBAC"
    - from: "k8s/observability/values/prometheus-values.yaml"
      to: "k8s/observability/10-prometheus.yaml"
      via: "the repository's committed Helm render contract validated by scripts/validate-obs-manifests.py"
---

<!-- markdownlint-disable MD013 MD033 MD041 -->

<objective>
Close the two verified Phase 19 blockers without crossing the dedicated memory, staging, or observability ownership boundaries.

Purpose: Make the existing memory runtime deployable by its least-privilege identity and make OPS-04 an actively consumed monitoring contract rather than a handoff artifact.

Output: A viable memory deploy workflow, a namespace-local observer, reciprocal NetworkPolicies, Prometheus scrape and rule configuration, and offline contract tests.
</objective>

## Artifacts this phase produces

| File | Exact additions or target state |
| --- | --- |
| `.github/workflows/deploy-memory.yml` | Steps `Open managed SSH tunnel`, `Setup kubeconfig (memory-ci-deployer, solidstats-memory)`, `Prove exact memory identity`, `Verify memory RBAC boundary`, `Server-side dry-run`, `Apply memory secrets`, `Apply memory workloads`, `Verify memory rollouts`, and `Stop managed SSH tunnel`; context `memory-k3s-staging`; flags `--namespace solidstats-memory`, `--server-side`, `--dry-run=server`, and `--timeout`. |
| `scripts/ssh-tunnel-up.sh` | Backward-compatible CLI flags `--start-managed` and `--stop-managed`; required managed-mode variable `SSH_TUNNEL_PID_FILE`; functions `write_managed_pid`, `validate_managed_process`, and `stop_managed_tunnel`; exact SSH command/forward validation before TERM or KILL. |
| `k8s/memory/50-monitoring.yaml` | ServiceAccount, ConfigMap, Service, and Deployment named `solidstats-memory-observer`; ConfigMap key `exporter.py`; functions `probe_http`, `parse_collection_health`, `latest_snapshot_timestamp`, `collect_metrics`, and `MetricsHandler`; Service port `metrics: 9108`. |
| `scripts/render-memory-manifests.py` | Required variable `MEMORY_OBSERVER_IMAGE` and replacement token `MEMORY_OPERATOR_SUPPLIED_OBSERVER_IMAGE_DIGEST`, validated by the existing immutable-digest validator. |
| `k8s/memory/30-network-policy.yaml` | `allow-prometheus-to-memory-observer`, `allow-memory-observer-egress`, and observer ingress additions for the existing MCP/Qdrant policies; direct Prometheus access to MCP/Qdrant removed. |
| `k8s/observability/values/prometheus-values.yaml` and `k8s/observability/10-prometheus.yaml` | Jobs `solidstats-memory-observer` and `kubernetes-nodes-volume-stats`; recording rules `solidstats_memory:mcp_ready:max`, `solidstats_memory:mcp_probe_duration_seconds:max`, `solidstats_memory:mcp_probe_errors:rate5m`, `solidstats_memory:qdrant_ready:max`, `solidstats_memory:qdrant_collection_healthy:max`, `solidstats_memory:qdrant_snapshot_age_seconds:max`, and `solidstats_memory:pvc_capacity_ratio:max`; alerts `SolidStatsMemoryMCPNotReady`, `SolidStatsMemoryMCPLatencyHigh`, `SolidStatsMemoryMCPProbeErrors`, `SolidStatsMemoryQdrantUnhealthy`, `SolidStatsMemoryQdrantCollectionUnavailable`, `SolidStatsMemorySnapshotMissingOrStale`, `SolidStatsMemoryPVCCapacityHigh`, and `SolidStatsMemoryPVCMetricsMissing`. |
| `k8s/observability/95-netpol-monitoring.yaml` | Prometheus egress limited to TCP 9108 on pods labeled `app.kubernetes.io/name=solidstats-memory-observer` in namespace `solidstats-memory`. |
| `scripts/validate-memory-manifests.py`, `scripts/validate-obs-manifests.py`, `tests/test-memory-runtime-contract.py` | Static validators and `unittest` classes `MemoryDeployWorkflowContractTests`, `MemoryObserverContractTests`, and `PrometheusMemoryContractTests`. |

<execution_context>
@/home/afgan0r/.codex/gsd-core/workflows/execute-plan.md
@/home/afgan0r/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-solidstats-memory-foundation/19-CONTEXT.md
@.planning/phases/19-solidstats-memory-foundation/19-RESEARCH.md
@.planning/phases/19-solidstats-memory-foundation/19-SPEC.md
@.planning/phases/19-solidstats-memory-foundation/19-02-SUMMARY.md
@.planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md
@docs/solidstats-memory.md
@.github/workflows/deploy-memory.yml
@.github/workflows/deploy-observability.yml
@.github/workflows/deploy-staging.yml
@scripts/ssh-tunnel-up.sh
@scripts/kubeconfig-setup.sh
@scripts/render-memory-manifests.py
@scripts/validate-memory-manifests.py
@scripts/validate-obs-manifests.py
@k8s/memory/01-ci-rbac.yaml
@k8s/memory/10-qdrant.yaml
@k8s/memory/20-mempalace.yaml
@k8s/memory/30-network-policy.yaml
@k8s/memory/40-backup.yaml
@k8s/memory/50-monitoring.yaml
@k8s/observability/values/prometheus-values.yaml
@k8s/observability/10-prometheus.yaml
@k8s/observability/95-netpol-monitoring.yaml
@k8s/staging/01-obs-rbac.yaml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one authenticated memory-only deploy path end to end</name>
  <files>.github/workflows/deploy-memory.yml, scripts/ssh-tunnel-up.sh, tests/test-memory-runtime-contract.py</files>
  <read_first>
    - `.planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md` — blocker 1 and the exact failed deployment truth.
    - `.github/workflows/deploy-memory.yml` — current render/validate flow and deliberate terminal failure.
    - `.github/workflows/deploy-staging.yml` — established SSH tunnel, kubeconfig, server dry-run, Secret apply, manifest apply, and rollout ordering.
    - `.github/workflows/deploy-observability.yml` — explicit multi-context use that must not be copied into the memory workflow.
    - `scripts/ssh-tunnel-up.sh` — current `ssh -fN` behavior, required SSH inputs, local-forward arguments, and the missing process-lifecycle handoff to add without breaking existing callers.
    - `scripts/kubeconfig-setup.sh` — explicit namespace/user/context inputs and anonymous-user rejection.
    - `k8s/memory/01-ci-rbac.yaml` — the only mutation contract available to memory-ci-deployer.
    - `scripts/render-memory-secrets.py` — stdout Secret rendering contract and secret-handling constraints.
  </read_first>
  <behavior>
    - Test 1: the workflow authenticates with `secrets.K8S_MEMORY_TOKEN`, user `memory-ci-deployer`, context `memory-k3s-staging`, and namespace `solidstats-memory`, then `kubectl --context memory-k3s-staging auth whoami` must yield exactly `system:serviceaccount:solidstats-memory:memory-ci-deployer` before any dry-run or apply.
    - Test 2: the exact file set used by both server-side dry-run and apply excludes `00-namespace.yaml` and `01-ci-rbac.yaml` and includes rendered Secrets separately.
    - Test 3: fail-closed authorization checks require allowed workload/Secret operations in `solidstats-memory` and reject namespace/RBAC mutation plus deployment mutation in `solid-stats-staging` and `monitoring`.
    - Test 4: neither a staging token/context nor an observability token/context is referenced, and no unconditional terminal failure remains.
    - Test 5: rendering, strict source/rendered validation, and exact identity proof precede every server-side dry-run and `kubectl apply`; workflow cleanup removes temporary rendered Secrets and kubeconfig material even after failure.
    - Test 6: managed tunnel start atomically writes the spawned SSH PID to the workflow-owned pidfile; managed stop refuses a nonnumeric, stale, or command-mismatched PID and signals only an SSH process whose command contains the expected `-N`, local forward, user, and host.
  </behavior>
  <action>
    Start with failing `MemoryDeployWorkflowContractTests` that parse the workflow as text and assert ordering, exact identity/context names, explicit exclusions, boundary checks, and cleanup. Do not load workflow expressions as secrets or print any secret-bearing environment value. [src: .planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md#gap-1]

    Extend `scripts/ssh-tunnel-up.sh` with an opt-in managed lifecycle while preserving its no-argument behavior for existing staging and observability callers. The exact new CLI is `bash scripts/ssh-tunnel-up.sh --start-managed` and `bash scripts/ssh-tunnel-up.sh --stop-managed`; both require `SSH_TUNNEL_PID_FILE`, while start retains the existing `DEPLOY_SSH_PRIVATE_KEY`, `DEPLOY_SSH_KNOWN_HOSTS`, `DEPLOY_SSH_HOST`, and `DEPLOY_SSH_USER` inputs. Managed start must launch `ssh -N` without `-f`, capture `$!`, create the workflow-owned pidfile with mode 0600 through a same-directory temporary file plus atomic rename, verify reachability, and stop/remove its own process and pidfile on startup failure. Managed stop must treat a missing pidfile as already stopped; otherwise it must reject symlinks, nonnumeric/stale PIDs, and any process whose executable/arguments do not match `ssh`, `-N`, the exact `127.0.0.1:16443:127.0.0.1:6443` forward, and `${DEPLOY_SSH_USER}@${DEPLOY_SSH_HOST}`. Validate identity immediately before every TERM or bounded-wait KILL, remove the pidfile only after confirmed exit, and never use `pkill`, `killall`, or an unvalidated PID. [src: scripts/ssh-tunnel-up.sh#ssh-background-launch]

    Add `SshTunnelLifecycleContractTests` before changing the helper. Use a temporary PATH with a fake long-running `ssh` executable whose captured arguments contain the real `127.0.0.1:16443:127.0.0.1:6443` forward: prove managed start writes a numeric live PID, managed stop terminates only that process and removes the pidfile, startup failure cleans up, and a pidfile pointing to a mismatched process or the wrong forward is rejected without signaling it. These tests must not open a real socket or SSH connection. [src: scripts/ssh-tunnel-up.sh#reachability-gate]

    Replace the terminal workflow failure with managed tunnel start using `SSH_TUNNEL_PID_FILE=${RUNNER_TEMP}/solidstats-memory-ssh.pid` and the repository kubeconfig helper. Pass `secrets.K8S_MEMORY_TOKEN` as `K8S_TOKEN`, `secrets.K8S_CA_CERT`, `K8S_NAMESPACE=solidstats-memory`, `K8S_USER_NAME=memory-ci-deployer`, and `K8S_CONTEXT_NAME=memory-k3s-staging`; use `memory-k3s-staging` explicitly on every `kubectl` call so the runner's default context cannot widen scope. [src: scripts/kubeconfig-setup.sh#L30-L77] [src: .github/workflows/deploy-staging.yml#L51-L80]

    Immediately after kubeconfig setup, run `kubectl --context memory-k3s-staging auth whoami -o jsonpath='{.status.userInfo.username}'` and compare the result byte-for-byte with `system:serviceaccount:solidstats-memory:memory-ci-deployer`. Empty output, command failure, a different ServiceAccount, a human identity, or a merely non-anonymous identity must terminate the job. Keep this exact assertion and the RBAC boundary checks in the same pre-mutation gate, ordered before the Secret/workload server-side dry-runs and before every apply. [src: scripts/kubeconfig-setup.sh#L70-L77] [src: k8s/memory/01-ci-rbac.yaml#memory-ci-deployer-service-account]

    Add a fail-closed authorization step before mutation. It must prove the required Secret, ConfigMap, Service, Deployment, StatefulSet, CronJob, NetworkPolicy, and ServiceAccount operations in `solidstats-memory`, then fail if the identity can create namespaces, mutate Roles/RoleBindings in `solidstats-memory`, or mutate Deployments in `solid-stats-staging` or `monitoring`. Do not add or apply RBAC to make a failed check pass; the operator-owned bootstrap remains the only source of the token and RoleBinding. [src: k8s/memory/01-ci-rbac.yaml#Role]

    Construct one deterministic workload manifest list from `rendered-memory/*.yaml`, explicitly excluding `00-namespace.yaml` and `01-ci-rbac.yaml`, and reuse that same list for server-side dry-run and apply. Dry-run the rendered Secret document and workload list before either apply. Apply Secrets from an ephemeral file with restrictive permissions, then apply workloads with `--server-side`; verify rollout of `statefulset/qdrant` and `deployment/mempalace` with bounded timeouts. Preserve the existing source and rendered strict validators before all Kubernetes operations. [src: .github/workflows/deploy-memory.yml#render-and-validate] [src: .github/workflows/deploy-staging.yml#server-side-dry-run-and-apply]

    Put cleanup in an `if: always()` step. Invoke `bash scripts/ssh-tunnel-up.sh --stop-managed` with the same `SSH_TUNNEL_PID_FILE`, `DEPLOY_SSH_USER`, and `DEPLOY_SSH_HOST`, then remove the rendered Secret file, rendered directory, and generated kubeconfig without echoing their contents. The helper must perform the process-identity validation; the workflow must not signal a PID directly. Keep all operator substitutions fail-closed and keep secret values in the GitHub environment only. [src: docs/solidstats-memory.md#operator-owned-bootstrap]
  </action>
  <acceptance_criteria>
    - `MemoryDeployWorkflowContractTests` pass and identify the memory token, namespace, user, context, and exact `kubectl auth whoami` username assertion.
    - `SshTunnelLifecycleContractTests` pass for normal stop, startup failure, stale PID, and command-mismatch refusal without opening a real SSH connection.
    - The workflow has a real authenticated dry-run/apply path and no terminal step whose unconditional result is failure.
    - Both dry-run and apply consume the same manifest list, and that list cannot contain `00-namespace.yaml` or `01-ci-rbac.yaml`.
    - The workflow does not reference `K8S_TOKEN`, `K8S_OBS_TOKEN`, `K8S_OBS_ET_TOKEN`, `ci-k3s-staging`, `obs-k3s-staging`, or `obs-et-k3s-staging`.
    - A merely authenticated or non-anonymous identity is insufficient: the workflow exits before dry-run/apply unless the explicit context reports exactly `system:serviceaccount:solidstats-memory:memory-ci-deployer`.
    - Any failed render, validation, authentication, exact identity proof, authorization, server-side dry-run, apply, or rollout ends the job non-zero before later mutation and still executes pidfile-validated cleanup.
  </acceptance_criteria>
  <verify>
    <automated>timeout 30s python3 tests/test-memory-runtime-contract.py MemoryDeployWorkflowContractTests SshTunnelLifecycleContractTests &amp;&amp; timeout 10s bash -n scripts/ssh-tunnel-up.sh &amp;&amp; timeout 10s python3 scripts/validate-memory-manifests.py --allow-operator-placeholders</automated>
  </verify>
  <done>The repository contains one production workflow path from a safely managed SSH PID through exact memory ServiceAccount identity proof, dry-run, apply, rollout, and validated cleanup, with bootstrap manifests and other namespaces unreachable by construction and contract test.</done>
</task>

<task type="auto">
  <name>Task 2: Export source-backed MCP, Qdrant, and snapshot signals inside the memory namespace</name>
  <files>.github/workflows/deploy-memory.yml, k8s/memory/30-network-policy.yaml, k8s/memory/50-monitoring.yaml, scripts/render-memory-manifests.py, scripts/validate-memory-manifests.py</files>
  <read_first>
    - `.planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md` — blocker 2 and the unsupported metric-name warning.
    - `.planning/phases/19-solidstats-memory-foundation/19-RESEARCH.md` — responsibility map and monitoring constraints.
    - `k8s/memory/10-qdrant.yaml` — Qdrant Service, runtime Secret, API-key key name, and immutable image/security conventions.
    - `k8s/memory/20-mempalace.yaml` — MCP Service, `/healthz`, Secret references, and security context conventions.
    - `k8s/memory/30-network-policy.yaml` — default deny, existing direct Prometheus ingress, and exact pod labels/ports.
    - `k8s/memory/40-backup.yaml` — collection name, snapshot API use, and backup CronJob identity.
    - `k8s/memory/50-monitoring.yaml` — current unconsumed handoff ConfigMap to replace.
    - `scripts/render-memory-manifests.py` — fail-closed image-digest substitution interface.
    - `scripts/validate-memory-manifests.py` — expected file/resource counts and security assertions.
    - `https://qdrant.tech/documentation/ops-monitoring/monitoring/` — official Qdrant readiness endpoint behavior.
    - `https://api.qdrant.tech/master/api-reference/collections/get-collection` — official collection status and optimizer status response.
    - `https://api.qdrant.tech/master/api-reference/snapshots/list-snapshots` — official snapshot `creation_time` response and API-key authentication.
  </read_first>
  <action>
    Replace the handoff ConfigMap with a deployable `solidstats-memory-observer` ServiceAccount, ConfigMap, Service, and single-replica Deployment. The ServiceAccount must set `automountServiceAccountToken: false`. The Deployment must use the fail-closed `MEMORY_OPERATOR_SUPPLIED_OBSERVER_IMAGE_DIGEST`, run as UID/GID 65532 with the repository's restricted pod/container security context, expose only TCP 9108, use read-only ConfigMap code, set CPU/memory requests and limits, and carry readiness/liveness probes on `/healthz`. Add `MEMORY_OBSERVER_IMAGE` to the renderer and workflow using the existing immutable-digest validator; add observer rollout verification to the workflow only after its manifest is in the applied set. [src: k8s/memory/10-qdrant.yaml#workload-security-context] [src: scripts/render-memory-manifests.py#valid_image]

    Implement `exporter.py` with Python standard library only. `probe_http` must use bounded connect/read timeouts and never log request headers or bodies. Probe MCP `GET /healthz`; measure request duration with a monotonic clock; expose `solidstats_memory_mcp_ready`, `solidstats_memory_mcp_probe_duration_seconds`, and cumulative `solidstats_memory_mcp_probe_errors_total`. Probe Qdrant `GET /readyz`, authenticated `GET /collections/{collection}`, and authenticated `GET /collections/{collection}/snapshots`; read the collection and API key from existing `qdrant-runtime` Secret keys, percent-encode the collection path, treat only HTTP 2xx plus documented healthy response fields as healthy, and parse RFC 3339 `creation_time` into `solidstats_memory_qdrant_latest_snapshot_timestamp_seconds`. [src: https://qdrant.tech/documentation/ops-monitoring/monitoring/] [src: https://api.qdrant.tech/master/api-reference/collections/get-collection] [src: https://api.qdrant.tech/master/api-reference/snapshots/list-snapshots]

    Export stable observer-owned metrics rather than assuming undocumented MemPalace or Qdrant metric names: `solidstats_memory_qdrant_ready`, `solidstats_memory_qdrant_collection_healthy`, `solidstats_memory_qdrant_probe_errors_total`, and the snapshot timestamp in addition to the MCP series. `parse_collection_health` must return healthy only for `result.status=green` and `result.optimizer_status=ok`; `latest_snapshot_timestamp` must return zero when the authenticated response has no snapshots and treat malformed responses as probe errors. `MetricsHandler` must serve `/metrics` as Prometheus text format and `/healthz` as the observer process health, use fixed metric names/labels, serialize concurrent collections safely, and return no secret or upstream response content. [src: .planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md#gap-2]

    Replace direct Prometheus ingress to MCP/Qdrant with `allow-prometheus-to-memory-observer`, selecting only the Prometheus pod in namespace `monitoring` and observer TCP 9108. Add `allow-memory-observer-egress` for DNS plus TCP 8765 to `mempalace` and TCP 6333 to `qdrant`; add the observer as an allowed ingress peer on the corresponding MCP/Qdrant ports. Do not grant the observer Kubernetes API access, Internet egress, S3 egress, or access to any staging workload. [src: k8s/memory/30-network-policy.yaml#default-deny-and-prometheus-scrape]

    Extend the strict memory validator to require the four observer resources, immutable image substitution, dedicated ServiceAccount, disabled token mount, restricted security context, resource bounds, fixed ports, Secret-key references, bounded probe code, the exact metric set, and the reciprocal NetworkPolicy selectors. Compile the embedded Python source with `compile()` during validation and reject any residual operator substitution token in rendered mode. [src: scripts/validate-memory-manifests.py#resource-and-security-validation]
  </action>
  <acceptance_criteria>
    - `50-monitoring.yaml` contains a runnable observer workload and no longer consists only of operator handoff data.
    - The observer emits readiness, latency, cumulative error, collection-health, and latest-snapshot timestamp signals derived from documented HTTP responses; no unproven upstream metric name is referenced.
    - The observer receives the Qdrant collection and API key through existing Secret references and does not render, log, label, or return either value.
    - Prometheus can reach only observer TCP 9108 in `solidstats-memory`; the observer can reach only DNS, MCP TCP 8765, and Qdrant TCP 6333.
    - The memory workflow requires an immutable observer image digest, applies the observer through the existing workload list, and waits for its rollout without adding any observability permission.
  </acceptance_criteria>
  <verify>
    <automated>timeout 10s python3 scripts/validate-memory-manifests.py --allow-operator-placeholders &amp;&amp; timeout 10s python3 -m py_compile scripts/render-memory-manifests.py scripts/validate-memory-manifests.py</automated>
  </verify>
  <done>The memory namespace exports a validated, least-privilege metrics surface backed by real MCP and Qdrant HTTP contracts, with an exact default-deny network path from Prometheus to the observer and from the observer to its two upstreams.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make Prometheus consume memory signals and evaluate complete OPS-04 rules</name>
  <files>k8s/observability/values/prometheus-values.yaml, k8s/observability/10-prometheus.yaml, k8s/observability/95-netpol-monitoring.yaml, scripts/validate-obs-manifests.py, tests/test-memory-runtime-contract.py</files>
  <read_first>
    - `.planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md` — required OPS-04 dimensions and the unconsumed-config finding.
    - `k8s/observability/values/prometheus-values.yaml` — authoritative Helm values, current static jobs, Kubernetes service discovery, and rule-file wiring.
    - `k8s/observability/10-prometheus.yaml` — committed generated output consumed by the observability workflow.
    - `k8s/observability/95-netpol-monitoring.yaml` — current Prometheus egress allowlist.
    - `k8s/staging/01-obs-rbac.yaml` — existing Prometheus node, nodes/proxy, and metrics read permissions.
    - `.github/workflows/deploy-observability.yml` — owner and apply path for observability manifests.
    - `scripts/validate-obs-manifests.py` — source/rendered synchronization and security checks.
    - `k8s/memory/40-backup.yaml` — suspended backup gate and daily schedule.
    - `https://prometheus.io/docs/prometheus/latest/configuration/recording_rules/` — official rule loading and evaluation contract.
    - `https://kubernetes.io/docs/reference/instrumentation/metrics/` — official `kubelet_volume_stats_used_bytes` and `kubelet_volume_stats_capacity_bytes` names and labels.
    - `https://github.com/kubernetes/kube-state-metrics/blob/main/docs/metrics/workload/cronjob-metrics.md` — stable `kube_cronjob_spec_suspend` gate.
  </read_first>
  <behavior>
    - Test 1: both authoritative values and rendered Prometheus config scrape `solidstats-memory-observer.solidstats-memory.svc.cluster.local:9108` with job `solidstats-memory-observer`.
    - Test 2: job `kubernetes-nodes-volume-stats` reuses authenticated node discovery and Kubernetes API proxy `/metrics`; it does not scrape an unauthenticated kubelet address.
    - Test 3: every required recording rule and alert name is present in source and rendered configuration, with selectors limited to `namespace=solidstats-memory` and the two expected PVC names.
    - Test 4: snapshot freshness alerts evaluate only when `qdrant-snapshot` is not suspended; missing kubelet volume metrics produce `SolidStatsMemoryPVCMetricsMissing` rather than a passing capacity result.
    - Test 5: the observability NetworkPolicy allows only Prometheus egress to observer TCP 9108, while the memory workflow and memory Role contain no monitoring permissions.
    - Test 6: extracted observer functions map healthy, unhealthy, malformed, missing-snapshot, and timeout fixtures to the promised stable metric values without exposing fixture secrets.
  </behavior>
  <action>
    Begin by extending `tests/test-memory-runtime-contract.py` with `MemoryObserverContractTests` and `PrometheusMemoryContractTests`. Use only standard-library YAML/text extraction and fake HTTP responses; never contact a cluster, DNS name, or external endpoint. Extract and compile `exporter.py` from the ConfigMap, inject deterministic clocks/responses, and test healthy plus failure paths before changing Prometheus configuration. [src: k8s/memory/50-monitoring.yaml#exporter.py]

    Add `solidstats-memory-observer` to `serverFiles.prometheus.yml.scrape_configs` with the exact in-cluster Service DNS and port 9108. Add `kubernetes-nodes-volume-stats` by reusing the existing authenticated Kubernetes node-discovery pattern, Prometheus ServiceAccount bearer token, CA, and API-server proxy relabeling, but point the metrics path to `/api/v1/nodes/${1}/proxy/metrics`. Do not add RBAC: the existing Prometheus ClusterRole already owns node/proxy reads, while the memory identity remains namespace-only. [src: k8s/observability/values/prometheus-values.yaml#prometheus-serverFiles] [src: k8s/staging/01-obs-rbac.yaml#prometheus-cluster-role] [src: https://kubernetes.io/docs/reference/instrumentation/metrics/]

    Add the seven recording rules named in `Artifacts this phase produces`. Limit observer rules to `job=solidstats-memory-observer`. Calculate the PVC ratio from `kubelet_volume_stats_used_bytes / kubelet_volume_stats_capacity_bytes`, limited to `namespace=solidstats-memory` and `persistentvolumeclaim=~\"mempalace-data|qdrant-data-qdrant-0\"`. Calculate snapshot age from the observer timestamp. [src: https://prometheus.io/docs/prometheus/latest/configuration/recording_rules/] [src: https://kubernetes.io/docs/reference/instrumentation/metrics/]

    Add all eight named alerts with explicit severities, runbook annotations pointing to `docs/solidstats-memory.md`, and bounded `for` durations. Use these thresholds: readiness/health/collection failures for 5 minutes, MCP latency above 2 seconds for 10 minutes, a positive 5-minute MCP probe-error rate for 5 minutes, snapshot missing or older than 26 hours for 15 minutes, PVC ratio above 0.85 for 15 minutes, and missing capacity metrics for either expected PVC for 15 minutes. Gate snapshot freshness with `kube_cronjob_spec_suspend{namespace=\"solidstats-memory\",cronjob=\"qdrant-snapshot\"} == 0`; the intentionally suspended pre-approval state must not page. Treat absent capacity series as an alert, never as a zero-usage success. [src: k8s/memory/40-backup.yaml#qdrant-snapshot-cronjob] [src: https://github.com/kubernetes/kube-state-metrics/blob/main/docs/metrics/workload/cronjob-metrics.md]

    Add only the reciprocal Prometheus egress rule required for TCP 9108 to the labeled observer in namespace `solidstats-memory`. Keep ownership one-way: `deploy-observability.yml` applies the changed Prometheus and monitoring NetworkPolicy files with the observability identity; `deploy-memory.yml` does not apply or authenticate to `monitoring`. [src: .github/workflows/deploy-observability.yml#apply-observability-manifests] [src: k8s/observability/95-netpol-monitoring.yaml#allow-prometheus-scrape-egress]

    Regenerate `k8s/observability/10-prometheus.yaml` from the pinned chart and changed values using the repository's established render procedure; do not hand-diverge source and rendered config. Extend `scripts/validate-obs-manifests.py` to assert both jobs, authenticated node proxying, all recording/alert rules, snapshot suspension gate, PVC selectors/missing-series alert, source/render parity, and reciprocal NetworkPolicy selectors. [src: k8s/observability/10-prometheus.yaml#generated-source-marker] [src: scripts/validate-obs-manifests.py#prometheus-validation]
  </action>
  <acceptance_criteria>
    - The committed Prometheus ConfigMap consumed by `deploy-observability.yml` contains both new jobs and all seven recording rules plus eight alerts.
    - MCP readiness, latency, and errors; Qdrant readiness and collection state; snapshot freshness; and both PVC capacities each have an evaluated recording or alert path.
    - Snapshot freshness is dormant only while the backup CronJob is intentionally suspended; once enabled, no snapshot, an invalid timestamp, or an age over 26 hours becomes alerting evidence.
    - Missing kubelet volume series for either `mempalace-data` or `qdrant-data-qdrant-0` raises `SolidStatsMemoryPVCMetricsMissing` and cannot masquerade as healthy capacity.
    - Prometheus reaches the observer through reciprocal namespace/pod/port selectors, and no memory ServiceAccount, Role, workflow token, or context can modify observability resources.
  </acceptance_criteria>
  <verify>
    <automated>timeout 30s python3 tests/test-memory-runtime-contract.py &amp;&amp; timeout 10s python3 scripts/validate-obs-manifests.py &amp;&amp; timeout 10s python3 scripts/validate-memory-manifests.py --allow-operator-placeholders &amp;&amp; timeout 10s python3 -m py_compile scripts/validate-obs-manifests.py</automated>
  </verify>
  <done>Prometheus consumes the memory observer and authenticated kubelet volume series, evaluates the complete OPS-04 rule set, exposes missing telemetry as failure, and preserves separate deploy identities and manifest ownership.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| GitHub runner to k3s API | Untrusted workflow environment crosses the SSH tunnel and authenticates with a namespace-scoped bearer token. |
| Prometheus to memory observer | Monitoring crosses the namespace default-deny boundary on one selected pod and port. |
| Memory observer to MCP/Qdrant | The observer handles upstream HTTP responses and a Qdrant API key without exposing either. |
| Source values to rendered Prometheus manifest | The committed generated manifest must remain mechanically consistent with its authoritative Helm values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-19-03-01 | Spoofing | `deploy-memory.yml` k3s authentication | high | mitigate | Pinned SSH known-host input, cluster CA, explicit `memory-k3s-staging` context, byte-exact `kubectl auth whoami` assertion for `system:serviceaccount:solidstats-memory:memory-ci-deployer`, and allowed/denied `auth can-i` gates before mutation. |
| T-19-03-02 | Elevation of Privilege | Memory workflow manifest selection | critical | mitigate | One tested manifest list excludes namespace/RBAC bootstrap; memory Role stays namespaced; workflow fails if it can mutate RBAC, namespaces, staging, or monitoring. |
| T-19-03-03 | Information Disclosure | Rendered Secrets, tunnel process, and Qdrant API key | high | mitigate | GitHub/Kubernetes Secret references only, restrictive temporary files/pidfile, exact SSH-process validation before signaling, no content logging, fixed metric labels, cleanup under `if: always()`, and no values committed. |
| T-19-03-04 | Tampering | Rendered memory/Prometheus configuration | high | mitigate | Strict validators, source/render parity tests, server-side dry-run before apply, deterministic file selection, and generated-manifest validation. |
| T-19-03-05 | Denial of Service | Observer upstream probes | medium | mitigate | Bounded timeouts, one replica, fixed collection cadence driven by Prometheus, resource requests/limits, and no unbounded response or label cardinality. |
| T-19-03-06 | Spoofing | Observer metric source | medium | mitigate | Reciprocal namespace/pod/port NetworkPolicies and fixed Service DNS; observer derives series from documented endpoints and does not accept caller-supplied labels. |
| T-19-03-07 | Repudiation | Runtime deploy and alert changes | low | accept | Git commit history and GitHub Actions logs provide the ASVS L2 audit trail; no additional in-cluster audit system is introduced by this gap closure. |
| T-19-03-08 | Elevation of Privilege | Cross-boundary observability deployment | high | mitigate | Only the observability workflow applies `k8s/observability`; the memory workflow never receives a monitoring token/context or observability API permissions. |
| T-19-03-SC | Tampering | Package installation | low | accept | No npm, pip, cargo, Helm plugin, or new package installation is introduced; exporter and tests use Python standard library and the existing pinned Prometheus chart render procedure. |

</threat_model>

<verification>
Run only repository-local, offline checks during execution:

1. `timeout 30s python3 tests/test-memory-runtime-contract.py`
2. `timeout 10s python3 scripts/validate-memory-manifests.py --allow-operator-placeholders`
3. Render memory manifests in a temporary directory with synthetic non-secret values and immutable test image digests, then run `timeout 10s python3 scripts/validate-memory-manifests.py --manifest-dir "$TEMP_DIR"` from the test harness; the executor must not read `.env`, `.env.*`, `.secrets`, GitHub secrets, or live Kubernetes Secrets.
4. `timeout 10s python3 scripts/validate-obs-manifests.py`
5. `timeout 10s bash -n scripts/ssh-tunnel-up.sh`
6. `timeout 10s python3 -m py_compile scripts/render-memory-manifests.py scripts/validate-memory-manifests.py scripts/validate-obs-manifests.py tests/test-memory-runtime-contract.py`
7. `timeout 10s git diff --check -- .github/workflows/deploy-memory.yml k8s/memory/30-network-policy.yaml k8s/memory/50-monitoring.yaml k8s/observability/values/prometheus-values.yaml k8s/observability/10-prometheus.yaml k8s/observability/95-netpol-monitoring.yaml scripts/render-memory-manifests.py scripts/ssh-tunnel-up.sh scripts/validate-memory-manifests.py scripts/validate-obs-manifests.py tests/test-memory-runtime-contract.py`

Do not run the deploy workflows, `kubectl`, SSH, DNS, nginx, or S3 commands while executing this plan.

Deferred operator UAT, not an automated pass: after the operator supplies real substitutions, bootstraps the ServiceAccount token, deploys through the approved workflows, and confirms the cluster uses kube-router NetworkPolicy enforcement, rerun the human test from `19-VERIFICATION.md`. It must prove default-denied traffic is rejected, only monitoring Prometheus reaches observer TCP 9108, only the observer reaches MCP/Qdrant health APIs, and the backup job retains its separately approved S3 path. Until that live evidence is recorded, the NetworkPolicy runtime item remains `unverified`.
</verification>

<success_criteria>

- Both blockers in `19-VERIFICATION.md` are closed by executable repository paths rather than prose or an unconsumed ConfigMap.
- OPS-01 has an authenticated, namespace-scoped apply path that preserves operator bootstrap and cannot apply `00-namespace.yaml` or `01-ci-rbac.yaml`.
- OPS-04 has actively scraped signals and evaluated rules for every required dimension, with missing data represented as an alerting failure.
- ISO-02 and ISO-04 remain least-privilege boundaries: memory cannot mutate staging/monitoring/RBAC, and monitoring can cross into memory only on observer TCP 9108.
- RUN-03 snapshot freshness is monitored only after the operator enables the backup CronJob; the plan does not enable backup or claim the live restore gate.
- All offline validators/tests pass without cluster access, external network access, or secret reads.
</success_criteria>

## Source Coverage Audit

| Source | Item | Coverage | Plan evidence |
| --- | --- | --- | --- |
| GOAL | Dedicated namespace/runtime/storage/network/backup/monitoring/CD boundary | COVERED | Tasks 1–3 close the two remaining verified blockers while preserving Phase 19's existing runtime/storage/backup artifacts. |
| REQ | OPS-01 viable dedicated deploy boundary | COVERED | Task 1. |
| REQ | OPS-04 consumed health, latency, errors, collection, snapshot, and capacity monitoring | COVERED | Tasks 2–3. |
| REQ | ISO-02 namespace/RBAC separation and ISO-04 network isolation | COVERED | Task 1 authorization gates and Tasks 2–3 reciprocal NetworkPolicies. |
| REQ | RUN-03 snapshot freshness without bypassing backup approval | COVERED | Task 3 alert gated by `qdrant-snapshot` suspension state. |
| RESEARCH | Do not invent unverified application metric names | COVERED | Task 2 owns a documented HTTP observer metric contract; Task 3 uses official Kubernetes metric names. |
| CONTEXT | Operator-owned bootstrap, immutable substitutions, no secret values in git, no live work in this phase | COVERED | User setup, Tasks 1–2, threat model, and offline verification constraints. |
| CONTEXT | Default-deny behavior must be proven under kube-router | DEFERRED OPERATOR UAT | Explicit deferred gate in `<verification>`; no automated completion claim. |

Exclusions are not gaps: Phase 20 migration execution and live ingress/DNS/nginx cutover remain outside this plan. No deferred idea from `19-CONTEXT.md` is implemented.

## Orchestrator-owned tracking correction

`ROADMAP.md` assigns MIG-03 through MIG-07 to Phase 19, while `REQUIREMENTS.md` maps those same IDs to Phase 20. This plan does not claim or reimplement migration work: the orchestrator must reconcile that traceability mismatch in the tracking artifacts after planning. The correction is not an executor task and is not required to perform the two repository gap fixes above, but Phase 19 must not be reported as fully traceable until it is resolved.

<output>
Create `.planning/phases/19-solidstats-memory-foundation/19-03-SUMMARY.md` when execution completes.
</output>

<!-- markdownlint-enable MD013 MD033 MD041 -->
