---
phase: 19-solidstats-memory-foundation
plan: 04
type: execute
gap_closure: true
wave: 4
depends_on:
  - 19-03
files_modified:
  - k8s/memory/30-network-policy.yaml
  - k8s/observability/10-prometheus.yaml
  - k8s/observability/values/prometheus-values.yaml
  - scripts/validate-memory-manifests.py
  - scripts/validate-obs-manifests.py
  - tests/test-memory-runtime-contract.py
autonomous: true
requirements:
  - ISO-04
  - OPS-04
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
premises:
  - claim: "The namespace default-deny selects all pods for ingress and egress, while observer egress already selects MemPalace TCP 8765."
    src: "k8s/memory/30-network-policy.yaml#default-deny-all-and-allow-memory-observer-egress"
    verify: "timeout 10s rg -n 'default-deny-all|allow-memory-observer-egress|app.kubernetes.io/name: mempalace|port: 8765' k8s/memory/30-network-policy.yaml"
  - claim: "The source-of-truth backup workload is the single CronJob named solidstats-memory-backup in solidstats-memory."
    src: "k8s/memory/40-backup.yaml#cronjob-solidstats-memory-backup"
    verify: "timeout 10s rg -n 'kind: CronJob|name: solidstats-memory-backup|namespace: solidstats-memory' k8s/memory/40-backup.yaml"
  - claim: "Prometheus Helm values are authoritative and k8s/observability/10-prometheus.yaml is the committed rendered artifact consumed by deployment."
    src: "k8s/observability/values/prometheus-values.yaml#render-command and .github/workflows/deploy-observability.yml#apply-observability-manifests"
    verify: "timeout 10s python3 scripts/validate-obs-manifests.py"
  - claim: "The current offline runtime test reads all three memory, backup, and Prometheus contracts with PyYAML."
    src: "tests/test-memory-runtime-contract.py#PrometheusMemoryContractTests"
    verify: "timeout 30s python3 tests/test-memory-runtime-contract.py"
carried_forward:
  - source: ".planning/phases/19-solidstats-memory-foundation/19-03-SUMMARY.md#deferred-operator-uat"
    item: "kube-router default-deny enforcement needs live operator evidence."
    disposition: "Preserved as a human gate; Task 1 proves source structure only."
  - source: ".planning/phases/19-solidstats-memory-foundation/19-03-SUMMARY.md#deviations-from-plan"
    item: "A fresh render from the pinned Prometheus chart was not performed offline."
    disposition: "Preserved as a human gate; Task 2 changes only the authoritative expression and its matching committed render."
must_haves:
  truths:
    - "Under the existing namespace default deny, only pods labeled solidstats-memory-observer can enter MemPalace on TCP 8765 through a dedicated ingress NetworkPolicy."
    - "The memory validator and runtime contract test fail if the observer-to-MemPalace path loses either side, changes target/peer labels or port, or broadens to another source."
    - "The snapshot-freshness alert in both authoritative Prometheus values and the committed render gates on the CronJob name read from k8s/memory/40-backup.yaml."
    - "Renaming or duplicating the backup CronJob without synchronizing both Prometheus artifacts makes offline validation and tests fail."
    - "No task changes the default deny, backup CronJob identity/suspension, observer egress, alert thresholds, unrelated Prometheus jobs/rules, secrets, or runtime cluster state."
  artifacts:
    - path: "k8s/memory/30-network-policy.yaml"
      provides: "NetworkPolicy/allow-memory-observer-to-mempalace with exact target, peer, and TCP port."
    - path: "scripts/validate-memory-manifests.py"
      provides: "Structural source gate for reciprocal observer-to-MemPalace policy."
    - path: "k8s/observability/values/prometheus-values.yaml"
      provides: "Authoritative snapshot alert expression keyed to the manifest-owned backup CronJob."
    - path: "k8s/observability/10-prometheus.yaml"
      provides: "Mechanically synchronized rendered snapshot alert expression."
    - path: "scripts/validate-obs-manifests.py"
      provides: "Cross-manifest CronJob-name extraction and Prometheus source/render validation."
    - path: "tests/test-memory-runtime-contract.py"
      provides: "Regression tests for exact NetworkPolicy reciprocity and dynamically derived backup selector."
  key_links:
    - from: "NetworkPolicy/allow-memory-observer-egress"
      to: "NetworkPolicy/allow-memory-observer-to-mempalace"
      via: "source pod solidstats-memory-observer to destination pod mempalace on TCP 8765"
    - from: "k8s/memory/40-backup.yaml"
      to: "SolidStatsMemorySnapshotMissingOrStale in Prometheus values and render"
      via: "the sole CronJob metadata.name, extracted by validator and test rather than duplicated as a test constant"
    - from: "k8s/observability/values/prometheus-values.yaml"
      to: "k8s/observability/10-prometheus.yaml"
      via: "identical snapshot alert selector enforced offline, followed by pinned-chart operator render evidence"
---

<!-- markdownlint-disable MD013 MD033 MD041 -->

<objective>
Close the two new Phase 19 source regressions without changing any accepted deployment, observer, backup, or wider observability behavior.

Purpose: Complete the default-deny observer path to MemPalace and bind snapshot freshness to the actual backup CronJob contract so both OPS-04 failures become mechanically detectable.

Output: One exact ingress NetworkPolicy, source/render snapshot-selector correction, and fail-closed validators/tests for both links.
</objective>

## Artifacts this phase produces

| File | Exact additions or target state |
| --- | --- |
| `k8s/memory/30-network-policy.yaml` | `NetworkPolicy/allow-memory-observer-to-mempalace`: target `app.kubernetes.io/name=mempalace`; source `app.kubernetes.io/name=solidstats-memory-observer`; `policyTypes: [Ingress]`; TCP 8765 only. |
| `scripts/validate-memory-manifests.py` | `require_network_contract` requires and structurally checks the new policy, including rejection of namespace-wide, empty-selector, IP-block, extra-port, or egress broadening. |
| `tests/test-memory-runtime-contract.py` | `MemoryObserverContractTests.test_observer_mempalace_policy_is_exact_and_reciprocal` compares parsed ingress/egress policies; `PrometheusMemoryContractTests.test_snapshot_gate_tracks_backup_cronjob_manifest` derives the sole CronJob name from `40-backup.yaml` and checks both Prometheus artifacts. |
| `k8s/observability/values/prometheus-values.yaml` | `SolidStatsMemorySnapshotMissingOrStale` gates on `kube_cronjob_spec_suspend{namespace="solidstats-memory",cronjob="solidstats-memory-backup"} == 0`. |
| `k8s/observability/10-prometheus.yaml` | The committed rendered `SolidStatsMemorySnapshotMissingOrStale` expression contains the same corrected selector and no unrelated generated change. |
| `scripts/validate-obs-manifests.py` | Constants/helper `MEMORY_BACKUP_MANIFEST`, `_metadata_name`, and `_memory_backup_cronjob_name`; `_check_memory_monitoring_contract` rejects missing/duplicate CronJobs and any snapshot selector that differs from the extracted manifest name in either source or render. |

<execution_context>
@/home/afgan0r/.codex/gsd-core/workflows/execute-plan.md
@/home/afgan0r/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-solidstats-memory-foundation/19-CONTEXT.md
@.planning/phases/19-solidstats-memory-foundation/19-RESEARCH.md
@.planning/phases/19-solidstats-memory-foundation/19-SPEC.md
@.planning/phases/19-solidstats-memory-foundation/19-03-SUMMARY.md
@.planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md
@k8s/memory/30-network-policy.yaml
@k8s/memory/40-backup.yaml
@k8s/observability/values/prometheus-values.yaml
@k8s/observability/10-prometheus.yaml
@scripts/validate-memory-manifests.py
@scripts/validate-obs-manifests.py
@tests/test-memory-runtime-contract.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Complete the observer-to-MemPalace path under default deny</name>
  <files>k8s/memory/30-network-policy.yaml, scripts/validate-memory-manifests.py, tests/test-memory-runtime-contract.py</files>
  <read_first>
    - `.planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md` — the failed observer-to-MemPalace truth and exact missing reciprocal edge.
    - `k8s/memory/30-network-policy.yaml` — default deny, existing observer egress, Qdrant reciprocity pattern, and host-nginx ingress that must remain separate.
    - `k8s/memory/20-mempalace.yaml` — authoritative MemPalace pod label and TCP 8765 Service/container contract.
    - `k8s/memory/50-monitoring.yaml` — authoritative observer pod label and MCP probe destination.
    - `scripts/validate-memory-manifests.py` — current required-policy set and string-based network contract checks.
    - `tests/test-memory-runtime-contract.py` — PyYAML parsing conventions and `MemoryObserverContractTests` fixtures.
  </read_first>
  <behavior>
    - Test 1: the new ingress policy selects only `app.kubernetes.io/name=mempalace` and accepts only `app.kubernetes.io/name=solidstats-memory-observer` on TCP 8765.
    - Test 2: the existing observer egress selects that same destination label and TCP port, so both policy directions describe one exact flow.
    - Test 3: an empty selector, namespace selector, IP block, different source/target label, different/additional port, Egress policy type, or extra peer fails the structural contract.
    - Test 4: existing host-nginx ingress, Qdrant reciprocity, DNS, backup, Prometheus, and default-deny policies remain unchanged and independently selected.
  </behavior>
  <action>
    Start with `MemoryObserverContractTests.test_observer_mempalace_policy_is_exact_and_reciprocal`. Parse `30-network-policy.yaml` with PyYAML, index NetworkPolicies by `metadata.name`, and compare the target policy's complete `spec` with the exact expected mapping. Separately inspect `allow-memory-observer-egress` and require its MemPalace destination/port tuple to match the new ingress target. This must be a structural assertion, not a global substring count. [src: tests/test-memory-runtime-contract.py#MemoryObserverContractTests] [src: k8s/memory/30-network-policy.yaml#allow-memory-observer-egress]

    Add `NetworkPolicy/allow-memory-observer-to-mempalace` in `solidstats-memory`. Set `spec.podSelector.matchLabels.app.kubernetes.io/name` to `mempalace`, `policyTypes` to exactly `Ingress`, and one ingress rule with one same-namespace `podSelector` peer labeled `solidstats-memory-observer` plus one TCP 8765 port. Do not combine this peer with `allow-host-nginx-to-mcp`: keeping them as separate policies preserves independent operator CIDR evidence and makes the observer permission auditable. [src: k8s/memory/20-mempalace.yaml#deployment-mempalace-labels] [src: k8s/memory/50-monitoring.yaml#deployment-solidstats-memory-observer-labels] [src: .agents/skills/kubernetes-specialist/references/networking.md#NetworkPolicy-Zero-Trust]

    Extend `require_network_contract` to require `NetworkPolicy/allow-memory-observer-to-mempalace` and validate its full destination/source/protocol/port shape inside that document. Reject an empty pod selector, `namespaceSelector`, `ipBlock`, an extra peer/port/rule, or any Egress policy type. Validate the matching MemPalace tuple in `allow-memory-observer-egress` at the same time so either side disappearing or drifting fails the existing offline validator. Preserve source-mode operator CIDR handling after these invariant checks; its early return must not bypass reciprocity validation. [src: scripts/validate-memory-manifests.py#require_network_contract] [src: .planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md#gaps]
  </action>
  <acceptance_criteria>
    - The parsed policy spec has one MemPalace target, one observer source, one TCP 8765 port, `Ingress` only, and no namespace/IP/empty selector.
    - The existing observer egress and new MemPalace ingress agree on both pod labels, protocol, and port.
    - `validate-memory-manifests.py --allow-operator-placeholders` performs reciprocity checks before its source-mode CIDR branch returns.
    - The dedicated host-nginx policy remains the only IP-based MemPalace ingress and retains its operator-measured CIDR gate.
  </acceptance_criteria>
  <verify>
    <automated>timeout 30s python3 tests/test-memory-runtime-contract.py MemoryObserverContractTests &amp;&amp; timeout 10s python3 scripts/validate-memory-manifests.py --allow-operator-placeholders &amp;&amp; timeout 10s python3 -m py_compile scripts/validate-memory-manifests.py tests/test-memory-runtime-contract.py</automated>
  </verify>
  <done>The repository proves a least-privilege, reciprocal observer-to-MemPalace TCP 8765 policy path under the existing default deny without broadening any other ingress or egress.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind snapshot freshness to the manifest-owned backup CronJob</name>
  <files>k8s/observability/values/prometheus-values.yaml, k8s/observability/10-prometheus.yaml, scripts/validate-obs-manifests.py, tests/test-memory-runtime-contract.py</files>
  <read_first>
    - `.planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md` — the failed snapshot-freshness truth and source/render/test drift evidence.
    - `k8s/memory/40-backup.yaml` — authoritative CronJob kind, metadata name, namespace, and `spec.suspend` state; read-only input for this task.
    - `k8s/observability/values/prometheus-values.yaml` — authoritative `SolidStatsMemorySnapshotMissingOrStale` expression.
    - `k8s/observability/10-prometheus.yaml` — committed rendered expression consumed by Prometheus.
    - `scripts/validate-obs-manifests.py` — `_split_documents`, `_top_value`, and `_check_memory_monitoring_contract` extension points.
    - `tests/test-memory-runtime-contract.py` — current hardcoded selector assertion to replace with cross-manifest derivation.
    - `.planning/phases/19-solidstats-memory-foundation/19-03-SUMMARY.md` — pinned-chart rerender remains an operator/CI evidence gate.
  </read_first>
  <behavior>
    - Test 1: parse `40-backup.yaml`, require exactly one `CronJob` in `solidstats-memory`, and derive its `metadata.name` without a duplicate test constant.
    - Test 2: locate `SolidStatsMemorySnapshotMissingOrStale` in authoritative values and committed render and require both expressions to select the derived CronJob name and namespace.
    - Test 3: a renamed, absent, or second backup CronJob, a mismatched selector in either Prometheus artifact, or source/render disagreement fails validation and tests.
    - Test 4: the alert continues to gate on `kube_cronjob_spec_suspend == 0`, keeps the 26-hour threshold and 15-minute duration, and remains dormant while the actual backup CronJob is suspended.
  </behavior>
  <action>
    First replace the hardcoded snapshot selector assertion with `PrometheusMemoryContractTests.test_snapshot_gate_tracks_backup_cronjob_manifest`. Load every YAML document from `40-backup.yaml`, require exactly one `CronJob` whose namespace is `solidstats-memory`, derive `metadata.name`, then inspect the named alert expression in both Prometheus files and require the exact namespace/name selector. Include negative fixtures or helper-level assertions for absent, duplicate, renamed, source-only, and render-only drift. [src: k8s/memory/40-backup.yaml#CronJob] [src: tests/test-memory-runtime-contract.py#PrometheusMemoryContractTests]

    In authoritative `prometheus-values.yaml`, change only the `SolidStatsMemorySnapshotMissingOrStale` expression's CronJob label to the manifest-owned `solidstats-memory-backup`. Preserve `kube_cronjob_spec_suspend == 0`, the snapshot-age operand, 93600-second threshold, and `for: 15m`. Apply the identical narrow expression change to the `rules` data in committed `10-prometheus.yaml`; do not alter other generated lines, jobs, recording rules, alerts, labels, or chart metadata. [src: k8s/memory/40-backup.yaml#metadata.name] [src: k8s/observability/values/prometheus-values.yaml#SolidStatsMemorySnapshotMissingOrStale] [src: k8s/observability/10-prometheus.yaml#SolidStatsMemorySnapshotMissingOrStale]

    Add `MEMORY_BACKUP_MANIFEST`, `_metadata_name`, and `_memory_backup_cronjob_name` to `validate-obs-manifests.py`. Reuse `_split_documents` and `_top_value` to read `40-backup.yaml`; return a validation error unless exactly one namespaced CronJob exists. In `_check_memory_monitoring_contract`, build the expected suspension selector from that extracted name and require it inside the named snapshot alert block in both values and rendered config. Also reject a different CronJob selector inside either block and require source/render equality for the full snapshot alert expression. The backup manifest remains the source of truth; do not change its metadata or suspension state to fit monitoring. [src: scripts/validate-obs-manifests.py#_check_memory_monitoring_contract] [src: .planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md#gaps]
  </action>
  <acceptance_criteria>
    - The snapshot alert in authoritative values and committed render selects `solidstats-memory-backup` in `solidstats-memory` and preserves all existing timing/age semantics.
    - The test obtains the expected name by parsing the sole CronJob from `40-backup.yaml`; it does not encode a separate expected-name constant.
    - The observability validator fails on zero/multiple backup CronJobs, source/render selector disagreement, or any selector different from the current backup manifest.
    - No task modifies `k8s/memory/40-backup.yaml`, enables the suspended backup, or changes another Prometheus job, rule, alert, threshold, or rendered resource.
  </acceptance_criteria>
  <verify>
    <automated>timeout 30s python3 tests/test-memory-runtime-contract.py PrometheusMemoryContractTests &amp;&amp; timeout 10s python3 scripts/validate-obs-manifests.py &amp;&amp; timeout 10s python3 -m py_compile scripts/validate-obs-manifests.py tests/test-memory-runtime-contract.py</automated>
  </verify>
  <done>Snapshot freshness is gated by the actual backup CronJob identity in both consumed Prometheus artifacts, and future CronJob/source/render name drift fails offline before deployment.</done>
</task>

</tasks>

## Prohibitions

- Do not modify `k8s/memory/40-backup.yaml`, its CronJob name, `suspend: true`, schedule, identity, storage, or S3 contract.
- Do not weaken `default-deny-all`, broaden observer egress, merge observer ingress with host-nginx ingress, or add a namespace-wide/IP-based observer allowance.
- Do not change the observer workload/exporter, Prometheus jobs, recording expressions, alert thresholds/durations, memory deployment workflow, RBAC, or Secrets.
- Do not fetch the Helm chart, install packages, contact Kubernetes/DNS/nginx/S3, read secret files, or perform any live apply during execution.
- Do not claim kube-router enforcement or fresh pinned-chart render parity from repository-only checks.

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Observer pod to MemPalace pod | Default-denied same-namespace traffic needs matching egress and ingress selectors on one port. |
| Backup manifest to Prometheus alert | A Kubernetes workload identity is duplicated into a monitoring expression and can silently drift. |
| Authoritative Helm values to committed render | Prometheus consumes generated YAML, so source-only correction would not fix runtime behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-19-04-01 | Elevation of Privilege | MemPalace ingress NetworkPolicy | high | mitigate | Exact target/peer labels, Ingress-only policy type, TCP 8765 only, complete-spec tests, and validator rejection of broad selectors or extra peers/ports. |
| T-19-04-02 | Tampering | Snapshot CronJob selector | high | mitigate | Derive the expected selector from the sole CronJob in `40-backup.yaml`; fail on absent/duplicate/mismatched identities. |
| T-19-04-03 | Repudiation | Source/render Prometheus parity | medium | mitigate | Require equal named alert expressions in authoritative values and committed render; retain fresh pinned-chart render as operator evidence. |
| T-19-04-04 | Denial of Service | Observer MCP probe path | low | mitigate | Open only the already required TCP 8765 path from one labeled observer; preserve all other default-deny behavior. |
| T-19-04-SC | Tampering | Package installation | low | accept | No dependency or package-manager action is part of this plan; existing Python/PyYAML test and validator surfaces are reused offline. |

</threat_model>

<verification>
Run only repository-local checks:

1. `timeout 30s python3 tests/test-memory-runtime-contract.py`
2. `timeout 10s python3 scripts/validate-memory-manifests.py --allow-operator-placeholders`
3. `timeout 10s python3 scripts/validate-obs-manifests.py`
4. `timeout 10s python3 -m py_compile scripts/validate-memory-manifests.py scripts/validate-obs-manifests.py tests/test-memory-runtime-contract.py`
5. `timeout 10s git diff --check -- k8s/memory/30-network-policy.yaml k8s/observability/values/prometheus-values.yaml k8s/observability/10-prometheus.yaml scripts/validate-memory-manifests.py scripts/validate-obs-manifests.py tests/test-memory-runtime-contract.py`

Do not run Helm downloads/renders, `kubectl`, SSH, DNS, nginx, S3, or secret-reading commands during execution.

Deferred kube-router operator gate: after approved deployment with real network substitutions, prove observer-to-MemPalace TCP 8765 succeeds, every undocumented source to MemPalace is denied, and the rest of the Phase 19 allow/deny matrix still behaves as declared. Repository validation must leave this item `unverified` until live evidence exists.

Deferred Helm operator gate: in an approved networked CI/operator context, render the pinned `prometheus-community/prometheus` chart v29.11.0 from `k8s/observability/values/prometheus-values.yaml` and compare it with committed `10-prometheus.yaml`. The result must preserve the corrected snapshot selector and all previously accepted memory jobs/rules. Offline execution must not claim this evidence.
</verification>

<success_criteria>

- The two failed truths in the latest `19-VERIFICATION.md` pass their new structural and cross-manifest offline tests.
- ISO-04 has a complete source-level observer-to-MemPalace path with no policy broadening; kube-router behavior remains a human gate.
- OPS-04 snapshot freshness gates on the actual backup CronJob in both consumed Prometheus artifacts, with future name drift rejected automatically.
- Existing Phase 19 deployment, observer, backup, and wider monitoring contracts remain unchanged.
- All offline validators/tests pass without live infrastructure, external downloads, or secret reads.
</success_criteria>

## Source Coverage Audit

| Source | Item | Coverage | Plan evidence |
| --- | --- | --- | --- |
| GOAL | Dedicated network and monitoring boundary | COVERED | Task 1 closes the missing reciprocal edge; Task 2 repairs the consumed snapshot gate. |
| REQ | ISO-04 exact network isolation | COVERED | Task 1 only. |
| REQ | OPS-04 operational MCP probing and snapshot freshness | COVERED | Tasks 1 and 2. |
| RESEARCH | Default deny requires both directions and observability owns Prometheus configuration | COVERED | Exact policy pair in Task 1; source/render ownership in Task 2. |
| CONTEXT | Preserve operator network evidence, backup suspension, immutable boundaries, and offline-only execution | COVERED | Prohibitions and deferred human gates. |

Exclusions are not gaps: kube-router enforcement and a fresh pinned-chart Helm render remain operator evidence; backup enablement, restore drills, ingress/DNS/nginx, migration, and cutover remain outside this plan.

<output>
Create `.planning/phases/19-solidstats-memory-foundation/19-04-SUMMARY.md` when execution completes.
</output>

<!-- markdownlint-enable MD013 MD033 MD041 -->
