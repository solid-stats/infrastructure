---
phase: 20-local-corpus-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/solidstats-memory/migration-policy.json
  - scripts/validate-solidstats-memory-policy.py
  - tests/test-solidstats-memory-policy.py
  - tests/test-solidstats-memory-migration.py
autonomous: true
requirements:
  - MIG-01
  - MIG-02
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
premises:
  - claim: "The Phase 19 policy validator is the single fail-closed contract for every Phase 20 migration artifact."
    src: "scripts/validate-solidstats-memory-policy.py#validate_bundle"
    verify: "timeout 10s rg -n 'def validate_bundle|--bundle' scripts/validate-solidstats-memory-policy.py"
  - claim: "Phase 20 has no SPEC, and the deterministic fallback left both MIG-01 and MIG-02 unclassified and unresolved."
    src: ".planning/phases/20-local-corpus-migration/20-VALIDATION.md#phase-requirements-test-map"
    verify: "timeout 10s rg -n 'MIG-01|MIG-02|Wave 0' .planning/phases/20-local-corpus-migration/20-VALIDATION.md"
  - claim: "The installed MemPalace is 3.4.1, so offline contract tests cannot certify a real v3.5.0 transform or Qdrant parity run."
    src: ".planning/phases/20-local-corpus-migration/20-RESEARCH.md#environment-availability"
    verify: "timeout 10s rg -n '3\.4\.1|v3\.5\.0|required version' .planning/phases/20-local-corpus-migration/20-RESEARCH.md"
flagged_assumptions:
  - id: MIG-01
    classification: unclassified
    status: unresolved
    statement: "The SPEC-less fallback could not classify the write-freeze requirement; execution therefore requires direct operator evidence and must not infer a freeze from repository policy."
  - id: MIG-02
    classification: unclassified
    status: unresolved
    statement: "The SPEC-less fallback could not classify the local transform/parity requirement; execution therefore withholds a real parity claim until the v3.5.0 and isolated-Qdrant gates pass."
must_haves:
  truths:
    - "A one-record synthetic frozen source bundle passes one repository-local validator path from freeze evidence through inventory, transform, and parity report references."
    - "Missing freeze evidence, source provenance, mapping evidence, vector strategy evidence, or parity evidence makes the bundle invalid."
    - "Bundle validation rejects path escape, symlink escape, oversized inputs, malformed metadata, duplicate source IDs, and secret-bearing evidence before any transform or target write."
    - "The offline quick gate remains runnable without Docker, MemPalace, Chroma, Qdrant, network access, or ambient credentials."
  artifacts:
    - path: "config/solidstats-memory/migration-policy.json"
      provides: "Machine-readable Phase 20 inventory, transform, and parity artifact contract."
    - path: "scripts/validate-solidstats-memory-policy.py"
      provides: "Fail-closed validators for source, transform, parity, containment, bounds, and secret exclusion."
    - path: "tests/test-solidstats-memory-policy.py"
      provides: "Regression tests for policy and untrusted-bundle failures."
    - path: "tests/test-solidstats-memory-migration.py"
      provides: "Synthetic end-to-end and mapping/vector contract fixtures for later tools."
  key_links:
    - from: "config/solidstats-memory/migration-policy.json"
      to: "scripts/validate-solidstats-memory-policy.py"
      via: "one schema/invariant contract consumed by validate_policy and validate_bundle"
    - from: "manifest.json"
      to: "source-inventory.json, transform-manifest.json, parity-report.json"
      via: "safe relative paths plus lowercase SHA-256 entries"
    - from: "tests/test-solidstats-memory-migration.py"
      to: "scripts/validate-solidstats-memory-policy.py"
      via: "one synthetic record exercising the same public validator entry point as a real bundle"
  prohibitions:
    - statement: "The migration must not reinterpret frozen historical or archive material as current verified semantic truth."
      status: resolved
      verification: judgment
    - statement: "The migration must not invent missing source fields, normalize away source wording, or silently replace malformed metadata to make conversion pass."
      status: resolved
      verification: judgment
    - statement: "The migration must not place corpus text, personal facts, or raw metadata values in repository files, chat output, logs, or shared diagnostics."
      status: resolved
      verification: judgment
---

<!-- markdownlint-disable MD013 MD033 MD041 -->

<objective>
Prove the Phase 20 artifact contract with one production-quality offline tracer, then make every later migration step fail closed against that contract.

Purpose: Catch a wrong freeze, mapping, metadata, vector, or parity shape before the operator supplies the real corpus or a target can be mutated.

Output: An extended single policy contract, its validator, and synthetic tests that distinguish offline contract success from real isolated-Qdrant parity.
</objective>

## Artifacts this phase produces

| File | Exact additions or target state |
| --- | --- |
| `config/solidstats-memory/migration-policy.json` | Adds required inventory, transform, and parity report contracts plus explicit bounds; retains the accepted rooms, wings, disabled features, local-only mode, and SHA-256 policy. |
| `scripts/validate-solidstats-memory-policy.py` | Adds `resolve_bundle_path`, `validate_freeze_attestation`, `validate_inventory`, `validate_transform_manifest`, `validate_parity_report`, and bounded JSON/JSONL validation. The existing `--bundle` flag validates the complete contract; no networked flag is added. |
| `tests/test-solidstats-memory-policy.py` | Adds focused negative tests for source evidence, report references, containment, symlinks, bounds, and secret-shaped keys/URLs. |
| `tests/test-solidstats-memory-migration.py` | New synthetic fixture module with `MigrationContractTests`, `MappingContractTests`, and reusable non-secret one-record source/bundle builders. |

No real source record, vector, secret, target endpoint, or successful parity claim is produced by this plan.

<execution_context>
@/home/afgan0r/.codex/gsd-core/workflows/execute-plan.md
@/home/afgan0r/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-local-corpus-migration/20-CONTEXT.md
@.planning/phases/20-local-corpus-migration/20-RESEARCH.md
@.planning/phases/20-local-corpus-migration/20-VALIDATION.md
@.planning/phases/20-local-corpus-migration/20-PATTERNS.md
@.planning/phases/19-solidstats-memory-foundation/19-VERIFICATION.md
@config/solidstats-memory/migration-policy.json
@scripts/validate-solidstats-memory-policy.py
@tests/test-solidstats-memory-policy.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Validate one synthetic record from freeze attestation to parity report</name>
  <files>config/solidstats-memory/migration-policy.json, scripts/validate-solidstats-memory-policy.py, tests/test-solidstats-memory-policy.py, tests/test-solidstats-memory-migration.py</files>
  <read_first>
    - `.planning/phases/20-local-corpus-migration/20-CONTEXT.md` — D-01 through D-09 and the Phase 21/22 boundaries.
    - `.planning/phases/20-local-corpus-migration/20-RESEARCH.md` — artifact flow, verified v3.5.0 mapping names, and validation gaps.
    - `.planning/phases/20-local-corpus-migration/20-VALIDATION.md` — offline quick gate and Wave 0 fixtures.
    - `config/solidstats-memory/migration-policy.json` — single accepted policy to extend.
    - `scripts/validate-solidstats-memory-policy.py` — `validate_policy`, `validate_bundle`, `sha256`, and CLI error behavior.
    - `tests/test-solidstats-memory-policy.py` — importlib loading, TemporaryDirectory fixtures, and exact-error assertions.
  </read_first>
  <behavior>
    - Test 1: a synthetic bundle containing one source record, one deterministic ID mapping, recorded vector evidence, and passing field/vector/recall summaries validates through `validate_bundle`.
    - Test 2: removing the freeze timestamp, source snapshot checksum, inventory reference, transform reference, or parity reference fails with one precise error per missing contract.
    - Test 3: a report that says parity passed without an isolated-target run identifier and target collection evidence fails.
  </behavior>
  <action>
    Start RED with a new `MigrationContractTests` happy path that creates one non-secret synthetic source record and the four linked artifact files in a temporary bundle. The manifest must carry a UTC write-freeze timestamp, immutable snapshot checksum, source inventory checksum, mapping-oracle revision/checksum fields, vector-strategy evidence, and a parity report reference. Make the fixture validate only through the public `validate_bundle` function, not private test-only helpers. [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-01] [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-09]

    Extend the existing policy rather than creating a parallel schema. Keep schema version 1 and add declarative required fields for `source-inventory.json`, `transform-manifest.json`, and `parity-report.json`; each artifact is included in the manifest file list and checksum-verified before semantic validation. Add pure validators returning `list[str]`, preserving exact type checks and deterministic error order. The tracer represents only an offline contract pass; its parity fixture must state `synthetic: true` so no caller can use it as real MIG-02 evidence. [src: config/solidstats-memory/migration-policy.json#schema_version] [src: scripts/validate-solidstats-memory-policy.py#validate_bundle]

    Keep ordinary execution offline and standard-library only. The validator must never import MemPalace, Chroma, Qdrant, Docker, read environment credentials, or contact any endpoint. Run the quick policy suite and the new migration contract class until GREEN. [src: .planning/phases/20-local-corpus-migration/20-VALIDATION.md#test-infrastructure]
  </action>
  <verify>
    <automated>timeout 10s python3 -m unittest tests/test-solidstats-memory-policy.py tests/test-solidstats-memory-migration.py</automated>
  </verify>
  <acceptance_criteria>
    - One synthetic record reaches a passing parity-report reference through the same `validate_bundle` entry point a real bundle will use.
    - D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, and D-09 evidence fields are represented without asserting that real evidence exists.
    - Every linked artifact is a safe relative path, checksum-bound, and semantically validated after its digest passes.
    - Synthetic parity is explicitly non-production evidence and cannot satisfy the real-parity field.
  </acceptance_criteria>
  <done>The one-record offline tracer proves the artifact architecture end to end without claiming a real source or target run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject hostile paths, lossy records, unbounded work, and secret-shaped evidence</name>
  <files>scripts/validate-solidstats-memory-policy.py, tests/test-solidstats-memory-policy.py, tests/test-solidstats-memory-migration.py</files>
  <read_first>
    - `.planning/phases/20-local-corpus-migration/20-RESEARCH.md` — untrusted snapshot, metadata-loss, partial extraction, and secret-exclusion pitfalls.
    - `scripts/validate-solidstats-memory-policy.py` — Task 1 artifact loaders and the existing safe-relative-path check.
    - `tests/test-solidstats-memory-policy.py` — exact malformed-fixture error style.
    - `/home/afgan0r/.codex/gsd-core/references/untrusted-input-boundary.md` — source content is data, never instructions.
  </read_first>
  <behavior>
    - Test 1: absolute paths, parent components, symlinks, and resolved paths outside the bundle root fail before file reads.
    - Test 2: non-dictionary metadata, non-lossless JSON, duplicate IDs, missing document bytes, and missing source timestamp presence fail.
    - Test 3: a file, record count, document, metadata, or vector dimension over the committed policy bound fails without reading beyond that bound.
    - Test 4: credential-shaped keys, bearer values, connection strings, or credential-bearing URLs in committed reports fail with redacted diagnostics.
  </behavior>
  <action>
    Add failing fixtures for direct symlinks, nested symlink escape, broken symlinks, absolute and parent paths, duplicate source IDs, NaN/non-round-trippable metadata, absent documents, invalid source timestamps, excessive counts/sizes/dimensions, and secret-shaped report fields. Use only synthetic values. Assert diagnostics contain artifact paths or field names but never the fixture's document, metadata value, token, username, password, or URL userinfo. [src: .planning/phases/20-local-corpus-migration/20-RESEARCH.md#pitfall-5-untrusted-snapshot-paths-or-hostile-metadata] [src: AGENTS.md#security-minimums]

    Resolve every candidate with `Path.resolve(strict=True)`, require it to remain under the resolved bundle root, reject every symlink component, and open only regular files after containment succeeds. Add explicit policy bounds for manifest bytes, artifact bytes, record count, document bytes, metadata bytes, vector dimension, and recall-fixture count. Read JSON/JSONL incrementally or with a size check before parsing; stop on the first over-bound resource without evaluating record text. [src: scripts/validate-solidstats-memory-policy.py#validate_bundle] [src: .planning/phases/20-local-corpus-migration/20-RESEARCH.md#security-domain]

    Add a recursive evidence-key/value scan limited to non-corpus reports. Reject common credential keys, authorization headers, connection strings, and URLs carrying userinfo; report only the JSON path and reason. Do not scan or print document/vector content as a substitute for structural validation. [src: AGENTS.md#security-minimums]
  </action>
  <verify>
    <automated>timeout 10s python3 -m unittest tests/test-solidstats-memory-policy.py tests/test-solidstats-memory-migration.py</automated>
  </verify>
  <acceptance_criteria>
    - No manifest-referenced file can be opened through a path or symlink that resolves outside the bundle root.
    - Validation is bounded before parsing or iterating attacker-controlled sizes.
    - Lossy or incomplete records are rejected rather than coerced, defaulted, or omitted.
    - Errors identify only safe field/path context and contain no source values or credential material.
  </acceptance_criteria>
  <done>The offline barrier treats the entire frozen bundle as untrusted and fails before unsafe I/O, lossy conversion, unbounded processing, or secret disclosure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Freeze the v3.5.0 identity, collection, vector, and recall report contracts</name>
  <files>config/solidstats-memory/migration-policy.json, scripts/validate-solidstats-memory-policy.py, tests/test-solidstats-memory-migration.py</files>
  <read_first>
    - `.planning/phases/20-local-corpus-migration/20-CONTEXT.md` — D-03 through D-09 locked mapping and parity decisions.
    - `.planning/phases/20-local-corpus-migration/20-RESEARCH.md` — verified payload keys, UUIDv5 rule, derived collection rule, and multi-oracle gate.
    - `config/solidstats-memory/migration-policy.json` — Task 1 contract keys and policy bounds.
    - `scripts/validate-solidstats-memory-policy.py` — Task 1/2 report validators.
    - `tests/test-solidstats-memory-migration.py` — synthetic builders and contract tests to extend.
  </read_first>
  <behavior>
    - Test 1: every source ID maps once to the same payload `mempalace_id` and the v3.5.0 UUIDv5 point ID; duplicates or collisions fail.
    - Test 2: the collection report records actual palace ID, namespace, source collection, derived target collection, oracle revision, and oracle checksum; a caller-supplied collection literal is not evidence.
    - Test 3: reuse requires complete model identity/configuration, dimension, source metric, target Cosine, and serialization evidence; any missing/incompatible field selects recorded local re-embedding instead of reuse.
    - Test 4: parity reports require per-field/vector comparison plus frozen recall fixtures with ordered IDs, filters, source distances, target distances, and an evidence-derived comparator.
  </behavior>
  <action>
    Add `MappingContractTests` for the exact payload field names and deterministic UUIDv5 relationship from D-04, but keep the pinned upstream oracle revision/checksum as the authority rather than treating copied fixture constants as sufficient. Require a mapping report to prove a one-to-one count, unique source IDs, unique point IDs, and equality between each source ID and payload `mempalace_id`. [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-04] [src: .planning/phases/20-local-corpus-migration/20-RESEARCH.md#pattern-2-source-derived-qdrant-mapping-with-preflight-assertions]

    Extend transform validation for source timestamps inside metadata, a separate ingestion-time `updated_at` exclusion, and a collection-derivation record containing palace ID, namespace, source collection, oracle source revision/checksum, and derived result. Reject a manifest that presents an operator-entered target name without an oracle result. [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-05] [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-06]

    Encode D-07/D-08 as a mutually exclusive strategy contract: `reuse` passes only with all compatibility evidence true; otherwise only `reembed` passes and it must identify a pinned local model artifact, revision/checksum, dimension, and corpus checksum. Require D-09 report sections for field equality, vector equality, recall order, distance comparison, and source/target run identity. The validator checks evidence shape and fail/pass consistency; it does not perform the real comparison. [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-07] [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-09]
  </action>
  <verify>
    <automated>timeout 10s python3 -m unittest tests/test-solidstats-memory-migration.py &amp;&amp; timeout 10s python3 scripts/validate-solidstats-memory-policy.py</automated>
  </verify>
  <acceptance_criteria>
    - The contract represents every locked D-03 through D-09 field and relationship.
    - Reuse cannot be selected from dimension equality alone; incomplete evidence requires a recorded re-embedding branch.
    - Count/checksum-only reports fail because field, vector, and ranked recall evidence are mandatory.
    - The offline validator states what evidence is required without asserting that v3.5.0, Docker, a frozen snapshot, or real parity is currently available.
  </acceptance_criteria>
  <done>Later tools have one fail-closed identity, collection, embedding, and parity report contract to implement against.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Frozen bundle to validator | Untrusted paths, JSON, metadata, document bytes, and vector data enter repository-owned code. |
| Evidence reports to Phase 21 | Non-secret summaries become cutover inputs and could falsely claim parity. |
| Synthetic fixtures to verification | Offline tests must prove shape without being mistaken for real migration evidence. |

## STRIDE Threat Register — ASVS Level 2

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-20-01-01 | Tampering | Bundle path resolution | high | mitigate | Resolve under the bundle root, reject every symlink component, verify lowercase SHA-256 before semantic parsing, and cover escape fixtures. |
| T-20-01-02 | Tampering | Record and report semantics | high | mitigate | Validate lossless metadata, ID bijection, timestamps, vector-strategy prerequisites, and report consistency; reject defaults and omissions. |
| T-20-01-03 | Information Disclosure | Diagnostics and reports | high | mitigate | Scan non-corpus evidence for credential shapes, redact values, and prohibit source text/metadata values in errors or committed reports. |
| T-20-01-04 | Elevation of Privilege | Untrusted text handling | high | mitigate | Parse structured data only, never evaluate documents/metadata or interpolate them into commands, and test hostile strings as inert values. |
| T-20-01-05 | Denial of Service | JSON/JSONL and vector processing | high | mitigate | Enforce byte/count/dimension/fixture bounds before allocation or iteration and add over-bound tests. |
| T-20-01-06 | Spoofing | Synthetic parity status | high | mitigate | Require `synthetic: true` on fixtures and reject synthetic reports wherever real-parity evidence is required. |
| T-20-01-07 | Repudiation | Evidence chain | medium | mitigate | Bind every report to source snapshot, oracle, corpus, and linked-file checksums with deterministic error ordering. |

All high-severity rows are blocking: a mitigation test failure prevents this plan and every dependent plan from completing.

</threat_model>

<verification>

1. `timeout 10s python3 -m unittest tests/test-solidstats-memory-policy.py`
2. `timeout 30s python3 -m unittest tests/test-solidstats-memory-policy.py tests/test-solidstats-memory-migration.py`
3. `timeout 10s python3 scripts/validate-solidstats-memory-policy.py`
4. `timeout 10s python3 -m py_compile scripts/validate-solidstats-memory-policy.py tests/test-solidstats-memory-policy.py tests/test-solidstats-memory-migration.py`
5. `timeout 10s git diff --check -- config/solidstats-memory/migration-policy.json scripts/validate-solidstats-memory-policy.py tests/test-solidstats-memory-policy.py tests/test-solidstats-memory-migration.py`

The first command remains the offline quick gate. No Docker, v3.5.0 runtime, snapshot, network, or secret is required.

</verification>

<success_criteria>

- MIG-01 has a machine-readable freeze/snapshot evidence contract without a fabricated operator attestation.
- MIG-02 has machine-readable mapping/vector/parity contracts without a fabricated real run.
- The one-record tracer passes offline; every untrusted-input, loss, bound, secret, and false-evidence negative fixture fails.
- All nine locked decisions are cited and represented, while Phase 21 deployment/cutover and Phase 22 distillation remain absent.

</success_criteria>

## Source Coverage Audit

| Source | ID | Feature or requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Freeze, source-review, local transform, isolated parity | 20-01 through 20-06 | COVERED | This plan creates the shared fail-closed contract; later plans produce the real evidence. |
| REQ | MIG-01 | Freeze legacy writes before export | 20-01, 20-03, 20-04 | COVERED | Contract, operator proof, and immutable inventory are all planned. |
| REQ | MIG-02 | Local reviewed transform and verification | 20-01, 20-03, 20-05, 20-06 | COVERED | Contract, prerequisite gates, local import, and parity are all planned. |
| RESEARCH | — | Contract tests and report schemas first | 20-01 | COVERED | Tasks 1-3 close all Wave 0 gaps. |
| RESEARCH | — | Operator-gated freeze and immutable inventory | 20-02 through 20-04 | COVERED | Tooling, operator gates, and real inventory are separate ordered plans. |
| RESEARCH | — | Local v3.5.0 transform/import | 20-05 | COVERED | No installed 3.4.1 or VPS substitute is accepted. |
| RESEARCH | — | Field/vector/recall parity handoff | 20-06 | COVERED | Real isolated target evidence is required. |
| CONTEXT | D-01 | Post-freeze immutable source | 20-01, 20-03, 20-04 | COVERED | Contract and operator evidence. |
| CONTEXT | D-02 | Complete Chroma palace and sidecars | 20-01, 20-02, 20-03, 20-04 | COVERED | Inventory proves provenance and completeness. |
| CONTEXT | D-03 | Preserve source fields without defaults | 20-01, 20-02, 20-05, 20-06 | COVERED | Lossless contract, extraction, transform, and parity. |
| CONTEXT | D-04 | Original ID, payload ID, UUIDv5 bijection | 20-01, 20-05, 20-06 | COVERED | Tested against the pinned oracle and real target. |
| CONTEXT | D-05 | Preserve semantic metadata and source timestamps | 20-01, 20-02, 20-05, 20-06 | COVERED | Ingestion time is excluded from source equality. |
| CONTEXT | D-06 | Derive collection through pinned source | 20-01, 20-05, 20-06 | COVERED | No manual target literal is accepted. |
| CONTEXT | D-07 | Reuse only with full compatibility evidence | 20-01, 20-04, 20-05, 20-06 | COVERED | Inventory selects the evidence branch; parity proves it. |
| CONTEXT | D-08 | Re-embed locally when evidence fails | 20-01, 20-03, 20-05, 20-06 | COVERED | Pinned model artifact is gated and recorded. |
| CONTEXT | D-09 | Deterministic field/vector/recall/ranking parity | 20-01, 20-02, 20-05, 20-06 | COVERED | Synthetic contract precedes real isolated comparison. |

Exclusions are not gaps: live restore, Kubernetes deployment, MCP registration/client cutover, legacy retirement, and recovery are Phase 21; archive distillation is Phase 22.

<output>
Create `.planning/phases/20-local-corpus-migration/20-01-SUMMARY.md` when execution completes.
</output>

<!-- markdownlint-enable MD013 MD033 MD041 -->
