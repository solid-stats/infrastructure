---
phase: 20-local-corpus-migration
plan: 05
type: execute
wave: 5
depends_on:
  - 20-04
files_modified:
  - scripts/build-solidstats-memory-bundle.py
  - tests/test-solidstats-memory-migration.py
  - .planning/phases/20-local-corpus-migration/20-TRANSFORM-MANIFEST.json
autonomous: true
requirements:
  - MIG-01
  - MIG-02
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
premises:
  - "MIG-01 is unclassified and unresolved without a phase SPEC; this plan consumes the frozen source inventory but does not restore it to the live runtime."
  - "MIG-02 is unclassified and unresolved without a phase SPEC; an isolated import is necessary but insufficient until field, vector, and recall parity pass."
must_haves:
  truths:
    - "Vector reuse occurs only when all D-07 predicates match; every other record is locally reembedded with one pinned artifact and an explicit reason."
    - "Every source ID maps bijectively to payload `mempalace_id` and the exact v3.5.0 deterministic UUIDv5 point ID."
    - "The target collection name and schema are derived by the approved v3.5.0 oracle and created empty in a disposable loopback-only Qdrant pinned by digest."
    - "A private import bundle and sanitized transform manifest are deterministic and contain no invented source fields."
  artifacts:
    - path: scripts/build-solidstats-memory-bundle.py
      provides: "Deterministic D-03 through D-08 transform and isolated Qdrant importer"
      contains: "def select_vector_strategy"
    - path: .planning/phases/20-local-corpus-migration/20-TRANSFORM-MANIFEST.json
      provides: "Sanitized transform/import provenance and aggregate evidence"
      contains: "transform_schema"
    - path: tests/test-solidstats-memory-migration.py
      provides: "Transform, vector-strategy, ID, and isolated-import contract tests"
      contains: "class TransformContractTests"
  key_links:
    - from: .planning/phases/20-local-corpus-migration/20-SOURCE-INVENTORY.json
      to: scripts/build-solidstats-memory-bundle.py
      via: "Digest-locks private inputs and strategy evidence before transformation"
      pattern: "source_inventory_sha256"
    - from: scripts/build-solidstats-memory-bundle.py
      to: isolated local Qdrant REST API
      via: "Loopback-only empty-target create and bounded batch upsert"
      pattern: "qdrant-url"
  prohibitions:
    - statement: "Frozen historical or archive material must not be reinterpreted as current semantic truth."
      status: resolved
      verification: judgment
    - statement: "Source wording, identifiers, metadata, or timestamps must not be invented or normalized to make migration pass."
      status: resolved
      verification: judgment
    - statement: "Corpus text, personal facts, raw metadata, and vectors must not enter git, chat, or shared diagnostic logs."
      status: resolved
      verification: judgment
---

<!-- markdownlint-disable MD013 MD033 MD041 -->

<objective>
Transform the private source inventory under the exact v3.5.0 mapping and vector rules, then import the resulting points into a newly empty, disposable, loopback-only local Qdrant collection.

Purpose: Produce the deterministic local bundle and isolated target that parity verification requires, while keeping Phase 21 live restore and cutover out of scope.
Output: Transform/import CLI, tests, private bundle, isolated Qdrant data, and sanitized `20-TRANSFORM-MANIFEST.json`.
</objective>

## Artifacts this phase produces

This plan adds:

- `scripts/build-solidstats-memory-bundle.py`
  - symbols: `VectorStrategy`, `select_vector_strategy`, `derive_collection_with_oracle`, `build_target_points`, `verify_empty_target`, `import_batches`, `write_transform_manifest`, `main`
  - CLI flags: `--inventory`, `--source-records`, `--source-vectors`, `--recall-fixtures`, `--output-dir`, `--oracle-python`, `--oracle-source-dir`, `--reembed-model-artifact`, `--qdrant-url`, `--batch-size`, `--check-only`
- `tests/test-solidstats-memory-migration.py`
  - symbol: `TransformContractTests`
- `.planning/phases/20-local-corpus-migration/20-TRANSFORM-MANIFEST.json`
  - JSON fields: `transform_schema`, `source_inventory_sha256`, `oracle_version`, `oracle_revision`, `mapping_contract_sha256`, `collection_derivation_sha256`, `source_vector_contract`, `target_vector_contract`, `vector_strategy`, `vector_strategy_reason`, `model_artifact_sha256`, `point_count`, `source_id_set_sha256`, `point_id_set_sha256`, `bundle_sha256`, `qdrant_image`, `qdrant_run_id`, `empty_target_proof`, `import_result`, `created_at`

Operator-private outputs under `$SOLIDSTATS_MEMORY_WORK_DIR/transform/` are `points.jsonl`, `id-map.jsonl`, `bundle-manifest.json`, and any locally reembedded vector shard. Local Qdrant data remains under a new private disposable directory.

<execution_context>
@/home/afgan0r/.codex/gsd-core/workflows/execute-plan.md
@/home/afgan0r/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-local-corpus-migration/20-CONTEXT.md
@.planning/phases/20-local-corpus-migration/20-RESEARCH.md
@.planning/phases/20-local-corpus-migration/20-VALIDATION.md
@.planning/phases/20-local-corpus-migration/20-04-SUMMARY.md
@.planning/phases/20-local-corpus-migration/20-SOURCE-INVENTORY.json
@k8s/memory/10-qdrant.yaml
@scripts/inventory-solidstats-memory.py
@tests/test-solidstats-memory-migration.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the deterministic v3.5.0 point bundle and strategy contract</name>
  <files>scripts/build-solidstats-memory-bundle.py, tests/test-solidstats-memory-migration.py</files>
  <read_first>
    <file>.planning/phases/20-local-corpus-migration/20-CONTEXT.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-RESEARCH.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-SOURCE-INVENTORY.json</file>
    <file>scripts/inventory-solidstats-memory.py</file>
    <file>tests/test-solidstats-memory-migration.py</file>
  </read_first>
  <behavior>
    - "Reuse is selected only when embedder identity, embedder configuration, dimension, source metric, target Cosine metric, and serialization all match; a single mismatch selects local reembedding."
    - "Reembedding requires one approved local pinned model artifact and records its checksum and incompatibility reason; it never downloads a model or mixes strategies within a run."
    - "Every point preserves exact document bytes, lossless source metadata/timestamps, payload `mempalace_id`, and v3.5.0 UUIDv5 identity; target ingestion `updated_at` is separate."
  </behavior>
  <reversibility rating="costly">The bundle format is versioned and regenerable, but changing it after Phase 21 consumption begins requires regenerating and re-verifying the entire corpus.</reversibility>
  <action>Write failing tests for every D-07 equality predicate, the D-08 fallback, UUIDv5 bijection, D-03/D-05 field preservation, collection derivation, deterministic batch order, and hostile inputs. Implement `select_vector_strategy` as a closed decision over the source evidence and v3.5.0 target contract; accept no manual strategy override. If reuse is allowed, copy exact vector bytes and record serialization evidence. Otherwise require an approved local model artifact path/checksum, use the v3.5.0 embedder configuration locally under a hard timeout, and record the precise mismatch reason without corpus values. Ask the approved oracle for collection name, namespace, UUIDv5 mapping, payload schema, and Cosine vector schema. Canonically emit points and ID map ordered by source ID digest, verify a bijection before output, and fail on missing/coerced/defaulted source fields. Treat private inputs as untrusted with contained regular-file reads, byte/depth/count bounds, fixed argv execution, value-free logs, and atomic private output. [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-03] [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-08]</action>
  <verify>
    <automated>timeout 20s python3 -m unittest tests/test-solidstats-memory-policy.py tests/test-solidstats-memory-migration.py &amp;&amp; timeout 10s python3 scripts/build-solidstats-memory-bundle.py --help</automated>
  </verify>
  <acceptance_criteria>
    - "Tests cover all six D-07 reuse predicates independently and prove any mismatch selects D-08 local reembedding."
    - "The source-ID, payload-ID, and v3.5.0 point-ID sets are bijective with no duplicate or missing mapping."
    - "Documents, metadata, source timestamps, wing, room, and archive classification survive unchanged; ingestion `updated_at` is separate."
    - "Two synthetic builds produce byte-identical points, ID maps, and bundle manifests."
  </acceptance_criteria>
  <done>The transform CLI deterministically implements D-03 through D-08 and fails closed on incompatible or lossy inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Import the private bundle into a new isolated local Qdrant target</name>
  <files>.planning/phases/20-local-corpus-migration/20-TRANSFORM-MANIFEST.json, $SOLIDSTATS_MEMORY_WORK_DIR/transform/points.jsonl, $SOLIDSTATS_MEMORY_WORK_DIR/transform/id-map.jsonl, $SOLIDSTATS_MEMORY_WORK_DIR/transform/bundle-manifest.json, $SOLIDSTATS_MEMORY_WORK_DIR/qdrant-data/</files>
  <read_first>
    <file>.planning/phases/20-local-corpus-migration/20-VALIDATION.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-SOURCE-INVENTORY.json</file>
    <file>k8s/memory/10-qdrant.yaml</file>
    <file>scripts/build-solidstats-memory-bundle.py</file>
    <file>tests/test-solidstats-memory-migration.py</file>
  </read_first>
  <precondition>Plan 20-03 approved exact v3.5.0 and local Docker, Plan 20-04 private input digests still match `20-SOURCE-INVENTORY.json`, and the selected reembedding branch has an approved pinned local model artifact when required.</precondition>
  <reversibility rating="reversible">The target is disposable local state created from a retained immutable snapshot and versioned private bundle.</reversibility>
  <action>Extract the exact Qdrant image plus digest from `k8s/memory/10-qdrant.yaml`, require that image to be locally available or obtain explicit operator approval for the exact digest, and start one disposable container bound only to `127.0.0.1` with a new empty contained data directory. Record a random run ID independent of source values. Use the CLI to revalidate all source/private-input digests, create only the oracle-derived collection with the exact dimension and Cosine metric, prove it has zero points before import, and upload deterministic bounded batches through the loopback REST API. Require per-batch acknowledgement, final exact count, collection schema equality, and target point-ID set digest before producing success evidence. Write `20-TRANSFORM-MANIFEST.json` from an allowlist of aggregate checksums/provenance and reject secrets, corpus values, vectors, absolute paths, and source IDs. Do not create snapshots, aliases, live collections, Kubernetes resources, MCP registration, client configuration, or recovery actions; those are Phase 21. [src: k8s/memory/10-qdrant.yaml#Deployment] [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-06]</action>
  <verify>
    <automated>timeout 1800s "$SOLIDSTATS_MEMORY_V350_PYTHON" scripts/build-solidstats-memory-bundle.py --inventory "$SOLIDSTATS_MEMORY_WORK_DIR/source/source-inventory.json" --source-records "$SOLIDSTATS_MEMORY_WORK_DIR/source/source-records.jsonl" --source-vectors "$SOLIDSTATS_MEMORY_WORK_DIR/source/source-vectors.jsonl" --recall-fixtures "$SOLIDSTATS_MEMORY_WORK_DIR/source/recall-fixtures.json" --output-dir "$SOLIDSTATS_MEMORY_WORK_DIR/transform" --oracle-python "$SOLIDSTATS_MEMORY_V350_PYTHON" --oracle-source-dir "$SOLIDSTATS_MEMORY_V350_SOURCE" --reembed-model-artifact "$SOLIDSTATS_MEMORY_REEMBED_MODEL" --qdrant-url "$SOLIDSTATS_MEMORY_LOCAL_QDRANT_URL"</automated>
  </verify>
  <acceptance_criteria>
    - "The Qdrant server is the exact pinned image, bound only to loopback, backed by a new empty local directory, and identified by a unique run ID."
    - "The target collection name/schema comes only from the approved v3.5.0 oracle and starts at zero points."
    - "Every bounded batch is acknowledged; final target count and point-ID set digest equal the private bundle."
    - "The manifest records the single reuse/reembed strategy and evidence without raw source data, secrets, or private paths."
  </acceptance_criteria>
  <done>A deterministic private bundle is loaded into one proven-empty isolated local Qdrant target, ready for parity tests but not yet accepted.</done>
</task>

</tasks>

<threat_model>

## ASVS level 2 trust boundaries

| Boundary | Untrusted input | Required control |
| --- | --- | --- |
| Private source files to transform | Paths, records, metadata, vectors, model artifact | Containment, strict schema/digests, bounds, approved artifact checksum, no shell |
| Transform to local Qdrant REST API | Point IDs, payload, vectors, collection requests | Loopback allowlist, derived collection/schema, empty-target proof, bounded batches |
| Private transform to repository manifest | Corpus-bearing bundle and runtime evidence | Aggregate allowlist, hashes, secret/privacy rejection, atomic write |

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- | --- |
| T-20-05-01 | Tampering | Vector strategy | high | mitigate | Closed six-predicate D-07 check; D-08 pinned local reembed on any mismatch; no manual override |
| T-20-05-02 | Tampering | Identity/metadata transform | high | mitigate | v3.5.0 oracle, bijection gate, lossless canonical fields, no defaults or coercion |
| T-20-05-03 | Spoofing | Qdrant target | high | mitigate | Exact image digest, loopback URL enforcement, unique run ID, empty data directory and collection proof |
| T-20-05-04 | Information disclosure | Bundle, REST errors, manifest | high | mitigate | Private permissions, value-free errors, allowlisted sanitized manifest, no request-body logging |
| T-20-05-05 | Elevation of privilege | Paths, model, subprocesses | high | mitigate | Reject links/escapes/special files; approved checksum; fixed argv; controlled environment and timeouts |
| T-20-05-06 | Denial of service | Embedding and import | high | mitigate | Record/vector dimension limits, byte limits, batch bounds, per-call and total timeout, fail without partial acceptance |
| T-20-05-07 | Repudiation | Import evidence | medium | mitigate | Run ID, image digest, empty proof, batch acknowledgements, counts, ID-set digest, canonical bundle digest |

All high-severity mitigations are blocking acceptance conditions under ASVS level 2.
</threat_model>

<verification>
1. Preserve the offline quick gate: `timeout 20s python3 -m unittest tests/test-solidstats-memory-policy.py tests/test-solidstats-memory-migration.py`.
2. Verify the exact v3.5.0 oracle and pinned model provenance before real transformation.
3. Run the build/import under a 30-minute hard timeout against the proven-empty loopback target.
4. Require exact source/bundle/target count and ID-set evidence; do not infer field, vector, or recall parity from import success.
</verification>

<success_criteria>

- MIG-01 source evidence is transformed with no field invention or loss.
- MIG-02 has a real isolated local target populated under D-03 through D-08, but acceptance waits for Plan 20-06 parity.
- Phase 21 receives no live changes from this plan.
</success_criteria>

## Source coverage audit

| Source | Item | Status | Plan coverage |
| --- | --- | --- | --- |
| GOAL | Transform locally under exact mapping and embedding strategy | COVERED | Tasks 1-2 implement bundle and isolated import |
| REQ | MIG-01 | COVERED | Lossless source-to-point mapping and bijection |
| REQ | MIG-02 | COVERED | Isolated target required for later parity |
| RESEARCH | Reuse-or-reembed decision and local Qdrant | COVERED | Task 1 strategy, Task 2 isolated import |
| CONTEXT | D-03 through D-08 | COVERED | Explicitly implemented in transform/import gates |

Deferred Phase 21 live restore/deployment/MCP/client/cutover/recovery and Phase 22 archive distillation are excluded, not missing.

<output>
Create `.planning/phases/20-local-corpus-migration/20-05-SUMMARY.md` when execution completes.
</output>

<!-- markdownlint-enable MD013 MD033 MD041 -->
