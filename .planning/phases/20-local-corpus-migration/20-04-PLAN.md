---
phase: 20-local-corpus-migration
plan: 04
type: execute
wave: 4
depends_on:
  - 20-03
files_modified:
  - .planning/phases/20-local-corpus-migration/20-SOURCE-INVENTORY.json
autonomous: true
requirements:
  - MIG-01
  - MIG-02
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
premises:
  - "MIG-01 is unclassified and unresolved without a phase SPEC; success here means a complete deterministic source inventory under D-01 through D-06, not live cutover."
  - "MIG-02 is unclassified and unresolved without a phase SPEC; this plan captures source recall fixtures but cannot claim target parity."
must_haves:
  truths:
    - "The real frozen corpus is inventoried once under the approved v3.5.0 contract without mutating the source."
    - "Every source record belongs to exactly one accepted wing/room/archive classification and has complete identity, document, metadata, timestamp, and vector evidence."
    - "A sanitized repository evidence file proves counts, digests, provenance, coverage, and vector-strategy inputs without containing corpus-bearing values or private paths."
  artifacts:
    - path: .planning/phases/20-local-corpus-migration/20-SOURCE-INVENTORY.json
      provides: "Non-sensitive source completeness and provenance evidence"
      contains: "inventory_schema"
  key_links:
    - from: private immutable snapshot
      to: private source-records.jsonl
      via: "Read-only v3.5.0 inventory with one emitted record per source ID"
      pattern: "records_sha256"
    - from: private source inventory
      to: .planning/phases/20-local-corpus-migration/20-SOURCE-INVENTORY.json
      via: "Allowlisted aggregate projection and secret/privacy scan"
      pattern: "source_inventory_sha256"
  prohibitions:
    - statement: "Frozen historical or archive material must not be reinterpreted as current semantic truth."
      status: resolved
      verification: judgment
    - statement: "Source wording, identifiers, metadata, or timestamps must not be invented or normalized to make migration pass."
      status: resolved
      verification: judgment
    - statement: "Corpus text, personal facts, raw metadata, and vectors must not enter git, chat, or shared diagnostic logs."
      status: resolved
      verification: judgment
---

<!-- markdownlint-disable MD013 MD033 MD041 -->

<objective>
Run the approved inventory command over the actual immutable snapshot, retain all corpus-bearing outputs privately, and commit only a deterministic sanitized proof of completeness and strategy inputs.

Purpose: Finish MIG-01's source-review evidence and freeze the source-side inputs required for MIG-02 without exposing or transforming the corpus yet.
Output: Private inventory artifacts plus `.planning/phases/20-local-corpus-migration/20-SOURCE-INVENTORY.json`.
</objective>

## Artifacts this phase produces

This plan adds one repository artifact:

- `.planning/phases/20-local-corpus-migration/20-SOURCE-INVENTORY.json`
  - JSON fields: `inventory_schema`, `freeze_attestation_sha256`, `snapshot_manifest_sha256`, `source_inventory_sha256`, `records_sha256`, `vectors_sha256`, `recall_fixtures_sha256`, `oracle_version`, `oracle_revision`, `source_collection_identity`, `derived_target_collection_sha256`, `counts`, `classification_coverage`, `source_vector_contract`, `limits`, `created_at`

The already implemented inventory CLI creates these operator-private files under `$SOLIDSTATS_MEMORY_WORK_DIR/source/`: `source-records.jsonl`, `source-vectors.jsonl`, `recall-fixtures.json`, and `source-inventory.json`. This plan adds no symbols or CLI flags.

<execution_context>
@/home/afgan0r/.codex/gsd-core/workflows/execute-plan.md
@/home/afgan0r/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-local-corpus-migration/20-CONTEXT.md
@.planning/phases/20-local-corpus-migration/20-RESEARCH.md
@.planning/phases/20-local-corpus-migration/20-VALIDATION.md
@.planning/phases/20-local-corpus-migration/20-03-SUMMARY.md
@config/solidstats-memory/migration-policy.json
@scripts/inventory-solidstats-memory.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory the immutable real snapshot into private evidence</name>
  <files>$SOLIDSTATS_MEMORY_WORK_DIR/source/source-records.jsonl, $SOLIDSTATS_MEMORY_WORK_DIR/source/source-vectors.jsonl, $SOLIDSTATS_MEMORY_WORK_DIR/source/recall-fixtures.json, $SOLIDSTATS_MEMORY_WORK_DIR/source/source-inventory.json</files>
  <read_first>
    <file>.planning/phases/20-local-corpus-migration/20-CONTEXT.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-RESEARCH.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-03-SUMMARY.md</file>
    <file>config/solidstats-memory/migration-policy.json</file>
    <file>scripts/inventory-solidstats-memory.py</file>
  </read_first>
  <precondition>Plan 20-03 records both `approved-freeze-snapshot` and `approved-v350-docker`, and the private paths still resolve to the approved immutable snapshot and exact v3.5.0 oracle.</precondition>
  <reversibility rating="reversible">The private inventory can be deleted and regenerated from the retained immutable snapshot; the source remains read-only.</reversibility>
  <action>Create a new private output directory with restrictive permissions, verify it is empty and outside the repository, and run the inventory CLI with the approved v3.5.0 interpreter over the immutable snapshot. Use policy limits and a hard timeout; never relax bounds based on partial output. Require complete Chroma pagination, unique source IDs, exact documents, lossless metadata, source timestamps, vector bytes, palace/namespace/collection identity, embedder configuration, source distance metric, target Cosine compatibility evidence, and serialization evidence per D-02 through D-07. Generate deterministic recall fixtures from repeated frozen-source queries per D-09. Do not transform, reembed, import, contact Qdrant, or mutate the source in this task. Capture only value-free progress and digests in executor logs. [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-03] [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-07]</action>
  <verify>
    <automated>timeout 1800s "$SOLIDSTATS_MEMORY_V350_PYTHON" scripts/inventory-solidstats-memory.py --snapshot-dir "$SOLIDSTATS_MEMORY_SNAPSHOT_DIR" --freeze-attestation "$SOLIDSTATS_MEMORY_FREEZE_ATTESTATION" --oracle-source-dir "$SOLIDSTATS_MEMORY_V350_SOURCE" --output-dir "$SOLIDSTATS_MEMORY_WORK_DIR/source"</automated>
  </verify>
  <acceptance_criteria>
    - "The command exits zero only after paging the complete immutable source exactly once and checking its start/end snapshot digest."
    - "Source record count, vector count, unique-ID count, and classified count are equal."
    - "Every required D-02 through D-07 field is present; no default or inferred value fills an absent source field."
    - "All four private outputs exist beneath the approved empty output root with restrictive permissions and stable digests."
  </acceptance_criteria>
  <done>The actual frozen corpus has complete, deterministic, private source evidence and recall fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Publish a sanitized deterministic inventory proof</name>
  <files>.planning/phases/20-local-corpus-migration/20-SOURCE-INVENTORY.json</files>
  <read_first>
    <file>.planning/phases/20-local-corpus-migration/20-VALIDATION.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-PATTERNS.md</file>
    <file>scripts/inventory-solidstats-memory.py</file>
    <file>$SOLIDSTATS_MEMORY_WORK_DIR/source/source-inventory.json</file>
  </read_first>
  <precondition>Task 1 completed with stable private-output digests and equal record/vector/unique/classified counts.</precondition>
  <reversibility rating="reversible">The sanitized proof is derived from private evidence and can be regenerated without changing the source.</reversibility>
  <action>Project only the allowlisted aggregate fields named in this plan's artifact table into `20-SOURCE-INVENTORY.json`. Represent collection identity and any namespace-derived value by versioned hashes where disclosure could reveal local naming; retain enough provenance to reproduce D-04/D-06 derivation with the private inventory. Record separate counts for current and archive classifications without copying labels, content, source IDs, documents, metadata values, vectors, query text, absolute paths, environment values, or credentials. Run a recursive secret/privacy rejection pass over keys and string values before atomically replacing the evidence file. Recompute private source digests after writing and require them to match Task 1 so the proof cannot bless a moving snapshot. [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-04] [src: .planning/phases/20-local-corpus-migration/20-PATTERNS.md#Pattern-5]</action>
  <verify>
    <automated>timeout 30s "$SOLIDSTATS_MEMORY_V350_PYTHON" scripts/inventory-solidstats-memory.py --snapshot-dir "$SOLIDSTATS_MEMORY_SNAPSHOT_DIR" --freeze-attestation "$SOLIDSTATS_MEMORY_FREEZE_ATTESTATION" --oracle-source-dir "$SOLIDSTATS_MEMORY_V350_SOURCE" --output-dir "$SOLIDSTATS_MEMORY_WORK_DIR/source" --check-only &amp;&amp; timeout 15s python3 -m unittest tests/test-solidstats-memory-policy.py tests/test-solidstats-memory-migration.py</automated>
  </verify>
  <acceptance_criteria>
    - "The committed JSON contains only the allowlisted aggregate/provenance schema and stable SHA-256 values."
    - "The JSON proves equal source/unique/vector/classified counts and complete recall strata without corpus-bearing values."
    - "A recursive test rejects synthetic secret names, corpus fragments, metadata values, vectors, queries, and absolute paths."
    - "The post-write check observes the exact same snapshot and private-output digests as Task 1."
  </acceptance_criteria>
  <done>The repository contains a safe reproducible proof of source completeness while all sensitive corpus material stays private.</done>
</task>

</tasks>

<threat_model>

## ASVS level 2 trust boundaries

| Boundary | Untrusted input | Required control |
| --- | --- | --- |
| Immutable snapshot to v3.5.0 inventory | Filesystem structure and corpus records | Pre/post digest, containment, no symlinks/special files, lossless strict schema, resource bounds |
| Private inventory to repository evidence | Potentially sensitive fields and attacker-controlled strings | Positive allowlist, hashed identities, recursive secret/privacy rejection, atomic write |
| Corpus values to process/log boundary | Documents, metadata, IDs, vectors, queries | No value logging, no shell interpolation, restrictive output permissions |

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- | --- |
| T-20-04-01 | Tampering | Snapshot during read | high | mitigate | Verify immutable manifest and full digest before and after bounded inventory |
| T-20-04-02 | Tampering | Field preservation | high | mitigate | Strict one-record/one-vector schema, no defaults/coercion, equal-count and digest gates |
| T-20-04-03 | Information disclosure | Repository evidence | high | mitigate | Allowlist only aggregate fields and hashes; recursive secret/privacy rejection blocks write |
| T-20-04-04 | Elevation of privilege | Paths and commands | high | mitigate | Contained non-symlink roots, fixed executable and argv, no shell or source-derived command text |
| T-20-04-05 | Denial of service | Real corpus inventory | high | mitigate | Policy bounds plus hard timeout; stop rather than partially accept |
| T-20-04-06 | Repudiation | Provenance proof | medium | mitigate | Oracle revision, freeze/snapshot/output digests, counts, and UTC timestamp in canonical evidence |

All high-severity mitigations are blocking acceptance conditions under ASVS level 2.
</threat_model>

<verification>
1. Preserve the offline quick gate: `timeout 15s python3 -m unittest tests/test-solidstats-memory-policy.py tests/test-solidstats-memory-migration.py`.
2. Run the real inventory once with a 30-minute hard timeout and require stable pre/post snapshot digests.
3. Validate the sanitized JSON against its exact allowlist and privacy/secret rejection tests.
4. Do not claim target or recall parity from this plan.
</verification>

<success_criteria>

- MIG-01 has complete, lossless, reproducible source evidence under D-01 through D-07.
- MIG-02 has frozen source recall fixtures and strategy inputs but remains unclaimed until isolated-target parity.
- Repository evidence is useful for audit and contains no corpus-bearing or secret material.
</success_criteria>

## Source coverage audit

| Source | Item | Status | Plan coverage |
| --- | --- | --- | --- |
| GOAL | Source-review exact mapping and embedding strategy | COVERED | Tasks 1-2 inventory every deciding input and produce safe evidence |
| REQ | MIG-01 | COVERED | Complete real frozen-source inventory |
| REQ | MIG-02 | COVERED | Frozen source recall fixtures and vector strategy inputs |
| RESEARCH | Private artifacts plus sanitized attestations | COVERED | Private outputs and allowlisted repository JSON |
| CONTEXT | D-02 through D-07 and source side of D-09 | COVERED | Task 1 lossless inventory and Task 2 proof |

Deferred Phase 21 live operations and Phase 22 archive distillation are excluded, not missing.

<output>
Create `.planning/phases/20-local-corpus-migration/20-04-SUMMARY.md` when execution completes.
</output>

<!-- markdownlint-enable MD013 MD033 MD041 -->
