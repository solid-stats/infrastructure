---
phase: 20-local-corpus-migration
plan: 08
type: execute
wave: 7
depends_on:
  - 20-04
files_modified:
  - .planning/phases/20-local-corpus-migration/20-MAPPING-CONTRACT.json
autonomous: false
gap_closure: true
requirements:
  - MIG-02
estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 1
  confidence: low
premises:
  - "MIG-02 remains blocked after source inventory until a human explicitly approves every target-routing and preservation decision; this checkpoint does not establish transform or parity acceptance."
must_haves:
  truths:
    - "No source timestamp, wing, room, archive label, excluded source, or preservation representation is mapped without explicit human approval."
    - "The approved decision record is bound to the exact sanitized Plan 20-04 inventory digest and contains all five required mapping decisions."
    - "Plan 20-05 remains fail-closed while the mapping contract is absent, incomplete, rejected, or not marked approved."
  artifacts:
    - path: .planning/phases/20-local-corpus-migration/20-MAPPING-CONTRACT.json
      provides: "Human-approved, inventory-bound source-to-target mapping and preservation contract"
      contains: "status"
  key_links:
    - from: .planning/phases/20-local-corpus-migration/20-SOURCE-INVENTORY.json
      to: .planning/phases/20-local-corpus-migration/20-MAPPING-CONTRACT.json
      via: "Exact source_inventory_sha256 binding plus explicit human approval"
      pattern: "source_inventory_sha256"
    - from: .planning/phases/20-local-corpus-migration/20-MAPPING-CONTRACT.json
      to: 20-05
      via: "Blocking dependency; downstream transform may start only from status=approved and a matching inventory digest"
      pattern: "status"
  prohibitions:
    - statement: "The plan must not preselect timestamp semantics, target wings, room/archive treatment, exclusions, or preservation representation."
      status: resolved
      verification: judgment
    - statement: "The checkpoint must not mutate source data, private inventory data, transform data, code, tests, policy, runtime, or external systems."
      status: resolved
      verification: judgment
---

<!-- markdownlint-disable MD013 MD033 MD041 -->

<objective>
Obtain explicit human approval for the complete source-to-target mapping contract after the lossless source inventory is available, then record that approval as an inventory-bound repository artifact.

Purpose: Close the mapping-review gate required by MIG-02 without selecting answers in advance or starting transformation.
Output: `.planning/phases/20-local-corpus-migration/20-MAPPING-CONTRACT.json`, or no artifact and a still-blocked phase when approval is withheld or incomplete.
</objective>

## Artifact this plan produces

- `.planning/phases/20-local-corpus-migration/20-MAPPING-CONTRACT.json`
  - JSON fields: `contract_schema`, `status`, `source_inventory_sha256`, `approved_at`, `approval_digest`, `timestamp_semantics`, `source_wing_to_target_wing`, `legacy_room_label_treatment`, `legacy_archive_label_treatment`, `excluded_source_disposition`, `preservation_representation`
  - `status` must be exactly `approved`; any other value or a missing field leaves Plan 20-05 blocked.
  - The artifact contains approved rules and public routing labels only. It contains no source records, source IDs, documents, metadata values, vectors, private paths, environment values, or credentials.

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-local-corpus-migration/20-CONTEXT.md
@.planning/phases/20-local-corpus-migration/20-RESEARCH.md
@.planning/phases/20-local-corpus-migration/20-04-SUMMARY.md
@.planning/phases/20-local-corpus-migration/20-SOURCE-INVENTORY.json
@.planning/debug/real-corpus-inventory-gap.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve the exact inventory-bound mapping contract</name>
  <read_first>
    <file>.planning/phases/20-local-corpus-migration/20-SOURCE-INVENTORY.json</file>
    <file>.planning/phases/20-local-corpus-migration/20-CONTEXT.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-03-SUMMARY.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-04-SUMMARY.md</file>
    <file>.planning/debug/real-corpus-inventory-gap.md</file>
  </read_first>
  <decision>Review the sanitized Plan 20-04 counts/digests and accepted decisions, then provide explicit values for all five unresolved areas: (1) the semantics and precedence of every observed source timestamp field, including whether any normalization is allowed and how missing or invalid candidates are handled; (2) the exact source-wing to target-wing mapping; (3) treatment of every legacy room label and archive-related label without silently rewriting it; (4) the disposition of excluded agent sources and every other-wing source; and (5) the representation that preserves the exact source ID, document bytes, metadata dictionary, source timestamp evidence, vector evidence, wing, room, archive label, and original date per D-03/D-05 while still enabling approved target routing. Do not approve a partial contract.</decision>
  <context>Plan 20-04 intentionally reports source observations only and marks target mapping unresolved. Its counts cannot choose timestamp precedence, normalization, target labels, exclusions, or preservation representation. Approval here authorizes only the mapping contract consumed by Plan 20-05; it does not authorize a live migration, establish MIG-02 parity, or mutate any source/private/target data.</context>
  <options>
    <option id="approve-complete-contract">
      <name>Approve complete contract</name>
      <pros>Unblocks Plan 20-05 only after every required value is explicit and bound to the exact source inventory digest.</pros>
      <cons>The approved routing and preservation semantics become a committed execution contract; later changes require a new approval and downstream rebuild.</cons>
    </option>
    <option id="withhold-approval">
      <name>Withhold approval</name>
      <pros>Keeps transformation closed when any source semantic or disposition remains uncertain.</pros>
      <cons>Plans 20-05 and 20-06 remain blocked.</cons>
    </option>
  </options>
  <resume-signal>To approve, provide an explicit value for each of the five decision areas and state `approve-complete-contract`. The executor must record the response verbatim as normalized contract rules in `20-MAPPING-CONTRACT.json`, bind it to the SHA-256 of `20-SOURCE-INVENTORY.json`, set `status` to `approved`, run the automated contract check below, and commit the artifact before completing this plan. Any missing value, ambiguity, rejection, or `withhold-approval` response leaves the checkpoint blocked and produces no approved artifact.</resume-signal>
  <verify>
    <automated>timeout 10s python3 -c 'import hashlib,json,pathlib; s=pathlib.Path(".planning/phases/20-local-corpus-migration/20-SOURCE-INVENTORY.json"); p=pathlib.Path(".planning/phases/20-local-corpus-migration/20-MAPPING-CONTRACT.json"); d=json.loads(p.read_text(encoding="utf-8")); required=("timestamp_semantics","source_wing_to_target_wing","legacy_room_label_treatment","legacy_archive_label_treatment","excluded_source_disposition","preservation_representation"); assert d.get("status")=="approved"; assert d.get("source_inventory_sha256")==hashlib.sha256(s.read_bytes()).hexdigest(); assert all(key in d and d[key] not in (None,"",[],{}) for key in required)'</automated>
  </verify>
  <acceptance_criteria>
    - "All five decision areas have explicit human-approved values; the plan contains no preselected answer."
    - "The contract is marked approved and bound to the exact sanitized inventory SHA-256; incomplete, rejected, stale, or absent artifacts fail the automated check."
    - "The approval preserves D-03/D-05 source evidence while supplying exact downstream routing rules."
    - "No source/private/target data, code, tests, policy, runtime, or external system is mutated; only the approved repository decision record is created."
    - "MIG-02 transform and parity remain unclaimed at this checkpoint."
  </acceptance_criteria>
  <done>The complete human-approved mapping contract is committed and inventory-bound; otherwise Plan 20-08 remains incomplete and Plan 20-05 cannot start.</done>
</task>

</tasks>

<threat_model>

## ASVS level 2 trust boundaries

| Boundary | Untrusted input | Required control |
| --- | --- | --- |
| Sanitized inventory to human decision | Stale, incomplete, or substituted evidence | Exact SHA-256 binding and explicit review of all five decision areas |
| Human approval to repository contract | Ambiguous, partial, or repudiable response | Blocking checkpoint, explicit approval token, approval digest, UTC approval time, complete required-field check |
| Approved contract to Plan 20-05 | Missing, rejected, or tampered contract | Dependency on 20-08 plus fail-closed `status=approved` and inventory-digest validation |

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- | --- |
| T-20-08-01 | Tampering | Inventory-to-contract binding | high | mitigate | Hash the exact sanitized inventory and reject stale or mismatched approval records |
| T-20-08-02 | Repudiation | Mapping approval | high | mitigate | Require explicit approval of every decision area and record approval digest/time in the committed contract |
| T-20-08-03 | Tampering | Downstream transform gate | high | mitigate | Keep 20-05 dependent on completed 20-08 and reject missing, incomplete, or non-approved contracts |
| T-20-08-04 | Information disclosure | Decision record | medium | mitigate | Allow only approved rules/public labels and reject corpus values, private paths, environment values, and credentials |
| T-20-08-05 | Elevation of privilege | Decision scope | medium | mitigate | State that approval covers mapping only and cannot authorize transform parity, live deployment, cutover, or external mutation |

</threat_model>

<verification>
1. Confirm Plan 20-04 completed and its sanitized report marks target mapping unresolved.
2. Present the five unresolved decision areas without proposed or default answers.
3. Keep the checkpoint blocked unless the user supplies and approves every exact value.
4. Validate the approved contract's required fields and exact source-inventory digest before Plan 20-05 becomes reachable.
5. Do not claim MIG-02 transformation or parity from this checkpoint.
</verification>

<success_criteria>

- Every timestamp, routing, legacy-label, exclusion, and preservation decision is explicitly human-approved after source inventory.
- The committed contract is complete, status-approved, and cryptographically bound to the sanitized inventory it reviewed.
- Plan 20-05 is unreachable when the contract is absent, partial, rejected, stale, or unapproved.
- No code, test, policy, source/private/target data, runtime, or external service changes occur in this plan.
- MIG-02 parity remains unclaimed until Plans 20-05 and 20-06 execute successfully.
</success_criteria>

## Source coverage audit

| Source | Item | Status | Plan coverage |
| --- | --- | --- | --- |
| GOAL | Review source mapping before local transformation | COVERED | Blocking checkpoint follows the complete source inventory and precedes Plan 20-05 |
| REQ | MIG-02 mapping-review prerequisite | COVERED | Approval contract closes only the prerequisite; transform and parity remain unclaimed |
| RESEARCH | Source-derived mapping and fail-closed evidence | COVERED | Exact inventory digest binds the explicit decision record |
| CONTEXT | D-03 and D-05 preservation constraints | COVERED | Fifth approval area requires lossless preservation representation before target routing |

Deferred Phase 21 live operations and Phase 22 archive distillation are excluded, not missing.

<output>
Create `.planning/phases/20-local-corpus-migration/20-08-SUMMARY.md` when done.
</output>

<!-- markdownlint-enable MD013 MD033 MD041 -->
