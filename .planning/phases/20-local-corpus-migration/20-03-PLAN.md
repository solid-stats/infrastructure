---
phase: 20-local-corpus-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 20-02
files_modified: []
autonomous: false
requirements:
  - MIG-01
  - MIG-02
user_setup:
  - service: legacy-solidstats-memory
    why: "Only its operator can freeze writes and provide a complete immutable snapshot plus attestation."
    dashboard_config:
      - task: "Freeze every legacy MemPalace write path while leaving reads available, then export Chroma plus identity/config/embedder sidecars to an immutable local snapshot."
        location: "Legacy host operational procedure"
  - service: local-migration-workstation
    why: "The required MemPalace v3.5.0 oracle artifact and Docker daemon are not currently available to the executor."
    dashboard_config:
      - task: "Provide an approved local v3.5.0 source or environment and grant access to a local Docker daemon."
        location: "Operator-managed local workstation"
estimate:
  tokens: 10000
  raw_tokens: 10000
  tasks: 2
  confidence: low
premises:
  - "MIG-01 remains unclassified and unresolved without a phase SPEC; the write-freeze and immutable-snapshot proof is therefore a blocking operator assertion rather than an inferred implementation detail."
  - "MIG-02 remains unclassified and unresolved without a phase SPEC; Docker and v3.5.0 availability only unlock validation and do not prove parity."
must_haves:
  truths:
    - "An operator has proven all legacy writes stopped before the snapshot point while reads may continue."
    - "The local snapshot is immutable, complete, checksummed, and contains Chroma plus palace identity, configuration, and embedder sidecars."
    - "An approved exact MemPalace v3.5.0 oracle artifact and a usable local Docker daemon exist before real corpus work begins."
    - "No VPS, shared Qdrant, or network execution substitutes for either local prerequisite."
  artifacts: []
  key_links:
    - from: operator freeze evidence
      to: immutable snapshot manifest
      via: "Freeze timestamp precedes snapshot creation and no post-freeze writes are observed"
      pattern: "legacy_writes_frozen"
    - from: approved v3.5.0 artifact
      to: scripts/inventory-solidstats-memory.py
      via: "Exact-version oracle check"
      pattern: "oracle-source-dir"
  prohibitions:
    - statement: "Frozen historical or archive material must not be reinterpreted as current semantic truth."
      status: resolved
      verification: judgment
    - statement: "Source wording, identifiers, metadata, or timestamps must not be invented or normalized to make migration pass."
      status: resolved
      verification: judgment
    - statement: "Corpus text, personal facts, raw metadata, and vectors must not enter git, chat, or shared diagnostic logs."
      status: resolved
      verification: judgment
---

<!-- markdownlint-disable MD013 MD033 MD041 -->

<objective>
Stop at the two irreversible trust gates: prove the legacy source is frozen and immutably captured, then prove the exact local v3.5.0 oracle and local Docker isolation prerequisites exist.

Purpose: Prevent extraction from a moving or incomplete source and prevent a locally blocked environment from being silently replaced with an unsupported version or VPS execution.
Output: Operator-approved, machine-checkable prerequisites; this plan creates no repository files.
</objective>

## Artifacts this phase produces

This plan creates no files, symbols, or CLI flags. It produces four verified conditions consumed by later plans: write freeze, immutable complete snapshot, approved MemPalace v3.5.0 oracle, and usable local Docker access.

<execution_context>
@/home/afgan0r/.codex/gsd-core/workflows/execute-plan.md
@/home/afgan0r/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-local-corpus-migration/20-CONTEXT.md
@.planning/phases/20-local-corpus-migration/20-RESEARCH.md
@.planning/phases/20-local-corpus-migration/20-VALIDATION.md
@.planning/phases/20-local-corpus-migration/20-02-SUMMARY.md
@docs/solidstats-memory.md
@k8s/memory/10-qdrant.yaml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Prove the write freeze and immutable complete source snapshot</name>
  <files>None — operator evidence and private snapshot only</files>
  <read_first>
    <file>.planning/phases/20-local-corpus-migration/20-CONTEXT.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-RESEARCH.md</file>
    <file>docs/solidstats-memory.md</file>
    <file>config/solidstats-memory/migration-policy.json</file>
    <file>scripts/inventory-solidstats-memory.py</file>
  </read_first>
  <precondition>The operator can identify every legacy writer and can export the complete legacy Chroma state plus identity/config/embedder sidecars.</precondition>
  <reversibility rating="costly">A faulty snapshot forces the operator to repeat the freeze and export; D-01 and D-02 already lock the decision, so this checkpoint verifies rather than revisits it.</reversibility>
  <action>Require the operator to supply `SOLIDSTATS_MEMORY_SNAPSHOT_DIR` and `SOLIDSTATS_MEMORY_FREEZE_ATTESTATION` as local, private paths. Inspect the attestation without printing values and confirm it names every legacy writer, records a UTC freeze instant, demonstrates zero successful writes after that instant, and explicitly allows reads only. Run the inventory CLI in check-only mode to validate containment, non-symlink regular files, an immutable snapshot marker, full-file SHA-256 inventory, Chroma consistency, and presence of palace identity, configuration, embedder identity/configuration/dimension/metric/serialization sidecars. Stop on any missing writer, mutable file, post-freeze write, symlink, path escape, incomplete sidecar, or checksum drift. Implement D-01 and D-02 exactly; do not copy the corpus into the repository or logs. [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-01] [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-02]</action>
  <verify>
    <automated>timeout 30s test -n "$SOLIDSTATS_MEMORY_SNAPSHOT_DIR" &amp;&amp; timeout 30s test -n "$SOLIDSTATS_MEMORY_FREEZE_ATTESTATION" &amp;&amp; timeout 60s python3 scripts/inventory-solidstats-memory.py --snapshot-dir "$SOLIDSTATS_MEMORY_SNAPSHOT_DIR" --freeze-attestation "$SOLIDSTATS_MEMORY_FREEZE_ATTESTATION" --oracle-source-dir "$SOLIDSTATS_MEMORY_V350_SOURCE" --output-dir "$SOLIDSTATS_MEMORY_WORK_DIR/source-check" --check-only</automated>
    <human-check>Confirm the freeze attestation covers every known write path, its UTC freeze time precedes every snapshot file timestamp, reads remain available if needed, and the immutable snapshot has been retained privately with its full checksum manifest.</human-check>
  </verify>
  <acceptance_criteria>
    - "Every legacy writer is named and proven inactive before snapshot creation."
    - "The snapshot contains the complete Chroma store and all D-02 sidecars under a stable full-file checksum manifest."
    - "Two consecutive check-only runs observe identical file sizes, timestamps, and digests."
    - "No corpus-bearing evidence is committed or printed."
  </acceptance_criteria>
  <what-built>The tested inventory gate from Plan 20-02, ready to validate operator evidence without reading corpus content.</what-built>
  <how-to-verify>Provide the two private paths, review the value-free automated result, and independently confirm the writer list, freeze instant, immutability control, snapshot completeness, and checksum retention.</how-to-verify>
  <resume-signal>Type `approved-freeze-snapshot` only after every acceptance criterion is true; otherwise describe the unmet criterion.</resume-signal>
  <done>The exact frozen source boundary for MIG-01 is operator-approved and machine-checkable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Prove exact v3.5.0 oracle provenance and local Docker access</name>
  <files>None — operator-provided local artifact and daemon access only</files>
  <read_first>
    <file>.planning/phases/20-local-corpus-migration/20-RESEARCH.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-VALIDATION.md</file>
    <file>k8s/memory/10-qdrant.yaml</file>
    <file>scripts/inventory-solidstats-memory.py</file>
  </read_first>
  <precondition>An operator can provide a provenance-verifiable MemPalace v3.5.0 source/environment and access to a Docker daemon on the local migration workstation.</precondition>
  <reversibility rating="reversible">The approved local artifact or Docker access can be replaced before any transform is accepted.</reversibility>
  <action>Require local paths in `SOLIDSTATS_MEMORY_V350_PYTHON` and `SOLIDSTATS_MEMORY_V350_SOURCE`. Verify the interpreter imports MemPalace from the approved artifact, reports exactly 3.5.0, and matches the operator-recorded source revision and artifact checksum; explicitly reject the installed 3.4.1 runtime. Verify `docker version` can reach a local daemon and record only non-sensitive engine/version evidence. Parse the exact pinned Qdrant image reference and digest from `k8s/memory/10-qdrant.yaml`; later plans must run that image bound to loopback with a new local data directory. Do not install packages dynamically, pull an unapproved image, contact a VPS, or accept a shared Docker/Qdrant endpoint. [src: .planning/phases/20-local-corpus-migration/20-RESEARCH.md#Local-Blockers] [src: k8s/memory/10-qdrant.yaml#Deployment]</action>
  <verify>
    <automated>timeout 20s "$SOLIDSTATS_MEMORY_V350_PYTHON" -c "import importlib.metadata as m, mempalace; assert m.version('mempalace') == '3.5.0'; print(m.version('mempalace')); print(mempalace.__file__)" &amp;&amp; timeout 20s git -C "$SOLIDSTATS_MEMORY_V350_SOURCE" status --short &amp;&amp; timeout 20s docker version &amp;&amp; timeout 10s python3 scripts/inventory-solidstats-memory.py --help</automated>
    <human-check>Compare the printed version/import path and source revision with the approved v3.5.0 provenance record, and confirm the Docker server is the local migration workstation.</human-check>
  </verify>
  <acceptance_criteria>
    - "The oracle imports exactly MemPalace 3.5.0 from the approved artifact and has recorded revision/checksum provenance."
    - "The existing 3.4.1 installation is not used for mapping, collection derivation, transform, or parity."
    - "Docker server access succeeds locally, and the Qdrant image reference is the exact pinned digest from the manifest."
    - "No VPS, shared target, dynamic package install, or unapproved image is involved."
  </acceptance_criteria>
  <what-built>The exact-version and local-isolation prerequisites for the real migration run.</what-built>
  <how-to-verify>Review the automated provenance and Docker-server output, compare it to the approved artifact record, and confirm the daemon is local and the target image digest matches the manifest.</how-to-verify>
  <resume-signal>Type `approved-v350-docker` only after every acceptance criterion is true; otherwise describe the unmet criterion.</resume-signal>
  <done>The version and local-runtime blockers are explicitly cleared without weakening D-03 through D-09.</done>
</task>

</tasks>

<threat_model>

## ASVS level 2 trust boundaries

| Boundary | Untrusted input | Required control |
| --- | --- | --- |
| Operator evidence to executor | Freeze claims, paths, checksums, artifact provenance | Human approval plus bounded machine checks; no value logging |
| Snapshot filesystem to later tooling | Links, mutable files, incomplete state | Containment, immutability, completeness, repeated checksum proof |
| Local Docker client to daemon | Daemon identity and image source | Verify local server, exact digest, loopback-only target; reject shared/VPS endpoints |

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- | --- |
| T-20-03-01 | Spoofing | Freeze attestation | high | mitigate | Blocking operator review of every writer plus machine-verifiable UTC and zero-write evidence |
| T-20-03-02 | Tampering | Snapshot | high | mitigate | Immutable marker, complete sidecars, containment, two stable checksum passes |
| T-20-03-03 | Spoofing | v3.5.0 oracle | high | mitigate | Exact version, import path, source revision, checksum, and human provenance approval |
| T-20-03-04 | Elevation of privilege | Docker daemon | high | mitigate | Blocking verification that daemon is local and target is loopback-only with pinned digest |
| T-20-03-05 | Information disclosure | Operator evidence | high | mitigate | Private paths, value-free output, no corpus reads or repository writes in this plan |
| T-20-03-06 | Denial of service | Gate commands | medium | mitigate | Every command is timeout bounded and check-only work is size bounded by policy |

All high-severity entries block both checkpoints under ASVS level 2.
</threat_model>

<verification>
1. Complete the freeze/snapshot checkpoint with two stable check-only passes.
2. Complete the v3.5.0/Docker checkpoint with exact provenance and local-daemon evidence.
3. Confirm neither checkpoint created repository changes or exposed corpus-bearing values.
</verification>

<success_criteria>

- D-01 and D-02 have explicit operator evidence, not inference.
- The known 3.4.1 and Docker-access blockers are cleared locally and explicitly.
- Later plans have checkable prerequisites and cannot substitute VPS or shared-service execution.
</success_criteria>

## Source coverage audit

| Source | Item | Status | Plan coverage |
| --- | --- | --- | --- |
| GOAL | Freeze legacy writes before source work | COVERED | Task 1 blocking checkpoint |
| REQ | MIG-01 | COVERED | Freeze and complete immutable source snapshot |
| REQ | MIG-02 | COVERED | Local v3.5.0 and isolated-Qdrant prerequisites, without parity claim |
| RESEARCH | Installed 3.4.1 and inaccessible Docker blockers | COVERED | Task 2 blocking checkpoint |
| CONTEXT | D-01 and D-02 | COVERED | Task 1 implements both exactly |

Deferred Phase 21 runtime restore/deployment/cutover/recovery and Phase 22 archive distillation are excluded, not missing.

<output>
Create `.planning/phases/20-local-corpus-migration/20-03-SUMMARY.md` after both checkpoints pass.
</output>

<!-- markdownlint-enable MD013 MD033 MD041 -->
