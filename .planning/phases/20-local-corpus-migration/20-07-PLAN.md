---
phase: 20-local-corpus-migration
plan: 07
type: tdd
wave: 4
depends_on:
  - 20-03
files_modified:
  - scripts/inventory-solidstats-memory.py
  - tests/test-solidstats-memory-migration.py
autonomous: true
gap_closure: true
requirements:
  - MIG-01
  - MIG-02
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
premises:
  - "The completed 20-03 freeze, immutable snapshot, and exact v3.5.0 oracle gates remain valid; this plan requires no private corpus or external service."
  - "The real-corpus failure is a source-boundary defect: inventory applies the target `mapping_contract`, active-room list, and target-wing list before source evidence exists."
  - "MIG-01 is directly covered by accepting every structurally valid source record losslessly; MIG-02 is covered only by preserving source evidence required by the later transform and parity plans."
must_haves:
  truths:
    - "Inventory accepts arbitrary valid legacy metadata per D-03 and D-05 without requiring a synthetic timestamp key or target wing/room membership."
    - "Each emitted source record contains metadata exactly equal to the oracle input, including existing timestamp fields and un-suffixed or legacy source labels, with no target field or routing label invented."
    - "Private inventory evidence contains deterministic aggregate field presence/type/format counts and source-label classification counts, but no metadata values, documents, vectors, source IDs, private paths, or credentials."
    - "Malformed, lossy, secret-shaped, invalid, duplicate, escaped, linked, oversized, drifted, oracle-mismatched, or protocol-invalid input still fails closed with value-free diagnostics."
    - "The repository target `mapping_contract` and Phase 20 transform/parity semantics remain byte-identical; unresolved timestamp and label mapping choices block 20-05 after 20-04 reports the real source shapes."
  artifacts:
    - path: scripts/inventory-solidstats-memory.py
      provides: "Source-safe lossless inventory validation and aggregate shape evidence"
      contains: "def lossless_metadata"
    - path: tests/test-solidstats-memory-migration.py
      provides: "Real-shape source-boundary and fail-closed regression coverage"
      contains: "class InventoryContractTests"
  key_links:
    - from: scripts/inventory-solidstats-memory.py
      to: private source-records.jsonl
      via: "Lossless source validation that does not consult target mapping eligibility"
      pattern: "_validated_record"
    - from: scripts/inventory-solidstats-memory.py
      to: private source-inventory.json
      via: "Streaming, value-free source-shape aggregation"
      pattern: "source_shape_evidence"
    - from: .planning/debug/real-corpus-inventory-gap.md
      to: tests/test-solidstats-memory-migration.py
      via: "Synthetic regressions reproduce the observed metadata and label shapes without private values"
      pattern: "filed_at"
  prohibitions:
    - statement: "Do not select timestamp precedence or normalize any timestamp representation in this plan."
      status: resolved
      verification: judgment
    - statement: "Do not rename wing or room labels, add archive labels, or drop agent/other wings in this plan."
      status: resolved
      verification: judgment
    - statement: "Do not change `bundle_contract.mapping_contract`, target transform behavior, vector strategy, or parity semantics in this plan."
      status: resolved
      verification: automated
    - statement: "Do not access the real corpus, a private snapshot path, Qdrant, Docker, a VPS, or any network service in this plan."
      status: resolved
      verification: automated
---

<!-- markdownlint-disable MD013 MD033 MD041 -->

<objective>
Repair the inventory source boundary so the frozen legacy corpus can be described losslessly before any target mapping decision is applied.

Purpose: Close the real-corpus inventory gap without weakening source safety or silently deciding the unresolved timestamp, wing, room, archive, transform, or parity contracts.
Output: TDD-backed source validation and value-free source-shape evidence in `scripts/inventory-solidstats-memory.py`, with regressions in `tests/test-solidstats-memory-migration.py`.
</objective>

## Artifacts this phase produces

This plan modifies two repository artifacts:

- `scripts/inventory-solidstats-memory.py`
  - source-validation behavior: arbitrary lossless metadata is eligible without a required `source_timestamp`, active-room membership, or target-wing membership
  - private summary field: `source_shape_evidence`
  - aggregate evidence: per-field presence counts, canonical JSON type counts, value-format counts, and source-only wing/room label classification counts
- `tests/test-solidstats-memory-migration.py`
  - real-shape acceptance regressions
  - exact metadata preservation and no-invention assertions
  - deterministic value-free evidence tests
  - fail-closed safety regressions

No policy, validator, transform, parity, planning evidence, private snapshot, or real-corpus artifact is produced by this plan. The real inventory rerun remains Plan 20-04 work owned by the main executor.

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/real-corpus-inventory-gap.md
@.planning/phases/20-local-corpus-migration/20-CONTEXT.md
@.planning/phases/20-local-corpus-migration/20-RESEARCH.md
@.planning/phases/20-local-corpus-migration/20-PATTERNS.md
@.planning/phases/20-local-corpus-migration/20-VALIDATION.md
@.planning/phases/20-local-corpus-migration/20-03-SUMMARY.md
@.planning/phases/20-local-corpus-migration/20-04-PLAN.md
@config/solidstats-memory/migration-policy.json
@scripts/inventory-solidstats-memory.py
@scripts/validate-solidstats-memory-policy.py
@tests/test_inventory_solidstats_memory.py
@tests/test-solidstats-memory-migration.py
@tests/test-solidstats-memory-policy.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Accept and preserve one real-shaped legacy record end to end</name>
  <files>scripts/inventory-solidstats-memory.py, tests/test-solidstats-memory-migration.py</files>
  <read_first>
    <file>.planning/debug/real-corpus-inventory-gap.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-CONTEXT.md</file>
    <file>config/solidstats-memory/migration-policy.json</file>
    <file>scripts/inventory-solidstats-memory.py</file>
    <file>tests/test-solidstats-memory-migration.py</file>
  </read_first>
  <behavior>
    - "RED: a synthetic oracle record shaped like the rejected source has `filed_at`, may have numeric `source_mtime`, may have `content_date`, uses an un-suffixed canonical repository wing and a synthetic non-active legacy room, and has no `source_timestamp`; current inventory rejects it."
    - "GREEN: the record is accepted, its output metadata object and canonical metadata bytes equal the input exactly, and its document/vector/ID evidence remains unchanged."
    - "GREEN: output metadata contains no added timestamp, renamed wing/room, archive label, routing field, or default."
    - "REGRESSION: records from agent/other source wings and records with valid extra metadata fields remain eligible and lossless."
  </behavior>
  <action>Begin with a failing end-to-end synthetic oracle test that runs `build_source_inventory` through private JSONL output. Build fixtures only from public project labels and clearly synthetic values; do not copy a private corpus value or path. Cover `filed_at` on every fixture, independently optional finite numeric `source_mtime` and optional string `content_date`, one un-suffixed canonical repository wing, one synthetic room absent from the target active-room list, and one agent/other source wing. Assert the parsed metadata object is exactly equal and its canonical bytes/digest are identical before and after inventory; assert the metadata key set is unchanged so no target timestamp, archive, or routing field appears.

Then change only source eligibility in `scripts/inventory-solidstats-memory.py`: retain `lossless_metadata`, document/vector/ID validation, bounds, and digest generation per D-03/D-05, but remove the required `source_timestamp` check from `_validated_record` and remove the active-room/target-wing rejection from `_build_source_inventory`. Continue loading policy only for source-safe processing bounds; do not use `bundle_contract.mapping_contract`, `active_rooms`, `archive_wings`, or `common_wing` to admit or reject a source record. Do not edit the policy or public bundle validator: their target transform contract belongs to 20-05/20-06. Do not choose timestamp precedence, normalize a timestamp, rename labels, add archive state, drop any wing, or change recall/transform/parity semantics. [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-03] [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-05]</action>
  <verify>
    <automated>timeout 20s python3 -m unittest tests/test_inventory_solidstats_memory.py tests/test-solidstats-memory-migration.py</automated>
  </verify>
  <acceptance_criteria>
    - "The real-shape synthetic records pass without `source_timestamp` and without target wing/room membership."
    - "Every emitted metadata object, key set, canonical byte sequence, and metadata SHA-256 equals its corresponding input."
    - "No timestamp field, archive label, route, wing suffix, room replacement, or default is invented."
    - "Existing ID, document, vector, losslessness, secret-shape, and resource-bound checks remain active."
    - "The test uses no external service, private data, or private path."
  </acceptance_criteria>
  <done>One production-quality source record path now traverses oracle protocol, validation, and private output without target mapping eligibility or data invention.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit deterministic value-free source shapes and lock safety regressions</name>
  <files>scripts/inventory-solidstats-memory.py, tests/test-solidstats-memory-migration.py</files>
  <read_first>
    <file>.planning/debug/real-corpus-inventory-gap.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-PATTERNS.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-VALIDATION.md</file>
    <file>scripts/inventory-solidstats-memory.py</file>
    <file>scripts/validate-solidstats-memory-policy.py</file>
    <file>tests/test-solidstats-memory-migration.py</file>
    <file>tests/test-solidstats-memory-policy.py</file>
  </read_first>
  <behavior>
    - "For each top-level metadata field, private `source_shape_evidence` reports a presence count, canonical JSON type histogram, and value-format histogram without recording a field value."
    - "Wing evidence counts canonical repository un-suffixed labels, suffix-marked labels, other strings, missing labels, and invalid label types; room evidence counts slug-shaped strings, other strings, missing labels, and invalid label types without consulting target eligibility lists."
    - "Repeated equivalent synthetic inventories produce byte-identical shape evidence and output checksums."
    - "Summary evidence contains no document, vector, source ID, metadata value, private path, query text, or secret-shaped material."
    - "Malformed/non-lossless/secret-shaped metadata, invalid documents/vectors/IDs, duplicates, path escapes, symlinks, bounds violations, start/end snapshot checksum drift, oracle mismatch, and protocol errors still fail closed."
  </behavior>
  <action>Write failing deterministic tests for `source_shape_evidence`, then add a bounded streaming accumulator to the inventory loop. For each top-level metadata field, record only field presence, canonical JSON type (`null`, `boolean`, `integer`, `number`, `string`, `array`, `object`), and value-free format categories (`utc_timestamp`, `date`, `other_string`, `finite_number`, or `not_applicable`). Report source-only label shape counts: wing categories `canonical_repository_unsuffixed`, `suffix_marked`, `other_string`, `missing`, and `invalid_type`; room categories `slug_string`, `other_string`, `missing`, and `invalid_type`. Derive the canonical repository set from the public SolidStats boundary map, and derive lexical suffix/slug categories without loading target room/wing eligibility. Sort every emitted map/category and bound distinct field tracking by the existing metadata byte/depth limits so output is deterministic and allocation remains bounded. Do not include raw label values, hashes of label values, example values, or corpus-derived identifiers.

Add a start/end snapshot digest equality gate around full inventory reads so checksum drift cannot be accepted. Extend the negative test matrix to prove the existing losslessness, secret-shape, document, vector, ID, duplicate, containment, symlink, limit, oracle-provenance, and protocol gates remain effective and value-free. Run two equivalent synthetic inventories and compare the entire `source_shape_evidence` object plus output checksums. Add assertions that `config/solidstats-memory/migration-policy.json` and `scripts/validate-solidstats-memory-policy.py` remain unchanged, specifically preserving the existing target `mapping_contract`. The resulting evidence informs a later explicit mapping decision; it must not select timestamp precedence, normalize timestamps, rename labels, invent archive labels, discard agent/other wings, or change D-06 through D-09. Surface those unresolved choices in the 20-07 summary as blockers that the main owner must resolve after the Plan 20-04 real inventory and before Plan 20-05. [src: .planning/debug/real-corpus-inventory-gap.md] [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-09]</action>
  <verify>
    <automated>timeout 25s python3 -m unittest tests/test_inventory_solidstats_memory.py tests/test-solidstats-memory-migration.py tests/test-solidstats-memory-policy.py &amp;&amp; git diff --exit-code -- config/solidstats-memory/migration-policy.json scripts/validate-solidstats-memory-policy.py</automated>
  </verify>
  <acceptance_criteria>
    - "Private source evidence contains deterministic field presence/type/format and source-label classification counts only."
    - "The summary has no raw metadata values, label hashes, source identifiers, documents, vectors, queries, private paths, credentials, or secret-shaped content."
    - "Two equivalent synthetic runs produce identical aggregate evidence and checksums."
    - "Every named hostile-input and drift case fails before a successful inventory can be accepted, with value-free diagnostics and incomplete output removed."
    - "The target policy and validator are unchanged, and the source inventory no longer uses their mapping taxonomy as an eligibility filter."
    - "The 20-07 summary names all unresolved mapping choices as blockers before 20-05; it does not resolve any of them."
  </acceptance_criteria>
  <done>The source inventory safely describes arbitrary legacy shapes without exposing values or predetermining the target transform.</done>
</task>

</tasks>

<threat_model>

## ASVS level 2 trust boundaries

| Boundary | Untrusted input | Required control |
| --- | --- | --- |
| Oracle rows to source inventory | Documents, metadata, IDs, vectors, labels, protocol order | Lossless strict JSON, no target eligibility filter, bounded validation, duplicate/order checks, exact preservation |
| Snapshot filesystem to inventory process | Paths, links, mutable files, sidecars, oracle artifact | Containment, non-symlink regular files, start/end digest equality, exact oracle attestation |
| Corpus-bearing records to private summary | Metadata values, labels, documents, vectors, IDs, paths | Streaming aggregate allowlist, value-free classifications, deterministic encoding, secret-shape rejection |
| Target policy to source boundary | Target timestamp/routing taxonomy | Use only processing bounds; prohibit target mapping eligibility and preserve policy bytes |

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- | --- |
| T-20-07-01 | Tampering | Source eligibility | high | mitigate | Real-shape TDD proves every lossless source shape is accepted without target timestamp or routing membership |
| T-20-07-02 | Tampering | Metadata preservation | high | mitigate | Exact object/key/canonical-byte/digest equality and no-invention assertions for every emitted record |
| T-20-07-03 | Information disclosure | Shape evidence and diagnostics | high | mitigate | Aggregate categories only, no values or label hashes, recursive secret checks, index/digest-only errors |
| T-20-07-04 | Elevation of privilege | Snapshot paths and oracle process | high | mitigate | Existing containment, symlink/special-file rejection, fixed argv/environment, exact oracle provenance |
| T-20-07-05 | Denial of service | Metadata shape aggregation and inventory loop | high | mitigate | Existing record/byte/depth/vector/time bounds plus bounded field tracking and streaming counters |
| T-20-07-06 | Spoofing | Oracle protocol and identity | high | mitigate | Exact v3.5.0 attestation, strict header/record/done protocol, sequential indexes, ID mapping checks |
| T-20-07-07 | Repudiation | Snapshot and output evidence | medium | mitigate | Start/end snapshot digest equality, canonical output checksums, deterministic repeated-run tests |
| T-20-07-08 | Tampering | Target migration contract | high | mitigate | Automated zero-diff gate for policy/validator and explicit prohibition on transform/parity changes |

All applicable medium- and high-severity threats are mitigated as blocking acceptance conditions under ASVS level 2.
</threat_model>

<verification>
1. Run `timeout 25s python3 -m unittest tests/test_inventory_solidstats_memory.py tests/test-solidstats-memory-migration.py tests/test-solidstats-memory-policy.py` with no network or private data.
2. Require real-shape exact-preservation, no-invention, deterministic aggregate, and hostile-input regression coverage.
3. Run `git diff --exit-code -- config/solidstats-memory/migration-policy.json scripts/validate-solidstats-memory-policy.py` to prove target policy and validator bytes did not change.
4. Confirm only the two declared implementation/test files changed during execution; Plan 20-04 remains the sole owner of the real-corpus rerun.
</verification>

<success_criteria>

- MIG-01 can inventory arbitrary valid legacy metadata and source labels losslessly instead of rejecting them against a target schema.
- MIG-02 receives deterministic source-shape and recall inputs without any changed transform or parity meaning.
- All existing source safety, containment, provenance, drift, resource, and privacy failures remain fail-closed.
- Timestamp precedence/normalization, wing-room mapping, archive labeling, agent/other-wing treatment, and Phase 20 target semantics remain unresolved and explicitly block 20-05 until the main owner reviews Plan 20-04 evidence.
- No private corpus, external service, policy file, validator, transform, parity, debug report, summary, configuration, ROADMAP, or STATE artifact changes in this plan.
</success_criteria>

## Source coverage audit

| Source | Item | Status | Plan coverage |
| --- | --- | --- | --- |
| GOAL | Review the source and freeze exact mapping/embedding inputs before local transform | COVERED | Tasks 1-2 repair lossless source review and emit mapping-decision evidence without performing the transform |
| REQ | MIG-01 | COVERED | Exact source metadata/document/vector/ID preservation and complete shape evidence |
| REQ | MIG-02 | COVERED | Source evidence remains deterministic and consumable by the unchanged transform/parity plans; no parity claim is made |
| RESEARCH | Bounded untrusted snapshot handling and private evidence | COVERED | Task 2 retains all safety bounds and value-free private evidence |
| CONTEXT | D-01/D-02 frozen-source prerequisites | COVERED | Completed Plan 20-03 remains the dependency and is not changed |
| CONTEXT | D-03 exact source preservation | COVERED | Task 1 exact metadata/object/byte/digest equality |
| CONTEXT | D-04 identity bijection | COVERED | Existing ID/mempalace_id/point_id checks remain regression-locked; mapping semantics are unchanged |
| CONTEXT | D-05 source timestamps and labels remain metadata | COVERED | Tasks 1-2 accept and describe them without precedence, normalization, or rename |
| CONTEXT | D-06 through D-09 target collection, vector, and parity semantics | COVERED | Explicitly unchanged and deferred to Plans 20-05/20-06 after the mapping blocker is resolved |
| DEBUG | Real corpus lacks synthetic timestamp/target taxonomy eligibility | COVERED | Task 1 removes those source-read requirements; Task 2 captures the observed shapes safely |

Phase 21 live restore/deployment/cutover/recovery and Phase 22 archive distillation remain excluded, not missing.

<output>
Create `.planning/phases/20-local-corpus-migration/20-07-SUMMARY.md` when execution completes. The summary must list the unresolved timestamp, wing, room, archive, and agent/other-wing mapping blockers before Plan 20-05.
</output>

<!-- markdownlint-enable MD013 MD033 MD041 -->
