---
phase: 20-local-corpus-migration
plan: 06
type: execute
wave: 6
depends_on:
  - 20-05
files_modified:
  - scripts/verify-solidstats-memory-parity.py
  - tests/test-solidstats-memory-migration.py
  - .planning/phases/20-local-corpus-migration/20-PARITY-REPORT.json
  - .planning/phases/20-local-corpus-migration/20-PHASE21-HANDOFF.json
autonomous: true
requirements:
  - MIG-01
  - MIG-02
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
premises:
  - "MIG-01 is unclassified and unresolved without a phase SPEC; parity evidence closes only the local migration contract described by ROADMAP and D-01 through D-09."
  - "MIG-02 is unclassified and unresolved without a phase SPEC; its acceptance requires a real isolated local Qdrant run and cannot be satisfied by fixtures, counts, or checksums alone."
must_haves:
  truths:
    - "Every source identifier, document byte sequence, metadata value/type, source timestamp, and vector strategy result matches the isolated target."
    - "Recall parity compares deterministic source and target results for unfiltered and representative filtered queries using source-derived repeatability rules."
    - "A parity pass names the exact frozen snapshot, private bundle, v3.5.0 oracle, vector strategy, pinned Qdrant image, isolated run ID, and target collection derivation by safe digests."
    - "Phase 21 receives a sanitized handoff sufficient to verify and restore the approved bundle without any live action occurring in Phase 20."
    - "After a passing handoff is sealed and its retained bundle is rechecked, only the disposable run-bound local Qdrant collection, container, and private runtime data are removed; parity failures preserve that isolated evidence for diagnosis."
  artifacts:
    - path: scripts/verify-solidstats-memory-parity.py
      provides: "Field, identity, vector, and recall parity verifier"
      contains: "def compare_recall_rankings"
    - path: .planning/phases/20-local-corpus-migration/20-PARITY-REPORT.json
      provides: "Sanitized real isolated-run parity verdict"
      contains: "parity_schema"
    - path: .planning/phases/20-local-corpus-migration/20-PHASE21-HANDOFF.json
      provides: "Sanitized immutable-bundle contract for Phase 21"
      contains: "handoff_schema"
    - path: tests/test-solidstats-memory-migration.py
      provides: "Parity comparator and report-contract tests"
      contains: "class ParityContractTests"
  key_links:
    - from: private frozen-source fixtures
      to: isolated local Qdrant collection
      via: "Same v3.5.0 query/filter path with source-derived repeatability rule"
      pattern: "compare_recall_rankings"
    - from: .planning/phases/20-local-corpus-migration/20-PARITY-REPORT.json
      to: .planning/phases/20-local-corpus-migration/20-PHASE21-HANDOFF.json
      via: "Handoff is emitted only from a passing real-run report with matching provenance digests"
      pattern: "parity_report_sha256"
    - from: .planning/phases/20-local-corpus-migration/20-PHASE21-HANDOFF.json
      to: disposable local Qdrant runtime
      via: "Pass-gated, run-ID-bound cleanup after retained bundle/report digest verification"
      pattern: "cleanup_isolated_target"
  prohibitions:
    - statement: "Frozen historical or archive material must not be reinterpreted as current semantic truth."
      status: resolved
      verification: judgment
    - statement: "Source wording, identifiers, metadata, or timestamps must not be invented or normalized to make migration pass."
      status: resolved
      verification: judgment
    - statement: "Corpus text, personal facts, raw metadata, and vectors must not enter git, chat, or shared diagnostic logs."
      status: resolved
      verification: judgment
---

<!-- markdownlint-disable MD013 MD033 MD041 -->

<objective>
Prove exact identifier/document/metadata/timestamp/vector parity and deterministic recall/ranking parity between the frozen source and the isolated local Qdrant target, then emit a sanitized Phase 21 handoff.

Purpose: Close MIG-01 and MIG-02 with real local evidence rather than count/checksum proxies, while leaving every live restore and cutover action to Phase 21.
Output: Parity verifier/tests, real sanitized parity report, and Phase 21 handoff contract.
</objective>

## Artifacts this phase produces

This plan adds:

- `scripts/verify-solidstats-memory-parity.py`
  - symbols: `ParityFailure`, `compare_id_bijection`, `compare_documents_metadata_timestamps`, `compare_vectors`, `derive_source_distance_rule`, `compare_recall_rankings`, `write_parity_report`, `write_phase21_handoff`, `cleanup_isolated_target`, `main`
  - CLI flags: `--inventory`, `--transform-manifest`, `--bundle-dir`, `--recall-fixtures`, `--oracle-python`, `--oracle-source-dir`, `--qdrant-url`, `--qdrant-container`, `--qdrant-data-dir`, `--output`, `--handoff-output`, `--source-repeat-runs`, `--top-k`, `--cleanup-after-pass`, `--check-only`
- `tests/test-solidstats-memory-migration.py`
  - symbol: `ParityContractTests`
- `.planning/phases/20-local-corpus-migration/20-PARITY-REPORT.json`
  - JSON fields: `parity_schema`, `verdict`, `source_inventory_sha256`, `transform_manifest_sha256`, `bundle_sha256`, `oracle_version`, `oracle_revision`, `qdrant_image`, `qdrant_run_id`, `target_collection_derivation_sha256`, `vector_strategy`, `field_parity`, `id_parity`, `vector_parity`, `recall_parity`, `bounds`, `created_at`
- `.planning/phases/20-local-corpus-migration/20-PHASE21-HANDOFF.json`
  - JSON fields: `handoff_schema`, `parity_report_sha256`, `bundle_manifest_sha256`, `bundle_file_digests`, `oracle_version`, `oracle_revision`, `qdrant_image`, `target_collection_derivation_sha256`, `target_vector_contract`, `vector_strategy`, `record_count`, `phase21_required_checks`

No private corpus artifact is added to git. Bundle file entries use basenames and digests, never absolute paths.

<execution_context>
@/home/afgan0r/.codex/gsd-core/workflows/execute-plan.md
@/home/afgan0r/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-local-corpus-migration/20-CONTEXT.md
@.planning/phases/20-local-corpus-migration/20-RESEARCH.md
@.planning/phases/20-local-corpus-migration/20-VALIDATION.md
@.planning/phases/20-local-corpus-migration/20-PATTERNS.md
@.planning/phases/20-local-corpus-migration/20-05-SUMMARY.md
@.planning/phases/20-local-corpus-migration/20-SOURCE-INVENTORY.json
@.planning/phases/20-local-corpus-migration/20-TRANSFORM-MANIFEST.json
@scripts/build-solidstats-memory-bundle.py
@tests/test-solidstats-memory-migration.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement exact field, vector, and source-derived recall comparators</name>
  <files>scripts/verify-solidstats-memory-parity.py, tests/test-solidstats-memory-migration.py</files>
  <read_first>
    <file>.planning/phases/20-local-corpus-migration/20-CONTEXT.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-RESEARCH.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-VALIDATION.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-TRANSFORM-MANIFEST.json</file>
    <file>scripts/build-solidstats-memory-bundle.py</file>
    <file>tests/test-solidstats-memory-migration.py</file>
  </read_first>
  <behavior>
    - "ID parity requires exact equality and bijection across source ID, payload `mempalace_id`, and oracle-derived UUIDv5 point ID."
    - "Field parity compares document bytes, metadata keys/values/types, wing, room, archive classification, and source timestamps exactly while treating target ingestion `updated_at` separately."
    - "Reuse vector parity compares exact serialized vector bytes; reembed parity recomputes with the pinned model and compares exact configured output/dimension/metric evidence."
    - "Recall parity requires exact ordered IDs for deterministic source results and applies a documented distance comparison rule derived before target queries from repeated source runs plus serialization precision."
    - "Cleanup is unreachable before a passing handoff, rejects every non-run-bound or non-private target, and never removes the frozen snapshot, private bundle, fixtures, or sanitized reports."
  </behavior>
  <reversibility rating="costly">Changing parity semantics after Phase 21 consumes the handoff invalidates the acceptance report and requires a full repeat; D-09 already locks deterministic field/vector/recall parity.</reversibility>
  <action>Write failing tests for missing/duplicate IDs, document byte drift, metadata value/type drift, source timestamp drift, ingestion timestamp separation, vector byte/dimension/metric drift, stable/tied/unstable source rankings, filter loss, target-derived tolerance attempts, cleanup before a passing handoff, mismatched run/container identity, and cleanup paths that overlap retained evidence. Implement streaming bounded comparators that query the isolated target with vectors and payloads enabled but never log returned values. For recall, repeat each frozen-source fixture using the approved v3.5.0 oracle before any target query, canonicalize documented exact ties deterministically, and derive the distance rule only from observed source repeatability plus the recorded float-serialization floor; reject unstable source fixtures rather than widening tolerance from target results. Require exact ordered target IDs and filter behavior for unfiltered plus representative wing/room/archive-state strata per D-09. Canonically report aggregate pass/fail counts, worst safe deltas, rule provenance, and digests; failures expose only field names, indexes, and hashes. Implement `cleanup_isolated_target` as a separate pass-gated operation that accepts only the exact Plan 20-05 run identity, oracle-derived collection, disposable container, and a resolved Qdrant data directory beneath the approved private work root; reject any overlap with the snapshot, source, bundle, fixture, report, or handoff roots. [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-09] [src: .planning/phases/20-local-corpus-migration/20-RESEARCH.md#Parity-Requirements]</action>
  <verify>
    <automated>timeout 25s python3 -m unittest tests/test-solidstats-memory-policy.py tests/test-solidstats-memory-migration.py &amp;&amp; timeout 10s python3 scripts/verify-solidstats-memory-parity.py --help</automated>
  </verify>
  <acceptance_criteria>
    - "Synthetic tests fail for every single-field, ID, vector, filter, distance, and ranking discrepancy."
    - "Tests prove the distance rule is fixed before target results are observed and never expands to make a target pass."
    - "All comparisons are streaming/bounded and every source/target call has a timeout."
    - "Captured reports and diagnostics contain no corpus text, source IDs, metadata values, vectors, queries, private paths, or secrets."
    - "Cleanup tests prove no failed, incomplete, mismatched, shared, or path-overlapping run can delete any collection, container, or file."
  </acceptance_criteria>
  <done>The verifier encodes D-09 as deterministic, bounded, privacy-preserving comparisons rather than aggregate proxies.</done>
</task>

<task type="auto">
  <name>Task 2: Run real isolated parity and seal the Phase 21 handoff</name>
  <files>.planning/phases/20-local-corpus-migration/20-PARITY-REPORT.json, .planning/phases/20-local-corpus-migration/20-PHASE21-HANDOFF.json, $SOLIDSTATS_MEMORY_WORK_DIR/qdrant-data/</files>
  <read_first>
    <file>.planning/phases/20-local-corpus-migration/20-VALIDATION.md</file>
    <file>.planning/phases/20-local-corpus-migration/20-SOURCE-INVENTORY.json</file>
    <file>.planning/phases/20-local-corpus-migration/20-TRANSFORM-MANIFEST.json</file>
    <file>$SOLIDSTATS_MEMORY_WORK_DIR/source/recall-fixtures.json</file>
    <file>$SOLIDSTATS_MEMORY_WORK_DIR/transform/bundle-manifest.json</file>
    <file>scripts/verify-solidstats-memory-parity.py</file>
  </read_first>
  <precondition>The exact Plan 20-05 loopback Qdrant run is still available, its run ID/image/collection/count match `20-TRANSFORM-MANIFEST.json`, and all private source/bundle digests still match Plans 20-04 and 20-05.</precondition>
  <reversibility rating="reversible">A failed report cannot advance; the disposable target can be recreated from the immutable snapshot and bundle for a fresh run.</reversibility>
  <action>First preserve the offline quick gate, then run the verifier with the approved v3.5.0 oracle against the exact isolated Qdrant run and private frozen fixtures. Recheck snapshot, inventory, bundle, image, run-ID, empty-target proof, target collection derivation, count, and ID-set digests before comparing. Traverse every point for identifier/document/metadata/timestamp/vector parity, then execute the deterministic unfiltered and representative filtered recall suite with at least three source repeat runs and the source-derived comparison rule. Emit `20-PARITY-REPORT.json` only after a complete run; set `verdict` to `pass` only when every category passes and the report records the real isolated run. Emit `20-PHASE21-HANDOFF.json` only from that passing report, containing versioned bundle basenames/digests and the explicit checks Phase 21 must repeat before live restore. Apply strict allowlists and recursive privacy/secret rejection to both files.

    Apply this failure policy before cleanup: if parity fails, the run is incomplete, the handoff is absent, or retained-artifact validation fails, preserve the isolated collection, container, private Qdrant data, private bundle/fixtures, and sanitized failure report for diagnosis; do not invoke cleanup. After a passing report and handoff are atomically written and schema/privacy validation reconfirms the digest-locked private bundle is retained, run `cleanup_isolated_target` with the Plan 20-05 run ID, exact oracle-derived collection, recorded disposable container identity, and private Qdrant data root. Delete that collection through loopback REST, stop and remove only that run-bound container, then remove only its resolved private Qdrant runtime directory. Verify the endpoint/collection, container, and data directory are absent while the immutable source snapshot, private source artifacts, digest-locked transform bundle, parity report, and handoff retain their pre-cleanup digests. Do not restore to live Qdrant, deploy, register MCP, change clients, retire legacy storage, execute recovery, or remove any retained evidence. [src: .planning/phases/20-local-corpus-migration/20-CONTEXT.md#D-09] [src: .planning/ROADMAP.md#Phase-21-Live-Cutover-and-Recovery]</action>
  <verify>
    <automated>timeout 3600s "$SOLIDSTATS_MEMORY_V350_PYTHON" scripts/verify-solidstats-memory-parity.py --inventory "$SOLIDSTATS_MEMORY_WORK_DIR/source/source-inventory.json" --transform-manifest .planning/phases/20-local-corpus-migration/20-TRANSFORM-MANIFEST.json --bundle-dir "$SOLIDSTATS_MEMORY_WORK_DIR/transform" --recall-fixtures "$SOLIDSTATS_MEMORY_WORK_DIR/source/recall-fixtures.json" --oracle-python "$SOLIDSTATS_MEMORY_V350_PYTHON" --oracle-source-dir "$SOLIDSTATS_MEMORY_V350_SOURCE" --qdrant-url "$SOLIDSTATS_MEMORY_LOCAL_QDRANT_URL" --qdrant-container "$SOLIDSTATS_MEMORY_LOCAL_QDRANT_CONTAINER" --qdrant-data-dir "$SOLIDSTATS_MEMORY_WORK_DIR/qdrant-data" --output .planning/phases/20-local-corpus-migration/20-PARITY-REPORT.json --handoff-output .planning/phases/20-local-corpus-migration/20-PHASE21-HANDOFF.json --source-repeat-runs 3 --cleanup-after-pass</automated>
  </verify>
  <acceptance_criteria>
    - "The report identifies a real isolated local Qdrant run and has `pass` for ID, document, metadata, source timestamp, vector, and recall parity."
    - "All source points are compared; recall covers unfiltered plus every available representative wing/room/archive-state stratum."
    - "The handoff digest-locks the passing report and private bundle and lists Phase 21 pre-restore/recovery checks without live credentials or paths."
    - "No count-only/checksum-only run, synthetic target, unavailable Docker run, or shared/VPS target can produce a passing report or handoff."
    - "After pass-gated bundle/report retention is verified, the exact isolated collection and run-bound container are absent and only their private Qdrant runtime directory has been deleted."
    - "The immutable source snapshot, private source artifacts, digest-locked bundle, parity report, and handoff remain present with unchanged digests after cleanup."
    - "A parity failure emits no handoff, skips cleanup, and preserves the isolated target/runtime plus sanitized failure evidence for diagnosis."
  </acceptance_criteria>
  <done>MIG-01 and MIG-02 have a real isolated local parity proof and safe immutable handoff; the disposable local Qdrant runtime is cleaned only after pass-gated retention checks, with all live operations deferred to Phase 21.</done>
</task>

</tasks>

<threat_model>

## ASVS level 2 trust boundaries

| Boundary | Untrusted input | Required control |
| --- | --- | --- |
| Frozen/private evidence to verifier | Paths, manifests, corpus records, fixtures | Containment, digest locks, strict schema, bounded streaming, no value logs |
| Isolated Qdrant API/runtime to verifier | Payloads, vectors, rankings, errors, cleanup targets | Loopback/run-ID/image/collection allowlist, response bounds, strict comparison, pass-gated exact-target cleanup |
| Verifier to repository reports | Potentially sensitive mismatch evidence | Aggregate allowlist, hashes, privacy/secret rejection, atomic pass-gated handoff |

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- | --- |
| T-20-06-01 | Spoofing | Parity target | high | mitigate | Require loopback endpoint, pinned image, unique run ID, derived collection, and transform-manifest match |
| T-20-06-02 | Tampering | Parity semantics | high | mitigate | Exact field/vector contracts and source-derived recall rule fixed before target observation |
| T-20-06-03 | Tampering | Private evidence | high | mitigate | Recompute inventory/bundle/snapshot digests before and after the run; reject drift |
| T-20-06-04 | Information disclosure | Queries, mismatches, reports | high | mitigate | No value logging; field/index/hash diagnostics; allowlisted sanitized report and handoff |
| T-20-06-05 | Elevation of privilege | Paths, API requests, and cleanup | high | mitigate | Contained regular files, fixed loopback URL/argv, exact run-bound collection/container, private data-root containment, retained-root exclusion, and no shell/source-derived command text |
| T-20-06-06 | Denial of service | Full-corpus and recall traversal | high | mitigate | Streaming comparison, response/query/top-k bounds, per-call timeouts, one-hour total timeout |
| T-20-06-07 | Repudiation | Passing verdict | medium | mitigate | Canonical report binds source, bundle, oracle, image, run ID, rule, category counts, and timestamp |

All high-severity mitigations are blocking acceptance conditions under ASVS level 2.
</threat_model>

<verification>
1. Preserve the offline gate: `timeout 25s python3 -m unittest tests/test-solidstats-memory-policy.py tests/test-solidstats-memory-migration.py`.
2. Require a live health check and provenance match for the exact isolated local Qdrant run.
3. Run full parity under a one-hour hard timeout; require every field/vector category and every recall fixture to pass.
4. Validate report and handoff schemas plus recursive privacy/secret rejection, then verify the retained bundle and reports before pass-gated cleanup.
5. On pass, prove the exact collection, run-bound container, and private Qdrant runtime data are absent while source/bundle/report digests remain unchanged; on failure, prove cleanup was skipped and evidence was preserved.
6. Confirm no Phase 21 or Phase 22 operation occurred.
</verification>

<success_criteria>

- MIG-01 is evidenced by complete exact field/identity/vector preservation from the frozen source.
- MIG-02 is evidenced by deterministic source-versus-isolated-target recall/ranking parity, not aggregate proxies.
- D-01 through D-09 remain traceable through the report and handoff.
- Phase 21 can consume a digest-locked private bundle and explicit verification contract without Phase 20 touching live runtime state.
- Passing runs leave no disposable local Qdrant collection, container, or private runtime data; failed runs retain them for diagnosis without deleting the source, bundle, or sanitized reports.
</success_criteria>

## Source coverage audit

| Source | Item | Status | Plan coverage |
| --- | --- | --- | --- |
| GOAL | Prove identifier/document/metadata/vector/recall parity in isolated Qdrant | COVERED | Tasks 1-2 implement and run all parity categories |
| REQ | MIG-01 | COVERED | Full-corpus exact identity/field/vector proof |
| REQ | MIG-02 | COVERED | Deterministic filtered/unfiltered recall and ranking proof |
| RESEARCH | Real isolated run, Phase 21 handoff, and disposable-target cleanup | COVERED | Task 2 binds run provenance, emits the digest-locked handoff, and cleans only the passed run's collection/container/private Qdrant data after retention checks |
| CONTEXT | D-03 through D-09 | COVERED | Exact mapping/vector rules and D-09 parity acceptance |

Deferred Phase 21 live restore/deployment/MCP/client/cutover/recovery and Phase 22 archive distillation are excluded, not missing.

<output>
Create `.planning/phases/20-local-corpus-migration/20-06-SUMMARY.md` when execution completes.
</output>

<!-- markdownlint-enable MD013 MD033 MD041 -->
