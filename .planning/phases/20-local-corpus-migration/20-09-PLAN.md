---
phase: 20-local-corpus-migration
plan: 09
type: tdd
wave: 5
depends_on:
  - 20-07
files_modified:
  - scripts/inventory-solidstats-memory.py
  - tests/test-solidstats-memory-migration.py
autonomous: true
gap_closure: true
requirements:
  - MIG-01
  - MIG-02
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
premises:
  - claim: "Plan 20-07 already established lossless source metadata preservation and value-free source-shape evidence."
    source: ".planning/phases/20-local-corpus-migration/20-07-SUMMARY.md#Phase-20-Plan-07-Source-Boundary-Repair-Summary"
    verify: "git log --oneline --all -- .planning/phases/20-local-corpus-migration/20-07-SUMMARY.md scripts/inventory-solidstats-memory.py tests/test-solidstats-memory-migration.py"
  - claim: "The remaining real-corpus failure is caused by lexical scanning of metadata values during source admission, before lossless preservation."
    source: ".planning/debug/real-corpus-secret-lexeme-gap.md#Root-Cause"
    verify: "ast-index symbol lossless_metadata && ast-index symbol _reject_secret_shape"
  - claim: "The policy, public validator, frozen snapshot, and exact v3.5.0 oracle contracts are unchanged by this gap closure."
    source: ".planning/phases/20-local-corpus-migration/20-CONTEXT.md#Decisions"
    verify: "git diff --exit-code -- config/solidstats-memory/migration-policy.json scripts/validate-solidstats-memory-policy.py"
must_haves:
  truths:
    - "Per D-03/D-05, a valid metadata value containing the synthetic lexical token `secret` in ordinary prose is admitted and preserved byte-for-byte."
    - "A metadata key matching the existing secret-shape pattern remains fail-closed with a value-free diagnostic."
    - "Sidecar and freeze admission continue to use the recursive secret-shape guard, including value inspection."
    - "Bounds, source IDs, vectors, snapshot-drift detection, oracle protocol checks, output permissions, and value-free summaries remain unchanged."
    - "No target timestamp, routing, archive, exclusion, or preservation mapping is selected; Plan 20-08 remains the blocking human mapping checkpoint."
  artifacts:
    - path: scripts/inventory-solidstats-memory.py
      provides: "Source-admission-specific recursive metadata-key guard used only by lossless_metadata()"
      contains: "_reject_secret_key_shape"
    - path: tests/test-solidstats-memory-migration.py
      provides: "Synthetic source-admission, fail-closed key, recursive sidecar/freeze, losslessness, and privacy regressions"
      contains: "lexical_secret"
  key_links:
    - from: scripts/inventory-solidstats-memory.py
      to: tests/test-solidstats-memory-migration.py
      via: "Synthetic oracle records exercise build_source_inventory() through lossless_metadata()"
      pattern: "lossless_metadata"
    - from: scripts/inventory-solidstats-memory.py
      to: "snapshot sidecars and freeze attestation"
      via: "Existing _reject_secret_shape() remains the recursive control-plane guard"
      pattern: "_reject_secret_shape"
  prohibitions:
    - statement: "Do not add a credential classifier or inspect metadata scalar values in the source-admission-specific guard."
      status: resolved
      verification: judgment
    - statement: "Do not infer broader sensitivity properties from admission of the synthetic prose value."
      status: resolved
      verification: judgment
    - statement: "Do not access the real corpus, private snapshot, Docker, network, VPS, or external services during plan execution."
      status: resolved
      verification: judgment
    - statement: "Do not modify policy, validator, mapping artifacts, runtime configuration, or any file outside the two declared production-ownership paths."
      status: resolved
      verification: scan
---

<!-- markdownlint-disable MD013 MD033 MD041 -->

<objective>
Close the diagnosed source-inventory admission gap without weakening control-plane secret-shape validation or selecting target mapping semantics.

Purpose: Make D-03/D-05 lossless source admission treat metadata values as untrusted data to preserve, while retaining fail-closed secret-shaped metadata keys and every existing containment, provenance, bounds, privacy, and determinism gate.

Output: A focused metadata-key-only source admission seam, synthetic TDD coverage, unchanged policy/validator contracts, and a value-free handoff for the main-owned exact-image rerun.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-local-corpus-migration/20-CONTEXT.md
@.planning/phases/20-local-corpus-migration/20-RESEARCH.md
@.planning/phases/20-local-corpus-migration/20-PATTERNS.md
@.planning/phases/20-local-corpus-migration/20-VALIDATION.md
@.planning/phases/20-local-corpus-migration/20-07-SUMMARY.md
@.planning/debug/real-corpus-secret-lexeme-gap.md
@config/solidstats-memory/migration-policy.json
@scripts/inventory-solidstats-memory.py
@tests/test_inventory_solidstats_memory.py
@tests/test-solidstats-memory-migration.py
@tests/test-solidstats-memory-policy.py

<interfaces>
- `SECRET_KEY_PATTERN` is the existing key-shape matcher; this plan does not introduce another classifier.
- `_reject_secret_shape(value: object) -> bool` is the existing recursive key-and-value guard for sidecars, freeze evidence, and sanitized privacy checks.
- `lossless_metadata(metadata: object, limits: InventoryLimits) -> dict[str, object]` is the source admission seam that must switch to `_reject_secret_key_shape()`.
- `build_source_inventory(**kwargs: object) -> dict[str, object]` is the tested end-to-end entry point and must retain its existing output, permission, bounds, drift, oracle, and protocol contracts.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Admit lexical prose losslessly through the real inventory path</name>
  <files>tests/test-solidstats-memory-migration.py, scripts/inventory-solidstats-memory.py</files>
  <behavior>
    - "RED: a synthetic oracle record whose metadata value is an ordinary sentence containing the lexical token `secret` and no recognized credential syntax reaches lossless_metadata() and initially fails under the diagnosed recursive value scan."
    - "GREEN: the same synthetic value is accepted, emitted unchanged, and has identical canonical JSON bytes before and after inventory."
    - "The synthetic sentence is absent from source-inventory.json, diagnostics, and exception text even though it is present in the private source-records artifact."
    - "A secret-shaped metadata key remains rejected before output creation, and the error exposes only the established record index/digest form."
  </behavior>
  <action>Follow RED -> GREEN for the diagnosed admission seam. First add an end-to-end synthetic regression in `InventoryContractTests` that drives `build_source_inventory()` through the fake v3.5.0 oracle with an ordinary public sentence containing the lexical token `secret` and no recognized credential syntax; assert exact metadata equality and canonical-byte equality in `source-records.jsonl`, and assert the sentence is absent from the public summary and every diagnostic. Confirm RED fails because `lossless_metadata()` calls the recursive `_reject_secret_shape()`. Then add `_reject_secret_key_shape(value: object) -> bool` in `scripts/inventory-solidstats-memory.py`: recursively traverse dictionaries and lists, apply the existing `SECRET_KEY_PATTERN` only to dictionary keys, and never inspect scalar metadata values. Use this helper only in `lossless_metadata()` per D-03/D-05. Keep JSON losslessness and metadata-byte bounds in their current order. Replace the old source-metadata-value rejection regression with a fail-closed secret-shaped-key regression whose fixtures and assertions are entirely synthetic and value-free. Do not claim that admission proves any broader property of the accepted value, and do not add a credential classifier.</action>
  <verify>
    <automated>timeout 20s python3 -m unittest tests/test-solidstats-memory-migration.py</automated>
  </verify>
  <done>The synthetic lexical-prose record passes end-to-end with byte-identical metadata, the public evidence contains no value, and a secret-shaped metadata key still fails before output creation.</done>
</task>

<task type="auto">
  <name>Task 2: Lock recursive control-plane guards and unchanged offline contracts</name>
  <files>tests/test-solidstats-memory-migration.py, scripts/inventory-solidstats-memory.py</files>
  <behavior>
    - "Synthetic sidecar and freeze payloads with secret-shaped keys or values remain rejected by `_reject_secret_shape()` with no raw value in stderr or exception text."
    - "Output directories remain mode 0700 and generated private files remain mode 0600."
    - "Record bounds, metadata bounds, duplicate-ID rejection, vector validation, snapshot drift, oracle order, and check-only behavior remain green."
    - "The migration policy and public validator have zero diff."
  </behavior>
  <action>Expand the regression around the new source-only seam without widening production scope. Keep `_reject_secret_shape()` recursive and unchanged for `_load_snapshot_contract()`, `_validate_freeze()`, and every existing sanitized-repository/privacy call. Add or sharpen synthetic tests that prove sidecar and freeze value inspection still rejects before corpus access, diagnostics never echo the supplied synthetic value, output permissions remain private, and the full inventory regression suite still covers losslessness, bounds, IDs, vectors, snapshot drift, oracle protocol, check-only, deterministic fixtures, and value-free summaries. Do not access the real corpus, private snapshot, Docker, network, VPS, or external services. Do not change `config/solidstats-memory/migration-policy.json` or `scripts/validate-solidstats-memory-policy.py`.</action>
  <verify>
    <automated>timeout 30s python3 -m unittest tests/test_inventory_solidstats_memory.py tests/test-solidstats-memory-migration.py tests/test-solidstats-memory-policy.py &amp;&amp; timeout 10s python3 -m py_compile scripts/inventory-solidstats-memory.py tests/test-solidstats-memory-migration.py &amp;&amp; timeout 10s python3 scripts/inventory-solidstats-memory.py --help &amp;&amp; timeout 10s python3 scripts/validate-solidstats-memory-policy.py &amp;&amp; git diff --exit-code -- config/solidstats-memory/migration-policy.json scripts/validate-solidstats-memory-policy.py</automated>
  </verify>
  <done>All offline suites and CLI/compile gates pass, recursive sidecar/freeze protection remains green, and policy/validator files are byte-for-byte unchanged.</done>
</task>

</tasks>

<execution_boundary>
The executor owns only the two declared files and offline synthetic verification. It must not access the real corpus, private snapshot, Docker, network, VPS, or external services, and it must not select any timestamp, routing, archive, exclusion, or preservation mapping.
</execution_boundary>

<main_owned_gate>
After the executor finishes and before Plan 20-09 is marked complete, the main agent reruns the already-approved exact-image inventory procedure against the same immutable snapshot with networking disabled. The main agent, not the executor, owns all private inputs and exact-image operations. A passing full run must preserve the established 0700 output-directory and 0600 file modes, emit only value-free public evidence, and complete without snapshot/oracle/protocol drift. A failure record may contain only the fixed safe category/detail pair and aggregate progress; it must contain no source value, identifier, metadata key or value, vector, private path, environment value, or credential. Plan 20-04 remains unreachable until this main-owned gate passes.
</main_owned_gate>

<threat_model>

## Trust boundaries

| Boundary | Untrusted input | Required disposition |
| --- | --- | --- |
| Frozen oracle record -> source admission | Document, metadata, identifier, and vector values | Preserve valid source data losslessly while enforcing bounds, types, key-shape rejection, and value-free failure output |
| Snapshot sidecars/freeze -> control plane | Manifest, identity, config, embedder, and freeze payloads | Retain recursive key-and-value secret-shape rejection before corpus processing |
| Private inventory -> public summary/logs | Source records, metadata, vectors, and paths | Emit only allowlisted counts, formats, checksums, fixed categories, and digests |
| Offline executor -> main-owned real rerun | Repository implementation versus private exact-image evidence | Keep executor offline and require a separate value-free main-owned completion gate |

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
| --- | --- | --- | --- | --- | --- |
| T-20-09-01 | Tampering | `lossless_metadata()` admission | high | mitigate | Apply the existing key-shape pattern recursively to metadata keys only, then retain canonical-byte equality and bounds checks per D-03/D-05 |
| T-20-09-02 | Information disclosure | Diagnostics and public inventory | high | mitigate | Assert no synthetic metadata value appears in summaries, stderr, or exceptions; retain index/digest-only errors and private file modes |
| T-20-09-03 | Spoofing | Snapshot sidecars and freeze evidence | high | mitigate | Keep `_reject_secret_shape()` recursive for control-plane payload keys and values and cover both boundaries synthetically |
| T-20-09-04 | Denial of service | Recursive metadata and corpus processing | medium | mitigate | Preserve configured record, document, metadata, vector, fixture, artifact, and dimension bounds plus ordered oracle protocol checks |
| T-20-09-05 | Repudiation | Gap closure and real-corpus proof | medium | mitigate | Bind TDD evidence to the diagnosed root cause, require all offline gates, and reserve exact-image proof for the main-owned value-free rerun |
| T-20-09-06 | Elevation of privilege | Executor access boundary | high | mitigate | Forbid executor access to private inputs, Docker, network, VPS, and external services; allow only the two declared repository files |

</threat_model>

<verification>
1. RED fails at the diagnosed recursive metadata-value scan, not on fixture setup, oracle syntax, permissions, or protocol.
2. GREEN admits and byte-preserves the synthetic ordinary sentence while rejecting a secret-shaped metadata key with value-free output.
3. The complete offline suite, `py_compile`, inventory CLI help, and public policy validator all pass within the declared timeouts.
4. `git diff --exit-code -- config/solidstats-memory/migration-policy.json scripts/validate-solidstats-memory-policy.py` proves both frozen contracts have zero diff.
5. The executor records no private evidence and performs no real-corpus, private-snapshot, Docker, network, VPS, or external-service operation.
6. The main-owned exact-image full inventory rerun passes before Plan 20-09 is closed and before Plan 20-04 starts.
</verification>

<success_criteria>

- The production diff is confined to `scripts/inventory-solidstats-memory.py` and `tests/test-solidstats-memory-migration.py`.
- A synthetic ordinary sentence containing the lexical token `secret` is accepted and preserved byte-for-byte as metadata data; public evidence and diagnostics contain no value.
- Secret-shaped metadata keys remain fail-closed, and recursive sidecar/freeze key-and-value tests remain green.
- Output permissions, losslessness, bounds, IDs, vectors, snapshot drift, deterministic fixtures, oracle checks, and protocol checks remain green.
- Policy and validator have zero diff, all offline suites pass, and compile/help gates pass.
- The main-owned exact-image real-corpus rerun passes with value-free evidence; only then is Plan 20-04 reachable.
- No mapping decision is selected; Plan 20-08 remains the blocking human checkpoint after complete real inventory.

</success_criteria>

## Source coverage audit

| Source | Item | Plan coverage | Status | Notes |
| --- | --- | --- | --- | --- |
| GOAL | Freeze, source-review, transform locally, and prove isolated parity | 20-01 through 20-09 | COVERED | 20-09 restores reachability of complete source inventory without claiming transform or parity |
| REQ | MIG-01: freeze and snapshot once | 20-03, 20-07, 20-09, 20-04 | COVERED | Main-owned exact-image rerun remains bound to the immutable frozen source |
| REQ | MIG-02: local transform and isolated parity | 20-01, 20-05, 20-06; prerequisite in 20-09 | COVERED | 20-09 closes source admission only; transform and parity remain downstream |
| RESEARCH | Exact v3.5.0 oracle, lossless metadata, private output, bounded processing, and value-free evidence | 20-02, 20-03, 20-07, 20-09 | COVERED | No external dependency or package change is introduced |
| CONTEXT | D-01 immutable raw snapshot and exact oracle | 20-02, 20-03 | COVERED | Preserved unchanged by this gap plan |
| CONTEXT | D-02 private artifacts and sanitized committed evidence | 20-01 through 20-09 | COVERED | 20-09 adds summary/diagnostic non-disclosure regression and a value-free main gate |
| CONTEXT | D-03 lossless source preservation before mapping | 20-07, 20-09 | COVERED | Metadata values are preserved data; secret-shaped keys remain fail-closed |
| CONTEXT | D-04 evidence-driven mapping decisions | 20-04, 20-08 | COVERED | 20-09 selects no mapping and keeps 20-08 blocking |
| CONTEXT | D-05 deterministic fixtures with no query/source text in public evidence | 20-02, 20-07, 20-09, 20-04 | COVERED | Synthetic regressions assert public evidence excludes the metadata sentence |

Deferred Phase 21 live restore/cutover and Phase 22 archive distillation are exclusions, not gaps.

<output>
Create `.planning/phases/20-local-corpus-migration/20-09-SUMMARY.md` when done. The summary must distinguish executor-complete offline work from the main-owned exact-image gate and must not mark the plan complete before that gate passes.
</output>

<!-- markdownlint-enable MD013 MD033 MD041 -->
