---
phase: 13-deploy-pipeline-metrics-stack
plan: 05
type: execute
wave: 2
depends_on: [13-01, 13-02, 13-03, 13-04]
files_modified:
  - docs/observability.md
autonomous: false
requirements: [DEP-04, MET-03]
user_setup:
  - service: github-actions
    why: "Three new GitHub environment (staging) secrets are required before the obs deploy workflow can run"
    env_vars:
      - name: K8S_OBS_TOKEN
        source: "kubectl -n monitoring get secret obs-ci-deployer-token -o jsonpath='{.data.token}' | base64 -d"
      - name: GRAFANA_ADMIN_PASSWORD
        source: "Operator-chosen strong password"
      - name: PG_MONITOR_PASSWORD
        source: "Operator-chosen strong password (same value used in the CREATE USER below)"
must_haves:
  truths:
    - "The Prometheus runtime SA + read-only ClusterRole/ClusterRoleBinding from 01-obs-rbac.yaml are applied to the live cluster by the operator"
    - "A non-superuser solid_monitor PostgreSQL role exists with GRANT pg_monitor, created against the live app database"
    - "K8S_OBS_TOKEN, GRAFANA_ADMIN_PASSWORD, PG_MONITOR_PASSWORD exist as GitHub staging environment secrets"
    - "The default StorageClass and node disk headroom are confirmed sufficient for the 8Gi + 2Gi obs PVCs before the stack is applied"
  artifacts:
    - path: "docs/observability.md"
      provides: "Operator bootstrap runbook: RBAC apply, pg_monitor role SQL, GitHub secret extraction, preflight"
      min_lines: 40
  key_links:
    - from: "live cluster RBAC"
      to: "k8s/staging/01-obs-rbac.yaml (prometheus-monitoring)"
      via: "operator kubectl apply -f k8s/staging/01-obs-rbac.yaml"
      pattern: "prometheus-monitoring"
    - from: "solid_monitor pg role"
      to: "postgres-monitor-secret DSN (rendered in 13-01)"
      via: "GRANT pg_monitor TO solid_monitor; DSN user matches"
      pattern: "solid_monitor"
---

<objective>
Operator-gated bootstrap that prepares the live cluster + CI for the obs deploy: apply the Prometheus runtime RBAC from `01-obs-rbac.yaml`, create the non-superuser `solid_monitor` pg_monitor role against live PostgreSQL, extract the obs SA token and add the three GitHub environment secrets, and confirm StorageClass + disk headroom. Nothing here is auto-runnable — every step touches live state or an operator-only secret store.

Purpose: The obs deploy workflow (13-04) cannot succeed until (a) the Prometheus SA + cluster-read ClusterRole exist (Pitfall 2), (b) the pg_monitor role exists so postgres-exporter's DSN authenticates (MET-03, T-13-04), and (c) the GitHub secrets exist (DEP-04 / Environment Availability gap). This plan does those once, by hand, and records them in a runbook.

Output: `docs/observability.md` bootstrap runbook + completed live RBAC apply + live pg_monitor role + three GitHub secrets.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-deploy-pipeline-metrics-stack/13-RESEARCH.md
@k8s/staging/01-obs-rbac.yaml
@k8s/staging/10-postgres.yaml
@.planning/phases/13-deploy-pipeline-metrics-stack/13-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/observability.md bootstrap runbook</name>
  <files>docs/observability.md</files>
  <read_first>.planning/phases/13-deploy-pipeline-metrics-stack/13-RESEARCH.md</read_first>
  <action>
    Author docs/observability.md as the operator bootstrap + operation runbook for the metrics stack, mirroring the six-section operator-runbook style used elsewhere (overview / policy / apply / probe / evidence / caveat). Document, with exact commands (no secret values): (1) the operator-only one-time apply `kubectl apply -f k8s/staging/01-obs-rbac.yaml` (adds prometheus SA + ClusterRole — excluded from CI glob, Pitfall 2); (2) the pg_monitor role SQL run via `kubectl -n solid-stats-staging exec -it postgres-0 -- psql -U solid -d solid_stats` then `CREATE USER solid_monitor WITH PASSWORD ...; GRANT pg_monitor TO solid_monitor;` (non-superuser, MET-03/T-13-04; note pg_monitor is built-in since PG10, A5); the PG_MONITOR_PASSWORD value must equal the GitHub secret; (3) extracting the obs SA token `kubectl -n monitoring get secret obs-ci-deployer-token -o jsonpath='{.data.token}' | base64 -d` and adding it as the GitHub staging env secret K8S_OBS_TOKEN, plus GRAFANA_ADMIN_PASSWORD and PG_MONITOR_PASSWORD (Open Question 3); (4) the preflight `kubectl get storageclass` (confirm local-path default, Open Question 2) and `ssh root@89.223.124.200 df -h` to confirm disk for the 8Gi + 2Gi PVCs. Include an evidence table (blank by design — operator fills after running) and the caveat that secrets never enter git. Leave an explicit operator checklist with checkboxes.
  </action>
  <verify>
    <automated>test -f docs/observability.md && grep -q 'pg_monitor' docs/observability.md && grep -q '01-obs-rbac.yaml' docs/observability.md && grep -q 'K8S_OBS_TOKEN' docs/observability.md && ! grep -Eiq 'password[[:space:]]*[:=][[:space:]]*[^ ]' docs/observability.md</automated>
  </verify>
  <done>docs/observability.md documents the RBAC apply, the pg_monitor SQL, the token extraction + 3 GitHub secrets, and the storage/disk preflight, with an operator checklist and no secret values.</done>
  <acceptance_criteria>
    - Runbook names the exact RBAC apply, the CREATE USER + GRANT pg_monitor SQL, the token extraction command, and the storageclass/df preflight.
    - No literal secret value anywhere in the file.
    - Operator checklist with checkboxes present.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Operator bootstrap — apply RBAC, create pg_monitor role, set GitHub secrets, preflight</name>
  <action>Operator-only live + secret-store steps. See how-to-verify for the exact sequence; nothing here is auto-runnable by Claude.</action>
  <what-built>The bootstrap runbook (docs/observability.md), the Prometheus RBAC addition (k8s/staging/01-obs-rbac.yaml from 13-04), and the obs secret renderer (13-01) are all in place. The following steps touch LIVE cluster state and the operator-only GitHub secret store, so they cannot be automated by Claude.</what-built>
  <how-to-verify>
    Working through docs/observability.md, on the operator workstation (WireGuard up):
    1. Apply Prometheus RBAC: `kubectl apply -f k8s/staging/01-obs-rbac.yaml` — confirm `kubectl get clusterrole prometheus-monitoring` and `kubectl -n monitoring get sa prometheus` exist.
    2. Confirm RBAC trap is closed: `kubectl auth can-i create clusterroles --as=system:serviceaccount:monitoring:obs-ci-deployer` returns `no` (Pitfall 2 — obs-ci-deployer must NOT be able to; the ClusterRole is operator-applied).
    3. Create the pg_monitor role: exec into postgres-0 and run the CREATE USER + GRANT pg_monitor SQL with the chosen PG_MONITOR_PASSWORD. Confirm `\du solid_monitor` shows the role is NOT a superuser.
    4. Preflight: `kubectl get storageclass` shows a default (local-path); `ssh root@89.223.124.200 df -h` shows enough free disk for 8Gi + 2Gi PVCs (Open Question 2/3).
    5. Add three GitHub staging-environment secrets: K8S_OBS_TOKEN (from the obs-ci-deployer-token Secret), GRAFANA_ADMIN_PASSWORD, PG_MONITOR_PASSWORD (matching step 3). Confirm via the GitHub UI or `gh secret list --env staging`.
  </how-to-verify>
  <resume-signal>Type "approved" once RBAC is applied, the non-superuser solid_monitor role exists, storage/disk preflight passed, and all three GitHub secrets are set — or describe what blocked.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live PostgreSQL | CREATE USER / GRANT against the live app DB (read-only monitor role) |
| operator → live cluster RBAC | Prometheus cluster-read ClusterRole applied once |
| operator → GitHub secret store | Three new staging-environment secrets added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-16 | Elevation of Privilege | solid_monitor pg role | mitigate | Built-in pg_monitor role only (read pg_stat_* views); NOT superuser; verified via \du |
| T-13-17 | Information Disclosure | PG_MONITOR_PASSWORD / GRAFANA_ADMIN_PASSWORD | mitigate | Stored only in GitHub staging env secrets + live pg role; never in git or runbook |
| T-13-18 | Elevation of Privilege | obs-ci-deployer attempting cluster RBAC | mitigate | Verified `can-i create clusterroles` == no; ClusterRole applied by operator only |
| T-13-SC | Tampering | helm binary on operator workstation | mitigate | helm obtained from official get.helm.sh release in 13-02; render is build-time only, not run here |
</threat_model>

<verification>
- `kubectl get clusterrole prometheus-monitoring` + `kubectl -n monitoring get sa prometheus` exist.
- `kubectl auth can-i create clusterroles --as=system:serviceaccount:monitoring:obs-ci-deployer` == no.
- `\du solid_monitor` shows non-superuser with pg_monitor.
- `gh secret list --env staging` includes the three new secrets.
</verification>

<success_criteria>
The live cluster has the Prometheus cluster-read RBAC, the non-superuser pg_monitor role exists for postgres-exporter (MET-03), the three GitHub secrets are set (DEP-04 deploy path unblocked), and storage/disk headroom is confirmed — all recorded in docs/observability.md. The obs deploy workflow can now run.
</success_criteria>

<output>
Create `.planning/phases/13-deploy-pipeline-metrics-stack/13-05-SUMMARY.md` when done, recording the operator evidence (RBAC applied, role created non-superuser, secrets set, preflight numbers).
</output>
