---
phase: 13-deploy-pipeline-metrics-stack
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/observability/values/grafana-values.yaml
  - k8s/observability/50-grafana.yaml
  - k8s/observability/dashboards/node-exporter-full.json
  - k8s/observability/dashboards/kube-state-metrics.json
  - k8s/observability/dashboards/postgresql.json
  - k8s/observability/dashboards/rabbitmq-overview.json
  - k8s/observability/60-grafana-dashboards.yaml
autonomous: true
requirements: [DEP-01, MET-05, MET-06]
user_setup: []
must_haves:
  truths:
    - "Grafana is rendered from the pinned grafana/grafana chart with Prometheus provisioned as the default datasource (http://prometheus-server.monitoring.svc:80)"
    - "Grafana admin password is sourced from the operator-rendered grafana-secrets (adminExistingSecret) — never inline in YAML"
    - "Grafana dashboard sidecar is enabled and watches ConfigMaps labelled grafana_dashboard"
    - "Four standard dashboard JSONs are vendored as files and wrapped one-per-ConfigMap (1 MiB limit) labelled grafana_dashboard"
    - "Grafana pod sets priorityClassName obs-background, namespace monitoring, securityContext fsGroup 472, tight resources"
  artifacts:
    - path: "k8s/observability/50-grafana.yaml"
      provides: "Grafana Deployment + Service + datasource/sidecar provisioning (MET-05)"
      contains: "obs-background"
    - path: "k8s/observability/60-grafana-dashboards.yaml"
      provides: "4 dashboard ConfigMaps (one per JSON), labelled grafana_dashboard (MET-06)"
    - path: "k8s/observability/dashboards/node-exporter-full.json"
      provides: "Node Exporter Full dashboard (grafana.com 1860)"
  key_links:
    - from: "k8s/observability/50-grafana.yaml"
      to: "k8s/observability/10-prometheus.yaml (prometheus-server svc)"
      via: "provisioned datasource url http://prometheus-server.monitoring.svc:80"
      pattern: "prometheus-server.monitoring.svc"
    - from: "k8s/observability/60-grafana-dashboards.yaml"
      to: "k8s/observability/50-grafana.yaml (sidecar)"
      via: "ConfigMap label grafana_dashboard discovered by sidecar"
      pattern: "grafana_dashboard"
    - from: "k8s/observability/50-grafana.yaml"
      to: "grafana-secrets (rendered by render-obs-secrets.py in 13-01)"
      via: "adminExistingSecret grafana-secrets, adminPasswordKey admin-password"
      pattern: "grafana-secrets"
---

<objective>
Render Grafana from the pinned chart with Prometheus as a provisioned datasource and the dashboard sidecar enabled, then vendor four standard dashboard JSONs as one-ConfigMap-per-dashboard. Delivers MET-05 (healthy provisioned datasource) and MET-06 (dashboards provisioned as code).

Purpose: Grafana must render live metrics with zero UI/manual import — datasource and dashboards are declarative and auditable in git. The admin password and cookie secret are pinned to the operator-rendered grafana-secrets (Pitfall 9) so deploys are idempotent and no credential is generated by helm.

Output: `grafana-values.yaml`, rendered `50-grafana.yaml`, 4 vendored dashboard JSONs under `dashboards/`, and `60-grafana-dashboards.yaml` (4 ConfigMaps).
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-deploy-pipeline-metrics-stack/13-RESEARCH.md
@k8s/staging/02-priority-classes.yaml
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author grafana-values.yaml + render 50-grafana.yaml (MET-05)</name>
  <files>k8s/observability/values/grafana-values.yaml, k8s/observability/50-grafana.yaml</files>
  <read_first>.planning/phases/13-deploy-pipeline-metrics-stack/13-RESEARCH.md</read_first>
  <action>
    Use the helm binary obtained in 13-02 (re-download the official helm v3 linux-amd64 release to a writable PATH dir if not present; never npx). Run `helm show values grafana/grafana --version 10.5.15` to confirm exact field names before authoring (A6 — verify appVersion). Author grafana-values.yaml per 13-RESEARCH § Code Examples: adminExistingSecret grafana-secrets, adminUser admin, adminPasswordKey admin-password (Pitfall 9 — never let helm generate credentials); persistence.enabled true, size 2Gi; priorityClassName obs-background; securityContext runAsUser/runAsGroup/fsGroup 472 (Pitfall 4 — SQLite write perms); resources 128Mi/256Mi mem + 50m/200m cpu; sidecar.dashboards.enabled true, label grafana_dashboard, labelValue "1"; datasources provisioning a single Prometheus datasource url http://prometheus-server.monitoring.svc:80, access proxy, isDefault true, jsonData.timeInterval 30s; service ClusterIP port 80 targetPort 3000. If the chart generates a cookie/secret-key, pin it via an existingSecret too (Pitfall 9). Set fullnameOverride so the Service is named exactly `grafana` (matches the rollout-status + port-forward in 13-06). Then render: helm template grafana grafana/grafana --version 10.5.15 --namespace monitoring --values k8s/observability/values/grafana-values.yaml > k8s/observability/50-grafana.yaml. Verify the rendered file has namespace monitoring everywhere, obs-background on the Grafana pod, the datasource url, adminExistingSecret referenced, and NO admin password literal.
  </action>
  <verify>
    <automated>python3 scripts/validate-obs-manifests.py && grep -q 'prometheus-server.monitoring.svc' k8s/observability/50-grafana.yaml && grep -q 'grafana-secrets' k8s/observability/50-grafana.yaml && grep -q 'grafana_dashboard' k8s/observability/50-grafana.yaml</automated>
  </verify>
  <done>grafana-values.yaml + rendered 50-grafana.yaml exist; static gate passes; datasource points at prometheus-server, admin password from grafana-secrets, sidecar label grafana_dashboard present, no password literal.</done>
  <acceptance_criteria>
    - validate-obs-manifests.py exits 0 against 50-grafana.yaml.
    - Rendered file: datasource url `http://prometheus-server.monitoring.svc:80`, `adminExistingSecret: grafana-secrets`, `priorityClassName: obs-background`, fsGroup 472.
    - No admin password literal anywhere in the file.
    - Service named `grafana`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Vendor 4 dashboard JSONs + wrap as ConfigMaps (MET-06)</name>
  <files>k8s/observability/dashboards/node-exporter-full.json, k8s/observability/dashboards/kube-state-metrics.json, k8s/observability/dashboards/postgresql.json, k8s/observability/dashboards/rabbitmq-overview.json, k8s/observability/60-grafana-dashboards.yaml</files>
  <read_first>.planning/phases/13-deploy-pipeline-metrics-stack/13-RESEARCH.md</read_first>
  <action>
    Download four standard dashboard JSONs from grafana.com and commit them under k8s/observability/dashboards/: Node Exporter Full (ID 1860 → node-exporter-full.json); a kube-state-metrics / Kubernetes cluster dashboard (verify the correct current ID per A7 — research suggests 13332 but CONFIRM at grafana.com before committing → kube-state-metrics.json); PostgreSQL Database (ID 9628 → postgresql.json); RabbitMQ Overview (ID 10991, the official rabbitmq-prometheus repo JSON → rabbitmq-overview.json). Do NOT import at runtime — vendor the files (Pitfall 8). Each dashboard's `__inputs`/datasource references must resolve to the provisioned Prometheus datasource (set the datasource template var default to the Prometheus datasource name, or rely on the sidecar's default-datasource substitution). Then author 60-grafana-dashboards.yaml as a multi-document YAML, ONE ConfigMap per dashboard (Pitfall 8 — 1 MiB object limit, do NOT bundle), each in namespace monitoring, labelled `grafana_dashboard: "1"` plus `app.kubernetes.io/part-of: solid-stats`, with the JSON file content under a `.data` key named after the file. Verify each rendered ConfigMap is < 1 MiB. The sidecar (enabled in Task 1) auto-discovers them.
  </action>
  <verify>
    <automated>test -f k8s/observability/dashboards/node-exporter-full.json && test -f k8s/observability/dashboards/postgresql.json && test -f k8s/observability/dashboards/rabbitmq-overview.json && test -f k8s/observability/dashboards/kube-state-metrics.json && python3 -c "import json,glob,sys; [json.load(open(f)) for f in glob.glob('k8s/observability/dashboards/*.json')]" && python3 scripts/validate-obs-manifests.py && test $(grep -c 'kind: ConfigMap' k8s/observability/60-grafana-dashboards.yaml) -ge 4</automated>
  </verify>
  <done>Four valid dashboard JSONs vendored; 60-grafana-dashboards.yaml has >=4 ConfigMaps each labelled grafana_dashboard, namespace monitoring, each < 1 MiB; static gate passes.</done>
  <acceptance_criteria>
    - All 4 JSON files parse as valid JSON.
    - 60-grafana-dashboards.yaml contains >=4 separate ConfigMap documents, each labelled `grafana_dashboard: "1"`.
    - validate-obs-manifests.py exits 0 (namespace monitoring; no secret values).
    - No single ConfigMap exceeds 1 MiB.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/browser → Grafana | Admin login; internal access via port-forward in Phase 13 (no public edge) |
| Grafana → Prometheus | Provisioned datasource read over ClusterIP |
| rendered YAML → git | Grafana manifest + dashboard ConfigMaps committed; no secret values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Information Disclosure | Grafana admin password in git | mitigate | adminExistingSecret grafana-secrets; password rendered from env by render-obs-secrets.py; never in YAML |
| T-13-09 | Spoofing/Session | Grafana cookie secret regen on deploy | mitigate | Pin cookie/secret-key via existingSecret (Pitfall 9) so sessions survive redeploy |
| T-13-10 | Information Disclosure | Grafana exposed externally | accept | ClusterIP only, no Ingress in Phase 13; TLS + public edge deferred to Phase 14 |
| T-13-11 | Tampering | dashboard JSON provenance | mitigate | JSONs vendored from grafana.com official dashboard IDs, committed + reviewable in PR diff |
| T-13-SC | Tampering | grafana helm chart provenance | mitigate | Chart pinned v10.5.15 (audited OK in 13-RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `python3 scripts/validate-obs-manifests.py` passes against 50-grafana.yaml + 60-grafana-dashboards.yaml.
- 50-grafana.yaml: datasource → prometheus-server, adminExistingSecret grafana-secrets, sidecar label grafana_dashboard, fsGroup 472, no password literal.
- 4 dashboard JSONs parse; >=4 ConfigMaps labelled grafana_dashboard.
</verification>

<success_criteria>
DEP-01 extended (Grafana + dashboards rendered/committed). MET-05 (Grafana with Prometheus provisioned datasource) and MET-06 (4 standard dashboards provisioned as code) manifests authored and passing the static gate. Live datasource-health and dashboard-render confirmation happens in Wave 2 (13-06).
</success_criteria>

<output>
Create `.planning/phases/13-deploy-pipeline-metrics-stack/13-03-SUMMARY.md` when done. Record the grafana render command, chart version, and the confirmed dashboard IDs used.
</output>
