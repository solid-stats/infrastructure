---
phase: 13-deploy-pipeline-metrics-stack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/render-obs-secrets.py
  - scripts/validate-obs-manifests.py
  - scripts/validate-phase-13.sh
autonomous: true
requirements: [DEP-04]
must_haves:
  truths:
    - "render-obs-secrets.py emits grafana-secrets + postgres-monitor-secret Secret YAML from env, never with values in git"
    - "render-obs-secrets.py exits 64 when GRAFANA_ADMIN_PASSWORD or PG_MONITOR_PASSWORD is missing"
    - "validate-obs-manifests.py fails when any rendered obs manifest contains a secret value, is not namespace monitoring, or lacks priorityClassName obs-background on a pod spec"
    - "validate-phase-13.sh asserts all MET-01..06 live checks and exits 1 on first failure"
  artifacts:
    - path: "scripts/render-obs-secrets.py"
      provides: "Renders obs Secrets from GitHub env into k8s Secret YAML (DEP-04)"
      min_lines: 40
    - path: "scripts/validate-obs-manifests.py"
      provides: "Static gate: no secret values, namespace=monitoring, obs-background on pod specs"
      min_lines: 40
    - path: "scripts/validate-phase-13.sh"
      provides: "Live MET-01..06 assertion harness"
      min_lines: 60
  key_links:
    - from: "scripts/render-obs-secrets.py"
      to: "scripts/render-staging-secrets.py"
      via: "mirrors required()/secret()/exit-64 pattern, NAMESPACE=monitoring"
      pattern: "def secret\\("
---

<objective>
Create the Phase 13 validation scaffold and obs secret renderer FIRST, so every later authoring plan has a static gate to commit against and the live wave has its assertion harness ready. This is the Wave 0 gap-closure work named in 13-VALIDATION.md and 13-RESEARCH.md § Validation Architecture.

Purpose: The Nyquist rule requires every `<verify>` in later plans to call an automated check that already exists. This plan creates `validate-obs-manifests.py` (static, runs in CI validate job + per-commit) and `validate-phase-13.sh` (live, runs per-wave-merge), plus `render-obs-secrets.py` which produces the Grafana admin + pg_monitor DSN Secrets from env (DEP-04 — no secret values in git).

Output: `scripts/render-obs-secrets.py`, `scripts/validate-obs-manifests.py`, `scripts/validate-phase-13.sh`.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-deploy-pipeline-metrics-stack/13-RESEARCH.md
@.planning/phases/13-deploy-pipeline-metrics-stack/13-VALIDATION.md

# Patterns to mirror exactly:
@scripts/render-staging-secrets.py
@scripts/validate-phase-12.sh
@scripts/validate-staging.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write render-obs-secrets.py (DEP-04)</name>
  <files>scripts/render-obs-secrets.py</files>
  <read_first>scripts/render-staging-secrets.py</read_first>
  <action>
    Create scripts/render-obs-secrets.py with `#!/usr/bin/env python3`, stdlib only, mirroring the structure of render-staging-secrets.py exactly: a `required(name)` helper that appends to a `missing` list, a `secret(name, values, secret_type="Opaque")` helper emitting a k8s Secret with `stringData`, and a final `print("\n---\n".join(documents))`. Set `NAMESPACE = "monitoring"` (obs secrets land in monitoring). Read GRAFANA_ADMIN_PASSWORD and PG_MONITOR_PASSWORD via `required()`; if `missing` is non-empty, print to stderr and `sys.exit(64)`. Build the pg DSN exactly as research § Code Examples: `postgresql://solid_monitor:{quote(pg_monitor_password, safe='')}@postgres.solid-stats-staging.svc:5432/solid_stats?sslmode=disable`. Emit two Secrets: `grafana-secrets` with key `admin-password` (consumed by Grafana chart `adminExistingSecret`/`adminPasswordKey` per 13-03), and `postgres-monitor-secret` with key `dsn` (consumed by postgres-exporter per 13-02, DATA_SOURCE_NAME). No secret values literal anywhere in the file — only env reads. Implements DEP-04.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/render-obs-secrets.py').read())" && ! GRAFANA_ADMIN_PASSWORD= PG_MONITOR_PASSWORD= python3 scripts/render-obs-secrets.py 2>/dev/null; test $? -eq 0 && echo "exits nonzero when unset OK"</automated>
  </verify>
  <done>Script parses; with both env vars set it prints two Secret documents (grafana-secrets, postgres-monitor-secret) joined by `---`; with either unset it exits 64.</done>
  <acceptance_criteria>
    - With GRAFANA_ADMIN_PASSWORD and PG_MONITOR_PASSWORD set, stdout contains `name: grafana-secrets`, `name: postgres-monitor-secret`, `namespace: monitoring`, key `admin-password`, key `dsn`.
    - With either var unset: exit code 64, stderr names the missing var.
    - No hardcoded password string appears in the file (only env reads + the DSN template).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write validate-obs-manifests.py (static DEP-04 gate)</name>
  <files>scripts/validate-obs-manifests.py</files>
  <read_first>scripts/validate-staging.py</read_first>
  <action>
    Create scripts/validate-obs-manifests.py, stdlib only, that scans every `*.yaml` under `k8s/observability/` (recursively including `values/` and `dashboards/`-derived ConfigMaps under top-level, but the rendered top-level manifests are the gate target). Three checks, exit 1 with a clear message on any failure: (1) NO secret values — grep each rendered manifest line by line for a `kind: Secret` document with a populated `stringData`/`data` value, and fail if found (rendered obs manifests must never carry secret literals; secrets come only from render-obs-secrets.py at deploy time). Also flag any line matching obvious credential patterns (a long base64 blob under a `data:` key, or the words admin-password/PASSWORD/dsn followed by a non-placeholder value). (2) NAMESPACE — every namespaced resource in the rendered top-level manifests declares `namespace: monitoring` (fail on any other namespace value). (3) PRIORITYCLASS — every pod-bearing spec (Deployment/StatefulSet/DaemonSet `template.spec`) sets `priorityClassName: obs-background`. Use a tolerant line/section scan (no PyYAML dependency — match the stdlib-only style of validate-staging.py); if PyYAML is importable, prefer it, else fall back to a documented line scan. Print `=== obs manifest validation PASSED ===` on success. The phrase the grep looks for as a forbidden token must be defined as a variable, not echoed in a head comment, to keep the gate self-consistent.
    <!-- planner-discipline-allow: obs-background -->
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/validate-obs-manifests.py').read())" && python3 scripts/validate-obs-manifests.py; echo "exit=$?"</automated>
  </verify>
  <done>Script parses and runs. With no k8s/observability/*.yaml yet, it exits 0 (nothing to fail) OR exits with a clear "no manifests found" note — choose exit 0 so it is safe to run before 13-02/03 land, but fail loudly once manifests exist and violate a rule.</done>
  <acceptance_criteria>
    - Running against an empty/absent k8s/observability returns success (exit 0) with a note.
    - A crafted manifest with `namespace: default` makes it exit 1.
    - A crafted Deployment template missing priorityClassName makes it exit 1.
    - A crafted Secret with a populated stringData value makes it exit 1.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Write validate-phase-13.sh (live MET-01..06 harness)</name>
  <files>scripts/validate-phase-13.sh</files>
  <read_first>scripts/validate-phase-12.sh</read_first>
  <action>
    Create scripts/validate-phase-13.sh with `#!/usr/bin/env bash` + `set -euo pipefail`, mirroring validate-phase-12.sh: an `assert label actual expected` helper that exits 1 on mismatch, a `--quick` flag (skip the slower Grafana port-forward checks). namespace defaults `monitoring`. Wire EVERY check from 13-VALIDATION.md Per-Requirement map and 13-RESEARCH.md § Validation Architecture: MET-01 — `kubectl -n monitoring get pod -l app.kubernetes.io/name=prometheus -o jsonpath` phase==Running, and `kubectl -n monitoring exec deploy/prometheus-server -- wget -qO- localhost:9090/api/v1/status/config` contains `15d`. MET-02/03/04 — query `localhost:9090/api/v1/targets` via exec+wget, parse activeTargets with a stdlib python3 one-liner, assert health==up for jobs matching kube-state-metrics, node-exporter, postgres-exporter, rabbitmq; additionally assert `pg_up==1` (MET-03) and presence of `rabbitmq_identity_info` (MET-04) via `/api/v1/query`. MET-05 — port-forward `svc/grafana 3000:80`, curl `-u admin:$GRAFANA_ADMIN_PASSWORD localhost:3000/api/datasources/1/health` status==OK (skip under `--quick`). MET-06 — curl `/api/search?query=` returns >=4 dashboards (skip under `--quick`). Use the `|| true` capture-then-assert pattern from phase-12 for `kubectl auth`-style commands. Print `=== Phase 13 validation PASSED ===` at the end. Manual-only data-render check (MET-06 panels non-zero) is printed as an operator note, not asserted.
  </action>
  <verify>
    <automated>bash -n scripts/validate-phase-13.sh && grep -q 'MET-0' scripts/validate-phase-13.sh && grep -q 'set -euo pipefail' scripts/validate-phase-13.sh && echo "syntax+markers OK"</automated>
  </verify>
  <done>`bash -n` passes; script contains assertions tagged MET-01 through MET-06 and the `assert` helper; `--quick` short-circuits the Grafana checks.</done>
  <acceptance_criteria>
    - `bash -n scripts/validate-phase-13.sh` exits 0.
    - Grep confirms an assertion line per MET-01..06.
    - `--quick` documented and gates the port-forward section.
    - Manual panel-data note printed for MET-06.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub env secrets → render script stdout | Grafana admin password + pg_monitor DSN cross from CI env into Secret YAML; must never be written to git or logs |
| repo (git) → committed YAML | Rendered obs manifests are committed; secret values must NOT be among them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | render-obs-secrets.py output | mitigate | Values read only from env; piped to a mktemp file in CI with trap-rm; never committed |
| T-13-02 | Information Disclosure | committed k8s/observability/*.yaml | mitigate | validate-obs-manifests.py greps for secret values + base64 blobs and fails CI validate job |
| T-13-03 | Tampering | validation harness self-consistency | mitigate | Forbidden-token strings stored as variables, not echoed in head comments (comment-text discipline) |
| T-13-SC | Tampering | python/pip installs | accept | No package installs in this plan — stdlib only |
</threat_model>

<verification>
- `python3 scripts/render-obs-secrets.py` with both env vars set emits valid Secret YAML; unset → exit 64.
- `python3 scripts/validate-obs-manifests.py` runs clean on empty obs dir; fails on crafted violations.
- `bash -n scripts/validate-phase-13.sh` passes; MET-01..06 markers present.
</verification>

<success_criteria>
All three scripts exist, parse/lint clean, follow the existing render-staging-secrets.py / validate-phase-12.sh conventions, and provide the static + live gates the rest of Phase 13 commits against. DEP-04 renderer in place (no secret values in git).
</success_criteria>

<output>
Create `.planning/phases/13-deploy-pipeline-metrics-stack/13-01-SUMMARY.md` when done.
</output>
