---
phase: 15-log-stack
plan: "04"
type: execute
wave: 2
depends_on: ["15-01", "15-02", "15-03"]
files_modified:
  - k8s/observability/values/loki-values.yaml
  - k8s/observability/70-loki.yaml
  - k8s/observability/values/alloy-values.yaml
  - k8s/observability/80-alloy.yaml
  - .github/workflows/deploy-observability.yml
autonomous: false
requirements: [LOG-01, LOG-02, LOG-03]
user_setup: []

must_haves:
  truths:
    - "Alloy ClusterRole/ClusterRoleBinding are applied to the live cluster (operator bootstrap)"
    - "Loki and Alloy are Running in monitoring; Loki PVC is Bound"
    - "loki_boltdb_shipper_compactor_running == 1 and loki_write_sent_entries_total > 0 in live Prometheus"
    - "Loki is a healthy Grafana datasource and a LogQL query returns recent server-2 lines"
    - "validate-phase-15.sh passes against the live cluster"
  artifacts:
    - path: ".github/workflows/deploy-observability.yml"
      provides: "rollout verification extended with loki statefulset + alloy daemonset"
      contains: "loki"
  key_links:
    - from: "live monitoring namespace"
      to: "validate-phase-15.sh assertions"
      via: "LOG-01/02/03 live checks pass with corrected metric names"
      pattern: "Phase 15 validation PASSED"
---

<objective>
Bring the log stack live: operator-apply the Alloy ClusterRole bootstrap, deploy the rendered Loki + Alloy manifests + the re-rendered Prometheus/Grafana through the obs deploy path, then prove LOG-01/02/03 with validate-phase-15.sh and right-size from live usage.

Purpose: This is the only operator-impacting wave (cluster RBAC bootstrap + rolling apply against live staging). The agent runs it with cluster access. It closes the phase: Loki Running with retention, Alloy shipping conservatively, Loki queryable in Grafana, all proven by the live harness.
Output: live Loki + Alloy in monitoring; deploy-observability.yml rollout checks extended; validate-phase-15.sh green; right-sized resources if live usage demands.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/15-log-stack/15-RESEARCH.md
@.planning/phases/15-log-stack/15-VALIDATION.md

# the live-apply pattern + defects-found-during-apply to anticipate
@.planning/phases/13-deploy-pipeline-metrics-stack/13-06-SUMMARY.md
@.github/workflows/deploy-observability.yml
@.agents/skills/kubernetes-specialist/SKILL.md

# produced by Wave 1 — apply targets and the harness
@k8s/staging/03-alloy-rbac.yaml
@scripts/validate-phase-15.sh
</context>

<read_first>
- 13-06-SUMMARY.md — the incremental live-apply approach (no-secret tier first), `--server-side --force-conflicts` apply, and the classes of defect that surfaced live (corrupt render, missing keys, sslmode). Loki/Alloy carry no secrets, so apply is simpler, but expect first-boot timing: compactor gauge and Alloy entries counter need a minute or two before they read non-zero.
- deploy-observability.yml — the WireGuard + obs-ci-deployer kubeconfig + `find k8s/observability -maxdepth 1 -name '*.yaml'` apply glob + the "Verify rollouts" step (extend with loki statefulset + alloy daemonset).
- 15-RESEARCH § "Validation Architecture" and Pitfall 3 (replication_factor) / Pitfall 5 (index.period) / Pitfall 7 (self-loop) — the live failure signatures to watch for in Loki/Alloy logs.
- k8s/staging/03-alloy-rbac.yaml — the file to operator-apply ONCE (CI cannot create ClusterRole).
</read_first>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm live obs deploy of the log stack</name>
  <action>
This is a live, operator-impacting deploy against staging k3s (cluster access via WireGuard; node is RAM-bound). Wave 1 authored: loki-values + 70-loki.yaml (SingleBinary/filesystem/168h retention), alloy-values + 80-alloy.yaml (conservative 5-label DaemonSet), 03-alloy-rbac.yaml (operator ClusterRole), prometheus loki+alloy scrape targets, grafana Loki datasource, validate-phase-15.sh. Before proceeding to the live apply tasks, confirm with the operator: (1) WireGuard tunnel up and `kubectl get nodes` reaches the staging cluster; (2) node headroom still adequate (`kubectl top nodes`) for ~192Mi additional requests (Loki 128Mi + Alloy 64Mi); (3) operator accepts a rolling apply of new workloads into the monitoring namespace.
  </action>
  <what-built>Wave 1 authored: loki-values + 70-loki.yaml (SingleBinary/filesystem/168h retention), alloy-values + 80-alloy.yaml (conservative 5-label DaemonSet), 03-alloy-rbac.yaml (operator ClusterRole), prometheus loki+alloy scrape targets, grafana Loki datasource, validate-phase-15.sh. This task applies them to LIVE staging.</what-built>
  <how-to-verify>
This is a live, operator-impacting deploy against staging k3s (cluster access via WireGuard; node is RAM-bound). Confirm before proceeding:
1. WireGuard tunnel up and `kubectl get nodes` reaches the staging cluster.
2. Node headroom still adequate (`kubectl top nodes`) for ~192Mi additional requests (Loki 128Mi + Alloy 64Mi).
3. You accept a rolling apply of new workloads into the monitoring namespace.
  </how-to-verify>
  <verify><human-check>Operator confirms tunnel up, headroom adequate, and accepts the rolling apply.</human-check></verify>
  <done>Operator approved the live apply; preconditions confirmed.</done>
  <resume-signal>Type "approved" to run the live apply + validation, or describe blockers.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Operator-apply 03-alloy-rbac.yaml + extend deploy-observability.yml rollout checks</name>
  <files>.github/workflows/deploy-observability.yml</files>
  <action>
Operator-apply the Alloy ClusterRole bootstrap ONCE (obs-ci-deployer is namespace-scoped and cannot create cluster RBAC — Pitfall 4): `kubectl apply -f k8s/staging/03-alloy-rbac.yaml` using the operator/cluster-admin context (NOT obs-ci-deployer). Confirm `kubectl get clusterrole alloy` and `kubectl get clusterrolebinding alloy` exist. Then extend the "Verify rollouts" step in deploy-observability.yml to add `kubectl -n monitoring rollout status statefulset/loki --timeout=300s` and `kubectl -n monitoring rollout status daemonset/alloy --timeout=180s` alongside the existing prometheus/grafana/kube-state/node-exporter/postgres-exporter checks. Do NOT add 03-alloy-rbac.yaml to any CI apply glob — it stays operator-only like 01-obs-rbac.yaml. The CD apply glob is `find k8s/observability -maxdepth 1` so k8s/staging/ files are already excluded; confirm no glob change pulls in the bootstrap file.
  </action>
  <verify>
    <automated>kubectl get clusterrole alloy >/dev/null 2>&1 && kubectl get clusterrolebinding alloy >/dev/null 2>&1 && grep -q 'statefulset/loki' .github/workflows/deploy-observability.yml && grep -q 'daemonset/alloy' .github/workflows/deploy-observability.yml</automated>
  </verify>
  <done>alloy ClusterRole + ClusterRoleBinding exist on the live cluster; deploy-observability.yml verifies loki + alloy rollouts; bootstrap file stays out of the CI glob.</done>
</task>

<task type="auto">
  <name>Task 2: Live-apply Loki + Alloy + re-rendered Prometheus/Grafana, then validate LOG-01/02/03</name>
  <files>k8s/observability/70-loki.yaml, k8s/observability/80-alloy.yaml</files>
  <action>
Apply the obs manifests to the live cluster the same way deploy-observability.yml does — `kubectl apply --server-side --force-conflicts -n monitoring` over the `find k8s/observability -maxdepth 1 -name '*.yaml' | sort` glob (this re-applies 10-prometheus.yaml + 50-grafana.yaml with the new scrape targets/datasource and applies 70-loki.yaml + 80-alloy.yaml). Wait for `rollout status statefulset/loki` and `rollout status daemonset/alloy`, plus a restart/reload of prometheus + grafana so the new scrape config and datasource take effect. Watch for the known live failure signatures: Loki push failing with "too many unhealthy instances in the ring" (replication_factor not 1 — Pitfall 3), Alloy "context deadline exceeded" pushing to Loki, or a runaway loki_write_sent_entries_total (monitoring-namespace self-loop not dropped — Pitfall 7). Give the compactor gauge and Alloy counter ~1-2 min to read non-zero. Then run `bash scripts/validate-phase-15.sh` (set GRAFANA_ADMIN_PASSWORD; use `--quick` only if port-forward is unavailable) — it must PASS: Loki Running + PVC Bound + compactor gauge==1 + retention 168h in /config (LOG-01); Alloy DaemonSet ready + loki_write_sent_entries_total>0 (LOG-02); Loki datasource health OK + LogQL `{namespace="solid-stats-staging",app=~"server-2.*"}` returns >=1 entry (LOG-03). Record any gap-closure fixes (re-render + re-apply) made during the live run in the SUMMARY, mirroring 13-06.
  </action>
  <verify>
    <automated>bash scripts/validate-phase-15.sh</automated>
  </verify>
  <done>validate-phase-15.sh prints "Phase 15 validation PASSED": Loki Running with 168h retention + compactor active, Alloy shipping conservatively, Loki a healthy Grafana datasource returning server-2 LogQL lines.</done>
</task>

<task type="auto">
  <name>Task 3: Right-size Loki + Alloy from live usage</name>
  <files>k8s/observability/values/loki-values.yaml, k8s/observability/values/alloy-values.yaml, k8s/observability/70-loki.yaml, k8s/observability/80-alloy.yaml</files>
  <action>
After the stack has run a few minutes, capture `kubectl top pods -n monitoring` and compare Loki/Alloy actual usage against the [ASSUMED] requests/limits (Loki 128Mi/256Mi, Alloy 64Mi/128Mi — 15-RESEARCH § Resource Sizing, A1/A2). If actual working set is comfortably within limits (as Phase 13 found, 13-06 decision), record "ASSUMED values confirmed adequate; no re-render" in the SUMMARY and make no change. ONLY if a pod is near/over its limit or evicted, raise the limit in the relevant values file and re-render that one manifest (`helm template ... > 70-loki.yaml` or `... > 80-alloy.yaml`, never `2>&1`), re-apply, and re-run validate-phase-15.sh. Do not pre-emptively inflate resources — match observed usage with headroom, per the RAM-bound-node constraint.
  </action>
  <verify>
    <automated>python3 scripts/validate-obs-manifests.py && bash scripts/validate-phase-15.sh --quick</automated>
  </verify>
  <done>Loki + Alloy resources reflect live usage (confirmed-adequate or raised-and-re-rendered); static gate green; validate-phase-15.sh still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → cluster RBAC apply | ClusterRole created with cluster-admin context, not from CI |
| obs-ci-deployer / agent → monitoring ns apply | namespace-scoped workload apply (no cluster RBAC) |
| Alloy → Loki push (live) | live verification that only the 5-label set crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Elevation of Privilege | live ClusterRole bootstrap | mitigate | applied once by operator with cluster-admin; read-only Alloy ClusterRole (from 15-02); never re-applied from CI; stays out of CD glob |
| T-15-11 | Denial of Service | RAM-bound node under new pods | mitigate | obs-background priority (evicted before app-critical); tight requests/limits; right-size task confirms live usage within headroom |
| T-15-05 | Information Disclosure | live label set leakage | mitigate | validate-phase-15.sh + manual Grafana Explore spot-check confirm stream labels ⊆ {namespace,pod,container,app,job} (LOG-02) |
| T-15-SC | Tampering | live apply of helm-rendered manifests | mitigate | manifests passed validate-obs-manifests.py (no secrets, no ClusterRole, no render splice) before apply; server-side apply, pinned images |
</threat_model>

<verification>
- `kubectl get clusterrole alloy` / `clusterrolebinding alloy` present.
- `kubectl -n monitoring get pod -l app.kubernetes.io/name=loki` Running; Loki PVC Bound; alloy DaemonSet ready.
- `bash scripts/validate-phase-15.sh` PASSES (LOG-01/02/03 with corrected metric names).
- `kubectl top pods -n monitoring` shows Loki/Alloy within limits.
</verification>

<success_criteria>
The log stack is live and proven: Loki collects cluster logs in SingleBinary/filesystem mode with ~7-day compactor retention, Alloy ships them conservatively (5 labels only), Loki is a healthy second Grafana datasource returning recent server-2 LogQL lines, and validate-phase-15.sh is green — all three LOG requirements satisfied on live staging.
</success_criteria>

<output>
Create `.planning/phases/15-log-stack/15-04-SUMMARY.md` when done.
</output>
