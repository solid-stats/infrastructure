# v3.0 Autonomous Run — Resume Notes

Cross-cutting state for resuming the `/gsd:autonomous` run of milestone **v3.0 Staging Observability
Stack** (phases 12–18) across a `/clear`. Authoritative status: `.planning/STATE.md` + `ROADMAP.md` +
per-phase `SUMMARY.md`/`VERIFICATION.md`. This file captures facts not obvious from one doc.

## Progress (as of 2026-06-14)

- ✅ **12** Resource Protection · ✅ **13** Metrics Stack · ✅ **14** Public Edge/TLS · ✅ **15** Log Stack — all live on staging.
- ◆ **16** GlitchTip: Wave 1 authored (16-01/02/03 committed). Pending: **16-04** (live deploy + forced-error test, autonomous) and **16-05** (errors. public cutover — now UNBLOCKED, see below).
- ⬜ **17** Network Isolation + stack-validation script (internal, autonomous). ⬜ **18** App-side Sentry SDK — PRs in *other* repos (server-2, replay-parser-2, replays-fetcher), cross-repo.

## Operator authorizations (durable for this run)

- Full carte-blanche + explicit SSH to `root@89.223.124.200` for live staging ops (apply manifests, swap, rolling restarts, certbot). `.claude/settings.local.json` allows `Bash(ssh root@89.223.124.200 *)`.
- Remote shell is **fish** — for bash heredocs use `ssh root@… 'bash -s' <<'EOF'`. Local Bash tool is bash.
- The user is relaxed about staging breakage ("не страшно если уроним").

## Public edge / DNS

- Public hostnames are **apex**: `grafana.solid-stats.ru` + `errors.solid-stats.ru` (matching auth./relay.), NOT the planner's longer `*.stats-staging.solid-stats.ru`. All config renamed to match.
- `grafana.solid-stats.ru` live over HTTPS (proxies in-cluster Grafana). `errors.solid-stats.ru` = valid LE cert + **503 placeholder** — its GlitchTip upstream swap is **16-05**, now doable: re-run `bootstrap-obs-edge.sh DOMAIN=errors.solid-stats.ru UPSTREAM=<glitchtip-web ClusterIP>:8000` on the VPS after GlitchTip is up. certbot email: `sanek9982@gmail.com`.
- vhost filenames still say `*-stats-staging-*` (internal artifact); server_name/cert paths use the short hosts.

## Secrets

- Live k8s Secrets created directly (values generated, never in git). GitHub `staging`-env secrets set: `K8S_OBS_TOKEN`, `K8S_OBS_ET_TOKEN`, `GRAFANA_ADMIN_PASSWORD`, `PG_MONITOR_PASSWORD` (+ pre-existing CA/WG/K8S_TOKEN/GHCR).
- **Still missing in GitHub:** the 4 `GLITCHTIP_*` secrets — set them when 16-04 deploys GlitchTip (generate, create k8s Secrets, then `gh secret set … --env staging`).

## Obs deploy pattern (reuse for 16/17)

- helm template (`~/.local/bin/helm`) → committed `k8s/observability/*.yaml` (no operator/CRDs). Render stdout only (stderr separate — a spliced helm error corrupts the manifest; `validate-obs-manifests.py` now catches it).
- `validate-obs-manifests.py` static gate: namespace ∈ {monitoring, error-tracking}, obs-background, no secret values, no ClusterRole in the obs dir. Cluster RBAC lives in operator-bootstrap `k8s/staging/01-obs-rbac.yaml` / `03-alloy-rbac.yaml` (excluded from CI glob, applied as admin).
- `render-obs-secrets.py` renders monitoring + error-tracking Secrets from env. Apply large/dashboard manifests with `kubectl apply --server-side --force-conflicts`.
- Node is tight (~2.5Gi headroom) — every obs workload: tight limits + `priorityClassName: obs-background`.

## Cross-repo mailbox

`SolidGames/plans/product/PARITY-COORDINATION.md` (outside this repo) — a parity run is in progress; infra owns `k8s/staging/50-replays-fetcher.yaml` (F4 live tuning baked in — don't clobber). A persistent Monitor watches it for @infra notes; reply there directly.
