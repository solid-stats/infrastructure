---
phase: 14-public-edge-grafana-tls
plan: 04
type: execute
wave: 3
depends_on: [14-01, 14-02, 14-03]
files_modified: []
autonomous: false
requirements: [EDGE-01, EDGE-02, EDGE-03, MET-07]
user_setup:
  - service: dns-registrar
    why: "EDGE-01 — the agent has no DNS API; both obs A records are operator/registrar-controlled"
    dashboard_config:
      - task: "Create A record grafana.solid-stats.ru → 89.223.124.200"
        location: "DNS provider zone for solid-stats.ru"
      - task: "Create A record errors.solid-stats.ru → 89.223.124.200"
        location: "DNS provider zone for solid-stats.ru"
must_haves:
  truths:
    - "Both grafana. and errors. A records resolve to 89.223.124.200"
    - "bootstrap-obs-edge.sh has run on the VPS for both domains, issuing per-domain LE certs"
    - "Grafana is reachable over HTTPS at its public URL behind its own local-user login"
    - "The errors. host serves a valid TLS cert with a 503 placeholder (no upstream yet)"
  artifacts: []
  key_links:
    - from: "operator DNS records"
      to: "certbot HTTP-01 issuance"
      via: "dig confirms resolution before bootstrap runs certbot certonly per domain"
      pattern: "dig"
    - from: "host nginx grafana vhost"
      to: "Grafana login over HTTPS"
      via: "curl/browser hits https://grafana.… → Grafana login page (MET-07)"
      pattern: "https://grafana"
---

<objective>
Operator-gated live execution: create the two DNS A records, run `bootstrap-obs-edge.sh` on the
VPS for both `grafana.` and `errors.` (per-domain certbot issuance), and verify Grafana is
reachable over HTTPS behind its own local-user auth. Closes EDGE-01 (DNS), EDGE-02 (live proxy),
EDGE-03 (live certs), MET-07 (Grafana over HTTPS).

Every step here requires the live VPS and DNS that the agent cannot provision, so all tasks are
operator checkpoints (autonomous: false). The authored artifacts (Waves 1-2) make each step a
single env-prefixed invocation following docs/obs-edge-bootstrap.md.

Purpose: take the authored obs-edge live behind real DNS + TLS.
Output: live grafana. (proxied, TLS) and errors. (503 placeholder, TLS) hosts; no repo files change.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-public-edge-grafana-tls/14-RESEARCH.md

# Operator runbook (the authoritative step-by-step for this plan):
@docs/obs-edge-bootstrap.md
@scripts/bootstrap-obs-edge.sh
</context>

<read_first>
- `docs/obs-edge-bootstrap.md` — the authoritative runbook authored in 14-03. Every step below
  references it. Operator memory: ssh root@89.223.124.200 reaches the staging edge; remote shell
  is fish; certbot full-renew hangs — use per-domain certonly only.
- `14-RESEARCH.md` "Autonomous vs Operator-Gated Work" — the exact operator commands.
</read_first>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Operator creates the two obs DNS A records (EDGE-01)</name>
  <action>Operator-only — the agent has no DNS API. Create both A records in the registrar zone and confirm propagation via dig before proceeding.</action>
  <what-built>The obs vhosts, bootstrap script, validator, and runbook are authored (Waves 1-2). DNS A records do not yet exist — the agent cannot create them.</what-built>
  <how-to-verify>
1. In the DNS provider zone for solid-stats.ru, create two A records:
   - `grafana.solid-stats.ru  A  89.223.124.200`
   - `errors.solid-stats.ru   A  89.223.124.200`
2. Wait for propagation, then confirm BOTH resolve (RESEARCH Pitfall 6):
   - `dig +short grafana.solid-stats.ru A @8.8.8.8` → 89.223.124.200
   - `dig +short errors.solid-stats.ru A @8.8.8.8` → 89.223.124.200
  </how-to-verify>
  <resume-signal>Type "resolved" once both dig checks return 89.223.124.200, or describe the issue.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Operator runs bootstrap-obs-edge.sh for both domains (EDGE-02, EDGE-03)</name>
  <action>Operator-only — runs as root on the live VPS and issues real Let's Encrypt certs. Run the two env-prefixed invocations from docs/obs-edge-bootstrap.md; per-domain certonly only, never full-renew.</action>
  <what-built>bootstrap-obs-edge.sh is on the VPS (git pull) and DNS now resolves, so certbot HTTP-01 can issue per-domain.</what-built>
  <how-to-verify>
On the VPS (ssh root@89.223.124.200; remote shell is fish — use one env-prefixed command per
line per docs/obs-edge-bootstrap.md), with the repo current:
1. Run the grafana. bootstrap (discovers the Grafana ClusterIP via kubectl):
   `DOMAIN=grafana.solid-stats.ru ADMIN_EMAIL=<ops-email> bash scripts/bootstrap-obs-edge.sh`
   Expect: ClusterIP discovered, HTTP-only vhost installed, `certbot certonly` issues the cert,
   TLS vhost installed, nginx reloaded.
2. Run the errors. bootstrap (placeholder, no upstream):
   `DOMAIN=errors.solid-stats.ru ADMIN_EMAIL=<ops-email> SKIP_UPSTREAM_CHECK=1 bash scripts/bootstrap-obs-edge.sh`
   Expect: cert issued, 503 placeholder TLS vhost installed, nginx reloaded.
3. Confirm both succeeded WITHOUT using the certbot full-renew flow (per-domain certonly only).
  </how-to-verify>
  <resume-signal>Type "bootstrapped" once both runs complete with certs issued and nginx reloaded, or paste the failing output.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Operator verifies Grafana over HTTPS + errors. 503 (MET-07)</name>
  <action>Operator-only live verification — confirm valid certs, Grafana login over HTTPS behind local-user auth, and the errors. 503 placeholder over TLS, per docs/obs-edge-bootstrap.md.</action>
  <what-built>Both obs hosts are live behind TLS. Final verification confirms EDGE-02/03 and MET-07.</what-built>
  <how-to-verify>
On the VPS / from any client (per docs/obs-edge-bootstrap.md "Operator-Only live verification"):
1. `nginx -t` on the VPS → syntax ok / test successful.
2. `certbot certificates` → lineages for BOTH grafana.… and errors.… present and valid.
3. `openssl s_client -connect grafana.solid-stats.ru:443 -servername grafana.solid-stats.ru </dev/null 2>/dev/null | openssl x509 -noout -subject`
   → CN matches grafana.solid-stats.ru, valid LE chain.
4. `curl -sI https://grafana.solid-stats.ru/` → 200 or 302 (to /login).
5. Open https://grafana.solid-stats.ru in a browser → the Grafana login page
   renders (MET-07: behind local-user auth; no anonymous access). If login redirects to http://,
   apply the root_url fix from docs/obs-edge-bootstrap.md.
6. `curl -sI https://errors.solid-stats.ru/` → 503 over valid TLS (placeholder).
  </how-to-verify>
  <resume-signal>Type "verified" once Grafana renders its login over HTTPS and errors. serves 503 over valid TLS, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → DNS registrar | A-record creation crosses to an external, operator-controlled system |
| operator → live VPS (ssh) | Bootstrap + certbot run as root on the staging host |
| Internet → live grafana. :443 | First real public TLS exposure of Grafana |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-12 | Denial of Service | certbot full-renew hang / LE rate limit | mitigate | per-domain `certbot certonly` only; both certs issued in one session; never the full-renew flow (operator memory) |
| T-14-13 | Information Disclosure | Grafana reachable publicly | mitigate | verified behind Grafana's own login (MET-07); step 5 confirms no anonymous access |
| T-14-14 | Spoofing | invalid/mismatched TLS cert served | mitigate | openssl s_client + certbot certificates confirm a valid LE cert with matching CN before sign-off |
| T-14-15 | Tampering | nginx reloaded with invalid config | mitigate | bootstrap's nginx -t gate + step 1 `nginx -t` re-confirm; reload refused on invalid config |
</threat_model>

<verification>
- EDGE-01: `dig +short` returns 89.223.124.200 for both hosts.
- EDGE-03: `certbot certificates` shows valid lineages for both; openssl confirms the chain.
- EDGE-02: grafana. proxies to the Grafana ClusterIP (200/302); errors. serves 503 over TLS.
- MET-07: Grafana login page renders over HTTPS behind local-user auth.
</verification>

<success_criteria>
Both A records resolve to the staging host; per-domain LE certs are issued and served for both
obs hosts; Grafana is reachable over HTTPS behind its own login; errors. serves a 503 placeholder
over a valid cert. All four Phase 14 requirements are closed.
</success_criteria>

<output>
Create `.planning/phases/14-public-edge-grafana-tls/14-04-SUMMARY.md` when done
</output>
