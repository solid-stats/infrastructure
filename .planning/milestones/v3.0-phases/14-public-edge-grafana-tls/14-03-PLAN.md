---
phase: 14-public-edge-grafana-tls
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - scripts/validate-obs-edge.py
  - docs/obs-edge-bootstrap.md
autonomous: true
requirements: [EDGE-01, EDGE-02, EDGE-03, MET-07]
must_haves:
  truths:
    - "An offline structural validator proves the obs-edge artifacts are well-formed without a live VPS"
    - "An operator runbook documents the DNS prerequisite, per-domain certbot caveat, and live verification"
    - "The runbook lists the exact two A records and the exact bootstrap invocations for grafana. and errors."
  artifacts:
    - path: "scripts/validate-obs-edge.py"
      provides: "Offline structural validator for the obs-edge script + both vhosts (mirrors validate-edge.py)"
      min_lines: 60
      contains: "ValidationError"
    - path: "docs/obs-edge-bootstrap.md"
      provides: "Operator runbook: DNS gate, per-domain certbot, live verification, Phase 16 reuse"
      contains: "grafana.solid-stats.ru"
  key_links:
    - from: "scripts/validate-obs-edge.py"
      to: "scripts/bootstrap-obs-edge.sh + both vhosts"
      via: "structural assertions: bash -n, set -euo pipefail, exit 64, vhost shape, no full-renew"
      pattern: "bash.*-n"
    - from: "docs/obs-edge-bootstrap.md"
      to: "operator live run"
      via: "documents dig DNS check, env-prefixed bootstrap invocations, curl/openssl smoke tests"
      pattern: "dig "
---

<objective>
Author the offline structural validator and the operator runbook for the obs-edge bootstrap:

1. `scripts/validate-obs-edge.py` — Python 3 stdlib offline validator mirroring
   `scripts/validate-edge.py`. Asserts the obs-edge script and both vhosts are well-formed
   without touching the live VPS (covers EDGE-02/03/MET-07 structurally offline; EDGE-01 is
   documented as operator-run since DNS is external).
2. `docs/obs-edge-bootstrap.md` — operator runbook covering the DNS prerequisite gate, the
   per-domain certbot caveat (never full-renew), the exact A records, the exact bootstrap
   invocations for both domains, live verification, and the Phase 16 errors. reuse note.

Purpose: provide the autonomous offline gate plus the operator instructions the Wave 3
human-action checkpoints reference.
Output: validate-obs-edge.py + docs/obs-edge-bootstrap.md.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-public-edge-grafana-tls/14-RESEARCH.md
@.planning/phases/14-public-edge-grafana-tls/14-VALIDATION.md

# Canonical artifacts to mirror (read in full before writing):
@scripts/validate-edge.py
@docs/edge-bootstrap.md

# Artifacts this plan validates/documents (authored in Wave 1):
@scripts/bootstrap-obs-edge.sh
@config/nginx/sites-available/grafana-stats-staging-solid-stats.conf
@config/nginx/sites-available/errors-stats-staging-solid-stats.conf
</context>

<read_first>
- `scripts/validate-edge.py` — the canonical offline validator. Mirror its shape: ROOT from
  __file__, ValidationError + require() helper, per-artifact check functions printing `ok:`
  lines, a main() that runs all checks and returns 1 on first ValidationError. Each check is an
  OFFLINE CHECK (no nginx/certbot/dig). Reuse the `bash -n` subprocess pattern.
- `14-VALIDATION.md` Per-Requirement Verification Map + Wave 0 Requirements — the exact checks
  to encode.
- `14-RESEARCH.md` Validation Architecture > Stage 1 — the full offline check list for the
  obs-edge script and both vhosts.
- `docs/edge-bootstrap.md` — the Phase 07 runbook to mirror (Context, Offline Checks, Step-by-
  step, Operator-Only live verification, Troubleshooting table).
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Offline structural validator for the obs-edge script + both vhosts</name>
  <files>scripts/validate-obs-edge.py</files>
  <behavior>
    Running `python3 scripts/validate-obs-edge.py` against the Wave 1 artifacts exits 0 and
    prints an `ok:` line per check group. If any asserted token is missing from an artifact,
    it exits 1 with an `error:` line naming the missing token. (Verified by running it against
    the real authored files — they must already satisfy every assertion.)
  </behavior>
  <action>
Create scripts/validate-obs-edge.py mirroring validate-edge.py exactly (shebang
`#!/usr/bin/env python3`, ROOT = Path(__file__).resolve().parents[1], ValidationError class,
require(condition, message) helper, main() running an ordered list of checks and returning 1 on
the first ValidationError). Each function is an OFFLINE CHECK.

Check group A — bootstrap-obs-edge.sh (mirror validate_shell_scripts + idempotency markers):
exists; `bash -n` passes (subprocess like validate-edge.py); contains `set -euo pipefail`;
contains `exit 64`; contains `mkdir -p`; contains `ln -sf`; contains a backup step
(`.bak`); contains `nginx -t`; contains the ClusterIP discovery
`kubectl get svc grafana -n monitoring`; contains `certbot certonly`; contains the
letsencrypt lineage guard `letsencrypt/live`; and asserts the script does NOT contain a
full hyphenated renew flag (negative assert that the dangerous certbot full-renew flag is
absent — this is the one VPS-hang anti-pattern, RESEARCH Pitfall 1).
Do NOT write that flag literally anywhere else in this file.

Check group B — grafana vhost: exists; contains `upstream grafana_obs`; contains `keepalive`;
contains `location /.well-known/acme-challenge/`; contains `return 301`; contains
`ssl_certificate`; contains `options-ssl-nginx.conf`; contains `ssl-dhparams.pem`; contains
`Strict-Transport-Security`; contains `proxy_set_header Upgrade` (WebSocket); asserts it is NOT
a hardcoded ClusterIP — accept either the substitutable placeholder token the bootstrap
replaces, or a 10.x.x.x address (reject a bare 127.0.0.1-only placeholder). Match validate-edge.py's
per-listen `http2` assertion: every `listen ... 443 ...` line includes `http2`.

Check group C — errors. vhost: exists; ACME block; `return 301`; `return 503`; full TLS block
(ssl_certificate, options-ssl-nginx.conf, ssl-dhparams.pem, Strict-Transport-Security); asserts
NO `proxy_pass` (placeholder has no upstream).

Check group D — docs + shared systemd artifacts: docs/obs-edge-bootstrap.md exists; the shared
Phase 07 systemd artifacts still present (config/systemd/certbot-deploy-hook.sh,
certbot.service.d/onfailure.conf, certbot-renew-failure.service) since the obs bootstrap reuses
them.

End each group's function with a `print("ok: ...")`. Add the validate-edge.py-style `warn:`
line that live nginx -t / dig / curl are operator-only.
  </action>
  <verify>
    <automated>python3 scripts/validate-obs-edge.py</automated>
  </verify>
  <done>`python3 scripts/validate-obs-edge.py` exits 0 against the Wave 1 artifacts and prints an ok: line per check group; removing any asserted token from an artifact would make it exit 1 with a naming error.</done>
</task>

<task type="auto">
  <name>Task 2: Operator runbook docs/obs-edge-bootstrap.md (DNS gate, per-domain certbot, live verification, Phase 16 reuse)</name>
  <files>docs/obs-edge-bootstrap.md</files>
  <action>
Create docs/obs-edge-bootstrap.md mirroring docs/edge-bootstrap.md's section shape, adapted to
the obs-edge. Sections:

- Title + intro: the obs-edge extends the live Phase 07 host edge with a second independent
  bootstrap for the observability subdomains; idempotent adopt-reconcile; offline checks run in
  CI via scripts/validate-obs-edge.py.
- Context: this is additive to the existing stats-staging edge — DO NOT touch
  stats-staging-solid-stats.conf or its cert.
- DNS Prerequisite (the hard gate, EDGE-01): both A records must be created by the operator
  before certbot can issue. List them verbatim:
  `grafana.solid-stats.ru  A  89.223.124.200` and
  `errors.solid-stats.ru   A  89.223.124.200`. State the agent cannot create DNS.
  Document the propagation check: `dig +short grafana.solid-stats.ru A @8.8.8.8`
  must return 89.223.124.200 before running certbot (RESEARCH Pitfall 6).
- Offline Checks (CI): `python3 scripts/validate-obs-edge.py`.
- Bootstrap invocations (operator, post-DNS): the EXACT grafana. command discovering the
  upstream via kubectl, then the EXACT errors. command with SKIP_UPSTREAM_CHECK=1 (lift from
  14-RESEARCH.md "Autonomous vs Operator-Gated Work" table). Note both certs are issued now to
  avoid LE rate limits even though errors. has no upstream until Phase 16.
- certbot caveat: per-domain `certbot certonly -d <domain>` only — NEVER the certbot full-renew
  flow (it hangs on the auth cert on this VPS, operator memory). `certbot renew --dry-run` is
  safe.
- Operator-Only live verification: `nginx -t`; `certbot certificates`;
  `openssl s_client -connect grafana.solid-stats.ru:443` shows a valid LE cert;
  `curl -sI https://grafana.solid-stats.ru/` → 200/302; open the URL and confirm
  the Grafana login page renders (MET-07, behind local-user auth).
- Known post-deploy check: if Grafana login redirects to http:// instead of https://, add
  `root_url = https://grafana.solid-stats.ru` to the Grafana ConfigMap and
  redeploy — do NOT preemptively change Phase 13 config (RESEARCH Open Question 1).
- Phase 16 reuse: the errors. upstream (GlitchTip ClusterIP) is wired by re-running
  bootstrap-obs-edge.sh with DOMAIN=errors.… UPSTREAM=<glitchtip-clusterip>:80 — no script change.
- Troubleshooting table mirroring edge-bootstrap.md (NXDOMAIN before DNS, certbot rate limit,
  502 on wrong ClusterIP, nginx -t fails because cert missing on first run).
  </action>
  <verify>
    <automated>test -f docs/obs-edge-bootstrap.md && grep -q "grafana.solid-stats.ru" docs/obs-edge-bootstrap.md && grep -q "errors.solid-stats.ru" docs/obs-edge-bootstrap.md && grep -q "certbot certonly" docs/obs-edge-bootstrap.md && grep -q "dig " docs/obs-edge-bootstrap.md && grep -q "89.223.124.200" docs/obs-edge-bootstrap.md</automated>
  </verify>
  <done>docs/obs-edge-bootstrap.md documents the two exact A records, the DNS propagation dig check, the exact per-domain bootstrap + certbot invocations (never full-renew), the operator live-verification steps, and the Phase 16 errors. reuse note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/local → repo artifacts | The validator reads repo files only — never the live host |
| operator → live VPS | Runbook steps that touch nginx/certbot/DNS cross to the live host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | Tampering | obs-edge artifacts drift from the proven shape | mitigate | validate-obs-edge.py structurally asserts script + both vhosts every commit; fails CI on drift |
| T-14-10 | Denial of Service | operator runs the hanging certbot full-renew | mitigate | validator negative-asserts the full-renew flag is absent from the script; runbook documents per-domain certonly only |
| T-14-11 | Repudiation | live verification skipped, broken edge claimed live | mitigate | runbook lists explicit OPERATOR-ONLY curl/openssl/login checks; Wave 3 checkpoints gate on them |
| T-14-SC | Tampering | Python stdlib only — no external packages | accept | validate-obs-edge.py uses only re/subprocess/sys/pathlib (RESEARCH: no package install, no audit needed) |
</threat_model>

<verification>
- `python3 scripts/validate-obs-edge.py` exits 0 against the Wave 1 artifacts.
- docs/obs-edge-bootstrap.md contains the two A records, the dig check, per-domain certbot, and the live verification steps.
- The validator and the runbook agree with the actually-authored 14-01 and 14-02 artifacts (no stale literals).
</verification>

<success_criteria>
The offline validator passes against the real Wave 1 artifacts and would fail on drift; the
operator runbook gives the operator everything needed to run the DNS-gated live bootstrap and
verify Grafana over HTTPS, plus the Phase 16 reuse path.
</success_criteria>

<output>
Create `.planning/phases/14-public-edge-grafana-tls/14-03-SUMMARY.md` when done
</output>
