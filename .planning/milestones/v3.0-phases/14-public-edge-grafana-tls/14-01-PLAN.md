---
phase: 14-public-edge-grafana-tls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/bootstrap-obs-edge.sh
autonomous: true
requirements: [EDGE-02, EDGE-03]
must_haves:
  truths:
    - "Operator can author both obs vhosts on the VPS without DNS existing (SKIP_CERTBOT=1)"
    - "Operator can issue a per-domain TLS cert once DNS resolves (certbot certonly -d, never full-renew)"
    - "The same script handles grafana. (real Grafana ClusterIP upstream) and errors. (placeholder, no upstream)"
    - "Re-running the script produces the same final state (idempotent adopt-reconcile)"
  artifacts:
    - path: "scripts/bootstrap-obs-edge.sh"
      provides: "Env-parameterized obs-edge adopt-reconcile bootstrap (DOMAIN/UPSTREAM/ADMIN_EMAIL)"
      min_lines: 120
      contains: "set -euo pipefail"
  key_links:
    - from: "scripts/bootstrap-obs-edge.sh"
      to: "config/nginx/sites-available/grafana-stats-staging-solid-stats.conf"
      via: "installs the repo vhost matching $DOMAIN into NGINX_SITES_DIR then nginx -t gate + reload"
      pattern: "NGINX_SITES_DIR"
    - from: "scripts/bootstrap-obs-edge.sh"
      to: "k3s monitoring/grafana Service"
      via: "kubectl get svc grafana -n monitoring -o jsonpath ClusterIP discovery at runtime"
      pattern: "kubectl get svc grafana -n monitoring"
---

<objective>
Author `scripts/bootstrap-obs-edge.sh`: an idempotent, env-parameterized adopt-reconcile
bootstrap for the observability subdomains, mirroring the canonical Phase 07
`scripts/bootstrap-edge.sh` 7-step structure. One script handles both `grafana.`
(real Grafana ClusterIP upstream, discovered at runtime via kubectl) and `errors.`
(placeholder, no upstream) via repeated invocation with different `DOMAIN`/`UPSTREAM`.

Implements EDGE-02 (host nginx proxy to ClusterIP via the reusable obs-edge bootstrap)
and EDGE-03 (per-domain certbot issuance, HTTP-first then TLS). This is the reusable
bootstrap the Phase 16 GlitchTip `errors.` vhost extends with no script changes.

Purpose: establish the parameterized edge automation so the operator-gated live run
(Wave 3) is a single env-prefixed invocation per domain.
Output: `scripts/bootstrap-obs-edge.sh` (authored, syntax-clean; not run here).
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-public-edge-grafana-tls/14-RESEARCH.md

# Canonical pattern to mirror exactly (read in full before writing):
@scripts/bootstrap-edge.sh
@AGENTS.md
</context>

<read_first>
- `scripts/bootstrap-edge.sh` — the canonical 7-step adopt-reconcile model. Mirror its
  structure: required-var check with `exit 64`, package check, webroot dir, backup-before-
  overwrite vhost adopt, `nginx -t` gate with auto-restore, certbot lineage-skip guard,
  deploy-hook + OnFailure drop-in idempotent `cp`, ufw step.
- `14-RESEARCH.md` Code Examples section — ClusterIP discovery, HTTP-only temp vhost,
  certbot per-domain issuance, nginx -t gate with auto-restore (copy these patterns).
- `14-RESEARCH.md` Pattern 2 (Option A) — install an inline HTTP-only temp vhost first,
  run certbot, then install the final repo TLS vhost. The repo vhost has TLS server blocks;
  `nginx -t` fails if installed before the cert exists, so first-run branches on cert lineage.
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Header, env contract, package + webroot steps (mirror bootstrap-edge.sh steps 1-2)</name>
  <files>scripts/bootstrap-obs-edge.sh</files>
  <action>
Create scripts/bootstrap-obs-edge.sh with `#!/usr/bin/env bash` and `set -euo pipefail`
(project convention). Write a head comment block (mirror bootstrap-edge.sh's): names the
script as the env-parameterized obs-edge adopt-reconcile bootstrap, states it is run on the
VPS as root, is idempotent and safe to re-run, and gives two usage examples — one for
grafana. with UPSTREAM discovered via kubectl, one for errors. with SKIP_UPSTREAM_CHECK=1.
Reference docs/obs-edge-bootstrap.md as the operator runbook.

Resolve REPO_ROOT from BASH_SOURCE exactly as bootstrap-edge.sh does.

Define the env contract per 14-RESEARCH.md script signature. REQUIRED (exit 64 if unset,
mirroring bootstrap-edge.sh's ADMIN_EMAIL guard): DOMAIN, ADMIN_EMAIL. Optional with `:` =
defaults: UPSTREAM (empty default — discovered for grafana when blank), WEBROOT_PATH=/var/www/html,
NGINX_SITES_DIR=/etc/nginx/sites-available, NGINX_SITES_ENABLED=/etc/nginx/sites-enabled,
SKIP_CERTBOT=0, SKIP_UFW=1 (default 1 — ports 80/443 already open from Phase 07, per RESEARCH
Pitfall 5), SKIP_UPSTREAM_CHECK=0. Each missing required var prints a FATAL line to stderr
and `exit 64`.

Step 1 (Package check): `apt-get update -qq` then `apt-get install -y certbot nginx curl openssl`
(idempotent). Print nginx/certbot versions with `|| true`. Do NOT install ufw here unless
needed — RESEARCH says SKIP_UFW default is 1.

Step 2 (Webroot): `mkdir -p "$WEBROOT_PATH/.well-known/acme-challenge"` and scoped `chmod 755`
on the three webroot dirs only (mirror bootstrap-edge.sh — do not recurse the whole webroot).
  </action>
  <verify>
    <automated>bash -n scripts/bootstrap-obs-edge.sh</automated>
  </verify>
  <done>Script parses with `bash -n`; contains `set -euo pipefail`, `exit 64` for missing DOMAIN and ADMIN_EMAIL, `SKIP_UFW:=1` default, and the step 1/2 package + webroot blocks.</done>
</task>

<task type="auto">
  <name>Task 2: ClusterIP discovery + HTTP-first vhost adopt with nginx -t gate (RESEARCH Pattern 2 Option A)</name>
  <files>scripts/bootstrap-obs-edge.sh</files>
  <action>
Add the upstream-resolution block. When UPSTREAM is empty AND SKIP_UPSTREAM_CHECK != 1:
discover the Grafana ClusterIP at runtime via
`kubectl get svc grafana -n monitoring -o jsonpath='{.spec.clusterIP}:{.spec.ports[0].port}'`
(copy the guard from 14-RESEARCH.md Code Examples: FATAL + exit 1 if the result is empty or
just ":"). Assign the discovered value to UPSTREAM and echo it. When SKIP_UPSTREAM_CHECK=1
(errors. placeholder case), do NOT discover — UPSTREAM stays empty and the placeholder vhost
serves 503 (no proxy_pass), so no upstream is needed.

Derive vhost paths from DOMAIN: VHOST_CONF="$NGINX_SITES_DIR/${DOMAIN}.conf" mapped to the
repo vhost basename. The repo ships one vhost per domain named by the RESEARCH structure:
`config/nginx/sites-available/grafana-stats-staging-solid-stats.conf` and
`config/nginx/sites-available/errors-stats-staging-solid-stats.conf`. Select the repo source
file by DOMAIN: grafana. -> grafana-…conf, errors. -> errors-…conf (a `case`/`if` on the
domain prefix). Install path keeps the same basename. Set BAK_VHOST="${VHOST_CONF}.bak".

HTTP-first adopt (RESEARCH Pattern 2, Option A — the cert may not exist yet):
1. If a cert lineage exists at /etc/letsencrypt/live/$DOMAIN, install the FINAL repo TLS vhost
   directly (cert files are present, so `nginx -t` will pass).
2. Otherwise, write an inline HTTP-only temp vhost (RESEARCH Code Examples "HTTP-Only Temp
   Vhost": listen 80, ACME location root /var/www/html, `return 301` to HTTPS; substitute
   $DOMAIN). Install the temp vhost so nginx serves the ACME challenge endpoint. The final TLS
   repo vhost is installed AFTER certbot succeeds (Task 3).

For whichever vhost is installed: back up any pre-existing live VHOST_CONF to BAK_VHOST only
if no .bak exists yet (mirror bootstrap-edge.sh backup block), `cp` the file into place,
`ln -sf` into NGINX_SITES_ENABLED. Then run the `nginx -t` gate with auto-restore EXACTLY as
in 14-RESEARCH.md Code Examples ("nginx -t Gate with Auto-Restore"): on failure restore
BAK_VHOST if present else remove VHOST_CONF and its symlink, re-validate, refuse reload if
still invalid, `exit 1`. Reload via `systemctl reload nginx` with a FATAL-on-failure check
(do not fall back to start).
  </action>
  <verify>
    <automated>bash -n scripts/bootstrap-obs-edge.sh && grep -q "kubectl get svc grafana -n monitoring" scripts/bootstrap-obs-edge.sh && grep -q "ln -sf" scripts/bootstrap-obs-edge.sh && grep -q "nginx -t" scripts/bootstrap-obs-edge.sh</automated>
  </verify>
  <done>Script discovers the Grafana ClusterIP via kubectl when UPSTREAM is empty, branches on cert-lineage existence for HTTP-first-vs-TLS install, backs up the live vhost before overwrite, and runs an nginx -t gate with auto-restore before reload.</done>
</task>

<task type="auto">
  <name>Task 3: certbot per-domain issuance + TLS vhost swap + shared systemd artifacts + ufw skip (steps 4-7)</name>
  <files>scripts/bootstrap-obs-edge.sh</files>
  <action>
Step 4 (certbot per-domain): branch exactly like 14-RESEARCH.md "Certbot Per-Domain Issuance":
if /etc/letsencrypt/live/$DOMAIN exists -> echo skip (lineage exists, rate-limit safety per
RESEARCH Pitfall 4); elif SKIP_CERTBOT=1 -> echo skip (DNS not live yet); else run
`certbot certonly --authenticator webroot --installer none --no-eff-email --agree-tos
--email "$ADMIN_EMAIL" --webroot-path "$WEBROOT_PATH" -d "$DOMAIN"`. NEVER use
`certbot renew`/`--full-renew` here (RESEARCH Pitfall 1 — hangs on this VPS). After a
successful issuance on a first run (the HTTP-only temp vhost path from Task 2), install the
FINAL repo TLS vhost over VHOST_CONF, re-run the nginx -t gate + reload (same auto-restore
helper) so the TLS server block goes live now that the cert exists.

Step 5 (deploy hook): idempotent `mkdir -p /etc/letsencrypt/renewal-hooks/deploy` then
`cp "$REPO_ROOT/config/systemd/certbot-deploy-hook.sh"
/etc/letsencrypt/renewal-hooks/deploy/reload-nginx.sh` + `chmod +x`. This reuses the EXISTING
shared Phase 07 hook (do not author a new one).

Step 6 (OnFailure drop-in): idempotent `cp` of config/systemd/certbot.service.d/onfailure.conf
and config/systemd/certbot-renew-failure.service into /etc/systemd/system, then
`systemctl daemon-reload` with FATAL-on-failure. Reuses the EXISTING shared Phase 07 units.

Step 7 (ufw): honor SKIP_UFW. With the default SKIP_UFW=1, echo that ufw is skipped because
ports 80/443 are already open from Phase 07 (RESEARCH Pitfall 5 — do not add duplicate rules).
Only when SKIP_UFW=0 add idempotent `ufw allow 80/tcp` and `ufw allow 443/tcp` with comments
(no 6443/wg0 rule — that is Phase 07's, untouched here).

Footer: print an OPERATOR VERIFICATION block (mirror bootstrap-edge.sh footer) listing the
post-run live checks: `nginx -t`, `certbot certificates`, `curl -I https://$DOMAIN/`, and a
pointer to docs/obs-edge-bootstrap.md.
  </action>
  <verify>
    <automated>bash -n scripts/bootstrap-obs-edge.sh && grep -q "certbot certonly" scripts/bootstrap-obs-edge.sh && grep -q "letsencrypt/live/\$DOMAIN" scripts/bootstrap-obs-edge.sh && grep -q "reload-nginx.sh" scripts/bootstrap-obs-edge.sh && ! grep -q "full-renew" scripts/bootstrap-obs-edge.sh</automated>
  </verify>
  <done>Script issues per-domain certs with `certbot certonly -d "$DOMAIN"` (never full-renew), skips issuance when lineage exists or SKIP_CERTBOT=1, swaps in the TLS vhost after issuance, reuses the shared deploy hook + OnFailure drop-in via idempotent cp, and skips ufw by default with a Phase-07 rationale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → host nginx :443 | Untrusted public TLS traffic terminates at the host edge |
| host nginx → k3s ClusterIP | Trusted intra-host hop over the k3s service CIDR (10.43.x.x) |
| operator → certbot/Let's Encrypt | Cert issuance crosses to an external CA over ACME HTTP-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | bootstrap-obs-edge.sh vhost install | mitigate | nginx -t gate with auto-restore before every reload; refuse reload on invalid config |
| T-14-02 | Denial of Service | certbot rate limits / full-renew hang | mitigate | per-domain `certbot certonly -d`, never `--full-renew`; lineage-existence skip guard avoids re-issuance |
| T-14-03 | Spoofing | ClusterIP misdiscovery → 502/wrong upstream | mitigate | runtime `kubectl get svc` discovery with empty-result FATAL guard; SKIP_UPSTREAM_CHECK only for the no-proxy placeholder |
| T-14-04 | Elevation of Privilege | ufw rule drift exposing k3s API | accept | SKIP_UFW=1 default — this script never touches the Phase 07 6443/wg0 rule; ports 80/443 already governed by Phase 07 |
| T-14-SC | Tampering | apt-get installs (certbot/nginx/curl/openssl) | accept | All host-installed from Phase 07; `apt-get install -y` is an idempotent no-op; no new external packages, no npm/pip/cargo (RESEARCH: no Package Legitimacy audit needed) |
</threat_model>

<verification>
- `bash -n scripts/bootstrap-obs-edge.sh` passes (syntax clean).
- `grep "set -euo pipefail"`, `grep "exit 64"` present (project convention).
- No `full-renew` token anywhere in the script.
- Offline structural validator (authored in 14-03) passes for this script in Wave 2.
</verification>

<success_criteria>
`scripts/bootstrap-obs-edge.sh` exists, is syntax-clean, mirrors the Phase 07 7-step
adopt-reconcile structure, is env-parameterized for both obs domains, discovers the Grafana
ClusterIP at runtime, issues per-domain certs (never full-renew), and reuses the shared
Phase 07 deploy hook + OnFailure drop-in. Not executed in this plan — live run is Wave 3.
</success_criteria>

<output>
Create `.planning/phases/14-public-edge-grafana-tls/14-01-SUMMARY.md` when done
</output>
