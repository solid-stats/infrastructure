---
phase: 14-public-edge-grafana-tls
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config/nginx/sites-available/grafana-stats-staging-solid-stats.conf
  - config/nginx/sites-available/errors-stats-staging-solid-stats.conf
autonomous: true
requirements: [EDGE-02, MET-07]
must_haves:
  truths:
    - "The grafana. vhost terminates TLS and proxies to the Grafana ClusterIP upstream"
    - "The grafana. vhost forwards WebSocket upgrades (Grafana Live) and correct proxy headers"
    - "The errors. vhost terminates TLS but returns 503 (no upstream until Phase 16)"
    - "Both vhosts serve the ACME challenge over HTTP and redirect / to HTTPS"
  artifacts:
    - path: "config/nginx/sites-available/grafana-stats-staging-solid-stats.conf"
      provides: "Grafana public TLS vhost with WebSocket-capable proxy to the obs upstream"
      contains: "Strict-Transport-Security"
    - path: "config/nginx/sites-available/errors-stats-staging-solid-stats.conf"
      provides: "errors. placeholder TLS vhost returning 503 (Phase 16 wires the upstream)"
      contains: "return 503"
  key_links:
    - from: "config/nginx/sites-available/grafana-stats-staging-solid-stats.conf"
      to: "k3s monitoring/grafana ClusterIP:80"
      via: "upstream grafana_obs { server <CLUSTERIP>:80 } + proxy_pass http://grafana_obs"
      pattern: "proxy_pass http://grafana_obs"
    - from: "config/nginx/sites-available/grafana-stats-staging-solid-stats.conf"
      to: "Grafana Live streaming"
      via: "proxy_set_header Upgrade $http_upgrade + Connection upgrade"
      pattern: "proxy_set_header Upgrade"
---

<objective>
Author the two observability nginx vhost templates the obs-edge bootstrap installs:

1. `grafana-stats-staging-solid-stats.conf` — TLS termination + WebSocket-capable proxy to the
   Grafana ClusterIP upstream (MET-07: Grafana over HTTPS behind its own local-user auth).
2. `errors-stats-staging-solid-stats.conf` — placeholder TLS vhost returning 503 (DNS + cert
   issued now per CONTEXT rate-limit avoidance; GlitchTip upstream wired in Phase 16).

Both mirror the Phase 07 `stats-staging-solid-stats.conf` structure (ACME block, HTTP→HTTPS
redirect, certbot-managed TLS includes, HSTS). Implements EDGE-02 (vhost proxy to ClusterIP)
and MET-07 (Grafana reachable over TLS behind local-user auth).

Purpose: ship the final TLS vhosts the bootstrap installs after certbot issuance.
Output: two nginx vhost config files under config/nginx/sites-available/.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-public-edge-grafana-tls/14-RESEARCH.md

# Canonical vhost to mirror (read in full before writing):
@config/nginx/sites-available/stats-staging-solid-stats.conf
</context>

<read_first>
- `config/nginx/sites-available/stats-staging-solid-stats.conf` — the live Phase 07 vhost.
  Mirror its exact structure: head comment block, named upstream with `keepalive`, HTTP server
  block (ACME location root /var/www/html + `return 301` redirect), HTTPS server block
  (`listen 443 ssl http2` on both IPv4 and IPv6, certbot ssl_certificate/key, include
  options-ssl-nginx.conf, ssl_dhparam ssl-dhparams.pem, HSTS add_header, proxy_set_header set).
- `14-RESEARCH.md` Pattern 3 — the Grafana vhost proxy block with WebSocket upgrade headers.
- `14-RESEARCH.md` Pattern 4 — the errors. placeholder returns 503 (no proxy_pass).
- `14-RESEARCH.md` Pitfall 7 — a TLS vhost with proxy_pass to a non-existent upstream breaks
  nginx; the errors. placeholder uses `return 503` only.
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Grafana public TLS vhost with WebSocket proxy to the obs ClusterIP upstream</name>
  <files>config/nginx/sites-available/grafana-stats-staging-solid-stats.conf</files>
  <action>
Create the Grafana vhost mirroring stats-staging-solid-stats.conf structure for
server_name grafana.solid-stats.ru. Include a head comment block stating the file
is repo-managed, applied by scripts/bootstrap-obs-edge.sh (operator-run, not CI), and that the
upstream ClusterIP is substituted by the bootstrap at install time (do NOT hand-edit on the
host; RESEARCH anti-pattern: never hard-code the dynamic ClusterIP).

Named upstream `upstream grafana_obs { server UPSTREAM_PLACEHOLDER; keepalive 16; }` where
UPSTREAM_PLACEHOLDER is a literal token the bootstrap replaces with the discovered
`<clusterip>:80`. Use this exact placeholder token so the offline validator (14-03) can assert
the file is not a hardcoded IP and the bootstrap can `sed`-substitute it. <!-- planner-discipline-allow: UPSTREAM_PLACEHOLDER -->

HTTP server block (listen 80 + [::]:80): `location /.well-known/acme-challenge/ { root /var/www/html; }`
and `location / { return 301 https://$host$request_uri; }`.

HTTPS server block: `listen 443 ssl http2;` and `listen [::]:443 ssl http2;`;
ssl_certificate /etc/letsencrypt/live/grafana.solid-stats.ru/fullchain.pem and
the matching privkey.pem; `include /etc/letsencrypt/options-ssl-nginx.conf;`;
`ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;`; HSTS
`add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;`.

`location /` proxies to the upstream per 14-RESEARCH.md Pattern 3: `proxy_pass http://grafana_obs;`
with Host/X-Real-IP/X-Forwarded-For/X-Forwarded-Proto/X-Forwarded-Host headers, `proxy_http_version 1.1;`,
and the WebSocket upgrade pair `proxy_set_header Upgrade $http_upgrade;` +
`proxy_set_header Connection "upgrade";` (Grafana Live), plus proxy_connect_timeout 5s /
proxy_send_timeout 60s / proxy_read_timeout 60s.
  </action>
  <verify>
    <automated>grep -q "upstream grafana_obs" config/nginx/sites-available/grafana-stats-staging-solid-stats.conf && grep -q "proxy_set_header Upgrade" config/nginx/sites-available/grafana-stats-staging-solid-stats.conf && grep -q "Strict-Transport-Security" config/nginx/sites-available/grafana-stats-staging-solid-stats.conf && grep -q "options-ssl-nginx.conf" config/nginx/sites-available/grafana-stats-staging-solid-stats.conf</automated>
  </verify>
  <done>Grafana vhost has a named grafana_obs upstream with a substitutable placeholder token, ACME + 301 HTTP block, full certbot-managed TLS block with HSTS, and a WebSocket-capable proxy_pass to the upstream.</done>
</task>

<task type="auto">
  <name>Task 2: errors. placeholder TLS vhost returning 503 (no upstream until Phase 16)</name>
  <files>config/nginx/sites-available/errors-stats-staging-solid-stats.conf</files>
  <action>
Create the errors. placeholder vhost for server_name errors.solid-stats.ru,
mirroring the same TLS structure but with NO upstream and NO proxy_pass (RESEARCH Pattern 4 +
Pitfall 7). Head comment states this is a placeholder issued now for DNS + cert (rate-limit
avoidance per CONTEXT) and that the GlitchTip upstream is wired in Phase 16 by re-running
bootstrap-obs-edge.sh with the GlitchTip ClusterIP.

HTTP server block (listen 80 + [::]:80): ACME location root /var/www/html, then
`location / { return 301 https://$host$request_uri; }` — identical to the Grafana HTTP block so
certbot HTTP-01 works.

HTTPS server block: `listen 443 ssl http2;` + IPv6; ssl_certificate/key for
/etc/letsencrypt/live/errors.solid-stats.ru/; include options-ssl-nginx.conf;
ssl_dhparam ssl-dhparams.pem; HSTS add_header. The single `location / { return 503; }` — no
proxy_pass, no upstream block (certbot checks /.well-known/acme-challenge over HTTP, not /, so a
503 at / does not affect issuance per RESEARCH Open Question 2).
  </action>
  <verify>
    <automated>grep -q "return 503" config/nginx/sites-available/errors-stats-staging-solid-stats.conf && grep -q "errors.solid-stats.ru" config/nginx/sites-available/errors-stats-staging-solid-stats.conf && grep -q "options-ssl-nginx.conf" config/nginx/sites-available/errors-stats-staging-solid-stats.conf && ! grep -q "proxy_pass" config/nginx/sites-available/errors-stats-staging-solid-stats.conf</automated>
  </verify>
  <done>errors. vhost has the ACME + 301 HTTP block and a full certbot-managed TLS block, but no upstream and no proxy_pass — its TLS `location /` returns 503 as a placeholder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → host nginx :443 | Public TLS traffic terminates here; clients trust the LE cert chain |
| host nginx → Grafana ClusterIP | Proxy hop carrying forwarded client headers |
| host nginx → Grafana auth | Grafana enforces its own local-user login behind the proxy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Information Disclosure | Grafana exposed publicly | mitigate | Grafana's own local-user auth (GF_SECURITY_ADMIN_*, anonymous disabled by default, Phase 13); nginx passes Host header through; no nginx-level auth layer per MET-07 |
| T-14-06 | Spoofing | downgrade to plain HTTP | mitigate | HTTP block `return 301` to HTTPS + HSTS `max-age=31536000; includeSubDomains` on both vhosts; ACME path is the only HTTP exception |
| T-14-07 | Denial of Service | errors. proxy_pass to absent upstream breaks nginx reload | mitigate | placeholder uses `return 503` only — no upstream, no proxy_pass (RESEARCH Pitfall 7) |
| T-14-08 | Tampering | hardcoded stale ClusterIP in repo | mitigate | repo vhost ships a substitutable placeholder token; bootstrap injects the runtime-discovered ClusterIP (validator rejects a bare hardcoded IP) |
</threat_model>

<verification>
- Both vhost files exist under config/nginx/sites-available/.
- Grafana vhost: named upstream, WebSocket headers, HSTS, certbot includes, no hardcoded ClusterIP.
- errors. vhost: `return 503`, no proxy_pass, HSTS, certbot includes.
- Offline structural validator (14-03) asserts both shapes in Wave 2.
- Live `nginx -t` is operator-only (Wave 3) — config references certbot files that exist only post-issuance.
</verification>

<success_criteria>
Both vhost templates exist, mirror the Phase 07 vhost structure exactly, and encode the
phase decisions: Grafana proxies the ClusterIP upstream with WebSocket support; errors. is a
503 placeholder with cert-ready ACME/TLS blocks but no upstream.
</success_criteria>

<output>
Create `.planning/phases/14-public-edge-grafana-tls/14-02-SUMMARY.md` when done
</output>
