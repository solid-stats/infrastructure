---
phase: 13-deploy-pipeline-metrics-stack
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/staging/01-obs-rbac.yaml
  - k8s/staging/20-rabbitmq.yaml
  - .github/workflows/deploy-observability.yml
autonomous: true
requirements: [DEP-02, DEP-03, MET-04]
user_setup:
  - service: github-actions
    why: "Operator must add new GitHub environment secrets before the obs deploy workflow can run (added/verified in 13-05)"
    env_vars:
      - name: K8S_OBS_TOKEN
        source: "kubectl -n monitoring get secret obs-ci-deployer-token -o jsonpath='{.data.token}' | base64 -d (operator step in 13-05)"
      - name: GRAFANA_ADMIN_PASSWORD
        source: "Operator-chosen Grafana admin password"
      - name: PG_MONITOR_PASSWORD
        source: "Operator-chosen pg_monitor role password"
must_haves:
  truths:
    - "Prometheus runtime ServiceAccount + read-only ClusterRole + ClusterRoleBinding are added to the operator-bootstrap 01-obs-rbac.yaml (NOT to the CI-applied obs manifests)"
    - "rabbitmq Service exposes port 15692 and the StatefulSet mounts an enabled_plugins ConfigMap enabling rabbitmq_prometheus, value ending in a period"
    - "deploy-observability.yml has its own concurrency group infrastructure-obs-deploy, uses K8S_OBS_TOKEN + obs-ci-deployer + WireGuard, namespace monitoring, applies k8s/observability/*.yaml"
    - "deploy-staging.yml is unchanged and has no dependency on the obs deploy"
  artifacts:
    - path: ".github/workflows/deploy-observability.yml"
      provides: "Independent obs CI deploy (DEP-02, DEP-03)"
      contains: "infrastructure-obs-deploy"
    - path: "k8s/staging/01-obs-rbac.yaml"
      provides: "Prometheus runtime SA + read-only SD ClusterRole/ClusterRoleBinding (operator-applied)"
      contains: "prometheus-monitoring"
    - path: "k8s/staging/20-rabbitmq.yaml"
      provides: "rabbitmq Service port 15692 + enabled_plugins ConfigMap mount (MET-04)"
      contains: "15692"
  key_links:
    - from: ".github/workflows/deploy-observability.yml"
      to: "scripts/render-obs-secrets.py + scripts/kubeconfig-setup.sh"
      via: "render obs secrets step + kubeconfig-setup with K8S_USER_NAME obs-ci-deployer"
      pattern: "render-obs-secrets.py"
    - from: "k8s/observability/10-prometheus.yaml (serviceAccountName prometheus)"
      to: "k8s/staging/01-obs-rbac.yaml (prometheus SA + ClusterRoleBinding)"
      via: "Prometheus pod binds to the pre-created cluster-read SA"
      pattern: "prometheus-monitoring"
---

<objective>
Wire the three runtime-side edits the metrics stack needs: (1) add Prometheus's runtime ServiceAccount + read-only ClusterRole/ClusterRoleBinding to the operator-bootstrap `01-obs-rbac.yaml` (the RBAC ClusterRole trap — obs-ci-deployer is namespace-scoped and cannot create cluster RBAC); (2) patch `20-rabbitmq.yaml` to expose port 15692 and enable the native `rabbitmq_prometheus` plugin (MET-04); (3) author the independent `deploy-observability.yml` CI workflow (DEP-02, DEP-03).

Purpose: These are the connective edits between the rendered obs manifests (13-02/03) and the live cluster. The ClusterRole must live in the operator-bootstrap file (excluded from every CI glob) — applying it from CI would 403. The rabbitmq edit is the only change to a runtime manifest; it ships via the runtime CD glob on next push and triggers a rolling restart (operator-gated apply happens in 13-06).

Output: edited `01-obs-rbac.yaml`, edited `20-rabbitmq.yaml`, new `.github/workflows/deploy-observability.yml`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-deploy-pipeline-metrics-stack/13-RESEARCH.md

# Files to edit / mirror:
@k8s/staging/01-obs-rbac.yaml
@k8s/staging/20-rabbitmq.yaml
@.github/workflows/deploy-staging.yml
@scripts/kubeconfig-setup.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Prometheus runtime SA + read-only ClusterRole to 01-obs-rbac.yaml</name>
  <files>k8s/staging/01-obs-rbac.yaml</files>
  <read_first>k8s/staging/01-obs-rbac.yaml</read_first>
  <action>
    Append (via Edit, preserving the existing operator-bootstrap content and its DO-NOT-apply-from-CI header) the three resources from 13-RESEARCH section "Obs Bootstrap Extension to 01-obs-rbac.yaml" verbatim in intent: a ServiceAccount prometheus in namespace monitoring; a ClusterRole prometheus-monitoring with READ-ONLY verbs (get/list/watch) on nodes, nodes/proxy, nodes/metrics, services, endpoints, pods (core) and ingresses (networking.k8s.io), plus nonResourceURLs /metrics + /metrics/cadvisor with verb get; a ClusterRoleBinding prometheus-monitoring binding that ClusterRole to the prometheus SA. Use standard solid-stats labels (app.kubernetes.io/name prometheus, app.kubernetes.io/part-of solid-stats). This resolves Pitfall 2 (obs-ci-deployer cannot create ClusterRole) and the RBAC ClusterRole trap: the cluster-read grant lives in the operator-applied bootstrap file, which every CI glob already excludes. Do NOT add any write/delete/create/patch/update verb. The rendered Prometheus Deployment (13-02) references serviceAccountName prometheus, binding it to this SA.
  </action>
  <verify>
    <automated>grep -q 'name: prometheus-monitoring' k8s/staging/01-obs-rbac.yaml && grep -q 'kind: ClusterRole' k8s/staging/01-obs-rbac.yaml && ! grep -A14 'name: prometheus-monitoring' k8s/staging/01-obs-rbac.yaml | grep -Eq 'create|update|delete|patch'</automated>
  </verify>
  <done>01-obs-rbac.yaml now contains the prometheus SA, the prometheus-monitoring ClusterRole (read-only verbs only), and the ClusterRoleBinding; existing obs-ci-deployer content untouched; header DO-NOT-apply-from-CI note intact.</done>
  <acceptance_criteria>
    - ServiceAccount prometheus (ns monitoring), ClusterRole prometheus-monitoring, ClusterRoleBinding prometheus-monitoring all present.
    - ClusterRole verbs limited to get/list/watch (+ get on nonResourceURLs) — no write verbs.
    - Pre-existing obs-ci-deployer namespaces/SAs/Roles/Secrets unchanged.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: rabbitmq port 15692 + enabled_plugins ConfigMap (MET-04)</name>
  <files>k8s/staging/20-rabbitmq.yaml</files>
  <read_first>k8s/staging/20-rabbitmq.yaml</read_first>
  <action>
    Edit 20-rabbitmq.yaml to enable the native prometheus plugin (MET-04), preserving everything else (the erlang-cookie initContainer, probes, QoS, app-critical priorityClass, automountServiceAccountToken false). Add a ConfigMap rabbitmq-enabled-plugins (namespace solid-stats-staging, standard labels) with data key enabled_plugins whose value is exactly the Erlang term list enabling the management and prometheus plugins, ending with a period and a trailing newline (Pitfall 5 — wrong format crashes the pod). Add to the StatefulSet pod: a volume enabled-plugins from that ConfigMap, and a volumeMount on the rabbitmq container at mountPath /etc/rabbitmq/enabled_plugins with subPath enabled_plugins. Add containerPort 15692 (name prometheus) to the rabbitmq container ports. Add a Service port prometheus port 15692 targetPort prometheus to the existing rabbitmq Service (keep amqp 5672 + management 15672). The Service stays ClusterIP (no external exposure — T-13-13). Add a YAML comment that applying this triggers a rolling restart of rabbitmq-0 (~30s AMQP downtime, acceptable for staging) and is operator-gated in 13-06. Confirm validate-staging.py still passes (this file is in the runtime glob).
  </action>
  <verify>
    <automated>grep -q '15692' k8s/staging/20-rabbitmq.yaml && grep -q 'rabbitmq_prometheus' k8s/staging/20-rabbitmq.yaml && grep -q 'enabled_plugins' k8s/staging/20-rabbitmq.yaml && python3 scripts/validate-staging.py</automated>
  </verify>
  <done>20-rabbitmq.yaml exposes port 15692 on Service + container, mounts the enabled_plugins ConfigMap with the period-terminated Erlang term list, Service still ClusterIP; validate-staging.py passes.</done>
  <acceptance_criteria>
    - Service has a 15692 port; container has containerPort 15692.
    - ConfigMap rabbitmq-enabled-plugins value ends with a period and a newline.
    - volumeMount uses subPath enabled_plugins at /etc/rabbitmq/enabled_plugins.
    - validate-staging.py exits 0 (runtime manifest shape still valid).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Author deploy-observability.yml (DEP-02, DEP-03)</name>
  <files>.github/workflows/deploy-observability.yml</files>
  <read_first>.github/workflows/deploy-staging.yml</read_first>
  <action>
    Create .github/workflows/deploy-observability.yml mirroring deploy-staging.yml structure (validate then deploy jobs; checkout@v6; WireGuard install + scripts/wg-tunnel-up.sh with WG_LOCAL_IP 10.8.0.3/32; scripts/kubeconfig-setup.sh) but fully independent per 13-RESEARCH section CI Workflow Design table: concurrency group infrastructure-obs-deploy (cancel-in-progress false); env K8S_NAMESPACE monitoring; trigger on push to master + workflow_dispatch (NOT pull_request, to avoid coupling). validate job: test -d k8s/observability, find k8s/observability -name '*.yaml' sorted, run python3 scripts/validate-obs-manifests.py. deploy job (environment staging, needs validate, master-only): kubeconfig-setup with env K8S_TOKEN from secrets.K8S_OBS_TOKEN, K8S_CA_CERT from secrets.K8S_CA_CERT, K8S_USER_NAME obs-ci-deployer, K8S_CONTEXT_NAME obs-k3s-staging, K8S_NAMESPACE monitoring; a Render-and-apply-obs-secrets step running python3 scripts/render-obs-secrets.py to a mktemp file with trap-rm and kubectl apply -n monitoring -f, env GRAFANA_ADMIN_PASSWORD + PG_MONITOR_PASSWORD from secrets; an Apply-obs-manifests step doing find k8s/observability -maxdepth 1 -name '*.yaml' sorted piped to sed -f and xargs kubectl apply -n monitoring (top-level only — values/ and dashboards/ are inputs; dashboards ship as 60-grafana-dashboards.yaml); a Verify-rollouts step with rollout status for deployment/prometheus-server, deployment/grafana, deployment/kube-state-metrics, daemonset/node-exporter, deployment/postgres-exporter. CRITICAL DEP-03: this workflow declares NO needs on any deploy-staging job and shares NO concurrency group with it — the two run in parallel and an obs failure never blocks runtime CD. Do NOT modify deploy-staging.yml.
  </action>
  <verify>
    <automated>grep -q 'infrastructure-obs-deploy' .github/workflows/deploy-observability.yml && grep -q 'K8S_OBS_TOKEN' .github/workflows/deploy-observability.yml && grep -q 'obs-ci-deployer' .github/workflows/deploy-observability.yml && grep -q 'render-obs-secrets.py' .github/workflows/deploy-observability.yml && git diff --quiet .github/workflows/deploy-staging.yml</automated>
  </verify>
  <done>deploy-observability.yml exists with its own concurrency group, obs-ci-deployer/K8S_OBS_TOKEN, obs secret render step, monitoring-namespace apply of k8s/observability/*.yaml, and rollout verification; deploy-staging.yml is byte-identical (DEP-03).</done>
  <acceptance_criteria>
    - Concurrency group infrastructure-obs-deploy; env K8S_NAMESPACE monitoring.
    - Uses secrets.K8S_OBS_TOKEN and K8S_USER_NAME obs-ci-deployer.
    - Renders obs secrets via render-obs-secrets.py; applies only top-level k8s/observability/*.yaml.
    - No needs/dependency on any deploy-staging job; deploy-staging.yml unchanged (git diff --quiet passes).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub runner → k3s API (WireGuard) | obs-ci-deployer SA token authenticates namespace-scoped apply into monitoring |
| operator → cluster RBAC | Prometheus cluster-read ClusterRole applied only by operator (bootstrap file) |
| Prometheus → rabbitmq:15692 (solid-stats-staging) | Cross-namespace metrics scrape over ClusterIP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-12 | Elevation of Privilege | Prometheus runtime ClusterRole | mitigate | Read-only verbs only (get/list/watch); applied via operator bootstrap, never widened; obs-ci-deployer stays namespace-scoped |
| T-13-13 | Information Disclosure | rabbitmq 15692 metrics port | mitigate | Service stays ClusterIP; port 15692 not exposed externally; no Ingress in Phase 13 |
| T-13-14 | Tampering/DoS | obs deploy coupling to runtime CD | mitigate | DEP-03: separate workflow, separate concurrency group, no needs on deploy-staging; obs failure cannot block runtime deploy |
| T-13-15 | Information Disclosure | obs secrets in CI logs | mitigate | render-obs-secrets.py output to mktemp with trap-rm; GRAFANA_ADMIN_PASSWORD/PG_MONITOR_PASSWORD from GitHub env secrets only |
| T-13-SC | Tampering | rabbitmq plugin config | mitigate | enabled_plugins term list pinned in repo (no remote fetch); rolling restart operator-gated in 13-06 |
</threat_model>

<verification>
- 01-obs-rbac.yaml: prometheus SA + read-only prometheus-monitoring ClusterRole + binding present; no write verbs.
- 20-rabbitmq.yaml: port 15692 + enabled_plugins ConfigMap; validate-staging.py passes.
- deploy-observability.yml: own concurrency group, obs-ci-deployer, renders obs secrets, applies obs manifests; deploy-staging.yml unchanged.
</verification>

<success_criteria>
DEP-02 (independent obs workflow with own concurrency group + obs-ci-deployer + WireGuard) and DEP-03 (runtime CD has no dependency on obs deploy) satisfied. MET-04 manifest path ready (rabbitmq 15692 + native plugin). The Prometheus cluster-read RBAC is staged in the operator-bootstrap file for application in 13-05.
</success_criteria>

<output>
Create `.planning/phases/13-deploy-pipeline-metrics-stack/13-04-SUMMARY.md` when done.
</output>
