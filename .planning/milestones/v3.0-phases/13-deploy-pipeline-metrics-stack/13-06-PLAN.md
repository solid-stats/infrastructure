---
phase: 13-deploy-pipeline-metrics-stack
plan: 06
type: execute
wave: 3
depends_on: [13-05]
files_modified:
  - k8s/observability/values/prometheus-values.yaml
  - k8s/observability/10-prometheus.yaml
  - k8s/observability/50-grafana.yaml
  - k8s/observability/40-postgres-exporter.yaml
  - k8s/observability/20-kube-state-metrics.yaml
  - k8s/observability/30-node-exporter.yaml
autonomous: false
requirements: [MET-01, MET-02, MET-03, MET-04, MET-05, MET-06]
user_setup: []
must_haves:
  truths:
    - "The metrics stack is live in monitoring: Prometheus, Grafana, kube-state-metrics, node-exporter, postgres-exporter all Running with priorityClassName obs-background"
    - "rabbitmq has been rolling-restarted with the prometheus plugin and exposes 15692"
    - "Prometheus /targets shows kube-state-metrics, node-exporter, postgres-exporter (pg_up==1), and rabbitmq (rabbitmq_identity_info) all UP, retention 15d"
    - "Grafana has Prometheus as a healthy datasource and >=4 provisioned dashboards rendering live data, reachable via port-forward (no public edge)"
    - "Observed working set from kubectl top pods is recorded and the ASSUMED resource sizing is right-sized if any pod approaches its limit"
  artifacts:
    - path: "k8s/observability/values/prometheus-values.yaml"
      provides: "Right-sized resource values after live observation (if adjusted)"
  key_links:
    - from: "Prometheus targets"
      to: "all four exporter Services + rabbitmq 15692"
      via: "scrapeConfigs static_configs; /targets health up"
      pattern: "activeTargets"
    - from: "Grafana"
      to: "Prometheus datasource + dashboard ConfigMaps"
      via: "provisioned datasource health OK; sidecar-discovered dashboards"
      pattern: "datasources"
---

<objective>
Operator-gated live application of the metrics stack and end-to-end validation. Apply the obs manifests (via the deploy-observability workflow or operator kubectl), trigger the rabbitmq rolling restart for the prometheus plugin, run `scripts/validate-phase-13.sh`, confirm `/targets` + Grafana, then right-size the ASSUMED resource budget from `kubectl top pods`. This closes MET-01..06 live.

Purpose: All sizing values and several config assumptions in 13-RESEARCH are flagged ASSUMED (A1–A9). They can only be confirmed against the live RAM-bound node. Live apply also exercises the cross-namespace scrape, the pg_monitor DSN, and the rabbitmq plugin restart — all of which touch live runtime state and so are operator-gated (autonomous:false).

Output: live healthy metrics stack; recorded `kubectl top pods -n monitoring` evidence; right-sized values + re-rendered manifests if any pod approaches its limit.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-deploy-pipeline-metrics-stack/13-RESEARCH.md
@.planning/phases/13-deploy-pipeline-metrics-stack/13-VALIDATION.md
@docs/observability.md
@.planning/phases/13-deploy-pipeline-metrics-stack/13-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Apply the obs stack live + rolling-restart rabbitmq</name>
  <action>Operator-gated live apply of the obs manifests and the rabbitmq plugin edit. See how-to-verify for the exact apply sequence; this touches live runtime state and is not auto-runnable.</action>
  <what-built>The full obs manifest set (13-02/03), the rabbitmq plugin edit (13-04), the deploy-observability workflow (13-04), and the bootstrap (13-05: RBAC, pg_monitor role, GitHub secrets) are all in place. This step applies the stack to the live RAM-bound staging node and triggers the rabbitmq rolling restart — both touch live runtime state.</what-built>
  <how-to-verify>
    With WireGuard up and the bootstrap (13-05) confirmed done:
    1. Apply the obs stack — either push to master to run deploy-observability.yml, or operator-apply: render obs secrets (`GRAFANA_ADMIN_PASSWORD=... PG_MONITOR_PASSWORD=... python3 scripts/render-obs-secrets.py | kubectl apply -n monitoring -f -`) then `find k8s/observability -maxdepth 1 -name '*.yaml' | sort | sed 's/^/-f /' | xargs kubectl apply -n monitoring`.
    2. Confirm all obs pods reach Running: `kubectl -n monitoring get pods` — prometheus-server, grafana, kube-state-metrics, node-exporter (DaemonSet), postgres-exporter. Watch for CrashLoopBackOff (Grafana SQLite Pitfall 4; check fsGroup 472).
    3. Apply the rabbitmq edit (ships via runtime CD glob on push, or `kubectl apply -f k8s/staging/20-rabbitmq.yaml -n solid-stats-staging`). It triggers a ~30s rolling restart of rabbitmq-0. Confirm rabbitmq-0 returns to Ready and `kubectl -n solid-stats-staging get svc rabbitmq` shows port 15692 (Pitfall 5 — if CrashLoopBackOff, check the enabled_plugins period/format).
    4. Confirm the app workloads (postgres, server-2) stay Running and Guaranteed — obs must not have displaced them.
  </how-to-verify>
  <resume-signal>Type "approved" once all obs pods are Running, rabbitmq-0 is Ready with port 15692, and the app workloads are unaffected — or describe failures.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run validate-phase-13.sh + confirm targets and Grafana (MET-01..06)</name>
  <files>scripts/validate-phase-13.sh</files>
  <read_first>.planning/phases/13-deploy-pipeline-metrics-stack/13-VALIDATION.md</read_first>
  <action>
    With the stack live and WireGuard up, run `GRAFANA_ADMIN_PASSWORD=... bash scripts/validate-phase-13.sh` (the harness from 13-01). It asserts MET-01 (Prometheus Running + retention 15d), MET-02 (kube-state-metrics + node-exporter targets UP), MET-03 (postgres-exporter UP + pg_up==1), MET-04 (rabbitmq target UP + rabbitmq_identity_info present), MET-05 (Grafana datasource health OK), MET-06 (>=4 dashboards provisioned). Fix-forward any RED: empty /targets → check Prometheus SD/ClusterRole (Pitfall 6) and static_config FQDNs; postgres-exporter down → check the pg_monitor DSN/role (13-05); rabbitmq down → recheck the plugin/port; Grafana datasource unhealthy → check the prometheus-server svc URL/port. After all targets are UP, manually port-forward Grafana (`kubectl -n monitoring port-forward svc/grafana 3000:80`) and confirm each of the 4 dashboards renders NON-ZERO live data (MET-06 manual check). Record the validation output.
  </action>
  <verify>
    <automated>GRAFANA_ADMIN_PASSWORD="${GRAFANA_ADMIN_PASSWORD:-x}" bash scripts/validate-phase-13.sh --quick</automated>
  </verify>
  <done>validate-phase-13.sh passes all MET-01..06 live assertions; operator confirms dashboards render non-zero data via port-forward.</done>
  <acceptance_criteria>
    - All four Prometheus targets (kube-state-metrics, node-exporter, postgres-exporter, rabbitmq) report health up.
    - pg_up==1 and rabbitmq_identity_info present.
    - Grafana datasource health == OK; >=4 dashboards listed.
    - Operator confirms non-zero panels on all 4 dashboards (manual MET-06).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Right-size from kubectl top pods (A1–A4) + re-render</name>
  <files>k8s/observability/values/prometheus-values.yaml, k8s/observability/10-prometheus.yaml, k8s/observability/50-grafana.yaml, k8s/observability/40-postgres-exporter.yaml, k8s/observability/20-kube-state-metrics.yaml, k8s/observability/30-node-exporter.yaml</files>
  <read_first>.planning/phases/13-deploy-pipeline-metrics-stack/13-RESEARCH.md</read_first>
  <action>
    Let the stack run long enough to settle (let Prometheus scrape a few cycles), then record `kubectl top pods -n monitoring` and `kubectl top nodes`. Compare each pod's working set to the ASSUMED limits in 13-RESEARCH § Resource Sizing (A1–A4). For ANY pod whose RSS approaches its memory limit (Pitfall 7 — kernel OOM does not respect PriorityClass), RAISE that limit in the corresponding values file to >= 2x observed idle RSS and re-render the affected manifest with the same pinned `helm template` command (do NOT hand-edit the rendered YAML — change the values file and re-render so git stays the source of truth). Also confirm `prometheus_tsdb_head_series` to validate the 8Gi PVC retention assumption (A1) — if series count is far above the ~50k estimate, note it. Confirm the total obs working set stays under the ~2.5Gi headroom and that postgres/server-2 were never evicted. Re-run validate-obs-manifests.py after any re-render. Record the observed numbers in the SUMMARY (replaces the ASSUMED flags with measured values).
  </action>
  <verify>
    <automated>python3 scripts/validate-obs-manifests.py && grep -q 'obs-background' k8s/observability/10-prometheus.yaml</automated>
  </verify>
  <done>kubectl top evidence recorded; any near-limit pod right-sized via values + re-render; static gate still passes; app workloads never evicted.</done>
  <acceptance_criteria>
    - `kubectl top pods -n monitoring` numbers recorded in the SUMMARY.
    - Any pod near its limit had its limit raised in the values file and the manifest re-rendered (not hand-edited).
    - validate-obs-manifests.py exits 0 after re-render.
    - Total obs working set < node headroom; postgres/server-2 not evicted.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/port-forward → Grafana/Prometheus | Internal-only access during validation (no public edge in Phase 13) |
| obs pods → node memory | obs-background pods must be evicted before app-critical under pressure |
| Prometheus → cross-namespace targets | Live read-only scrape of postgres/rabbitmq exporters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-19 | Denial of Service | obs pods OOM under app load | mitigate | obs-background evicts first; limits raised to >=2x observed RSS (Pitfall 7); app stays Guaranteed |
| T-13-20 | Denial of Service | rabbitmq rolling restart | accept | ~30s AMQP downtime acceptable for staging; operator-gated, app-critical priority preserved |
| T-13-21 | Information Disclosure | Grafana/Prometheus during validation | accept | Access only via operator port-forward; no Ingress/TLS until Phase 14 |
| T-13-SC | Tampering | re-render reproducibility | mitigate | Right-sizing changes values files + re-renders with pinned chart versions; rendered YAML never hand-edited |
</threat_model>

<verification>
- `bash scripts/validate-phase-13.sh` passes all MET-01..06.
- Operator confirms non-zero dashboard panels (manual MET-06).
- `kubectl top pods -n monitoring` recorded; near-limit pods right-sized; validate-obs-manifests.py still passes.
- postgres/server-2 never evicted.
</verification>

<success_criteria>
MET-01 through MET-06 confirmed LIVE: Prometheus standalone with bounded retention and all four targets UP, Grafana with a healthy Prometheus datasource and >=4 dashboards rendering live data reachable internally (no public edge). Resource sizing right-sized from real measurements. Phase 13 internally validated and ready for `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/13-deploy-pipeline-metrics-stack/13-06-SUMMARY.md` when done, recording the validate-phase-13.sh output, kubectl top numbers, any limit adjustments, and the prometheus_tsdb_head_series count.
</output>
