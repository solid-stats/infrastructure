---
phase: 13-deploy-pipeline-metrics-stack
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/observability/values/prometheus-values.yaml
  - k8s/observability/values/kube-state-metrics-values.yaml
  - k8s/observability/values/node-exporter-values.yaml
  - k8s/observability/values/postgres-exporter-values.yaml
  - k8s/observability/10-prometheus.yaml
  - k8s/observability/20-kube-state-metrics.yaml
  - k8s/observability/30-node-exporter.yaml
  - k8s/observability/40-postgres-exporter.yaml
autonomous: true
requirements: [DEP-01, MET-01, MET-02, MET-03]
user_setup: []
must_haves:
  truths:
    - "helm template renders Prometheus + kube-state-metrics + node-exporter + postgres-exporter into committed static YAML under k8s/observability/ (no operator, no CRDs)"
    - "Prometheus rendered manifest sets retention 15d + retentionSize 5GB on an 8Gi PVC, priorityClassName obs-background, namespace monitoring, and references the pre-created serviceAccountName prometheus"
    - "Every obs pod spec carries priorityClassName obs-background and tight requests/limits per the research sizing table"
    - "Prometheus scrape_configs use static_configs targeting kube-state-metrics, node-exporter, postgres-exporter ClusterIP services and rabbitmq.solid-stats-staging.svc:15692"
    - "postgres-exporter reads its DATA_SOURCE_NAME from the postgres-monitor-secret (pg_monitor non-superuser DSN), never a superuser DSN in YAML"
  artifacts:
    - path: "k8s/observability/10-prometheus.yaml"
      provides: "Prometheus server Deployment/ConfigMap/Service/PVC (MET-01)"
      contains: "obs-background"
    - path: "k8s/observability/20-kube-state-metrics.yaml"
      provides: "kube-state-metrics workload (MET-02)"
    - path: "k8s/observability/30-node-exporter.yaml"
      provides: "node-exporter DaemonSet (MET-02)"
    - path: "k8s/observability/40-postgres-exporter.yaml"
      provides: "postgres-exporter Deployment + Service (MET-03)"
    - path: "k8s/observability/values/prometheus-values.yaml"
      provides: "helm values: retention, scrapeConfigs, resources, SA name"
  key_links:
    - from: "k8s/observability/10-prometheus.yaml"
      to: "k8s/staging/01-obs-rbac.yaml (prometheus SA + ClusterRole, added in 13-04)"
      via: "serviceAccountName: prometheus (server.serviceAccount.create=false)"
      pattern: "serviceAccountName: prometheus"
    - from: "k8s/observability/40-postgres-exporter.yaml"
      to: "postgres-monitor-secret (rendered by render-obs-secrets.py in 13-01)"
      via: "DATA_SOURCE_NAME from secretKeyRef postgres-monitor-secret.dsn"
      pattern: "postgres-monitor-secret"
---

<objective>
Render the Prometheus server and the three metrics exporters (kube-state-metrics, node-exporter, postgres-exporter) from pinned helm charts into committed static YAML under `k8s/observability/`. This is the DEP-01 render-then-commit core and delivers MET-01, MET-02, MET-03 manifests.

Purpose: Establish the git-as-source-of-truth metrics stack. `helm template` is a build-time tool only — no in-cluster helm, no operator, no CRDs (per REQUIREMENTS Out of Scope). The rendered YAML is what CI applies; the values files are the auditable input.

Output: 4 values files under `k8s/observability/values/` + 4 rendered manifests (`10-prometheus.yaml`, `20-kube-state-metrics.yaml`, `30-node-exporter.yaml`, `40-postgres-exporter.yaml`).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-deploy-pipeline-metrics-stack/13-RESEARCH.md

# Conventions + live targets:
@k8s/staging/02-priority-classes.yaml
@k8s/staging/10-postgres.yaml
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install helm locally + author the 4 values files</name>
  <files>k8s/observability/values/prometheus-values.yaml, k8s/observability/values/kube-state-metrics-values.yaml, k8s/observability/values/node-exporter-values.yaml, k8s/observability/values/postgres-exporter-values.yaml</files>
  <read_first>.planning/phases/13-deploy-pipeline-metrics-stack/13-RESEARCH.md</read_first>
  <action>
    helm is NOT installed locally (confirmed). First obtain it: download the official helm v3 release tarball for linux-amd64 from get.helm.sh, extract the `helm` binary to a writable PATH location (e.g. ~/.local/bin or a repo-local tools dir) — do NOT use `npx`. Confirm `helm version --short`. Add the chart repos: `helm repo add prometheus-community https://prometheus-community.github.io/helm-charts` and `helm repo add grafana https://grafana.github.io/helm-charts`, then `helm repo update`. Run `helm show chart prometheus-community/prometheus --version 29.11.0` to confirm appVersion v3.x and resolve Open Question 1 (the rendered SA name). Then author the four values files per 13-RESEARCH § Code Examples and § Resource Sizing:
    - prometheus-values.yaml: `server.retention: "15d"`, `server.retentionSize: "5GB"`, `server.priorityClassName: obs-background`, `server.persistentVolume.size: 8Gi` (default storageClass), `server.resources` 256Mi/512Mi mem + 100m/500m cpu, `server.securityContext` runAsNonRoot 65534/fsGroup 65534, `server.service.type: ClusterIP`, `server.serviceAccount.create: false` + `server.serviceAccount.name: prometheus` (the SA + ClusterRole are operator-applied in 13-04), `alertmanager.enabled: false`, `pushgateway.enabled: false`, `kube-state-metrics.enabled: false`, `prometheus-node-exporter.enabled: false`. scrapeConfigs: keep prometheus self-scrape; disable default kube-api/cadvisor/pods jobs; add static_configs jobs `kube-state-metrics` → `kube-state-metrics.monitoring.svc:8080`, `node-exporter` → `node-exporter.monitoring.svc:9100`, `postgres-exporter` → `postgres-exporter.monitoring.svc:9187`, `rabbitmq` → `rabbitmq.solid-stats-staging.svc:15692` (cross-namespace FQDN, MET-04 target). Global scrape interval 30s.
    - kube-state-metrics-values.yaml: `priorityClassName: obs-background`, resources 32Mi/128Mi + 10m/100m, ClusterIP service on 8080.
    - node-exporter-values.yaml: `priorityClassName: obs-background`, resources 20Mi/64Mi + 10m/100m, hostNetwork as chart default, service on 9100.
    - postgres-exporter-values.yaml: `priorityClassName: obs-background`, resources 32Mi/64Mi + 10m/50m, chart `config.datasourceSecret` pointing at `postgres-monitor-secret` key `dsn` (DATA_SOURCE_NAME via existingSecret — never an inline DSN), app image ≥ v0.15.0 (chart 8.0.0 → app v0.19.1, MET-03), service on 9187. Confirm the exact existing-secret value key names with `helm show values prometheus-community/prometheus-postgres-exporter --version 8.0.0` and use the chart's native existing-secret field.
    Pin EVERY chart to the versions in 13-RESEARCH § Standard Stack. No secret values in any values file.
  </action>
  <verify>
    <automated>test -f k8s/observability/values/prometheus-values.yaml && grep -q '15d' k8s/observability/values/prometheus-values.yaml && grep -q 'obs-background' k8s/observability/values/prometheus-values.yaml && grep -q 'serviceAccount' k8s/observability/values/prometheus-values.yaml && grep -q 'postgres-monitor-secret' k8s/observability/values/postgres-exporter-values.yaml</automated>
  </verify>
  <done>helm binary available; 4 values files exist with retention 15d, obs-background priorityClass, prometheus SA create=false/name=prometheus, static_configs for all 4 targets incl rabbitmq cross-ns, postgres-exporter wired to postgres-monitor-secret.</done>
  <acceptance_criteria>
    - `helm version --short` prints a v3 version.
    - prometheus-values.yaml contains retention 15d, retentionSize 5GB, obs-background, serviceAccount.create false, the four static_configs target FQDNs.
    - postgres-exporter-values.yaml references postgres-monitor-secret (no inline DSN).
    - No secret value literal in any values file.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render the 4 manifests with helm template (DEP-01, MET-01/02/03)</name>
  <files>k8s/observability/10-prometheus.yaml, k8s/observability/20-kube-state-metrics.yaml, k8s/observability/30-node-exporter.yaml, k8s/observability/40-postgres-exporter.yaml</files>
  <read_first>k8s/observability/values/prometheus-values.yaml</read_first>
  <action>
    Render each chart with `helm template <release> <chart> --version <pinned> --namespace monitoring --values <values-file> > k8s/observability/<NN>-<name>.yaml`, using the release names that produce the service names referenced in scrapeConfigs: prometheus (server svc → `prometheus-server`), kube-state-metrics (svc → `kube-state-metrics`), node-exporter (svc → `node-exporter`), postgres-exporter (svc → `postgres-exporter`). If a chart's default service name does not match the static_configs FQDN, set the chart's `fullnameOverride`/`nameOverride` in the values file and re-render so the Service name matches exactly. After rendering, manually verify (do NOT inline code in this action) that every `namespace:` in each file is `monitoring` (Pitfall 3) and every pod template carries `priorityClassName: obs-background` (Pitfall 3/7) — fix the values file and re-render if not. Confirm `10-prometheus.yaml` references `serviceAccountName: prometheus` (Pitfall 2/8, A8) and does NOT contain a ClusterRole/ClusterRoleBinding (those are operator-applied in 13-04). Confirm `40-postgres-exporter.yaml` sources DATA_SOURCE_NAME from postgres-monitor-secret and contains no superuser DSN literal. The render command pattern lives in the SUMMARY so re-rendering after a chart bump is reproducible. Numeric filename prefixes preserve apply order.
  </action>
  <verify>
    <automated>python3 scripts/validate-obs-manifests.py && grep -q 'serviceAccountName: prometheus' k8s/observability/10-prometheus.yaml && grep -q '15d' k8s/observability/10-prometheus.yaml && ! grep -rq 'kind: ClusterRole' k8s/observability/10-prometheus.yaml && echo "render checks OK"</automated>
  </verify>
  <done>Four rendered manifests exist; validate-obs-manifests.py passes (namespace monitoring + obs-background on all pods + no secret values); Prometheus references the pre-created SA and 15d retention; no ClusterRole in the rendered obs YAML.</done>
  <acceptance_criteria>
    - validate-obs-manifests.py (from 13-01) exits 0 against the rendered manifests.
    - 10-prometheus.yaml: `serviceAccountName: prometheus`, retention `15d`, no `kind: ClusterRole`.
    - Service names match the scrapeConfig FQDNs (kube-state-metrics, node-exporter, postgres-exporter on 8080/9100/9187).
    - 40-postgres-exporter.yaml has no superuser DSN literal.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prometheus (monitoring) → targets in solid-stats-staging | Cross-namespace scrape of rabbitmq:15692; read-only metrics pull |
| postgres-exporter → postgres (solid-stats-staging) | DB connection using pg_monitor (non-superuser) DSN from Secret |
| rendered YAML → git | Manifests committed; must contain zero secret values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Elevation of Privilege | postgres-exporter DSN | mitigate | DSN uses built-in pg_monitor role (created in 13-05); never superuser; loaded from postgres-monitor-secret only |
| T-13-05 | Information Disclosure | Prometheus /metrics + /targets | accept | ClusterIP only, no Ingress in Phase 13; operator access via port-forward (TLS deferred to Phase 14) |
| T-13-06 | Information Disclosure | rendered obs manifests in git | mitigate | validate-obs-manifests.py greps for secret values in CI validate job |
| T-13-07 | Elevation of Privilege | Prometheus runtime SA over-broad | mitigate | server.serviceAccount.create=false; SA bound to read-only ClusterRole defined in 13-04 (get/list/watch only) |
| T-13-SC | Tampering | helm chart provenance | mitigate | Charts pinned to versions audited in 13-RESEARCH § Package Legitimacy Audit (all OK/Approved); pinned `--version` on every render |
</threat_model>

<verification>
- `helm version --short` confirms a usable binary.
- `python3 scripts/validate-obs-manifests.py` passes against the 4 rendered manifests.
- Prometheus manifest: SA=prometheus, retention 15d, no ClusterRole; scrapeConfigs target all 4 jobs.
- postgres-exporter manifest: DSN from postgres-monitor-secret, no superuser literal.
</verification>

<success_criteria>
DEP-01 satisfied (charts rendered + committed under k8s/observability/, no operator/CRDs). MET-01 (Prometheus standalone, tuned interval, bounded retention on PVC), MET-02 (kube-state-metrics + node-exporter), and MET-03 (postgres-exporter ≥ v0.15.0, pg_monitor DSN) manifests authored and passing the static gate.
</success_criteria>

<output>
Create `.planning/phases/13-deploy-pipeline-metrics-stack/13-02-SUMMARY.md` when done. Record the exact `helm template` commands + chart versions so re-render is reproducible.
</output>
