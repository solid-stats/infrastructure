---
phase: 16-error-tracking-glitchtip
plan: 04
type: execute
wave: 2
depends_on: ["16-01", "16-02", "16-03"]
files_modified:
  - k8s/observability/90-glitchtip-postgres.yaml
  - k8s/observability/91-glitchtip.yaml
  - scripts/validate-phase-16.sh
autonomous: true
requirements: [ERR-01, ERR-02, ERR-03]
must_haves:
  truths:
    - "GlitchTip web + worker + own-postgres are Running in error-tracking with no Valkey/Redis pod (ERR-01)"
    - "The migrate Job completed and the superuser seed Job created the only login account (ERR-01)"
    - "GET /api/0/config/ reports user_registration_enabled:false and registration POSTs are rejected (ERR-02)"
    - "An org + project + DSN exist and a forced Sentry envelope appears as an issue in GlitchTip (ERR-03 internal)"
  artifacts:
    - path: "scripts/validate-phase-16.sh"
      provides: "the harness that is RUN here and confirmed green (resource limits may be re-tuned in 90/91)"
      contains: "user_registration_enabled"
  key_links:
    - from: "test-glitchtip-ingest.sh"
      to: "glitchtip-web /api/PROJECT_ID/envelope/"
      via: "forced Sentry envelope POST via port-forward, then issue verify"
      pattern: "envelope"
---

<objective>
Live-deploy GlitchTip into the staging cluster's `error-tracking` namespace and prove ERR-01/02/03 end-to-end internally (no public DNS needed): apply the manifests, run the migrate + seed Jobs in order, create the org/project/DSN with the seeded superuser, POST a forced Sentry test error via port-forward, confirm it appears, and right-size the resource limits against `kubectl top`. This is the autonomous internal acceptance of the phase — only the public TLS cutover (16-05) remains operator-gated.

Purpose: Wave 1 authored the manifests/scripts/pipeline. This plan executes them against live k3s (the agent has cluster access via the staging VPS / WireGuard) and turns the ASSUMED sizing + ASSUMED bin/start.sh/createsuperuser behaviors into verified facts, tuning 90/91 if needed.
Output: live GlitchTip running + a green `validate-phase-16.sh --internal`; possibly-adjusted resource limits in 90/91; the project DSN recorded in the SUMMARY (no secret values).
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-error-tracking-glitchtip/16-RESEARCH.md
@.planning/phases/16-error-tracking-glitchtip/16-VALIDATION.md
@.planning/phases/16-error-tracking-glitchtip/16-01-SUMMARY.md
@.planning/phases/16-error-tracking-glitchtip/16-02-SUMMARY.md
@.planning/phases/16-error-tracking-glitchtip/16-03-SUMMARY.md

@k8s/observability/90-glitchtip-postgres.yaml
@k8s/observability/91-glitchtip.yaml
@scripts/validate-phase-16.sh
@scripts/test-glitchtip-ingest.sh
@docs/glitchtip.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-apply GlitchTip + run migrate/seed in first-run order, verify ERR-01</name>
  <files>k8s/observability/90-glitchtip-postgres.yaml, k8s/observability/91-glitchtip.yaml</files>
  <read_first>
    - 16-02-SUMMARY.md (the recorded bin/start.sh SERVER_ROLE finding + createsuperuser fallback) — if SERVER_ROLE dispatch was NOT confirmed offline, confirm it live here and apply the command fallback to 91 if needed
    - 16-RESEARCH.md "Pattern 3", "Pitfall 3/4/7", "Resource Sizing", "Assumptions Log" A1/A2/A4
    - the obs deploy mechanics in deploy-observability.yml (the error-tracking token + apply order)
  </read_first>
  <action>
    Ensure the operator prerequisites are in place: the four GLITCHTIP_* GitHub secrets + K8S_OBS_ET_TOKEN are set (per docs/glitchtip.md); the error-tracking namespace + obs-ci-deployer RBAC exist (01-obs-rbac.yaml — operator-applied). If any are missing, this becomes a blocking operator step (surface it, do not fabricate secret values).
    Deploy via the obs pipeline (preferred: trigger deploy-observability.yml) OR, with direct cluster access, render the error-tracking secrets and apply 90/91/92/93 in order with the error-tracking-scoped context:
      1. apply glitchtip-postgres-auth + glitchtip-secrets (error-tracking) from render-obs-secrets.py output.
      2. apply 90-glitchtip-postgres.yaml; wait rollout statefulset/glitchtip-postgres Ready.
      3. apply 92-glitchtip-migrate.yaml; `kubectl wait --for=condition=complete job/glitchtip-migrate -n error-tracking --timeout=180s`. If migrate fails, capture logs and fix (most likely VALKEY_URL not empty → Pitfall 3, or DATABASE_URL wrong).
      4. apply 91-glitchtip.yaml; wait deployment/glitchtip-web + glitchtip-worker Ready. If the worker pod logs show Granian (not runworker) → the SERVER_ROLE dispatch assumption (A1) is wrong: apply the explicit-command fallback from 16-RESEARCH Pitfall 4 to 91-glitchtip.yaml (web granian command, worker runworker --scheduler command) and re-apply. If the worker crashes on redis ConnectionError → VALKEY_URL is not empty in that container; fix.
      5. apply 93-glitchtip-seed.yaml; `kubectl wait --for=condition=complete job/glitchtip-seed`. If createsuperuser --noinput is unsupported (A4) → use the documented manage.py shell create_superuser fallback (run it once via `kubectl exec` into the web pod or as a patched seed Job).
    Then run `kubectl top pods -n error-tracking` after a few minutes of warm-up. If any pod is near/over its memory limit or was OOMKilled (Pitfall 7), raise the limit in 90/91 to >= observed P95 and re-apply; if a pod is heavily over-provisioned, you MAY lower it — but never below observed usage. Record before/after numbers in the SUMMARY.
    Confirm ERR-01: glitchtip-postgres + web + worker Running; NO valkey/redis workload; migrate Job succeeded; VALKEY_URL empty in the running web/worker spec.
  </action>
  <verify>
    <automated>bash scripts/validate-phase-16.sh --internal 2>&1 | tee /tmp/v16-04a.txt; grep -Eq 'ERR-01.*PASS' /tmp/v16-04a.txt</automated>
  </verify>
  <done>glitchtip-postgres, glitchtip-web, glitchtip-worker are Running in error-tracking with no Valkey/Redis pod; the migrate Job and seed Job both completed (in that order); VALKEY_URL is empty in the live web/worker spec; resource limits in 90/91 reflect observed kubectl top (any change recorded). validate-phase-16.sh reports ERR-01 PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Verify ERR-02 (registration closed) + create org/project/DSN + forced-error test (ERR-03)</name>
  <files>scripts/validate-phase-16.sh</files>
  <read_first>
    - 16-RESEARCH.md "ERR-02 Verification Protocol", "Pattern 6", "Pitfall 8", "Open Questions" #3 (org-creation API vs manage.py fallback)
    - scripts/test-glitchtip-ingest.sh (the forced-error harness to run)
    - docs/glitchtip.md (the first-run + DSN handoff runbook to keep accurate)
  </read_first>
  <action>
    ERR-02: port-forward glitchtip-web and `curl -s localhost:8000/api/0/config/`; assert user_registration_enabled is false (Method A). Also attempt a registration POST and confirm it is rejected (4xx) (Method B, best-effort — record the exact status for A3). Confirm the seeded superuser CAN log in (obtain an auth token via the login endpoint using the superuser creds from the Secret — read them at runtime from the live Secret, do NOT print them).
    ERR-03 (create org/project/DSN): with the superuser token, create an organization + project via the GlitchTip Sentry-compatible API (POST /api/0/organizations/ then the project endpoint), then retrieve the project's DSN (client keys endpoint). If the API path differs in v6 (Open Questions #3), fall back to the documented manage.py path. Extract PUBLIC_KEY + PROJECT_ID from the DSN. Record the DSN's project slug + the DSN host shape in the SUMMARY (the public key is a non-secret client key, but treat the full DSN conservatively — record only what plan 18/operators need to wire the SDK, per docs/glitchtip.md handoff).
    ERR-03 (forced error): run `GLITCHTIP_DSN=<dsn> SUPERUSER_TOKEN=<token> bash scripts/test-glitchtip-ingest.sh`; it POSTs a unique forced Sentry envelope via port-forward and asserts the issue appears. If the issue does not appear, debug (Pitfall 8 — DSN key mismatch; or the worker not processing — check worker logs; in postgres-only mode the event may be processed inline or by the worker, confirm which).
    If validate-phase-16.sh needs a small adjustment so the live checks pass cleanly (e.g., the exact config-endpoint JSON key, or the issues-query path), fix the script — keep it the single source of truth for the phase gate. Re-run the full harness.
    Update docs/glitchtip.md if any API path / fallback differed from what was documented (keep the runbook truthful).
  </action>
  <verify>
    <automated>bash scripts/validate-phase-16.sh --internal 2>&1 | tee /tmp/v16-04b.txt; grep -Eq 'ERR-02.*PASS' /tmp/v16-04b.txt && grep -Eq 'ERR-03.*PASS' /tmp/v16-04b.txt</automated>
  </verify>
  <done>GET /api/0/config/ shows user_registration_enabled:false and registration POSTs are rejected; the seeded superuser logs in; an org+project+DSN exist; a forced Sentry envelope POSTed via port-forward appears as an issue in GlitchTip. validate-phase-16.sh reports ERR-02 and ERR-03 PASS. docs/glitchtip.md reflects any real-world deviation from the assumed API paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent (cluster access) → error-tracking | live apply + Job runs + port-forward; namespace-scoped |
| port-forward → glitchtip-web | the only ingress used; ClusterIP stays internal (public path is 16-05) |
| superuser creds (live Secret) → login | read at runtime, never printed/committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-14 | Elevation of Privilege | registration silently open after deploy | mitigate | live GET /api/0/config/ asserts user_registration_enabled:false (ERR-02); a POST register assert backs it up |
| T-16-15 | Information Disclosure | superuser password printed during login/DSN creation | mitigate | read creds from the live Secret at runtime; never echo; SUMMARY records only the non-sensitive DSN handoff shape |
| T-16-16 | Spoofing | forced-error accepted with a bogus DSN (false positive) | mitigate | test-glitchtip-ingest.sh uses the REAL retrieved DSN and asserts non-403 + issue appears (Pitfall 8) |
| T-16-17 | Denial of Service | GlitchTip OOMs and evicts app pods | mitigate | obs-background priority guarantees GlitchTip evicts BEFORE app pods; limits tuned to observed kubectl top P95 (Pitfall 7) |
| T-16-SC | Tampering | image/package supply chain | accept | no new packages; pinned v6.1.8 official image (16-RESEARCH audit) |
</threat_model>

<verification>
- `bash scripts/validate-phase-16.sh --internal` reports ERR-01, ERR-02, ERR-03 all PASS.
- `kubectl get pods -n error-tracking` shows postgres + web + worker Ready; `kubectl get job -n error-tracking` shows migrate + seed Complete.
- No valkey/redis workload exists in error-tracking.
- The forced test error is queryable as an issue via the GlitchTip API.
</verification>

<success_criteria>
- GlitchTip is live and internally proven for all three requirements (ERR-01/02/03) without any public DNS.
- Resource limits reflect real usage; any ASSUMED value from 16-RESEARCH is now verified or corrected in 90/91.
- The project DSN is recorded for the operator/Phase 18 handoff (per docs/glitchtip.md), with no secret values leaked.
</success_criteria>

<output>
Create `.planning/phases/16-error-tracking-glitchtip/16-04-SUMMARY.md` when done (record: bin/start.sh finding, createsuperuser path used, kubectl top before/after, the DSN handoff shape).
</output>
