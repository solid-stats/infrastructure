---
phase: 16-error-tracking-glitchtip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/observability/90-glitchtip-postgres.yaml
  - k8s/observability/91-glitchtip.yaml
autonomous: true
requirements: [ERR-01]
must_haves:
  truths:
    - "GlitchTip has its own dedicated PostgreSQL StatefulSet in error-tracking, separate from the app DB (ERR-01)"
    - "GlitchTip web (SERVER_ROLE=web) and worker (SERVER_ROLE=worker) run from the same image with no Valkey/Redis pod (VALKEY_URL='') (ERR-01)"
    - "Registration is closed from the first boot via ENABLE_USER_REGISTRATION=False on web (ERR-01 init order)"
  artifacts:
    - path: "k8s/observability/90-glitchtip-postgres.yaml"
      provides: "GlitchTip's own postgres StatefulSet + headless Service + ServiceAccount in error-tracking"
      contains: "kind: StatefulSet"
    - path: "k8s/observability/91-glitchtip.yaml"
      provides: "glitchtip-web Deployment + glitchtip-worker Deployment + glitchtip-web Service + glitchtip ServiceAccount"
      contains: "SERVER_ROLE"
  key_links:
    - from: "k8s/observability/91-glitchtip.yaml"
      to: "k8s/observability/90-glitchtip-postgres.yaml"
      via: "DATABASE_URL points at glitchtip-postgres.error-tracking.svc:5432 (from glitchtip-secrets)"
      pattern: "glitchtip-postgres\\.error-tracking\\.svc"
    - from: "k8s/observability/91-glitchtip.yaml"
      to: "glitchtip-secrets"
      via: "SECRET_KEY + DATABASE_URL via secretKeyRef"
      pattern: "secretKeyRef"
---

<objective>
Author the two long-lived GlitchTip workload manifests for the `error-tracking` namespace: (1) GlitchTip's own dedicated PostgreSQL StatefulSet (mirroring `k8s/staging/10-postgres.yaml`, separate from the app DB) and (2) the GlitchTip web + worker Deployments plus the `glitchtip-web` ClusterIP Service. This is the durable runtime that captures errors (ERR-01).

Purpose: ERR-01 requires GlitchTip running in PostgreSQL-only mode (no Valkey/Redis) with its own DB, with registration closed from first boot. These manifests are the steady-state workloads; the one-shot migrate/seed Jobs (plan 16-02) gate their first start.
Output: `k8s/observability/90-glitchtip-postgres.yaml`, `k8s/observability/91-glitchtip.yaml`.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-error-tracking-glitchtip/16-RESEARCH.md

# Patterns to mirror exactly
@k8s/staging/10-postgres.yaml
@k8s/observability/70-loki.yaml
@k8s/staging/01-obs-rbac.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author GlitchTip's own PostgreSQL StatefulSet (90-glitchtip-postgres.yaml)</name>
  <files>k8s/observability/90-glitchtip-postgres.yaml</files>
  <read_first>
    - k8s/staging/10-postgres.yaml (the StatefulSet + headless Service pattern to mirror: ports, readinessProbe, automountServiceAccountToken:false, volumeClaimTemplates)
    - k8s/observability/70-loki.yaml (obs-namespace pod securityContext pattern: runAsNonRoot/runAsUser/runAsGroup/fsGroup, container securityContext drop ALL + allowPrivilegeEscalation:false)
  </read_first>
  <action>
    Create a single multi-document YAML mirroring k8s/staging/10-postgres.yaml but adapted for GlitchTip's OWN database in the error-tracking namespace (ERR-01: separate from the app DB). Documents in order:
    1. ServiceAccount `glitchtip-postgres` (namespace: error-tracking, standard app.kubernetes.io labels, part-of: solid-stats).
    2. Headless Service `glitchtip-postgres` (namespace: error-tracking, port 5432 name postgres targetPort postgres, selector on app.kubernetes.io/name: glitchtip-postgres).
    3. StatefulSet `glitchtip-postgres` (namespace: error-tracking): serviceName glitchtip-postgres, replicas 1, image postgres:17-alpine, imagePullPolicy IfNotPresent, containerPort 5432 name postgres.
    Pod spec MUST set: priorityClassName: obs-background (this evicts before app workloads, and the static gate REQUIRES obs-background on every pod-bearing kind), serviceAccountName: glitchtip-postgres, automountServiceAccountToken: false.
    Env: POSTGRES_DB=glitchtip, POSTGRES_USER=glitchtip, POSTGRES_PASSWORD via secretKeyRef name glitchtip-postgres-auth key POSTGRES_PASSWORD (this Secret is rendered by plan 16-03's renderer — never inline a value).
    readinessProbe + livenessProbe: exec pg_isready -U glitchtip -d glitchtip (same cadence as 10-postgres.yaml).
    Add a pod securityContext (runAsNonRoot/runAsUser/runAsGroup/fsGroup) and container securityContext (allowPrivilegeEscalation:false, capabilities drop ALL) following the Loki pattern — postgres:17-alpine runs as uid 70 by default, so set runAsUser/runAsGroup/fsGroup to 70 and mount the data volume so the postgres user owns it. Do NOT set readOnlyRootFilesystem on postgres (it writes to its data dir and sockets).
    Resources (per 16-RESEARCH.md sizing, ASSUMED — plan 16-04 tunes vs kubectl top): requests cpu 100m memory 128Mi, limits cpu 250m memory 256Mi.
    volumeMounts: glitchtip-postgres-data at /var/lib/postgresql/data. volumeClaimTemplates: glitchtip-postgres-data, ReadWriteOnce, storage 5Gi.
    Use standard app.kubernetes.io/name: glitchtip-postgres + app.kubernetes.io/part-of: solid-stats labels on every resource.
  </action>
  <verify>
    <automated>python3 scripts/validate-obs-manifests.py 2>&1 | tee /tmp/v16-01a.txt; grep -q "PASSED" /tmp/v16-01a.txt && grep -Eq "namespace: error-tracking" k8s/observability/90-glitchtip-postgres.yaml && grep -Eq "priorityClassName: obs-background" k8s/observability/90-glitchtip-postgres.yaml</automated>
  </verify>
  <done>90-glitchtip-postgres.yaml declares a StatefulSet + headless Service + ServiceAccount in error-tracking with obs-background priority, automountServiceAccountToken:false, pg_isready probes, a 5Gi PVC, and POSTGRES_PASSWORD sourced only from secretKeyRef. NOTE: the static gate only passes once plan 16-03 extends validate-obs-manifests.py to accept error-tracking; if the gate still rejects error-tracking when this task runs, the manifest is correct — re-run the gate after 16-03 lands (same wave, parallel).</done>
</task>

<task type="auto">
  <name>Task 2: Author GlitchTip web + worker Deployments + web Service (91-glitchtip.yaml)</name>
  <files>k8s/observability/91-glitchtip.yaml</files>
  <read_first>
    - 16-RESEARCH.md "Pattern 1: PostgreSQL-Only Mode", "Pattern 2: web vs worker via SERVER_ROLE", "GlitchTip v6 Environment Variables Reference", "Resource Sizing", "Common Pitfalls" (3, 4, 6, 7)
    - k8s/observability/70-loki.yaml (securityContext + ServiceAccount + automountServiceAccountToken pattern)
    - the 90-glitchtip-postgres.yaml you just wrote (the postgres Service DNS the DATABASE_URL must target)
  </read_first>
  <action>
    Create a single multi-document YAML with these documents in order:
    1. ServiceAccount `glitchtip` (namespace: error-tracking).
    2. Service `glitchtip-web` (namespace: error-tracking, type ClusterIP, port 8000 name http targetPort http, selector app.kubernetes.io/name: glitchtip app.kubernetes.io/component: web). ClusterIP-only — no NodePort/LoadBalancer (the public cutover is operator-gated, plan 16-05).
    3. Deployment `glitchtip-web` (component: web), 4. Deployment `glitchtip-worker` (component: worker).
    Both Deployments: replicas 1, image glitchtip/glitchtip:v6.1.8, imagePullPolicy IfNotPresent, command ["./bin/start.sh"], priorityClassName: obs-background, serviceAccountName: glitchtip, automountServiceAccountToken: false.
    SHARED env (define on BOTH; use an anchor-free explicit list since this is hand-authored):
      - VALKEY_URL="" (MUST be explicit empty string — Pitfall 3: absent var defaults to redis:// and the worker crashes with ConnectionError),
      - DATABASE_URL via secretKeyRef glitchtip-secrets/DATABASE_URL (the secret value targets glitchtip-postgres.error-tracking.svc:5432; sslmode=disable per 16-RESEARCH anti-pattern, set in 16-03 renderer — do not inline),
      - SECRET_KEY via secretKeyRef glitchtip-secrets/SECRET_KEY,
      - GLITCHTIP_DOMAIN="https://errors.solid-stats.ru",
      - ENABLE_USER_REGISTRATION="False" (ERR-01 init order / ERR-02: closed from first boot — use ENABLE_USER_REGISTRATION, NOT the old ENABLE_OPEN_USER_REGISTRATION; v6 ignores the old name, Pitfall 1),
      - EMAIL_BACKEND="django.core.mail.backends.console.EmailBackend",
      - DEFAULT_FROM_EMAIL="glitchtip@stats-staging.solid-stats.ru",
      - TRUSTED_PROXIES="*",
      - GRANIAN_WORKERS="1", VTASKS_CONCURRENCY="2", DATABASE_POOL_MAX_SIZE="5".
    web Deployment ALSO sets SERVER_ROLE="web", PORT="8000", containerPort 8000 name http, and a readinessProbe + livenessProbe httpGet on a GlitchTip health path. Use httpGet path /api/0/config/ on port 8000 for readiness (it returns 200 without auth and proves the app + DB are up); liveness on / with a generous initialDelaySeconds. (NOTE: port is 8000 not 8080 — Pitfall 6.)
    worker Deployment sets SERVER_ROLE="worker" and NO ports/probes (it is not an HTTP server); add a tolerant livenessProbe via exec of a trivial process check or omit liveness and keep a restartPolicy default. Worker fallback: if plan 16-02's bin/start.sh bootstrap-check finds SERVER_ROLE is NOT dispatched by the image, replace the worker command with ["./manage.py","runworker","--scheduler"] and the web command with the granian invocation from 16-RESEARCH Pitfall 4 — leave a comment in the manifest pointing to that fallback.
    Pod securityContext: runAsNonRoot true, runAsUser/runAsGroup/fsGroup per the glitchtip image's non-root uid (the glitchtip image runs as a non-root app user; set runAsNonRoot:true and a non-zero uid — if the exact uid is unknown, set runAsNonRoot:true and fsGroup and leave runAsUser to the image default by omitting it, but you MUST keep runAsNonRoot:true). Container securityContext: allowPrivilegeEscalation:false, capabilities drop ALL. Do NOT set readOnlyRootFilesystem (GlitchTip writes temp files).
    Resources per 16-RESEARCH sizing (ASSUMED, tuned in 16-04): web requests cpu 100m mem 192Mi limits cpu 300m mem 384Mi; worker requests cpu 50m mem 128Mi limits cpu 200m mem 256Mi.
    Add a head comment block: image SHA-pinning note — v6.1.8 tag is pinned per AGENTS.md convention; record that 16-02 confirms bin/start.sh dispatch.
  </action>
  <verify>
    <automated>python3 scripts/validate-obs-manifests.py 2>&1 | tee /tmp/v16-01b.txt; grep -q "PASSED" /tmp/v16-01b.txt && grep -c 'SERVER_ROLE' k8s/observability/91-glitchtip.yaml | grep -qx 2 && grep -Eq 'VALKEY_URL' k8s/observability/91-glitchtip.yaml && grep -Eq 'ENABLE_USER_REGISTRATION' k8s/observability/91-glitchtip.yaml && grep -Eq 'glitchtip/glitchtip:v6\.1\.8' k8s/observability/91-glitchtip.yaml</automated>
  </verify>
  <done>91-glitchtip.yaml declares glitchtip ServiceAccount, glitchtip-web ClusterIP Service (port 8000), and two Deployments (web SERVER_ROLE=web with /api/0/config/ readiness, worker SERVER_ROLE=worker) both with VALKEY_URL="" explicit, ENABLE_USER_REGISTRATION=False, DATABASE_URL+SECRET_KEY via secretKeyRef, obs-background priority, automountServiceAccountToken:false, drop-ALL container securityContext, pinned image v6.1.8. Static gate passes after 16-03 extends the validator for error-tracking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → cluster (error-tracking) | obs-ci-deployer SA applies these manifests; namespace-scoped RBAC only |
| glitchtip pods → glitchtip-postgres | intra-namespace DB connection (no TLS, sslmode=disable on a private overlay) |
| internet → glitchtip-web | NOT reachable in this plan — ClusterIP only; public path is operator-gated (16-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Elevation of Privilege | glitchtip-web open registration | mitigate | ENABLE_USER_REGISTRATION="False" on web env from first boot (ERR-01/ERR-02); verified GET /api/0/config/ in 16-04 |
| T-16-02 | Information Disclosure | SECRET_KEY / DB password in manifest | mitigate | Both sourced only via secretKeyRef (glitchtip-secrets, glitchtip-postgres-auth); no literal value in YAML; static gate _check_no_secret_values enforces |
| T-16-03 | Tampering | GlitchTip postgres reachable cross-namespace | mitigate | Dedicated glitchtip-postgres Service in error-tracking; separate from app DB; default-deny NetworkPolicy is Phase 17 (accepted gap for this phase) |
| T-16-04 | Elevation of Privilege | default ServiceAccount token automount | mitigate | automountServiceAccountToken:false on every pod; dedicated glitchtip / glitchtip-postgres SAs |
| T-16-05 | Spoofing | container runs as root | mitigate | runAsNonRoot:true + drop-ALL capabilities + allowPrivilegeEscalation:false on all containers |
| T-16-SC | Tampering | glitchtip/glitchtip:v6.1.8 image | accept | Official Docker Hub image (16-RESEARCH Package Legitimacy Audit, verdict OK); tag pinned, IfNotPresent; no npm/pip/cargo install in this phase |
</threat_model>

<verification>
- `python3 scripts/validate-obs-manifests.py` passes (after 16-03 extends it for error-tracking — same wave).
- `kubectl apply --dry-run=client -f k8s/observability/90-glitchtip-postgres.yaml -f k8s/observability/91-glitchtip.yaml` parses cleanly (offline schema check; live apply is plan 16-04).
- No secret literal appears in either file (grep for inline POSTGRES_PASSWORD/SECRET_KEY values returns only secretKeyRef references).
</verification>

<success_criteria>
- Two manifests exist in k8s/observability/ declaring error-tracking-namespace resources with obs-background priority.
- GlitchTip web + worker + own-postgres are fully specified for PostgreSQL-only mode (VALKEY_URL="") with closed registration and secret-sourced credentials.
- No image/namespace/priority/security-context convention from AGENTS.md is violated.
</success_criteria>

<output>
Create `.planning/phases/16-error-tracking-glitchtip/16-01-SUMMARY.md` when done.
</output>
