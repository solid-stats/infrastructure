---
phase: 16-error-tracking-glitchtip
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/observability/92-glitchtip-migrate.yaml
  - k8s/observability/93-glitchtip-seed.yaml
autonomous: true
requirements: [ERR-01]
must_haves:
  truths:
    - "A one-shot migrate Job runs manage.py migrate before web/worker serve traffic (ERR-01 first-run order step 1)"
    - "A one-shot seed Job creates the superuser non-interactively AFTER migrate completes, never before (ERR-01 first-run order step 3)"
    - "The seed Job will not run createsuperuser against an unmigrated DB (Pitfall 2 guarded)"
  artifacts:
    - path: "k8s/observability/92-glitchtip-migrate.yaml"
      provides: "migrate Job (manage.py migrate) gated on postgres readiness, in error-tracking"
      contains: "kind: Job"
    - path: "k8s/observability/93-glitchtip-seed.yaml"
      provides: "superuser seed Job (createsuperuser --noinput) gated on migrate completion"
      contains: "createsuperuser"
  key_links:
    - from: "k8s/observability/93-glitchtip-seed.yaml"
      to: "k8s/observability/92-glitchtip-migrate.yaml"
      via: "seed Job initContainer waits for migrate Job completion before createsuperuser"
      pattern: "showmigrations|job/glitchtip-migrate|wait"
    - from: "k8s/observability/93-glitchtip-seed.yaml"
      to: "glitchtip-secrets"
      via: "DJANGO_SUPERUSER_EMAIL/PASSWORD via secretKeyRef"
      pattern: "DJANGO_SUPERUSER"
---

<objective>
Author the two one-shot Kubernetes Jobs that enforce GlitchTip's strict first-run order (ERR-01): (1) the migrate Job (`manage.py migrate`) that creates the Django schema, gated on postgres readiness; and (2) the superuser seed Job (`createsuperuser --noinput`) that runs ONLY after migrate completes. Also confirm the v6.1.8 `bin/start.sh` SERVER_ROLE dispatch and record the command fallback (de-risking plan 16-01's Deployments).

Purpose: ERR-01 mandates migrate → close registration → create superuser, in that order. Registration is closed declaratively on the web env (16-01); these Jobs cover migrate (before web serves) and the superuser seed (after migrate). Running the seed before migrate fails with a missing-table error (Pitfall 2), so the seed Job must be sequenced.
Output: `k8s/observability/92-glitchtip-migrate.yaml`, `k8s/observability/93-glitchtip-seed.yaml`, plus a recorded SERVER_ROLE/start.sh finding in the SUMMARY.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-error-tracking-glitchtip/16-RESEARCH.md

@k8s/staging/01-obs-rbac.yaml
@k8s/observability/70-loki.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm bin/start.sh SERVER_ROLE dispatch + record the command fallback</name>
  <files>k8s/observability/92-glitchtip-migrate.yaml</files>
  <read_first>
    - 16-RESEARCH.md "Pattern 2", "Pattern 3: First-Run Job Ordering", "Pitfall 4", "Open Questions" #1
    - k8s/staging/01-obs-rbac.yaml (the error-tracking obs-ci-deployer Role grants batch/jobs verbs — confirm Jobs are apply-able)
  </read_first>
  <action>
    First, run the bootstrap check (Open Questions #1): `docker run --rm glitchtip/glitchtip:v6.1.8 cat bin/start.sh 2>&1 | head -60` (or, if docker is unavailable in the executor, `kubectl run gt-probe --rm -i --restart=Never --image=glitchtip/glitchtip:v6.1.8 -n error-tracking --command -- cat bin/start.sh` once the namespace exists — otherwise defer the live confirmation to plan 16-04 and use the documented fallback). Record in the SUMMARY whether SERVER_ROLE is dispatched by bin/start.sh, and the exact migrate/worker/web commands the image expects. If the check cannot run offline, proceed with the migrate command below (which does not depend on SERVER_ROLE) and flag the web/worker command confirmation as a 16-04 live-deploy gate.
    Then author 92-glitchtip-migrate.yaml (namespace: error-tracking), a single Job document:
    - metadata name glitchtip-migrate, standard labels.
    - spec: completions 1, backoffLimit 3, ttlSecondsAfterFinished 600 (auto-cleanup so re-apply re-runs cleanly).
    - template.spec: restartPolicy Never, serviceAccountName glitchtip (reuse the SA from 16-01; if 16-01 has not merged when this applies, add the glitchtip SA here too — keep it idempotent), automountServiceAccountToken false, priorityClassName obs-background (static gate requires it on Jobs too).
    - initContainer wait-for-postgres: image postgres:17-alpine, command sh -c "until pg_isready -h glitchtip-postgres.error-tracking.svc -U glitchtip; do sleep 2; done" (Pitfall 2 / Pattern 3 — never migrate before postgres is ready).
    - container migrate: image glitchtip/glitchtip:v6.1.8, imagePullPolicy IfNotPresent, command ["./manage.py","migrate","--noinput"]. Env: VALKEY_URL="" (explicit — Pitfall 3), DATABASE_URL via secretKeyRef glitchtip-secrets/DATABASE_URL, SECRET_KEY via secretKeyRef glitchtip-secrets/SECRET_KEY.
    - container securityContext drop ALL + allowPrivilegeEscalation:false + runAsNonRoot:true; resources requests cpu 50m mem 128Mi limits cpu 200m mem 256Mi (one-shot sizing per 16-RESEARCH).
    Add a head comment: "First-run order step 1 (ERR-01): migrate. The seed Job (93) waits for this Job to complete. SERVER_ROLE finding recorded in 16-02-SUMMARY."
  </action>
  <verify>
    <automated>python3 scripts/validate-obs-manifests.py 2>&1 | tee /tmp/v16-02a.txt; grep -q "PASSED" /tmp/v16-02a.txt && grep -Eq 'manage\.py","migrate' k8s/observability/92-glitchtip-migrate.yaml && grep -Eq 'pg_isready' k8s/observability/92-glitchtip-migrate.yaml && grep -Eq 'priorityClassName: obs-background' k8s/observability/92-glitchtip-migrate.yaml</automated>
  </verify>
  <done>92-glitchtip-migrate.yaml is a Job in error-tracking that waits for glitchtip-postgres readiness then runs manage.py migrate --noinput with VALKEY_URL="" and secret-sourced DATABASE_URL/SECRET_KEY, obs-background priority, restartPolicy Never. The SERVER_ROLE/start.sh dispatch finding (or a deferral note for 16-04) is recorded for the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Author the superuser seed Job gated on migrate completion (93-glitchtip-seed.yaml)</name>
  <files>k8s/observability/93-glitchtip-seed.yaml</files>
  <read_first>
    - 16-RESEARCH.md "Pattern 4: Superuser Seed Job", "Pitfall 2", "Open Questions" #2 (createsuperuser fallback), "Assumptions Log" A4
    - the 92-glitchtip-migrate.yaml you just wrote (the Job name the seed waits on)
  </read_first>
  <action>
    Author 93-glitchtip-seed.yaml (namespace: error-tracking), a single Job document, name glitchtip-seed:
    - spec: completions 1, backoffLimit 3, ttlSecondsAfterFinished 600.
    - template.spec: restartPolicy Never, serviceAccountName glitchtip, automountServiceAccountToken false, priorityClassName obs-background.
    - initContainer wait-for-migrate: image bitnami/kubectl (or the kubectl image already used elsewhere in the repo — check; if none, use the postgres image with a showmigrations poll instead). PREFERRED guard (no extra RBAC needed): a glitchtip-image initContainer running command ["sh","-c","until ./manage.py showmigrations 2>/dev/null | grep -q '\\[X\\]' && ! ./manage.py showmigrations 2>/dev/null | grep -q '\\[ \\]'; do echo waiting-for-migrate; sleep 3; done"] with the same VALKEY_URL/DATABASE_URL/SECRET_KEY env — this polls the DB directly for "no unapplied migrations" and needs NO pods/jobs RBAC (Pitfall 2). Use this DB-poll guard, not `kubectl wait`, to avoid granting the SA job/get verbs.
    - container seed: image glitchtip/glitchtip:v6.1.8, command ["./manage.py","createsuperuser","--noinput"]. Env: VALKEY_URL="", DATABASE_URL + SECRET_KEY via secretKeyRef, DJANGO_SUPERUSER_EMAIL via secretKeyRef glitchtip-secrets/GLITCHTIP_SUPERUSER_EMAIL, DJANGO_SUPERUSER_PASSWORD via secretKeyRef glitchtip-secrets/GLITCHTIP_SUPERUSER_PASSWORD.
    - createsuperuser is idempotent-ish: it errors if the user already exists. Set backoffLimit 3 but document in a head comment that a re-run after the user exists may report "already exists" — that is acceptable (the user is seeded). Per Open Questions #2, if createsuperuser --noinput is unsupported by GlitchTip's user model, the live-deploy plan (16-04) falls back to `manage.py shell -c "from glitchtip's user model import User; User.objects.create_superuser(...)"` — leave a comment pointing at that fallback rather than hard-coding it now.
    - container securityContext drop ALL + allowPrivilegeEscalation false + runAsNonRoot true; resources one-shot (same as migrate Job).
    Head comment: "First-run order step 3 (ERR-01): create superuser. Gated on migrate completion via a showmigrations DB poll (no extra RBAC). Org/project/DSN creation happens in 16-04 (live, superuser-authed API)."
  </action>
  <verify>
    <automated>python3 scripts/validate-obs-manifests.py 2>&1 | tee /tmp/v16-02b.txt; grep -q "PASSED" /tmp/v16-02b.txt && grep -Eq 'createsuperuser' k8s/observability/93-glitchtip-seed.yaml && grep -Eq 'showmigrations' k8s/observability/93-glitchtip-seed.yaml && grep -Eq 'DJANGO_SUPERUSER_EMAIL' k8s/observability/93-glitchtip-seed.yaml</automated>
  </verify>
  <done>93-glitchtip-seed.yaml is a Job in error-tracking whose initContainer polls showmigrations until no migrations are unapplied (needs no extra RBAC), then runs createsuperuser --noinput with DJANGO_SUPERUSER_EMAIL/PASSWORD from glitchtip-secrets, obs-background priority, restartPolicy Never. The shell-based create_superuser fallback is documented as a comment for 16-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → cluster (error-tracking) | obs-ci-deployer applies the Jobs (batch/jobs verb already granted in 01-obs-rbac.yaml) |
| seed Job → glitchtip DB | runs createsuperuser with credentials from a Secret, only after migrate completes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Information Disclosure | superuser email/password in Job spec | mitigate | DJANGO_SUPERUSER_* sourced only via secretKeyRef (glitchtip-secrets); rendered by 16-03; static gate forbids inline values |
| T-16-07 | Tampering | seed runs against unmigrated DB (corrupt/partial admin) | mitigate | initContainer DB-poll on showmigrations blocks createsuperuser until migrate is fully applied (Pitfall 2) |
| T-16-08 | Elevation of Privilege | seed Job needs broad RBAC to check migrate status | mitigate | Use a DB-level showmigrations poll, NOT `kubectl wait` — no jobs/pods get verbs added to the SA |
| T-16-09 | Elevation of Privilege | default SA token automount on Jobs | mitigate | automountServiceAccountToken:false on both Jobs |
| T-16-SC | Tampering | glitchtip/glitchtip:v6.1.8 image | accept | Official image (16-RESEARCH audit OK); pinned tag, IfNotPresent |
</threat_model>

<verification>
- `python3 scripts/validate-obs-manifests.py` passes (after 16-03 extends it for error-tracking).
- `kubectl apply --dry-run=client -f k8s/observability/92-glitchtip-migrate.yaml -f k8s/observability/93-glitchtip-seed.yaml` parses cleanly.
- The migrate Job runs `manage.py migrate`; the seed Job's init guard references showmigrations and its main container runs createsuperuser with secret-sourced superuser creds.
- The SERVER_ROLE/bin/start.sh finding (or an explicit deferral to 16-04) is captured in 16-02-SUMMARY.
</verification>

<success_criteria>
- Both one-shot Jobs exist in k8s/observability/ in the error-tracking namespace with obs-background priority and no inline secrets.
- First-run ordering is enforced: postgres-ready → migrate; migrate-applied → seed superuser.
- No extra cluster/namespace RBAC verbs are required by the seed guard (DB poll, not kubectl).
</success_criteria>

<output>
Create `.planning/phases/16-error-tracking-glitchtip/16-02-SUMMARY.md` when done (include the SERVER_ROLE/start.sh finding).
</output>
