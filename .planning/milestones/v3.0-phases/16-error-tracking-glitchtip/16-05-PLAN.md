---
phase: 16-error-tracking-glitchtip
plan: 05
type: execute
wave: 3
depends_on: ["16-04"]
files_modified:
  - config/nginx/sites-available/errors-stats-staging-solid-stats.conf
  - scripts/bootstrap-obs-edge.sh
autonomous: false
requirements: [ERR-03]
must_haves:
  truths:
    - "The errors. vhost proxies HTTPS to GlitchTip's ClusterIP instead of returning 503 (ERR-03 public)"
    - "errors.solid-stats.ru serves GlitchTip over valid TLS reusing the Phase 14 cert (ERR-03 public)"
  artifacts:
    - path: "config/nginx/sites-available/errors-stats-staging-solid-stats.conf"
      provides: "the errors. vhost with a GlitchTip reverse-proxy block (no longer a 503 placeholder)"
      contains: "proxy_pass"
  key_links:
    - from: "config/nginx/sites-available/errors-stats-staging-solid-stats.conf"
      to: "glitchtip-web ClusterIP:8000"
      via: "proxy_pass to the upstream substituted by bootstrap-obs-edge.sh"
      pattern: "proxy_pass"
---

<objective>
Wire the public, TLS-terminated `errors.solid-stats.ru` URL to the live GlitchTip ClusterIP, completing ERR-03's public path. The vhost edit + bootstrap upstream-discovery are authored autonomously; the DNS A record creation and the on-host `bootstrap-obs-edge.sh` re-run (cert issuance + nginx reload) are operator-gated, because the DNS for the errors. subdomain is operator-controlled and does not resolve yet.

Purpose: ERR-03's success criterion includes the public TLS URL. Everything else (GlitchTip live, forced error visible internally) is done in 16-04; this plan flips the Phase 14 errors. 503 placeholder into a GlitchTip reverse proxy and drives the operator cutover.
Output: an updated errors. vhost (proxy_pass block), a bootstrap-obs-edge.sh that can discover the glitchtip-web ClusterIP, and a completed operator cutover with a green public smoke test.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-error-tracking-glitchtip/16-RESEARCH.md
@.planning/phases/16-error-tracking-glitchtip/16-04-SUMMARY.md

@config/nginx/sites-available/errors-stats-staging-solid-stats.conf
@scripts/bootstrap-obs-edge.sh
@docs/obs-edge-bootstrap.md
@docs/glitchtip.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the errors. 503 placeholder with a GlitchTip reverse-proxy vhost + bootstrap upstream discovery</name>
  <files>config/nginx/sites-available/errors-stats-staging-solid-stats.conf, scripts/bootstrap-obs-edge.sh</files>
  <read_first>
    - config/nginx/sites-available/errors-stats-staging-solid-stats.conf (the current HTTP+HTTPS blocks; the 503 location to replace; the ssl_certificate paths + HSTS already present)
    - 16-RESEARCH.md "Pattern 8: nginx vhost cutover" (the upstream + proxy_set_header block to use)
    - scripts/bootstrap-obs-edge.sh (the `case "$DOMAIN"` upstream-discovery block — grafana discovers via kubectl get svc grafana -n monitoring; errors. currently uses SKIP_UPSTREAM_CHECK)
    - the grafana vhost (config/nginx/sites-available/grafana-stats-staging-solid-stats.conf) for the exact proxy_set_header + ssl include conventions to mirror
  </read_first>
  <action>
    errors vhost: keep the HTTP server block (ACME challenge + 301 redirect) unchanged. In the HTTPS server block, the current `location /` block is the Phase 14 unavailable-service placeholder (see 16-RESEARCH Pattern 8) — remove that placeholder location entirely and wire a GlitchTip reverse proxy in its place, mirroring Pattern 8 and the grafana vhost conventions: add an `upstream glitchtip_obs { server UPSTREAM_PLACEHOLDER; keepalive 8; }` block (UPSTREAM_PLACEHOLDER is substituted at runtime by bootstrap-obs-edge.sh), and a `location / { proxy_pass http://glitchtip_obs; ... }` with proxy_set_header Host/X-Real-IP/X-Forwarded-For/X-Forwarded-Proto/X-Forwarded-Host, proxy_http_version 1.1, and sane timeouts. Keep ssl_certificate/ssl_certificate_key/options-ssl-nginx/ssl_dhparam + the HSTS header exactly as-is (Phase 14 cert lineage). Update the head comment to say the upstream is now wired to GlitchTip's ClusterIP (Phase 16), discovered by bootstrap-obs-edge.sh; do NOT mention the old placeholder status code in the comment text.
    bootstrap-obs-edge.sh: extend the `case "$DOMAIN"` upstream block so `errors.*` discovers the GlitchTip ClusterIP the same way grafana does: `UPSTREAM=$(kubectl get svc glitchtip-web -n error-tracking -o jsonpath='{.spec.clusterIP}:{.spec.ports[0].port}')` when UPSTREAM is empty and SKIP_UPSTREAM_CHECK is not 1. Then substitute UPSTREAM into UPSTREAM_PLACEHOLDER in the installed errors. vhost (mirror how grafana's upstream is substituted — add a sed substitution step for the errors. vhost analogous to grafana's). Preserve the existing errors. placeholder/cert-only path: if SKIP_UPSTREAM_CHECK=1 is passed (no upstream yet), the script must still behave as the Phase 14 cert-only bootstrap. Do NOT change the grafana path. Keep set -euo pipefail, the nginx -t gate + auto-restore, and the operator-verification echo.
  </action>
  <verify>
    <automated>bash -n scripts/bootstrap-obs-edge.sh && grep -q 'proxy_pass http://glitchtip_obs' config/nginx/sites-available/errors-stats-staging-solid-stats.conf && grep -q 'upstream glitchtip_obs' config/nginx/sites-available/errors-stats-staging-solid-stats.conf && test "$(grep -v '^[[:space:]]*#' config/nginx/sites-available/errors-stats-staging-solid-stats.conf | grep -c 'return 503;')" = "0" && grep -q 'glitchtip-web' scripts/bootstrap-obs-edge.sh && grep -q 'error-tracking' scripts/bootstrap-obs-edge.sh</automated>
  </verify>
  <done>The errors. HTTPS block proxies to upstream glitchtip_obs (no 503 left); bootstrap-obs-edge.sh discovers the glitchtip-web ClusterIP in error-tracking for errors.* and substitutes it into the vhost, with the grafana path and the cert-only Phase 14 fallback unchanged. bootstrap-obs-edge.sh passes bash -n.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Checkpoint: Operator DNS + cert cutover for errors.solid-stats.ru</name>
  <what-built>
    The errors. vhost now reverse-proxies to GlitchTip's ClusterIP (replacing the 503 placeholder), and bootstrap-obs-edge.sh discovers the glitchtip-web ClusterIP in error-tracking. GlitchTip is live and internally verified (16-04). The Phase 14 TLS cert lineage for errors.solid-stats.ru already exists.
  </what-built>
  <how-to-verify>
    This step is OPERATOR-gated because the DNS A record for errors.solid-stats.ru is operator-controlled and does not resolve yet (no CLI/API in this repo can create it). Operator steps on the live edge VPS (ssh root@89.223.124.200):
      1. Create the DNS A record: errors.solid-stats.ru → the edge public IP (89.223.124.200). Wait for it to resolve: `dig +short errors.solid-stats.ru` returns the edge IP.
      2. Re-run the obs-edge bootstrap with the GlitchTip upstream and cert issuance enabled (the cert lineage from Phase 14 is reused — no new issuance if it already exists):
         DOMAIN=errors.solid-stats.ru \
         ADMIN_EMAIL=<ops email> \
         SKIP_CERTBOT=0 \
         scripts/bootstrap-obs-edge.sh
         (UPSTREAM is auto-discovered from k3s; if running off-cluster, pass UPSTREAM=<glitchtip-web ClusterIP>:8000 explicitly.)
      3. Operator verification (from the bootstrap echo):
         - nginx -t  → ok
         - curl -I https://errors.solid-stats.ru/  → 200 (GlitchTip login), NOT 503/502
         - open the URL in a browser → the GlitchTip login page renders over valid TLS; self-registration is not offered (only the seeded superuser can log in)
         - certbot certificates  → cert lineage valid for errors.
      4. (Optional but recommended) re-run `bash scripts/validate-phase-16.sh --public` to confirm the public path serves GlitchTip.
    See docs/glitchtip.md and docs/obs-edge-bootstrap.md for the full runbook.
  </how-to-verify>
  <resume-signal>Type "approved" once https://errors.solid-stats.ru/ returns the GlitchTip login over valid TLS (200, not 503/502), or describe what failed (e.g. DNS not resolving, 502 = port/upstream mismatch per Pitfall 6, cert error).</resume-signal>
  <action>OPERATOR-gated, no repo CLI/API can perform it: create the operator-controlled DNS A record errors.solid-stats.ru → the edge IP (89.223.124.200), then on the edge VPS re-run scripts/bootstrap-obs-edge.sh with DOMAIN=errors.solid-stats.ru, ADMIN_EMAIL set, SKIP_CERTBOT=0 (the Phase 14 cert lineage is reused; UPSTREAM auto-discovered from the glitchtip-web ClusterIP). Verify per the how-to-verify steps. The agent must NOT fabricate DNS/cert state — wait for the operator to confirm.</action>
  <verify>
    <human-check>curl -I https://errors.solid-stats.ru/ returns 200 over valid TLS (not 503/502); the GlitchTip login renders with no open registration; certbot certificates shows a valid errors. lineage</human-check>
  </verify>
  <done>The public errors. URL serves GlitchTip over the reused Phase 14 TLS cert with closed registration; operator has typed "approved".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → host nginx (errors.) | public TLS termination at the edge; first time GlitchTip is reachable off-cluster |
| host nginx → glitchtip-web ClusterIP | reverse proxy over the private overlay into error-tracking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-18 | Spoofing | public GlitchTip allows open signup once reachable | mitigate | ENABLE_USER_REGISTRATION=False (16-01) is enforced BEFORE this public cutover; operator verifies no signup is offered at the public URL |
| T-16-19 | Information Disclosure | TLS misconfig / cert mismatch on the public vhost | mitigate | reuse the Phase 14 cert lineage + options-ssl-nginx + HSTS unchanged; nginx -t gate + certbot certificates verification before declaring done |
| T-16-20 | Tampering | bootstrap edit breaks the live grafana vhost | mitigate | the grafana case path is untouched; nginx -t gate with auto-restore from .bak; the cutover is on errors. only |
| T-16-21 | Denial of Service | 502 from port/upstream mismatch | mitigate | proxy_pass targets the discovered glitchtip-web ClusterIP:8000 (port 8000, Pitfall 6); curl -I asserts 200 not 502 |
| T-16-SC | Tampering | no new packages installed | accept | only nginx/certbot already present on the edge; no npm/pip/cargo |
</threat_model>

<verification>
- vhost has a proxy_pass to glitchtip_obs (no active unavailable-service placeholder directive remains in the HTTPS location); bootstrap-obs-edge.sh discovers the glitchtip-web ClusterIP for errors.*.
- bash -n passes on bootstrap-obs-edge.sh; the grafana path is unchanged.
- (operator) curl -I https://errors.solid-stats.ru/ returns 200 over valid TLS; the GlitchTip login renders with no open registration.
</verification>

<success_criteria>
- The errors. public URL serves GlitchTip over the reused Phase 14 TLS cert (ERR-03 public path complete).
- No 503/502; registration remains closed at the public URL.
- The grafana edge path and the Phase 14 cert-only fallback are both untouched.
</success_criteria>

<output>
Create `.planning/phases/16-error-tracking-glitchtip/16-05-SUMMARY.md` when done.
</output>
