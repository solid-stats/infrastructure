---
phase: 17-network-isolation-stack-validation
plan: 03
type: execute
wave: 2
depends_on: ["17-01", "17-02"]
files_modified:
  - k8s/observability/95-netpol-monitoring.yaml
  - k8s/observability/96-netpol-error-tracking.yaml
  - docs/network-policies.md
autonomous: false
requirements: [NET-01, NET-02, VAL-01]
user_setup:
  - service: staging-k3s-node
    why: "NET-01 enforcement proof + live NetworkPolicy apply run live on the cluster (SSH root@89.223.124.200, WireGuard tunnel) as in Phases 13-16"
must_haves:
  truths:
    - "NetworkPolicy enforcement under k3s/kube-router is empirically confirmed (a throwaway deny blocks a probe-client curl) BEFORE any default-deny is relied upon"
    - "The source IP a pod sees from host nginx (A1/A2) and the Alloy k8s-API egress target (A3) and the postgres-exporter pod label (A4) are confirmed live and the placeholder tokens in the netpol manifests are substituted with the confirmed values"
    - "validate-stack.sh runs green BEFORE the NetworkPolicies are applied"
    - "After the NetworkPolicies are applied, all Prometheus targets remain UP, all Grafana datasources remain healthy, and both public URLs (grafana + errors) stay reachable — validate-stack.sh runs green again"
    - "The NET-01 finding and the before/after evidence are recorded in a runbook"
  artifacts:
    - path: "docs/network-policies.md"
      provides: "NET-01 enforcement finding (A1/A2/A3/A4 resolution) + NET-02 apply runbook + before/after validate-stack evidence"
      min_lines: 30
    - path: "k8s/observability/95-netpol-monitoring.yaml"
      provides: "placeholder tokens substituted with the live-confirmed node IP and Alloy API egress target"
    - path: "k8s/observability/96-netpol-error-tracking.yaml"
      provides: "node-IP placeholder substituted with the live-confirmed value"
  key_links:
    - from: "k8s/observability/95-netpol-monitoring.yaml"
      to: "node host nginx edge"
      via: "ipBlock cidr = confirmed node IP for grafana :3000 ingress (A1/A2)"
      pattern: "ipBlock"
    - from: "k8s/observability/95-netpol-monitoring.yaml"
      to: "k8s API server"
      via: "allow-alloy-egress targets the confirmed API endpoint (A3) so Alloy keeps pod discovery"
      pattern: "allow-alloy-egress"
---

<objective>
Operator-gated, run LIVE on the staging cluster (SSH root@89.223.124.200 / WireGuard,
as Phases 13-16 were). This plan: (1) proves NetworkPolicy enforcement empirically and
resolves the open assumptions A1/A2 (host-nginx source IP), A3 (Alloy API egress), A4
(postgres-exporter label) BEFORE relying on default-deny; (2) substitutes the
placeholder tokens left in the netpol manifests with the confirmed values; (3) applies
the operator-bootstrap RBAC, runs validate-stack.sh green as a baseline, applies the
NetworkPolicies, and re-runs validate-stack.sh green to prove isolation broke nothing —
including both public URLs surviving; (4) records the finding + evidence in a runbook.

If the post-apply validation fails OR either public URL breaks, the rollout is rolled
back (delete the netpols) and the failure investigated — the phase fails loudly rather
than leaving a half-applied isolation. Per CONTEXT live constraints 1-7 and RESEARCH
§NET-01 Enforcement Proof Protocol.

Purpose: NET-01 (confirm enforcement first), NET-02 (apply default-deny + minimal-allow
without breaking scraping/datasources/edge), VAL-01 (re-runnable green before and after).
Output: substituted netpol manifests + docs/network-policies.md runbook + live evidence.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-network-isolation-stack-validation/17-CONTEXT.md
@.planning/phases/17-network-isolation-stack-validation/17-RESEARCH.md

# Authored in 17-01 / 17-02 — this plan resolves their placeholders and runs them live
@k8s/observability/95-netpol-monitoring.yaml
@k8s/observability/96-netpol-error-tracking.yaml
@k8s/staging/01-obs-rbac.yaml
@scripts/validate-stack.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: NET-01 enforcement proof + resolve A1/A2/A3/A4 against the live cluster</name>
  <files>docs/network-policies.md</files>
  <action>
Run LIVE (WireGuard tunnel up; node SSH available). Execute the RESEARCH §NET-01
Enforcement Proof Protocol in a THROWAWAY netpol-probe namespace (Pitfall 7: never in
monitoring/error-tracking):
1. Create namespace netpol-probe; apply a deny-all ingress NetworkPolicy there.
2. Run a probe-target pod (nginx:alpine, fallback busybox) and a probe-client pod
   (curlimages/curl, fallback appropriate/curl); curl probe-target's pod IP. PASS = the
   curl times out / connection-refused. FAIL (blocking) = curl succeeds → enforcement
   absent → STOP, do not apply any default-deny, escalate per RESEARCH (check
   k3s --disable-network-policy, kube-router logs).
3. On the node, confirm `iptables -L | grep KUBE-NWPLCY` shows active chains.

Then resolve the assumptions and capture each finding:
- A1/A2 (host-nginx source IP + does kube-router need the ipBlock): from the node, curl
  a ClusterIP that maps to a probe pod and observe the source IP the pod sees (e.g. a
  netcat/`nc -lp` listener in the probe pod, or test that the deny-all blocks a node→pod
  curl). Record the exact CIDR host nginx traffic arrives as — the node public IP
  (89.223.124.200/32), the flannel/pod CIDR, or another — and whether src-type LOCAL is
  implicitly allowed. This is the value the grafana/glitchtip ingress ipBlock must use.
- A3 (Alloy API egress): `kubectl exec -n monitoring ds/alloy -- env | grep -E
  'KUBERNETES|API'`. Record whether it is the node IP:6443 or kubernetes.default.svc
  (10.43.0.1:443). This is the allow-alloy-egress target.
- A4 (postgres-exporter label): `kubectl get pod -n monitoring -l
  app.kubernetes.io/name=prometheus-postgres-exporter -o name` (confirm the selector
  matches a live pod). If the label differs, note the correct one.
Tear down the netpol-probe namespace completely (Pitfall 7).

Write docs/network-policies.md (AGENTS.md doc style) with an "NET-01 Enforcement
Finding" section recording: the probe result (PASS/FAIL), the KUBE-NWPLCY confirmation,
and the resolved A1/A2/A3/A4 values with the exact CIDRs/addresses to use. Leave the
NET-02 apply runbook and the before/after evidence table as headed sections for the
later tasks to fill.
  </action>
  <verify>
    <human-check>
NET-01 probe shows the probe-client curl was BLOCKED (timeout/refused) and KUBE-NWPLCY chains are active; docs/network-policies.md records the PASS plus the resolved node-IP CIDR (A1/A2), the Alloy API egress address (A3), and the confirmed postgres-exporter label (A4). The netpol-probe namespace is deleted.
    </human-check>
  </verify>
  <done>Enforcement empirically confirmed (blocked probe + KUBE-NWPLCY chains). A1/A2/A3/A4 resolved and recorded in docs/network-policies.md. Throwaway probe namespace cleaned up. If the probe did NOT block, the phase stops here (enforcement absent) and no default-deny is applied.</done>
</task>

<task type="auto">
  <name>Task 2: Substitute placeholders, apply RBAC + baseline validate-stack green, then apply netpols and re-validate green</name>
  <files>k8s/observability/95-netpol-monitoring.yaml, k8s/observability/96-netpol-error-tracking.yaml, docs/network-policies.md</files>
  <action>
Using the Task 1 findings, edit the two netpol manifests in the repo:
- Substitute NODE_IP_PLACEHOLDER (both 95- and 96-) with the confirmed node-IP CIDR
  from A1/A2.
- In 95-, resolve K8S_API_EGRESS_PLACEHOLDER in allow-alloy-egress to the A3-confirmed
  target (uncomment the correct candidate, delete the wrong one).
- If A4 showed a different postgres-exporter label, correct the
  allow-postgres-exporter-egress podSelector.
Re-run the static parse checks from 17-01 (both files still pure-NetworkPolicy, correct
namespaces) and `python3 scripts/validate-obs-manifests.py`.

Then, against the live cluster:
1. Operator applies the updated operator-bootstrap RBAC (k8s/staging/01-obs-rbac.yaml —
   CI-glob-excluded) so obs-ci-deployer gains the networkpolicies verb in both namespaces.
2. BASELINE: run `bash scripts/validate-stack.sh --quick` (or full) and confirm GREEN —
   this is the pre-policy proof that the stack is healthy before isolation.
3. Apply the NetworkPolicies (95- into monitoring, 96- into error-tracking — via the
   obs deploy workflow, or kubectl apply per-context matching the workflow routing).
4. POST-APPLY: re-run `bash scripts/validate-stack.sh` (full, non-quick) and confirm
   GREEN — Prometheus targets all UP, Grafana datasources healthy, Loki server-2 query
   returns lines, forced GlitchTip event ingests. Watch for Pitfall 1/2/3/4/6 symptoms
   (DNS break, 502 on public URLs, rabbitmq/postgres scrape target down, Alloy
   loki_write_sent_entries_total flatlining).
5. Independently confirm BOTH public URLs survive: `curl -fsS -I
   https://grafana.solid-stats.ru/` and `https://errors.solid-stats.ru/` return 2xx/3xx
   (NOT 502). These are the live public hostnames (STATE: grafana.solid-stats.ru /
   errors.solid-stats.ru).
If POST-APPLY validation fails or a public URL returns 502, roll back (`kubectl delete
-f` the netpols / delete by namespace) so the stack returns to the green baseline, then
diagnose against the pitfalls before retrying.
Fill the docs/network-policies.md NET-02 apply runbook section and the before/after
evidence table (validate-stack result + both public-URL curls, before and after).
  </action>
  <verify>
    <human-check>
The placeholder tokens are gone from both manifests (substituted with live values). validate-stack.sh ran GREEN before AND after applying the NetworkPolicies. After apply, all Prometheus targets are UP, all Grafana datasources are healthy, and both https://grafana.solid-stats.ru/ and https://errors.solid-stats.ru/ return non-502. docs/network-policies.md records the before/after evidence.
    </human-check>
  </verify>
  <done>NODE_IP/API placeholders substituted with confirmed values; both manifests still parse and pass validate-obs-manifests.py. RBAC applied. validate-stack.sh green before and after the netpol apply. Both public URLs survive (non-502). Evidence recorded in docs/network-policies.md. On failure, the netpols were rolled back to the green baseline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator verification — full stack green + both public URLs survive isolation</name>
  <files>docs/network-policies.md</files>
  <what-built>NET-02 NetworkPolicies are live on monitoring + error-tracking; NET-01 enforcement was empirically proven first; validate-stack.sh (VAL-01) is green both before and after; both public obs URLs survive isolation. docs/network-policies.md holds the finding + before/after evidence.</what-built>
  <action>
Operator final-verification gate (blocking). Run the checks below and approve only if all pass:
1. Re-run the full stack validation against the cluster (WireGuard up):
   `bash scripts/validate-stack.sh` — expect it to end with "FULL STACK VALIDATION PASSED".
2. Confirm both public URLs in a browser or via curl:
   `curl -fsS -I https://grafana.solid-stats.ru/` and
   `curl -fsS -I https://errors.solid-stats.ru/` — neither returns 502; Grafana loads
   behind local-user auth and renders dashboards; GlitchTip loads.
3. Confirm the policies are live: `kubectl get networkpolicy -n monitoring` and
   `-n error-tracking` list the default-deny + allow policies.
4. Confirm the throwaway probe namespace is gone: `kubectl get ns netpol-probe` returns NotFound.
5. Skim docs/network-policies.md: the NET-01 finding and the before/after evidence table are filled.
  </action>
  <how-to-verify>
1. Re-run the full stack validation against the cluster (WireGuard up):
   `bash scripts/validate-stack.sh` — expect it to end with "FULL STACK VALIDATION PASSED".
2. Confirm both public URLs in a browser or via curl:
   `curl -fsS -I https://grafana.solid-stats.ru/` and
   `curl -fsS -I https://errors.solid-stats.ru/` — neither returns 502; Grafana loads
   behind local-user auth and renders dashboards; GlitchTip loads.
3. Confirm the policies are live: `kubectl get networkpolicy -n monitoring` and
   `-n error-tracking` list the default-deny + allow policies.
4. Confirm the throwaway probe namespace is gone: `kubectl get ns netpol-probe` returns NotFound.
5. Skim docs/network-policies.md: the NET-01 finding and the before/after evidence table are filled.
  </how-to-verify>
  <verify>
    <human-check>
validate-stack.sh ends with "FULL STACK VALIDATION PASSED"; both public URLs return non-502; the netpols are listed in both namespaces; netpol-probe is NotFound; the runbook holds the finding + before/after evidence.
    </human-check>
  </verify>
  <done>Operator approved: full stack validation green and both public obs URLs reachable through the NetworkPolicy layer; policies live in both namespaces; probe namespace cleaned up; runbook evidence captured. On a broken check, rollback + diagnosis is triggered instead of approval.</done>
  <resume-signal>Type "approved" if validation is green and both URLs work, or describe what broke (which target/datasource/URL) to trigger rollback + diagnosis.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host nginx (node) → obs pod | The default-deny ingress must keep the edge reachable via the confirmed ipBlock or both public URLs break |
| obs namespace → app namespace | Prometheus/postgres-exporter egress into solid-stats-staging is the only allowed obs→app path; no default-deny is added to the app namespace |
| throwaway probe namespace | netpol-probe is a temporary trust-test surface; must be fully deleted (Pitfall 7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | DoS | applying default-deny while enforcement is unproven | mitigate | NET-01 empirical proof BEFORE apply; on a passed probe (enforcement absent) the rollout stops |
| T-17-08 | DoS | public edge 502 after default-deny ingress | mitigate | ipBlock CIDR confirmed live (A1/A2) before apply; post-apply curl of both public URLs gates acceptance; rollback on 502 |
| T-17-09 | DoS | Alloy loses pod discovery after egress deny (A3) | mitigate | A3 resolved live (kubectl exec env) before apply; allow-alloy-egress targets the confirmed API endpoint; post-apply Loki query in validate-stack checks the path |
| T-17-10 | Tampering | half-applied isolation left after a failure | mitigate | fail-loud + documented rollback (delete netpols) returns to the green baseline before retry |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; only built-in NetworkPolicy API + existing scripts + throwaway public images (nginx:alpine, curlimages/curl) pulled for the probe (RESEARCH §Package Legitimacy: none) |
</threat_model>

<verification>
- NET-01: throwaway-deny probe blocked + KUBE-NWPLCY chains active; finding recorded.
- A1/A2/A3/A4 resolved live and placeholders substituted; manifests still parse and pass validate-obs-manifests.py.
- validate-stack.sh GREEN before the netpol apply (baseline).
- validate-stack.sh GREEN after the netpol apply (Prometheus targets UP, Grafana datasources healthy, Loki server-2 query, forced GlitchTip event).
- Both public URLs (grafana.solid-stats.ru, errors.solid-stats.ru) return non-502 after apply.
- netpol-probe namespace deleted.
- docs/network-policies.md holds the finding + before/after evidence.
</verification>

<success_criteria>
- NetworkPolicy enforcement empirically confirmed before any default-deny (NET-01).
- Default-deny + minimal-allow live on monitoring + error-tracking, with all Prometheus targets UP and all Grafana datasources healthy afterward (NET-02).
- validate-stack.sh re-runnable and green both before and after the policies (VAL-01).
- Both public obs URLs survive isolation; evidence + finding captured in docs/network-policies.md.
</success_criteria>

<output>
Create `.planning/phases/17-network-isolation-stack-validation/17-03-SUMMARY.md` when done
</output>
