---
phase: 15-log-stack
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/observability/values/loki-values.yaml
  - k8s/observability/70-loki.yaml
  - scripts/validate-phase-15.sh
autonomous: true
requirements: [LOG-01, LOG-02, LOG-03]
user_setup: []

must_haves:
  truths:
    - "Loki renders as a SingleBinary StatefulSet (not SimpleScalable read/write/backend pods)"
    - "Loki uses filesystem storage on a ~10Gi PVC with compactor retention ~168h"
    - "validate-phase-15.sh asserts LOG-01/02/03 live behaviors with the corrected metric names"
  artifacts:
    - path: "k8s/observability/values/loki-values.yaml"
      provides: "Loki SingleBinary/filesystem/compactor helm values"
      contains: "deploymentMode: SingleBinary"
    - path: "k8s/observability/70-loki.yaml"
      provides: "rendered Loki StatefulSet + Service + ConfigMap (namespace monitoring)"
      contains: "kind: StatefulSet"
    - path: "scripts/validate-phase-15.sh"
      provides: "live LOG-01..03 assertion harness"
      contains: "loki_boltdb_shipper_compactor_running"
  key_links:
    - from: "k8s/observability/70-loki.yaml"
      to: "k8s/observability/values/loki-values.yaml"
      via: "helm template render input"
      pattern: "deploymentMode:\\s*SingleBinary"
    - from: "scripts/validate-phase-15.sh"
      to: "loki + alloy + grafana live APIs"
      via: "kubectl exec / port-forward queries with corrected metric names"
      pattern: "loki_write_sent_entries_total"
---

<objective>
Render the Loki SingleBinary/filesystem log store (LOG-01) and author the live phase-15 validation harness (LOG-01/02/03 with corrected metric names).

Purpose: Loki is the durable log store everything else feeds and queries. Getting the chart mode (SingleBinary, NOT the SimpleScalable default), `replication_factor: 1`, filesystem storage, and compactor retention right is the keystone of the phase. The validation harness encodes the corrected metric names so the phase gate is provable.
Output: `k8s/observability/values/loki-values.yaml`, rendered `k8s/observability/70-loki.yaml`, `scripts/validate-phase-15.sh`.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# THE key input — chart versions, full loki-values.yaml, corrected metric names, pitfalls
@.planning/phases/15-log-stack/15-RESEARCH.md
@.planning/phases/15-log-stack/15-VALIDATION.md

# Mirror these existing Phase 13 patterns
@scripts/validate-phase-13.sh
@k8s/observability/values/prometheus-values.yaml
</context>

<read_first>
- 15-RESEARCH.md § "Code Examples → loki-values.yaml (complete)" — the authoritative values file. Copy its structure verbatim.
- 15-RESEARCH.md § "Common Pitfalls" 1 (deploymentMode default), 2 (metric names), 3 (replication_factor), 5 (index.period 24h), 9 (PVC whenScaled Retain).
- 15-RESEARCH.md § "Validation Architecture → Correct metric names for validate-phase-15.sh" — the two python3 one-liners.
- scripts/validate-phase-13.sh — the assert() helper, kubectl exec query pattern, --quick flag, port-forward + trap cleanup pattern to mirror.
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Author loki-values.yaml (SingleBinary, filesystem, compactor retention)</name>
  <files>k8s/observability/values/loki-values.yaml</files>
  <action>
Create the Loki helm values file from 15-RESEARCH § "loki-values.yaml (complete)". MUST set `deploymentMode: SingleBinary` (Pitfall 1 — chart default is SimpleScalable which spawns read/write/backend pods that OOM the node). MUST set `loki.commonConfig.replication_factor: 1` (Pitfall 3 — writes fail with quorum errors otherwise). MUST set `loki.schemaConfig.configs[0].index.period: 24h` with TSDB store + schema v13 (Pitfall 5 — retention silently no-ops without 24h period). Set `loki.storage.type: filesystem`, `loki.compactor.retention_enabled: true`, `loki.limits_config.retention_period: 168h` (~7-day retention, LOG-01). Set `fullnameOverride: loki` so the Service is named `loki` (push URL and Grafana datasource depend on this exact name). Set `singleBinary.replicas: 1`, `singleBinary.image.tag: "3.6.11"` (latest patch; chart appVersion is 3.6.7), `singleBinary.priorityClassName: obs-background`, requests cpu 50m/mem 128Mi, limits cpu 200m/mem 256Mi, `singleBinary.persistence.size: 10Gi`, `whenScaled: Retain`, `whenDeleted: Retain` (Pitfall 9). Set ALL microservice replica counts to 0 (backend/read/write/ingester/querier/queryFrontend/queryScheduler/distributor/compactor/indexGateway/bloomCompactor/bloomGateway) as belt-and-suspenders. Disable `gateway`, `monitoring.selfMonitoring`, `monitoring.lokiCanary`, and `test`. Keep the head comment block describing the render command and key constraints, mirroring the prometheus-values.yaml comment style — but do NOT inline any literal that a negative-grep gate matches (no raw `SimpleScalable` outside a clearly-labeled "must NOT be" note).
  </action>
  <verify>
    <automated>test -f k8s/observability/values/loki-values.yaml && grep -q 'deploymentMode: SingleBinary' k8s/observability/values/loki-values.yaml && grep -q 'replication_factor: 1' k8s/observability/values/loki-values.yaml && grep -q 'period: 24h' k8s/observability/values/loki-values.yaml && grep -q 'retention_period: 168h' k8s/observability/values/loki-values.yaml && grep -q 'fullnameOverride: loki' k8s/observability/values/loki-values.yaml</automated>
  </verify>
  <done>loki-values.yaml exists with SingleBinary mode, replication_factor 1, 24h index period, 168h retention, fullnameOverride loki, all microservice replicas 0.</done>
</task>

<task type="auto">
  <name>Task 2: Render 70-loki.yaml via helm template</name>
  <files>k8s/observability/70-loki.yaml</files>
  <action>
Render the Loki manifest with: `helm template loki grafana/loki --version 7.0.0 --namespace monitoring --values k8s/observability/values/loki-values.yaml > k8s/observability/70-loki.yaml`. Use `helm` on PATH (v3.17.3, grafana repo already added; run `helm repo update grafana` first if the chart is not found). NEVER use `> file 2>&1` — render errors must go to stderr, not get spliced into the YAML (validate-obs-manifests.py has a render-error-signature guard from Phase 13). After render, verify the output contains a StatefulSet (not loki-read/loki-write/loki-backend StatefulSets — that would mean SingleBinary did not take, Pitfall 1) and contains no `kind: ClusterRole` (Loki SingleBinary needs no ClusterRole; if the chart emits one, it must be stripped like Alloy's in 15-02). Confirm the rendered Service is named exactly `loki` and exposes port 3100. Run `python3 scripts/validate-obs-manifests.py` — it must stay green (namespace monitoring, obs-background priority, no secrets, no render-error splice).
  </action>
  <verify>
    <automated>test -f k8s/observability/70-loki.yaml && grep -q 'kind: StatefulSet' k8s/observability/70-loki.yaml && ! grep -q 'loki-read' k8s/observability/70-loki.yaml && ! grep -q 'kind: ClusterRole' k8s/observability/70-loki.yaml && python3 scripts/validate-obs-manifests.py</automated>
  </verify>
  <done>70-loki.yaml is a clean render: SingleBinary StatefulSet, Service named loki on 3100, no SimpleScalable read/write StatefulSets, no ClusterRole, validate-obs-manifests.py passes.</done>
</task>

<task type="auto">
  <name>Task 3: Author validate-phase-15.sh (LOG-01/02/03, corrected metric names)</name>
  <files>scripts/validate-phase-15.sh</files>
  <action>
Create the live validation harness mirroring scripts/validate-phase-13.sh structure (bash `set -euo pipefail`, `assert()` helper, `--quick` flag, `kubectl -n monitoring exec` Prometheus queries, port-forward + EXIT trap for Grafana). Cover all three requirements with the CORRECTED metric names from 15-RESEARCH § "Correct metric names" (Pitfall 2 — the ROADMAP names `loki_compactor_runs_total` / `alloy_logs_entries_total` do NOT exist):
- LOG-01: Loki pod Running (`kubectl -n monitoring get pod -l app.kubernetes.io/name=loki -o jsonpath='{.items[0].status.phase}'` == Running); Loki PVC Bound; compactor active via Prometheus query `loki_boltdb_shipper_compactor_running` == 1; Loki `/config` (exec into the loki pod, `wget -qO- localhost:3100/config`) contains `168h`.
- LOG-02: Alloy DaemonSet `numberReady` == 1; entries shipped via Prometheus query `loki_write_sent_entries_total` > 0 (assert value != "0").
- LOG-03: Loki datasource health OK via Grafana API (`/api/datasources` find type=loki, then its `/health`), gated behind the same port-forward + `--quick` skip as Phase 13; LogQL `query_range` for `{namespace="solid-stats-staging",app=~"server-2.*"}` (exec from prometheus pod, wget the loki Service) returns >=1 entry.
Use the two python3 one-liners from 15-RESEARCH verbatim for metric extraction. Make the script `chmod +x`. End with a manual-note block (LogQL-in-Grafana-Explore visual check) like Phase 13's MET-06 note. Do NOT hardcode any non-existent metric name; the file must contain `loki_boltdb_shipper_compactor_running` and `loki_write_sent_entries_total` and must NOT contain `loki_compactor_runs_total` or `alloy_logs_entries_total`.
  </action>
  <verify>
    <automated>bash -n scripts/validate-phase-15.sh && test -x scripts/validate-phase-15.sh && grep -q 'loki_boltdb_shipper_compactor_running' scripts/validate-phase-15.sh && grep -q 'loki_write_sent_entries_total' scripts/validate-phase-15.sh && ! grep -q 'loki_compactor_runs_total' scripts/validate-phase-15.sh && ! grep -q 'alloy_logs_entries_total' scripts/validate-phase-15.sh</automated>
  </verify>
  <done>validate-phase-15.sh parses clean, is executable, asserts LOG-01/02/03 with the corrected metric names, and contains none of the non-existent ROADMAP metric names.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alloy → Loki push (in-cluster) | log content crosses; mitigated in 15-02 (label allowlist) |
| in-cluster client → Loki :3100 | Loki `auth_enabled: false`; ClusterIP only, no Ingress in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | Loki log line storage | accept | Log line bodies stored as-is; secret-in-logs is an app-source concern (out of infra scope); label leakage mitigated at Alloy in 15-02 (LOG-02) |
| T-15-02 | Elevation of Privilege | Loki admin API (auth_enabled:false) | accept | ClusterIP only, no Ingress in Phase 15; NetworkPolicy isolation lands Phase 17 |
| T-15-03 | Denial of Service | Loki unbounded disk growth | mitigate | compactor retention_enabled + retention_period 168h + index.period 24h (Pitfall 5); PVC sized 10Gi against 25G free disk |
| T-15-SC | Tampering | helm chart download (grafana/loki v7.0.0) | mitigate | Package Legitimacy Audit in 15-RESEARCH = OK/Approved; chart+image versions pinned; render never `2>&1`-spliced (validate-obs-manifests.py guard) |
</threat_model>

<verification>
- `helm template` produces a SingleBinary StatefulSet, Service `loki:3100`, no read/write/backend StatefulSets, no ClusterRole.
- `python3 scripts/validate-obs-manifests.py` green on the new 70-loki.yaml.
- `bash -n scripts/validate-phase-15.sh` parses; corrected metric names present, ROADMAP-wrong names absent.
</verification>

<success_criteria>
loki-values.yaml + rendered 70-loki.yaml deliver Loki in SingleBinary/filesystem mode with replication_factor 1 and 168h compactor retention; validate-phase-15.sh is ready to prove LOG-01/02/03 live with the corrected metric names.
</success_criteria>

<output>
Create `.planning/phases/15-log-stack/15-01-SUMMARY.md` when done.
</output>
