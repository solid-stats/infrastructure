---
phase: 15-log-stack
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/observability/values/prometheus-values.yaml
  - k8s/observability/10-prometheus.yaml
  - k8s/observability/values/grafana-values.yaml
  - k8s/observability/50-grafana.yaml
autonomous: true
requirements: [LOG-01, LOG-03]
user_setup: []

must_haves:
  truths:
    - "Prometheus scrapes loki:3100/metrics and alloy:12345/metrics as two new static targets"
    - "Grafana provisions Loki as a second (non-default) datasource at http://loki.monitoring.svc:3100"
  artifacts:
    - path: "k8s/observability/values/prometheus-values.yaml"
      provides: "loki + alloy scrape jobs added to scrapeConfigs"
      contains: "job_name"
    - path: "k8s/observability/10-prometheus.yaml"
      provides: "re-rendered Prometheus config with loki + alloy targets"
      contains: "loki.monitoring.svc:3100"
    - path: "k8s/observability/values/grafana-values.yaml"
      provides: "Loki additionalDataSources entry"
      contains: "additionalDataSources"
    - path: "k8s/observability/50-grafana.yaml"
      provides: "re-rendered Grafana with Prometheus + Loki datasources"
      contains: "type: loki"
  key_links:
    - from: "k8s/observability/10-prometheus.yaml"
      to: "loki + alloy Services"
      via: "static_configs targets loki:3100 and alloy:12345"
      pattern: "alloy\\.monitoring\\.svc:12345"
    - from: "k8s/observability/50-grafana.yaml"
      to: "Loki Service"
      via: "provisioned datasource url loki.monitoring.svc:3100"
      pattern: "http://loki\\.monitoring\\.svc:3100"
---

<objective>
Wire Loki and Alloy into the existing Phase 13 metrics+Grafana stack: add Prometheus scrape targets for both (so the LOG-01/LOG-02 metrics exist) and add Loki as a second Grafana datasource (LOG-03).

Purpose: Without these scrape targets the validation harness has no `loki_boltdb_shipper_compactor_running` or `loki_write_sent_entries_total` to read; without the Loki datasource LOG-03's LogQL-in-Grafana acceptance is impossible. This plan extends two existing Phase 13 values files and re-renders their manifests — the Service names are fixed (`loki`, `alloy`) by the fullnameOverride set in 15-01/15-02, so no runtime dependency on those plans completing first.
Output: extended `prometheus-values.yaml` + re-rendered `10-prometheus.yaml`; extended `grafana-values.yaml` + re-rendered `50-grafana.yaml`.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# key inputs — scrape target snippet, additionalDataSources snippet, re-render commands, Open Q4
@.planning/phases/15-log-stack/15-RESEARCH.md
@.planning/phases/15-log-stack/15-VALIDATION.md

# Files this plan extends + re-renders (current Phase 13 state)
@k8s/observability/values/prometheus-values.yaml
@k8s/observability/values/grafana-values.yaml
</context>

<read_first>
- 15-RESEARCH.md § "Code Examples → prometheus-values.yaml extension (scrape targets)" — the loki + alloy job_name blocks + the re-render command.
- 15-RESEARCH.md § "Code Examples → grafana-values.yaml extension (additionalDataSources)" — the Loki datasource entry + the re-render command.
- 15-RESEARCH.md § "Common Pitfalls" 6 (Grafana datasource URL must be http://loki.monitoring.svc:3100 — fullnameOverride loki) and Open Question 4 (inspect the rendered grafana datasource diff; additionalDataSources may land in a separate ConfigMap entry — verify after render).
- k8s/observability/values/prometheus-values.yaml — the existing `scrapeConfigs:` map shape (per-job key with enabled + static_configs). Add `loki:` and `alloy:` jobs in the same style; chart version is prometheus-community/prometheus 29.11.0.
- k8s/observability/values/grafana-values.yaml — the existing `datasources.datasources.yaml` block (Prometheus, isDefault:true); chart version grafana/grafana 10.5.15. Add the Loki datasource via `additionalDataSources` (isDefault:false).
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Add loki + alloy scrape targets and re-render 10-prometheus.yaml</name>
  <files>k8s/observability/values/prometheus-values.yaml, k8s/observability/10-prometheus.yaml</files>
  <action>
Extend the `scrapeConfigs:` map in prometheus-values.yaml with two new enabled static jobs in the same style as the existing kube-state-metrics/node-exporter entries: `loki` → target `loki.monitoring.svc:3100` (LOG-01; scrapes `loki_boltdb_shipper_compactor_running`), and `alloy` → target `alloy.monitoring.svc:12345` (LOG-02; scrapes `loki_write_sent_entries_total`). The Service names are fixed by fullnameOverride in 15-01/15-02. Re-render: `helm template prometheus prometheus-community/prometheus --version 29.11.0 --namespace monitoring --values k8s/observability/values/prometheus-values.yaml > k8s/observability/10-prometheus.yaml`. NEVER `> file 2>&1` (Phase 13 render-error-splice guard). After render, confirm both targets appear in the rendered config and that the file still has `serviceAccountName: prometheus` and no ClusterRole (rbac.create:false unchanged). Run `python3 scripts/validate-obs-manifests.py` — must stay green.
  </action>
  <verify>
    <automated>grep -q 'loki.monitoring.svc:3100' k8s/observability/values/prometheus-values.yaml && grep -q 'alloy.monitoring.svc:12345' k8s/observability/values/prometheus-values.yaml && grep -q 'loki.monitoring.svc:3100' k8s/observability/10-prometheus.yaml && grep -q 'alloy.monitoring.svc:12345' k8s/observability/10-prometheus.yaml && ! grep -q 'kind: ClusterRole' k8s/observability/10-prometheus.yaml && python3 scripts/validate-obs-manifests.py</automated>
  </verify>
  <done>prometheus-values.yaml has loki + alloy scrape jobs; 10-prometheus.yaml re-rendered with both targets, no ClusterRole, static gate green.</done>
</task>

<task type="auto">
  <name>Task 2: Add Loki as a second Grafana datasource and re-render 50-grafana.yaml</name>
  <files>k8s/observability/values/grafana-values.yaml, k8s/observability/50-grafana.yaml</files>
  <action>
Add an `additionalDataSources:` block to grafana-values.yaml below the existing `datasources:` block with one entry: name Loki, type loki, url `http://loki.monitoring.svc:3100` (Pitfall 6 — exact Service name via fullnameOverride; access proxy; isDefault false so Prometheus stays default), jsonData maxLines 1000 and timeout "60". Do NOT remove or alter the existing Prometheus datasource (LOG-03 must add Loki, never displace Prometheus — scope discipline). Re-render: `helm template grafana grafana/grafana --version 10.5.15 --namespace monitoring --values k8s/observability/values/grafana-values.yaml > k8s/observability/50-grafana.yaml`. NEVER `> file 2>&1`. Per Open Question 4, after render INSPECT the diff: `additionalDataSources` may render into a separate datasource ConfigMap entry rather than merging the existing key — confirm the rendered 50-grafana.yaml contains BOTH a prometheus datasource and a `type: loki` datasource pointed at loki.monitoring.svc:3100, and that `testFramework.enabled: false` / fullnameOverride grafana / admin existingSecret are all still intact. Run `python3 scripts/validate-obs-manifests.py` — must stay green.
  </action>
  <verify>
    <automated>grep -q 'additionalDataSources' k8s/observability/values/grafana-values.yaml && grep -q 'type: loki' k8s/observability/values/grafana-values.yaml && grep -q 'type: loki' k8s/observability/50-grafana.yaml && grep -q 'http://loki.monitoring.svc:3100' k8s/observability/50-grafana.yaml && grep -q 'type: prometheus' k8s/observability/50-grafana.yaml && python3 scripts/validate-obs-manifests.py</automated>
  </verify>
  <done>grafana-values.yaml gains a Loki additionalDataSources entry without touching Prometheus; 50-grafana.yaml re-rendered with both prometheus and loki datasources; static gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prometheus → loki/alloy /metrics | in-cluster scrape of two new ClusterIP targets; read-only metrics |
| Grafana → Loki query (proxy) | Grafana proxies LogQL to loki:3100; in-cluster, no new external surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | Information Disclosure | Grafana Loki datasource (no auth to Loki) | accept | Loki auth_enabled:false but ClusterIP only; Grafana access is already behind local-user auth (Phase 14); NetworkPolicy lands Phase 17 |
| T-15-09 | Tampering | re-render displaces existing Prometheus datasource / dashboards | mitigate | additionalDataSources is additive; render diff inspected (Open Q4); verify asserts both type:prometheus and type:loki present |
| T-15-SC | Tampering | helm chart re-render (prometheus 29.11.0, grafana 10.5.15) | mitigate | charts already approved in Phase 13; versions pinned; render never `2>&1`-spliced |
</threat_model>

<verification>
- 10-prometheus.yaml re-render contains loki:3100 + alloy:12345 targets, no ClusterRole.
- 50-grafana.yaml re-render contains both a prometheus and a loki datasource at the correct URL; Prometheus datasource preserved.
- `python3 scripts/validate-obs-manifests.py` green on both re-rendered manifests.
</verification>

<success_criteria>
Prometheus now has scrape targets that expose the LOG-01/LOG-02 proof metrics, and Grafana provisions Loki as a healthy second datasource for the LOG-03 LogQL acceptance — both via additive extension of the Phase 13 stack with no regression to the existing metrics datasource.
</success_criteria>

<output>
Create `.planning/phases/15-log-stack/15-03-SUMMARY.md` when done.
</output>
