---
phase: 12-resource-protection-obs-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/resource-preflight.sh
  - scripts/validate-phase-12.sh
  - scripts/validate-staging.py
autonomous: true
requirements: [PREP-01]
tags: [kubernetes, observability, validation, bash]

must_haves:
  truths:
    - "Operator can run scripts/resource-preflight.sh and get a timestamped snapshot of node CPU/memory/disk and existing pod allocations (PREP-01)"
    - "scripts/validate-phase-12.sh exists and wraps every PREP-01..05 kubectl/SSH assertion, exiting 1 on any failure"
    - "Both new scripts pass bash -n syntax checks and are registered in validate-staging.py's bash syntax list"
  artifacts:
    - path: "scripts/resource-preflight.sh"
      provides: "Re-runnable node/pod resource snapshot (PREP-01)"
      contains: "kubectl describe node"
    - path: "scripts/validate-phase-12.sh"
      provides: "Phase 12 assertion harness for all PREP requirements"
      contains: "qosClass"
    - path: "scripts/validate-staging.py"
      provides: "CI manifest/script validation, extended with the two new bash scripts"
      contains: "validate-phase-12.sh"
  key_links:
    - from: "scripts/validate-staging.py"
      to: "scripts/resource-preflight.sh"
      via: "bash -n syntax check in validate_scripts()"
      pattern: "resource-preflight.sh"
    - from: "scripts/validate-staging.py"
      to: "scripts/validate-phase-12.sh"
      via: "bash -n syntax check in validate_scripts()"
      pattern: "validate-phase-12.sh"
---

<objective>
Create the Phase 12 validation harness BEFORE any manifest or live-cluster change: the re-runnable resource preflight snapshot script (PREP-01) and the single phase-validation script that wraps every PREP-01..05 assertion. Register both in validate-staging.py's bash syntax list so CI keeps them honest.

Purpose: The Nyquist strategy (12-VALIDATION.md) requires `scripts/resource-preflight.sh` and `scripts/validate-phase-12.sh` to exist in Wave 0/1 so later waves have an automated gate. This plan is pure authoring — no live cluster or host is touched — so it is autonomous and runs in parallel with the other authoring plans.
Output: scripts/resource-preflight.sh, scripts/validate-phase-12.sh, and an edit to scripts/validate-staging.py.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-resource-protection-obs-foundation/12-RESEARCH.md
@.planning/phases/12-resource-protection-obs-foundation/12-PATTERNS.md
@.planning/phases/12-resource-protection-obs-foundation/12-VALIDATION.md

# Style analogs — read before authoring
@scripts/backup-postgres-now.sh
@scripts/validate-staging.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/resource-preflight.sh (PREP-01)</name>
  <read_first>
    - scripts/backup-postgres-now.sh — shebang, `set -euo pipefail`, `namespace="${K8S_NAMESPACE:-solid-stats-staging}"` style
    - 12-RESEARCH.md § Resource Preflight Script — the full production-ready script body (already convention-compliant)
    - 12-PATTERNS.md § scripts/resource-preflight.sh — env-var defaults and tee output-capture pattern
  </read_first>
  <files>scripts/resource-preflight.sh</files>
  <action>
    Author a re-runnable Bash snapshot script implementing PREP-01. Use `#!/usr/bin/env bash` + `set -euo pipefail`. Default env vars with `: "${NAMESPACE:=solid-stats-staging}"` and `: "${OUTPUT_DIR:=${PREFLIGHT_OUTPUT_DIR:-/tmp}}"`. Build a UTC timestamp `snapshot_ts="$(date -u +%Y%m%dT%H%M%SZ)"` and write to `${OUTPUT_DIR}/resource-preflight-${snapshot_ts}.txt` via a `{ ... } | tee "${out_file}"` block. The snapshot block must capture, in order: a header with timestamp + namespace; `kubectl describe node` (node allocatable vs allocated); `kubectl top nodes` with graceful fallback `|| echo "(metrics-server not available)"`; `kubectl top pods --all-namespaces` with the same fallback; a per-namespace `kubectl -n "${NAMESPACE}" get pods` with custom-columns showing NAME/QOS/CPU_REQ/CPU_LIM/MEM_REQ/MEM_LIM (qosClass from `.status.qosClass`); `df -h`; and `free -h` (host memory + swap). After the block, echo "Snapshot written to: ${out_file}". Copy the body from 12-RESEARCH.md § Resource Preflight Script verbatim — it already follows project conventions. Make the file executable (chmod +x).
  </action>
  <verify>
    <automated>bash -n scripts/resource-preflight.sh && grep -q 'kubectl describe node' scripts/resource-preflight.sh && grep -q 'qosClass' scripts/resource-preflight.sh && grep -q 'free -h' scripts/resource-preflight.sh && test -x scripts/resource-preflight.sh && echo OK</automated>
  </verify>
  <done>scripts/resource-preflight.sh passes bash -n, contains the node-describe / qosClass / free -h captures, and is executable. (Live run against the cluster happens in Plan 05, not here.)</done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/validate-phase-12.sh (assertion harness for PREP-01..05)</name>
  <read_first>
    - 12-VALIDATION.md § Per-Task Verification Map — the exact assertion commands and expected values for every PREP requirement
    - 12-PATTERNS.md § scripts/validate-phase-12.sh — the `assert()` helper and `auth can-i` negative-assertion pattern
    - k8s/staging/01-ci-rbac.yaml — the SA naming convention being mirrored (obs-ci-deployer)
  </read_first>
  <files>scripts/validate-phase-12.sh</files>
  <action>
    Author a single re-runnable validation script that asserts every Phase 12 requirement and exits 1 on the first failure. Use `#!/usr/bin/env bash` + `set -euo pipefail` and `namespace="${K8S_NAMESPACE:-solid-stats-staging}"`. Define an `assert(label, actual, expected)` helper that prints `ok: <label>` on match and `FAIL: <label> — got '<actual>', want '<expected>'` to stderr then `exit 1` on mismatch. Implement these assertions sourced from 12-VALIDATION.md: (1) PREP-03 PriorityClasses exist — `kubectl get priorityclass app-critical obs-background` succeeds and each value via jsonpath equals 1000000 / 100; (2) PREP-03 each app workload pod carries priorityClassName app-critical — query pods in the namespace and assert the field is `app-critical` for postgres / server-2 / replay-parser-2 / rabbitmq; (3) PREP-04 postgres-0 `.status.qosClass` equals `Guaranteed`; (4) PREP-04 server-2 pod (label app.kubernetes.io/name=server-2) `.status.qosClass` equals `Guaranteed`; (5) PREP-05 `kubectl get namespace monitoring error-tracking` succeeds; (6) PREP-05 `obs-ci-deployer` SA exists in both monitoring and error-tracking; (7) PREP-05 positive — `kubectl auth can-i create deployments --as=system:serviceaccount:monitoring:obs-ci-deployer -n monitoring` equals `yes`; (8) PREP-05 negative isolation — `kubectl auth can-i get pods --as=system:serviceaccount:monitoring:obs-ci-deployer -n solid-stats-staging` equals `no`. For PREP-02 (host swap), the script cannot SSH from CI context, so emit a note line directing the operator to the manual SSH check (do NOT attempt SSH inside this script). Make the file executable.
  </action>
  <verify>
    <automated>bash -n scripts/validate-phase-12.sh && grep -q 'auth can-i' scripts/validate-phase-12.sh && grep -q 'Guaranteed' scripts/validate-phase-12.sh && grep -q 'obs-ci-deployer' scripts/validate-phase-12.sh && test -x scripts/validate-phase-12.sh && echo OK</automated>
  </verify>
  <done>scripts/validate-phase-12.sh passes bash -n, contains positive and negative `auth can-i` assertions, the Guaranteed QoS checks, and the priorityclass checks; it is executable. (It is RUN against the live cluster only in Plan 05, after the manifests are applied.)</done>
</task>

<task type="auto">
  <name>Task 3: Register both scripts in validate-staging.py</name>
  <read_first>
    - scripts/validate-staging.py lines 200-211 (validate_scripts bash list)
    - 12-PATTERNS.md § validate-staging.py Impact — bootstrap files are NOT added to EXPECTED_MANIFESTS; only the two bash scripts get the syntax-check entry
  </read_first>
  <files>scripts/validate-staging.py</files>
  <action>
    In `validate_scripts()`, add `"scripts/resource-preflight.sh"` and `"scripts/validate-phase-12.sh"` to the existing bash-script list that runs `bash -n` (the list currently holding backup-postgres-now.sh, restore-drill.sh, apply-s3-lifecycle.sh, cutover.sh). Do NOT touch EXPECTED_MANIFESTS — `01-obs-rbac.yaml` and `02-priority-classes.yaml` are operator-bootstrap files excluded from CI and must not be added there (per 12-PATTERNS.md § validate-staging.py Impact). Make no other change.
  </action>
  <verify>
    <automated>grep -q 'resource-preflight.sh' scripts/validate-staging.py && grep -q 'validate-phase-12.sh' scripts/validate-staging.py && python3 scripts/validate-staging.py && echo OK</automated>
  </verify>
  <done>validate-staging.py lists both new scripts in its bash syntax-check loop and `python3 scripts/validate-staging.py` exits 0 with the new scripts present and syntax-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation → k3s API | resource-preflight.sh and validate-phase-12.sh issue read-only kubectl over the WireGuard tunnel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | resource-preflight.sh snapshot output | accept | Snapshot is node/pod resource metadata only (CPU/mem/disk, QoS) — no secret values; written to operator-local /tmp, not committed |
| T-12-02 | Tampering | validate-staging.py bash list | mitigate | Only two filename strings added; `python3 scripts/validate-staging.py` re-run proves no behavioral regression |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; pure Bash + stdlib Python — slopcheck N/A |
</threat_model>

<verification>
- `bash -n` clean on both new scripts.
- `python3 scripts/validate-staging.py` exits 0 with both scripts registered.
- Both scripts executable (`test -x`).
- No live cluster or host contacted by this plan (read-only authoring only).
</verification>

<success_criteria>
- scripts/resource-preflight.sh implements the PREP-01 snapshot (node describe, top, df, free, per-ns QoS columns) and is executable.
- scripts/validate-phase-12.sh implements every PREP-01..05 assertion incl. the obs-ci-deployer positive/negative `auth can-i` isolation checks, and is executable.
- validate-staging.py runs both through bash -n in CI.
</success_criteria>

<output>
Create `.planning/phases/12-resource-protection-obs-foundation/12-01-SUMMARY.md` when done.
</output>
