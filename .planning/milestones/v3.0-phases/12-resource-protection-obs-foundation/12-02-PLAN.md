---
phase: 12-resource-protection-obs-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/staging/01-obs-rbac.yaml
  - k8s/staging/02-priority-classes.yaml
  - .github/workflows/deploy-staging.yml
autonomous: true
requirements: [PREP-03, PREP-05]
tags: [kubernetes, rbac, priorityclass, observability, ci]

must_haves:
  truths:
    - "k8s/staging/01-obs-rbac.yaml declares the monitoring and error-tracking namespaces, each with a non-default obs-ci-deployer ServiceAccount, long-lived token Secret, namespace-scoped Role, and RoleBinding (PREP-05)"
    - "k8s/staging/02-priority-classes.yaml declares app-critical (value 1000000) and obs-background (value 100) PriorityClasses, both globalDefault:false (PREP-03)"
    - "Both new files carry the operator-bootstrap DO-NOT-apply-from-CI header and are excluded from the CI deploy glob in both the dry-run and deploy steps"
    - "obs-ci-deployer Role grants get/list/watch/create/update/patch on apps (deployments/statefulsets/daemonsets), batch cronjobs, core services/configmaps/secrets/pvcs/serviceaccounts, and read on pods — with NO access to solid-stats-staging and NO cluster-scoped or delete verbs (PREP-05)"
  artifacts:
    - path: "k8s/staging/01-obs-rbac.yaml"
      provides: "monitoring + error-tracking namespaces and obs-ci-deployer least-privilege RBAC (PREP-05)"
      contains: "name: obs-ci-deployer"
    - path: "k8s/staging/02-priority-classes.yaml"
      provides: "app-critical and obs-background PriorityClasses (PREP-03)"
      contains: "name: app-critical"
    - path: ".github/workflows/deploy-staging.yml"
      provides: "CI deploy glob extended to exclude the two new operator-bootstrap files"
      contains: "01-obs-rbac.yaml"
  key_links:
    - from: "k8s/staging/01-obs-rbac.yaml"
      to: ".github/workflows/deploy-staging.yml"
      via: "glob exclusion ! -name '01-obs-rbac.yaml' in both find commands"
      pattern: "! -name '01-obs-rbac.yaml'"
    - from: "k8s/staging/02-priority-classes.yaml"
      to: ".github/workflows/deploy-staging.yml"
      via: "glob exclusion ! -name '02-priority-classes.yaml' in both find commands"
      pattern: "! -name '02-priority-classes.yaml'"
---

<objective>
Author the two operator-bootstrap manifests that establish the observability foundation: the namespace + least-privilege RBAC file (PREP-05) and the PriorityClass file (PREP-03 definitions). Extend the CI deploy glob to exclude both, mirroring the `01-ci-rbac.yaml` / `00-namespace.yaml` precedent so CI never overwrites operator-managed bootstrap.

Purpose: These objects are cluster-scoped or cross-namespace (Namespace, ClusterRole-adjacent RBAC) and must be applied once by the operator — a namespace-scoped Role cannot create namespaces. Authoring is pure file work (no live cluster touched here), so this plan is autonomous and runs in parallel with Plans 01 and 03. The actual `kubectl apply` to the live cluster + the obs-ci-deployer isolation verification happen in Plan 05.
Output: k8s/staging/01-obs-rbac.yaml, k8s/staging/02-priority-classes.yaml, and a glob-exclusion edit to .github/workflows/deploy-staging.yml.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-resource-protection-obs-foundation/12-RESEARCH.md
@.planning/phases/12-resource-protection-obs-foundation/12-PATTERNS.md

# Exact analogs — mirror their structure
@k8s/staging/01-ci-rbac.yaml
@k8s/staging/00-namespace.yaml
@.github/workflows/deploy-staging.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author k8s/staging/01-obs-rbac.yaml (PREP-05)</name>
  <read_first>
    - k8s/staging/01-ci-rbac.yaml — exact structural mirror (header, SA, token Secret, Role verbs, RoleBinding)
    - k8s/staging/00-namespace.yaml — Namespace label style (app.kubernetes.io/part-of + solid-stats.io/environment: staging)
    - 12-PATTERNS.md § 01-obs-rbac.yaml — document order and the per-namespace block to repeat
    - 12-RESEARCH.md § Namespace + RBAC — the daemonsets addition and the obs-vs-runtime separation rationale
  </read_first>
  <files>k8s/staging/01-obs-rbac.yaml</files>
  <action>
    Create the operator-bootstrap RBAC manifest for PREP-05, mirroring 01-ci-rbac.yaml. Start with the four-line operator-bootstrap header naming THIS file (DO NOT apply from CI; applied once by the operator; the CI deploy glob must exclude 01-obs-rbac.yaml). Then, in this document order: Namespace monitoring → Namespace error-tracking (both with labels app.kubernetes.io/part-of: solid-stats and solid-stats.io/environment: staging, copied from 00-namespace.yaml style); then for the monitoring namespace a ServiceAccount obs-ci-deployer, a Secret obs-ci-deployer-token (type kubernetes.io/service-account-token, annotation kubernetes.io/service-account.name: obs-ci-deployer), a Role obs-ci-deployer, and a RoleBinding obs-ci-deployer; then repeat the identical SA + Secret + Role + RoleBinding block verbatim with namespace: error-tracking. The Role rules grant get/list/watch/create/update/patch on apps deployments+statefulsets+daemonsets and batch cronjobs; get/list/watch on core pods; get/list/create/update/patch on core services+configmaps+secrets+persistentvolumeclaims and core serviceaccounts. This is the ci-deployer verb set PLUS daemonsets (for Grafana Alloy in Phase 15). Least-privilege: NO delete verb, NO cluster-scoped resources, NO RBAC self-modification, and NO reference to solid-stats-staging anywhere. Apply standard labels (app.kubernetes.io/name: obs-ci-deployer, app.kubernetes.io/part-of: solid-stats) on every named resource. The obs-ci-deployer namespace-scoped Role into monitoring/error-tracking deliberately keeps it separate from the runtime ci-deployer per PREP-05.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; docs=list(yaml.safe_load_all(open('k8s/staging/01-obs-rbac.yaml'))); kinds=[d['kind'] for d in docs if d]; ns=sorted({d['metadata']['name'] for d in docs if d and d['kind']=='Namespace'}); assert ns==['error-tracking','monitoring'], ns; assert kinds.count('ServiceAccount')==2 and kinds.count('Role')==2 and kinds.count('RoleBinding')==2 and kinds.count('Secret')==2, kinds; assert 'solid-stats-staging' not in open('k8s/staging/01-obs-rbac.yaml').read(); assert 'delete' not in open('k8s/staging/01-obs-rbac.yaml').read(); print('OK')"</automated>
  </verify>
  <done>01-obs-rbac.yaml parses as valid YAML; declares exactly the monitoring + error-tracking namespaces and a matched SA/Secret/Role/RoleBinding set per namespace; contains no `solid-stats-staging` reference and no `delete` verb; carries the operator-bootstrap header.</done>
</task>

<task type="auto">
  <name>Task 2: Author k8s/staging/02-priority-classes.yaml (PREP-03 definitions)</name>
  <read_first>
    - 12-PATTERNS.md § 02-priority-classes.yaml — the full manifest body and header
    - 12-RESEARCH.md § PriorityClass Design — value separation rationale and globalDefault:false requirement (Pitfall 6)
    - k8s/staging/01-ci-rbac.yaml lines 1-4 — operator-bootstrap header to mirror
  </read_first>
  <files>k8s/staging/02-priority-classes.yaml</files>
  <action>
    Create the cluster-scoped PriorityClass manifest for PREP-03, with the operator-bootstrap header (DO NOT apply from CI; applied once: kubectl apply -f k8s/staging/02-priority-classes.yaml). Declare two scheduling.k8s.io/v1 PriorityClass objects: `app-critical` with value 1000000, and `obs-background` with value 100. Both set preemptionPolicy: PreemptLowerPriority and globalDefault: false (globalDefault MUST be false on both — Pitfall 6: a true value would retroactively re-prioritise unclassed pods). Both carry label app.kubernetes.io/part-of: solid-stats (PriorityClass is non-namespaced — no namespace field, and no app.kubernetes.io/name since there is no workload). Add a description on each explaining the eviction ordering (app-critical evicted AFTER obs-background under node memory pressure; obs-background evicted FIRST). Copy the body from 12-PATTERNS.md § 02-priority-classes.yaml verbatim.
  </action>
  <verify>
    <automated>python3 -c "import yaml; docs=[d for d in yaml.safe_load_all(open('k8s/staging/02-priority-classes.yaml')) if d]; m={d['metadata']['name']:d for d in docs}; assert m['app-critical']['value']==1000000 and m['obs-background']['value']==100, m; assert all(d['globalDefault'] is False for d in docs); assert all(d['kind']=='PriorityClass' for d in docs); print('OK')"</automated>
  </verify>
  <done>02-priority-classes.yaml parses as valid YAML; declares app-critical=1000000 and obs-background=100, both globalDefault:false; carries the operator-bootstrap header.</done>
</task>

<task type="auto">
  <name>Task 3: Exclude both bootstrap files from the CI deploy glob</name>
  <read_first>
    - .github/workflows/deploy-staging.yml lines 66-73 (dry-run find) and 124-131 (deploy find) — the two identical glob commands and the comment above each
    - 12-PATTERNS.md § deploy-staging.yml — the patched find command with the two added `! -name` clauses
  </read_first>
  <files>.github/workflows/deploy-staging.yml</files>
  <action>
    In BOTH the dry-run step (around line 72) and the deploy step (around line 130), extend the `find k8s/staging -maxdepth 1 -name '*.yaml'` exclusion list to add `! -name '01-obs-rbac.yaml'` and `! -name '02-priority-classes.yaml'` alongside the existing `! -name '00-namespace.yaml' ! -name '01-ci-rbac.yaml'`. Keep the exclusions on the same find invocation in the same style (multi-clause find). Extend the explanatory comment above each find to name the two new operator-bootstrap files and why they are excluded (cluster-scoped PriorityClass + cross-namespace obs RBAC that the namespace-scoped ci-deployer cannot and must not apply). Make the identical change in both steps so dry-run and deploy stay in sync.
  </action>
  <verify>
    <automated>test "$(grep -c "! -name '01-obs-rbac.yaml'" .github/workflows/deploy-staging.yml)" = "2" && test "$(grep -c "! -name '02-priority-classes.yaml'" .github/workflows/deploy-staging.yml)" = "2" && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy-staging.yml')); print('OK')"</automated>
  </verify>
  <done>Both find commands (dry-run + deploy) exclude 01-obs-rbac.yaml and 02-priority-classes.yaml; the workflow YAML still parses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| obs-ci-deployer SA → k3s API | a future obs CI pipeline authenticates as obs-ci-deployer and applies manifests into monitoring/error-tracking only |
| CI deploy glob → cluster bootstrap objects | CI must NOT touch operator-managed Namespace/PriorityClass/obs-RBAC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Elevation of Privilege | obs-ci-deployer reaching solid-stats-staging | mitigate | Role is namespace-scoped to monitoring/error-tracking only; manifest contains no solid-stats-staging reference; cross-namespace `auth can-i ... -n solid-stats-staging → no` is verified in Plan 05 |
| T-12-04 | Tampering | CI overwriting operator-managed bootstrap RBAC/PriorityClass | mitigate | Both files carry the DO-NOT-apply header and are added to the CI deploy glob exclusion in both steps (Pitfall 5) |
| T-12-05 | Denial of Service | PriorityClass abuse / globalDefault re-prioritising system pods | mitigate | globalDefault:false on both classes (Pitfall 6); obs-background (100) far below app-critical (1000000); k3s system pods keep their explicit system-*-critical classes |
| T-12-06 | Information Disclosure | long-lived obs-ci-deployer token | mitigate | Token lives in a k8s Secret populated by the control plane at apply time; no token value in git (same pattern as ci-deployer-token) |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; pure YAML — slopcheck N/A |
</threat_model>

<verification>
- 01-obs-rbac.yaml: valid YAML, two namespaces, matched SA/Secret/Role/RoleBinding per namespace, no solid-stats-staging reference, no delete verb, operator-bootstrap header present.
- 02-priority-classes.yaml: valid YAML, app-critical=1000000 + obs-background=100, both globalDefault:false, operator-bootstrap header present.
- deploy-staging.yml: both find commands exclude the two new files; YAML still parses.
- No live cluster contacted in this plan (operator applies in Plan 05).
</verification>

<success_criteria>
- The monitoring + error-tracking namespace and obs-ci-deployer RBAC are fully authored and least-privilege (PREP-05).
- app-critical and obs-background PriorityClasses are authored with correct values and globalDefault:false (PREP-03).
- CI will never apply the two bootstrap files.
</success_criteria>

<output>
Create `.planning/phases/12-resource-protection-obs-foundation/12-02-SUMMARY.md` when done.
</output>
