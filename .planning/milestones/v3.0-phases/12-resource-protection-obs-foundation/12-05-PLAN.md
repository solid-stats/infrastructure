---
phase: 12-resource-protection-obs-foundation
plan: 05
type: execute
wave: 2
depends_on: ["12-01", "12-02", "12-03"]
files_modified:
  - k8s/staging/10-postgres.yaml
  - k8s/staging/35-server-2-deployment.yaml
autonomous: false
requirements: [PREP-01, PREP-03, PREP-04, PREP-05]
tags: [kubernetes, qos, priorityclass, rbac, observability, live-cluster]

must_haves:
  truths:
    - "Operator ran scripts/resource-preflight.sh against the live cluster and recorded node headroom + observed postgres/server-2 CPU/memory before any change (PREP-01)"
    - "The Guaranteed QoS values in 10-postgres.yaml and 35-server-2-deployment.yaml are confirmed >= observed live P95 (or adjusted from the ASSUMED defaults) before rollout, then committed (PREP-04)"
    - "02-priority-classes.yaml and 01-obs-rbac.yaml are applied to the live cluster by the operator; app-critical and obs-background PriorityClasses exist and the monitoring + error-tracking namespaces + obs-ci-deployer RBAC exist (PREP-03, PREP-05)"
    - "After the workload rollout, live postgres-0 and the server-2 pod report status.qosClass == Guaranteed and carry priorityClassName app-critical (PREP-03, PREP-04)"
    - "obs-ci-deployer CAN create deployments in monitoring but CANNOT get pods in solid-stats-staging — confirmed via kubectl auth can-i (PREP-05)"
    - "scripts/validate-phase-12.sh exits 0 against the live cluster"
  artifacts:
    - path: "k8s/staging/10-postgres.yaml"
      provides: "postgres Guaranteed QoS values confirmed against live usage (PREP-04)"
      contains: "priorityClassName: app-critical"
    - path: "k8s/staging/35-server-2-deployment.yaml"
      provides: "server-2 Guaranteed QoS values confirmed against live usage (PREP-04)"
      contains: "priorityClassName: app-critical"
  key_links:
    - from: "scripts/validate-phase-12.sh"
      to: "live k3s cluster"
      via: "kubectl assertions for priorityclass/QoS/namespaces/RBAC over WireGuard"
      pattern: "auth can-i"
    - from: "k8s/staging/10-postgres.yaml"
      to: "live postgres-0 pod"
      via: "kubectl apply triggers StatefulSet rolling restart → Guaranteed QoS"
      pattern: "qosClass"
---

<objective>
Apply Phase 12 protection to the LIVE staging cluster and prove it: run the live resource preflight to baseline headroom and size the Guaranteed QoS values (PREP-01), operator-apply the two bootstrap manifests (PriorityClasses + obs namespaces/RBAC — PREP-03 definitions, PREP-05), apply the patched workload manifests so postgres/server-2 roll to Guaranteed QoS and all runtime pods carry app-critical (PREP-03 assignment, PREP-04), then run scripts/validate-phase-12.sh to confirm QoS, priority, and obs-ci-deployer isolation.

Purpose: This is the only plan that mutates the live cluster. It depends on Plans 01 (scripts), 02 (bootstrap manifests), and 03 (workload patches). Because the QoS rollout restarts postgres/server-2 (brief downtime, single-node single-replica) and applies operator-bootstrap RBAC/PriorityClass, it is operator-gated (autonomous:false). The ASSUMED QoS values from Plan 03 are confirmed against live `kubectl top` HERE before rollout — this is where Open Question 1 is resolved.
Output: live cluster carrying PriorityClasses + obs namespaces/RBAC + Guaranteed-QoS postgres/server-2; any QoS value adjustments committed back to the two manifests; validate-phase-12.sh green.
</objective>

<execution_context>
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/workflows/execute-plan.md
@/home/afgan0r/Projects/SolidGames/infrastructure/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-resource-protection-obs-foundation/12-RESEARCH.md
@.planning/phases/12-resource-protection-obs-foundation/12-VALIDATION.md
@.planning/phases/12-resource-protection-obs-foundation/12-01-PLAN.md
@.planning/phases/12-resource-protection-obs-foundation/12-02-PLAN.md
@.planning/phases/12-resource-protection-obs-foundation/12-03-PLAN.md

# Manifests being applied / possibly value-adjusted
@k8s/staging/10-postgres.yaml
@k8s/staging/35-server-2-deployment.yaml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1 (checkpoint): Run live preflight + decide Guaranteed QoS values (PREP-01)</name>
  <action>Operator brings up the WireGuard tunnel, runs scripts/resource-preflight.sh against the live cluster, reviews observed postgres/server-2 CPU/memory, and decides whether the ASSUMED Plan-03 QoS values stand or need raising. Blocking human checkpoint — no autonomous file change.</action>
  <what-built>Plans 01-03 produced the preflight + validation scripts, the bootstrap manifests (02-priority-classes.yaml, 01-obs-rbac.yaml), and the patched workload manifests with ASSUMED Guaranteed QoS values. Before mutating the live cluster, the operator brings up the WireGuard tunnel, runs the live preflight to baseline headroom, and reviews observed postgres/server-2 CPU/memory so the QoS values can be confirmed or adjusted.</what-built>
  <how-to-verify>
    1. Bring up the WireGuard tunnel + kubeconfig per the Phase 6 path (operator workstation).
    2. Run the live preflight (PREP-01): `bash scripts/resource-preflight.sh` — confirm it writes a snapshot and captures `kubectl describe node` Allocated resources, `kubectl top pods` (if metrics-server present), df -h, free -h.
    3. Review the snapshot: note observed CPU/memory for postgres-0 and the server-2 pod. Compare against the ASSUMED Plan-03 values (postgres 500m/1Gi, server-2 250m/512Mi). If observed P95 CPU/memory is HIGHER than the authored limit, the limit MUST be raised (else CFS throttle / OOM — Pitfall 1). If comfortably lower, the ASSUMED values stand.
    4. Decide final Guaranteed values for postgres and server-2.
    Expected: a preflight snapshot exists and the operator has decided whether the ASSUMED QoS values are safe or need raising.
  </how-to-verify>
  <resume-signal>Reply "values ok" to keep the ASSUMED Plan-03 values, or give the adjusted numbers (e.g. "postgres cpu 750m mem 1Gi, server-2 cpu 300m mem 512Mi").</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Confirm/adjust Guaranteed QoS values from the live preflight (PREP-04)</name>
  <read_first>
    - k8s/staging/10-postgres.yaml + k8s/staging/35-server-2-deployment.yaml — current ASSUMED requests==limits values from Plan 03
    - The preflight snapshot reviewed in the checkpoint above
  </read_first>
  <files>k8s/staging/10-postgres.yaml, k8s/staging/35-server-2-deployment.yaml</files>
  <action>
    Apply the operator's decision from the checkpoint. If the operator replied "values ok", make NO edit — the ASSUMED values stand and the inline "confirm against live P95" comments can be tightened to note they were confirmed on the preflight date. If the operator gave adjusted numbers, update the requests AND limits for postgres and/or server-2 to the agreed values, keeping requests == limits exactly (Guaranteed QoS requirement) and keeping server-2's two initContainer requests==limits blocks intact. Do not change any other field. Re-run `python3 scripts/validate-staging.py` to confirm the manifests still pass shape checks after any edit.
  </action>
  <verify>
    <automated>python3 -c "import yaml; [ [ __import__('sys').exit('FAIL '+f+' '+c['name']) for c in (d['spec']['template']['spec'].get('initContainers',[])+d['spec']['template']['spec']['containers']) if not (c['resources']['requests']['cpu']==c['resources']['limits']['cpu'] and c['resources']['requests']['memory']==c['resources']['limits']['memory']) ] for f,k in [('k8s/staging/10-postgres.yaml','StatefulSet'),('k8s/staging/35-server-2-deployment.yaml','Deployment')] for d in yaml.safe_load_all(open(f)) if d and d.get('kind')==k ]; print('OK')" && python3 scripts/validate-staging.py && echo OK</automated>
  </verify>
  <done>postgres and server-2 manifests keep requests==limits on every container (incl. server-2 initContainers) at the operator-confirmed values; validate-staging.py exits 0. If no adjustment was needed, the files are unchanged from Plan 03.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2 (checkpoint): Operator applies bootstrap + workload manifests to the live cluster (PREP-03, PREP-05)</name>
  <action>Operator takes a fresh backup, applies the two operator-bootstrap files out-of-band (PriorityClasses + obs RBAC), applies the patched workload manifests, and watches postgres/server-2 roll out. Blocking human checkpoint — live-impacting, no autonomous file change.</action>
  <what-built>The bootstrap manifests (02-priority-classes.yaml cluster-scoped PriorityClasses; 01-obs-rbac.yaml monitoring + error-tracking namespaces and obs-ci-deployer RBAC) and the QoS-confirmed workload manifests are ready. The operator now applies them to the LIVE cluster. The workload apply triggers a rolling restart of postgres (StatefulSet) and server-2 (Deployment) — brief downtime on this single-node single-replica cluster.</what-built>
  <how-to-verify>
    1. Take a fresh backup first (Pitfall 3): `bash scripts/backup-postgres-now.sh` and confirm the backup gate summary prints a non-empty backup_id.
    2. Apply the two operator-bootstrap files (NOT via CI — these are excluded from the deploy glob):
       `kubectl apply -f k8s/staging/02-priority-classes.yaml`
       `kubectl apply -f k8s/staging/01-obs-rbac.yaml`
    3. Apply the patched workload manifests (or let a master-push CI deploy do it for the non-bootstrap files; postgres/server-2 will roll). Watch the rollout:
       `kubectl -n solid-stats-staging rollout status statefulset/postgres --timeout=300s`
       `kubectl -n solid-stats-staging rollout status deployment/server-2 --timeout=300s`
    4. Confirm server-2 recovers (its wait-for-postgres initContainer self-heals if postgres briefly restarts).
    Expected: PriorityClasses + obs namespaces/RBAC exist; postgres-0 and server-2 rolled to new pods with no crash-loop.
  </how-to-verify>
  <resume-signal>Type "applied" once both bootstrap files are applied and postgres + server-2 have rolled out healthy, or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify live protection with scripts/validate-phase-12.sh (PREP-03, PREP-04, PREP-05)</name>
  <read_first>
    - scripts/validate-phase-12.sh — the assertion harness from Plan 01
    - 12-VALIDATION.md § Per-Task Verification Map — expected values for every assertion
  </read_first>
  <files>k8s/staging/10-postgres.yaml</files>
  <action>
    With the WireGuard tunnel up and the bootstrap + workload manifests applied (previous checkpoint), run `bash scripts/validate-phase-12.sh` against the live cluster. It must assert: PriorityClasses app-critical=1000000 / obs-background=100 exist (PREP-03); each runtime pod carries priorityClassName app-critical (PREP-03); postgres-0 and the server-2 pod report status.qosClass == Guaranteed (PREP-04); monitoring + error-tracking namespaces and the obs-ci-deployer SA in each exist (PREP-05); obs-ci-deployer CAN create deployments in monitoring (yes) and CANNOT get pods in solid-stats-staging (no) (PREP-05 isolation). The script must exit 0. This task touches no manifest edit — 10-postgres.yaml is listed only as the anchor file; if any assertion fails, diagnose (e.g. old pods not yet restarted → Pitfall 2: force `kubectl rollout restart`), fix on the live cluster, and re-run until green. Record the green run + the obs-ci-deployer isolation result in the SUMMARY.
  </action>
  <verify>
    <automated>bash scripts/validate-phase-12.sh</automated>
  </verify>
  <done>scripts/validate-phase-12.sh exits 0 against the live cluster: PriorityClasses present, all app pods at app-critical, postgres-0 + server-2 at Guaranteed QoS, obs namespaces + RBAC present, and obs-ci-deployer proven unable to reach solid-stats-staging.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live k3s API (WireGuard) | applying cluster-scoped PriorityClass + cross-namespace obs RBAC + workload QoS changes |
| obs-ci-deployer SA → solid-stats-staging | must be DENIED — cross-namespace isolation is verified live here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13 | Denial of Service | postgres/server-2 downtime during the QoS rolling restart | mitigate | Operator-gated checkpoint takes a fresh backup first (Pitfall 3), watches rollout status, low-traffic window; server-2 initContainer self-heals |
| T-12-14 | Elevation of Privilege | obs-ci-deployer reaching the runtime namespace | mitigate | validate-phase-12.sh asserts `auth can-i get pods --as=...obs-ci-deployer -n solid-stats-staging → no` (PREP-05); blocks phase completion if it returns yes |
| T-12-15 | Tampering | CI overwriting the operator-applied PriorityClass/obs-RBAC | mitigate | Both files excluded from the CI deploy glob (Plan 02); operator applies them out-of-band |
| T-12-16 | Denial of Service | priorityClassName not inherited by still-running pods | mitigate | Pitfall 2 remediation in Task 2 — force `kubectl rollout restart` and re-assert; validate-phase-12.sh fails loudly until pods carry app-critical |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; kubectl apply of repo YAML only — slopcheck N/A |
</threat_model>

<verification>
- Live preflight snapshot recorded; QoS values confirmed/adjusted (PREP-01, PREP-04).
- Bootstrap manifests applied; PriorityClasses + obs namespaces/RBAC live (PREP-03, PREP-05).
- postgres-0 + server-2 at Guaranteed QoS with app-critical priority post-rollout (PREP-03, PREP-04).
- obs-ci-deployer isolation proven: yes in monitoring, no in solid-stats-staging (PREP-05).
- `bash scripts/validate-phase-12.sh` exits 0.
</verification>

<success_criteria>
- The live staging cluster is OOM-protected: app workloads at app-critical, postgres/server-2 Guaranteed, obs pods (future) will be evicted first.
- The monitoring + error-tracking obs foundation exists with verified least-privilege, runtime-isolated RBAC.
- The whole phase is provable via one green validate-phase-12.sh run.
</success_criteria>

<output>
Create `.planning/phases/12-resource-protection-obs-foundation/12-05-SUMMARY.md` when done.
</output>