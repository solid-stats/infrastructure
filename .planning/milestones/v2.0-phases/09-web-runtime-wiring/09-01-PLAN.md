---
phase: 09-web-runtime-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/staging/36-web.yaml
  - k8s/staging/37-web-deployment.yaml
  - scripts/validate-staging.py
  - .github/workflows/deploy-staging.yml
autonomous: true
requirements:
  - WEB-01
  - WEB-02
  - WEB-03

must_haves:
  truths:
    - "web Deployment, Service, ConfigMap, and ServiceAccount exist in k8s/staging/ following server-2 conventions (WEB-01)"
    - "web Deployment has replicas: 0 and a pinned inert placeholder image — no real pod ever starts (WEB-02)"
    - "kubectl rollout status deployment/web exits 0 immediately because 0/0 desired replicas is already satisfied (WEB-02)"
    - "python3 scripts/validate-staging.py exits 0 with web in EXPECTED_MANIFESTS and EXPECTED_WORKLOADS (WEB-03)"
    - ".github/workflows/deploy-staging.yml Verify rollouts step includes deployment/web and web Service (WEB-03)"
  artifacts:
    - path: "k8s/staging/36-web.yaml"
      provides: "ConfigMap web-config + Service web"
      contains: "kind: ConfigMap"
    - path: "k8s/staging/37-web-deployment.yaml"
      provides: "ServiceAccount web + Deployment web"
      contains: "replicas: 0"
    - path: "scripts/validate-staging.py"
      provides: "Extended validation with web entries"
      contains: "36-web.yaml"
    - path: ".github/workflows/deploy-staging.yml"
      provides: "Rollout gate includes web"
      contains: "deployment/web"
  key_links:
    - from: "k8s/staging/37-web-deployment.yaml"
      to: "k8s/staging/36-web.yaml"
      via: "envFrom configMapRef web-config"
      pattern: "configMapRef"
    - from: "scripts/validate-staging.py EXPECTED_WORKLOADS"
      to: "k8s/staging/37-web-deployment.yaml"
      via: "workload safety check reads file: 37-web-deployment.yaml"
      pattern: "37-web-deployment\\.yaml"
    - from: ".github/workflows/deploy-staging.yml Verify rollouts"
      to: "k8s/staging/37-web-deployment.yaml"
      via: "kubectl rollout status deployment/web"
      pattern: "rollout status deployment/web"
---

<objective>
Create the conventions-compliant Kubernetes slot for the future `web` application and wire it into
validation and the rollout-status gate.

Purpose: Phase 11 (Production Cutover) depends on Phase 9. The web slot must exist before cutover
planning — the real image drop-in must require only a one-line change (replicas + image), with zero
new wiring. This plan delivers the complete slot and all validation coverage.

Output:
- k8s/staging/36-web.yaml — ConfigMap web-config + Service web
- k8s/staging/37-web-deployment.yaml — ServiceAccount web + Deployment web (replicas: 0, pause placeholder)
- scripts/validate-staging.py extended with web EXPECTED_MANIFESTS + EXPECTED_WORKLOADS entries
- .github/workflows/deploy-staging.yml Verify rollouts step extended with deployment/web + Service web
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-web-runtime-wiring/09-CONTEXT.md

# Analogs to mirror (read before writing manifests)
@k8s/staging/30-server-2.yaml
@k8s/staging/35-server-2-deployment.yaml

# Integration targets to extend (read before modifying)
@scripts/validate-staging.py
@.github/workflows/deploy-staging.yml

# Project conventions
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create web manifests (ConfigMap + Service + ServiceAccount + Deployment)</name>
  <files>k8s/staging/36-web.yaml, k8s/staging/37-web-deployment.yaml</files>
  <action>
Create two manifest files mirroring the server-2 split layout. Read 30-server-2.yaml and
35-server-2-deployment.yaml as direct analogs before writing.

**k8s/staging/36-web.yaml** — two YAML documents separated by ---:

Document 1: ConfigMap
- apiVersion: v1, kind: ConfigMap
- metadata.name: web-config, metadata.namespace: solid-stats-staging
- labels: app.kubernetes.io/name: web, app.kubernetes.io/part-of: solid-stats
- data: minimal starting-point config, no secrets. Include:
    NODE_ENV: production
    PORT: "3001"
    API_BASE_URL: http://server-2:3000
  Add a comment above data: "# Minimal starting-point config — extend as the web app evolves. No secrets here."

Document 2: Service
- apiVersion: v1, kind: Service
- metadata.name: web, metadata.namespace: solid-stats-staging
- labels: app.kubernetes.io/name: web, app.kubernetes.io/part-of: solid-stats
- spec.ports: name: http, port: 3001, targetPort: http
- spec.selector: app.kubernetes.io/name: web

**k8s/staging/37-web-deployment.yaml** — two YAML documents separated by ---:

Document 1: ServiceAccount
- apiVersion: v1, kind: ServiceAccount
- metadata.name: web, metadata.namespace: solid-stats-staging
- labels: app.kubernetes.io/name: web, app.kubernetes.io/part-of: solid-stats

Document 2: Deployment
- apiVersion: apps/v1, kind: Deployment
- metadata.name: web, metadata.namespace: solid-stats-staging
- labels: app.kubernetes.io/name: web, app.kubernetes.io/part-of: solid-stats
- spec.replicas: 0
- spec.selector.matchLabels: app.kubernetes.io/name: web
- spec.template.metadata.labels: app.kubernetes.io/name: web, app.kubernetes.io/part-of: solid-stats
- spec.template.spec:
    serviceAccountName: web
    automountServiceAccountToken: false
    imagePullSecrets:
      - name: ghcr-pull
    containers:
      - name: web
        image: registry.k8s.io/pause:3.9
        # PLACEHOLDER — swap for ghcr.io/solid-stats/web@sha256:<digest> and set replicas > 0
        # when the web image is published. This inert image is never pulled (replicas: 0).
        imagePullPolicy: IfNotPresent
        ports:
          - name: http
            containerPort: 3001
        envFrom:
          - configMapRef:
              name: web-config
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 30
          periodSeconds: 20
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: "1"
            memory: 512Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]

**Image choice rationale (per D-discretion from CONTEXT.md):** registry.k8s.io/pause:3.9 is the
inert pause container used by k8s itself — it binds no ports, runs no code, cannot serve traffic.
Because validate_app_image_pins only checks lines containing "image: ghcr.io/solid-stats/", the
pause image is invisible to that check and requires no exception. When the web image is ready,
swap image: and set replicas: N — nothing else changes.

**Future swap comment:** Add a prominent comment block above replicas in the Deployment:
  # WEB-02: stub at 0 replicas pending real image.
  # To activate: set replicas: N and replace the image line with:
  #   image: ghcr.io/solid-stats/web@sha256:<digest>
  # No other manifest changes required.
  </action>
  <verify>
    <automated>
python3 -c "
import subprocess, sys
result = subprocess.run(['python3', 'scripts/validate-staging.py'],
    capture_output=True, text=True,
    cwd='.')
print(result.stdout); print(result.stderr, file=sys.stderr)
sys.exit(result.returncode)
"
# Expect: validator fails because web is not yet in EXPECTED_MANIFESTS/WORKLOADS —
# that is correct at this point (Task 2 adds it). Instead verify manifest shape directly:
python3 -c "
from pathlib import Path
root = Path('k8s/staging')
for f in ['36-web.yaml', '37-web-deployment.yaml']:
    txt = (root / f).read_text()
    assert 'namespace: solid-stats-staging' in txt, f'{f} missing namespace'
    assert 'app.kubernetes.io/name: web' in txt, f'{f} missing name label'
print('manifest structure ok')
"
grep -c 'replicas: 0' k8s/staging/37-web-deployment.yaml
grep -c 'registry.k8s.io/pause:3.9' k8s/staging/37-web-deployment.yaml
grep -c 'automountServiceAccountToken: false' k8s/staging/37-web-deployment.yaml
grep -c 'readinessProbe:' k8s/staging/37-web-deployment.yaml
grep -c 'livenessProbe:' k8s/staging/37-web-deployment.yaml
    </automated>
  </verify>
  <done>
Both files exist in k8s/staging/. 37-web-deployment.yaml has replicas: 0, registry.k8s.io/pause:3.9
as image, automountServiceAccountToken: false, readinessProbe, livenessProbe, securityContext with
allowPrivilegeEscalation: false and drop ALL, resource requests and limits. 36-web.yaml has web-config
ConfigMap and web Service on port 3001. No secret values in any ConfigMap field.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire web into validate-staging.py and deploy-staging.yml</name>
  <files>scripts/validate-staging.py, .github/workflows/deploy-staging.yml</files>
  <action>
**scripts/validate-staging.py — two targeted edits:**

Edit 1: EXPECTED_MANIFESTS list (around line 17-26). Add after "35-server-2-deployment.yaml":
    "36-web.yaml",
    "37-web-deployment.yaml",
Keep list order matching numeric filename order.

Edit 2: EXPECTED_WORKLOADS dict (around line 49-56). Add after the "server-2" entry:
    "web": {"kind": "Deployment", "file": "37-web-deployment.yaml", "long_running": True},
Setting long_running: True activates readinessProbe + livenessProbe safety checks — the manifest
carries those probes (defined in Task 1) so validation passes.

Do NOT add web to APP_IMAGES — that dict is exclusively for ghcr.io/solid-stats/ images requiring
the image-pin check. The pause placeholder must not be checked for GHCR pinning.

Do NOT add web to EXPECTED_SECRETS — the web slot has no runtime Secret (no DB, no S3 creds at
this stage).

No exception comments needed in validate_workload_safety: pause:3.9 passes all existing checks
(has serviceAccountName, automountServiceAccountToken: false, resources, securityContext,
readinessProbe, livenessProbe). The 0-replica count is not inspected by the validator.

**deploy-staging.yml — two targeted edits within the "Verify rollouts" step (around lines 127-134):**

Edit 1: After the line `kubectl -n "$K8S_NAMESPACE" rollout status deployment/replay-parser-2 --timeout=300s`
add:
          kubectl -n "$K8S_NAMESPACE" rollout status deployment/web --timeout=300s

A 0-replica Deployment: kubectl rollout status reports success immediately ("deployment web
successfully rolled out" with 0/0 desired). This does NOT stall the deploy (WEB-02).

Edit 2: In the "Verify services and CronJobs" step (around line 136-141), find:
          kubectl -n "$K8S_NAMESPACE" get service postgres rabbitmq server-2 -o wide
Replace with:
          kubectl -n "$K8S_NAMESPACE" get service postgres rabbitmq server-2 web -o wide
  </action>
  <verify>
    <automated>
cd .
python3 scripts/validate-staging.py
    </automated>
  </verify>
  <done>
python3 scripts/validate-staging.py exits 0 and prints:
  ok: script syntax
  ok: manifest shape
  ok: drill manifest safety
  ok: workload safety
  ok: app image pins
  ok: rendered secret structure
deploy-staging.yml Verify rollouts step contains "rollout status deployment/web" and the
get service line includes "web". APP_IMAGES unchanged (still server-2, replay-parser-2,
replays-fetcher only).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git → CD → k8s API | Manifests in repo applied by the namespace-scoped CI ServiceAccount |
| ConfigMap → Pod env | web-config values injected as environment into any future web pod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | 37-web-deployment.yaml — placeholder image accidentally runs | mitigate | replicas: 0 in manifest; 0-replica Deployment never schedules a pod regardless of image. The pause:3.9 image is also inert (no network services). |
| T-09-02 | Information Disclosure | web-config ConfigMap contains secrets | mitigate | Only non-secret config (NODE_ENV, PORT, API_BASE_URL). No secretRef, no credentials. Validated by code review of ConfigMap data block. |
| T-09-03 | Elevation of Privilege | web ServiceAccount token auto-mounted | mitigate | automountServiceAccountToken: false in ServiceAccount and Deployment spec (per server-2 convention). |
| T-09-04 | Tampering | web stub trips rollout-status gate and blocks CD | mitigate | kubectl rollout status on a 0-replica Deployment exits 0 immediately (0/0 desired = already satisfied). Verified offline by Task 2 acceptance criteria. |
| T-09-05 | Elevation of Privilege | pause container process escalates privileges | mitigate | securityContext.allowPrivilegeEscalation: false + capabilities.drop: ALL on the container. pause:3.9 itself runs nothing but exists as a placeholder. |
| T-09-SC | Tampering | npm/pip/cargo package installs | accept | No package manager invocations in this phase — only YAML manifests and Python edits to existing scripts. No new dependencies. |
</threat_model>

<verification>
Full offline validation gate:

```
cd .
python3 scripts/validate-staging.py
```

Expected output (all six checks pass):
```
ok: script syntax
ok: manifest shape
ok: drill manifest safety
ok: workload safety
ok: app image pins
ok: rendered secret structure
```

Additional spot-checks:
- grep confirms `replicas: 0` and `registry.k8s.io/pause:3.9` in 37-web-deployment.yaml
- grep confirms `deployment/web` in .github/workflows/deploy-staging.yml
- grep confirms web NOT in APP_IMAGES dict in validate-staging.py
</verification>

<success_criteria>
1. python3 scripts/validate-staging.py exits 0 with all six checks passing.
2. k8s/staging/36-web.yaml and 37-web-deployment.yaml exist with all WEB-01 conventions:
   dedicated ServiceAccount, automountServiceAccountToken: false, imagePullSecrets ghcr-pull,
   imagePullPolicy: IfNotPresent, named port http, readinessProbe, livenessProbe,
   resources requests/limits, securityContext allowPrivilegeEscalation: false + drop ALL,
   app.kubernetes.io labels, explicit namespace solid-stats-staging.
3. 37-web-deployment.yaml has replicas: 0 and image: registry.k8s.io/pause:3.9 (WEB-02).
4. .github/workflows/deploy-staging.yml includes `rollout status deployment/web --timeout=300s`
   and `web` in the get service line (WEB-03).
5. No secret values appear in 36-web.yaml.
6. APP_IMAGES in validate-staging.py remains unchanged (pause is not a GHCR image, no pin check needed).
</success_criteria>

<output>
Create `.planning/phases/09-web-runtime-wiring/09-01-SUMMARY.md` when done.
</output>
