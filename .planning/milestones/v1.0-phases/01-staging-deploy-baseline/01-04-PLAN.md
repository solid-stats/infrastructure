---
phase: "01"
plan: "01-04"
title: "Scope and exception documentation"
type: "execute"
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - README.md
  - docs/staging.md
autonomous: true
requirements:
  - VAL-04
  - OWN-01
requirements_addressed:
  - VAL-04
  - OWN-01
---

<objective>
Make Phase 1 ownership boundaries, deliberate exceptions, and safety warnings
visible in human-facing docs rather than only in planning artifacts.
</objective>

<threat_model>
- T-01-09: Operator assumes infra owns production, edge, or app build systems.
  Mitigation: docs state v1 exclusions beside deploy instructions.
- T-01-10: Legacy app CD overwrites infra-managed resources unexpectedly.
  Mitigation: docs warn about temporary overlap and point to Phase 3 handoff.
</threat_model>

<tasks>
<task id="01-04-01" type="auto">
<read_first>
- README.md
- docs/staging.md
- .planning/REQUIREMENTS.md
- .planning/ROADMAP.md
</read_first>
<action>
Update `README.md` and `docs/staging.md` with a concise Phase 1 scope section:
owned resources are namespace, PostgreSQL, RabbitMQ, `server-2`,
`replay-parser-2`, suspended `replays-fetcher`, and `postgres-backup`.
Out-of-scope resources are production cutover, host nginx/cert/firewall
automation, `web`, application source/image builds, immediate removal of legacy
app deploy workflows, scheduled replay fetching, backup gate execution, full
run, and diff readiness.
</action>
<acceptance_criteria>
- `grep -q "postgres-backup" README.md`
- `grep -q "host nginx" docs/staging.md`
- `grep -q "web" docs/staging.md`
- `grep -q "legacy deploy" docs/staging.md`
- `grep -q "production cutover" docs/staging.md`
</acceptance_criteria>
</task>

<task id="01-04-02" type="auto">
<read_first>
- docs/staging.md
- k8s/staging/50-replays-fetcher.yaml
</read_first>
<action>
Add operator safety warnings to `docs/staging.md` near deploy instructions:
- `replays-fetcher` must remain `suspend: true` until backup verification and
  controlled full-run phases pass.
- Legacy app repository deploy workflows may still overlap during v1 and should
  not be removed until Phase 3.
- Any Kubernetes hardening exceptions from Phase 1 are intentional only until
  they can be verified against the staging k3s runtime.
</action>
<acceptance_criteria>
- `grep -q "suspend: true" docs/staging.md`
- `grep -q "Phase 3" docs/staging.md`
- `grep -q "Kubernetes hardening" docs/staging.md`
</acceptance_criteria>
</task>
</tasks>

<verification>
- Run `python3 scripts/validate-staging.py`.
- Run `grep -q "Not owned here" docs/staging.md`.
- Run `grep -q "suspend: true" docs/staging.md`.
</verification>

<success_criteria>
- Human-facing docs state what this repo owns in Phase 1.
- Human-facing docs state which resources remain intentionally outside infra
  ownership.
- Safety warnings are next to operator workflows, not only in planning docs.
</success_criteria>

<must_haves>
- D-Docs-Scope: Phase 1 scope and exclusions are documented.
- D-App-CD-Overlap: temporary legacy deploy overlap is documented.
- D-Fetcher-Suspended: suspended fetcher behavior is documented.
</must_haves>
