---
milestone: v3.0
milestone_name: Staging Observability Stack
audited: "2026-06-14"
status: tech_debt
scores:
  requirements: "26/26 satisfied (SDK-01 scope-adjusted by operator)"
  phases: "7/7 complete (all live)"
  integration: "proven live — validate-stack.sh green E2E this session"
  flows: "metrics + logs + errors + public edge + isolation all live-verified"
gaps:
  requirements: []     # no unsatisfied capability requirements
  integration: []
  flows: []
tech_debt:
  - phase: 13-deploy-pipeline-metrics-stack
    items:
      - "VERIFICATION status human_needed: MET-06 non-zero panel render is a visual-only op check (datasource health + 4 dashboards provisioned are auto-verified). Accepted."
      - "CI-path GitHub secrets follow-up — largely RESOLVED this run (GLITCHTIP_*/SENTRY_DSN + earlier GRAFANA/PG/K8S set)."
  - phase: 14-public-edge-grafana-tls
    items:
      - "No VERIFICATION.md artifact, but EDGE-01/02/03 + MET-07 are live-complete (grafana.solid-stats.ru HTTPS proven; errors. cert issued). Doc artifact gap only."
  - phase: 15-log-stack
    items:
      - "VERIFICATION gaps_found: a narrow CI deploy-workflow `rollout status` hardening step for Loki/Alloy. LOW impact — the live log path works and is validated by validate-stack.sh. Not a goal blocker."
  - phase: 16-error-tracking-glitchtip
    items:
      - "No VERIFICATION.md artifact, but ERR-01/02/03 are live-proven (validate-stack.sh green; forced error visible). Doc artifact gap only."
  - phase: 18-app-side-error-sdk
    items:
      - "SDK-01 scope-adjusted by operator: infra wiring (SENTRY_DSN in 3 runtime secrets) + per-app wire briefs in the plans repo. App-repo PRs + live end-to-end forced error deferred to app owners."
---

# v3.0 Staging Observability Stack — Milestone Audit

**Verdict: `tech_debt`** — all milestone capabilities are LIVE and end-to-end verified; the open
items are documentation artifacts and accepted low-impact follow-ups, not capability gaps.

## Definition of done — met

The full self-hosted observability stack runs on the RAM-bound single-node staging k3s cluster with
real OOM protection and a deploy path independent of runtime CD:

| Capability | Phase(s) | Live status |
|-----------|----------|-------------|
| OOM protection (swap + PriorityClasses + Guaranteed QoS) + obs namespaces/RBAC | 12 | ✓ verified passed |
| Metrics (Prometheus + Grafana + kube-state-metrics + node-exporter + pg/rabbitmq exporters) | 13 | ✓ all 7 targets UP; datasource healthy; 4 dashboards |
| Public edge + TLS (grafana.solid-stats.ru HTTPS, errors. cert) | 14 | ✓ live HTTPS (302) |
| Logs (Loki + Alloy, ~7d retention, server-2 LogQL) | 15 | ✓ compactor running; entries shipping; LogQL returns server-2 |
| Error tracking (GlitchTip own-postgres, closed reg, public TLS, forced error) | 16 | ✓ ERR-01/02/03 live; errors.solid-stats.ru 200 |
| Network isolation + one re-runnable stack validator | 17 | ✓ 17 netpols live; validate-stack green before+after |
| App-side error SDK | 18 | ✓ infra wiring + briefs (scope-adjusted) |

## Integration / E2E evidence (live, this session)

`scripts/validate-stack.sh` ran **green end-to-end** AFTER the Phase 17 NetworkPolicies were applied,
exercising every phase's capability in one pass:
- Phase 13: all Prometheus targets UP; Grafana Prometheus datasource healthy; 4 dashboards.
- Phase 15: Loki compactor running; Alloy still shipping (`loki_write_sent_entries_total` rising);
  LogQL returns recent server-2 lines (Grafana→Loki datasource healthy).
- Phase 16: GlitchTip pods Running; registration closed; a forced Sentry envelope ingested and the
  issue appeared.
- Phase 17: both public URLs non-502 through the default-deny layer.

This live cross-phase run is stronger evidence than a static integration check, so no separate
integration-checker pass was spawned.

## Requirements coverage

26/26 capability requirements satisfied (PREP-01..05, DEP-01..04, MET-01..07, EDGE-01..03,
LOG-01..03, ERR-01..03, NET-01..02, VAL-01). Stale checkboxes corrected this audit: NET-01 → [x]
(empirically proven in 17), PREP-02 traceability → Complete. **SDK-01** is operator-scoped to infra
wiring + briefs (app-repo PRs deferred to owners) — recorded, not a blocker.

## Recommendation

Complete the milestone, tracking the tech-debt items above in the backlog. The accepted follow-ups
(MET-06 visual confirmation, the Phase 15 CI `rollout status` step) and the missing VERIFICATION.md
artifacts for 14/16 are documentation/CI-hardening debt; the live stack is fully proven. Optionally
backfill 14/16 VERIFICATION.md and the Phase 15 CI step in a small hardening pass before close.
